commands:

# begin achievement
  - ns: 'Anonymization'
    name: 'AdminAnonymizeUserAchievement'
    summary: 'Anonymize user''s achievement'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAchievementClient(configRepo)
        anonymizationService := achievement.AnonymizationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := anonymizationService.AdminAnonymizeUserAchievementShort(
            &anonymization.AdminAnonymizeUserAchievementParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'User Achievements'
    name: 'AdminBulkUnlockAchievement'
    summary: 'Bulk unlock achievements'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAchievementClient(configRepo)
        userAchievementsService := achievement.UserAchievementsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userAchievementsService.AdminBulkUnlockAchievementShort(
            &user_achievements.AdminBulkUnlockAchievementParams{
                Body:      ..., // *achievementclientmodels.ModelsBulkUnlockAchievementRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Achievements'
    name: 'AdminCreateNewAchievement'
    summary: 'Create new achievement'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAchievementClient(configRepo)
        achievementsService := achievement.AchievementsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := achievementsService.AdminCreateNewAchievementShort(
            &achievements.AdminCreateNewAchievementParams{
                Body:      ..., // *achievementclientmodels.ModelsAchievementRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Achievements'
    name: 'AdminDeleteAchievement'
    summary: 'Delete an achievement'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAchievementClient(configRepo)
        achievementsService := achievement.AchievementsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := achievementsService.AdminDeleteAchievementShort(
            &achievements.AdminDeleteAchievementParams{
                AchievementCode: ..., // string
                Namespace:       ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Achievements'
    name: 'AdminGetAchievement'
    summary: 'Get an achievement'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAchievementClient(configRepo)
        achievementsService := achievement.AchievementsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := achievementsService.AdminGetAchievementShort(
            &achievements.AdminGetAchievementParams{
                AchievementCode: ..., // string
                Namespace:       ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Achievements'
    name: 'AdminListAchievements'
    summary: 'Query achievements'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAchievementClient(configRepo)
        achievementsService := achievement.AchievementsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := achievementsService.AdminListAchievementsShort(
            &achievements.AdminListAchievementsParams{
                Namespace: ..., // string
                Global:    ..., // *bool
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                SortBy:    ..., // *string
                Tags:      ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Global Achievements'
    name: 'AdminListGlobalAchievementContributors'
    summary: 'Admin list contributors of global achievement'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAchievementClient(configRepo)
        globalAchievementsService := achievement.GlobalAchievementsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := globalAchievementsService.AdminListGlobalAchievementContributorsShort(
            &global_achievements.AdminListGlobalAchievementContributorsParams{
                AchievementCode: ..., // string
                Namespace:       ..., // string
                Limit:           ..., // *int64
                Offset:          ..., // *int64
                SortBy:          ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Global Achievements'
    name: 'AdminListGlobalAchievements'
    summary: 'Admin query global achievements [include achieved and in-progress]'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAchievementClient(configRepo)
        globalAchievementsService := achievement.GlobalAchievementsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := globalAchievementsService.AdminListGlobalAchievementsShort(
            &global_achievements.AdminListGlobalAchievementsParams{
                Namespace:        ..., // string
                AchievementCodes: ..., // *string
                Limit:            ..., // *int64
                Offset:           ..., // *int64
                SortBy:           ..., // *string
                Status:           ..., // *string
                Tags:             ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Tags'
    name: 'AdminListTags'
    summary: 'Query tags'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAchievementClient(configRepo)
        tagsService := achievement.TagsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := tagsService.AdminListTagsShort(
            &tags.AdminListTagsParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Name:      ..., // *string
                Offset:    ..., // *int64
                SortBy:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'User Achievements'
    name: 'AdminListUserAchievements'
    summary: 'Query user achievements [include achieved and in-progress]'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAchievementClient(configRepo)
        userAchievementsService := achievement.UserAchievementsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userAchievementsService.AdminListUserAchievementsShort(
            &user_achievements.AdminListUserAchievementsParams{
                Namespace:      ..., // string
                UserID:         ..., // string
                Limit:          ..., // *int64
                Offset:         ..., // *int64
                PreferUnlocked: ..., // *bool
                SortBy:         ..., // *string
                Tags:           ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Global Achievements'
    name: 'AdminListUserContributions'
    summary: 'Admin list of global achievements that has been contributed by the user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAchievementClient(configRepo)
        globalAchievementsService := achievement.GlobalAchievementsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := globalAchievementsService.AdminListUserContributionsShort(
            &global_achievements.AdminListUserContributionsParams{
                Namespace:        ..., // string
                UserID:           ..., // string
                AchievementCodes: ..., // *string
                Limit:            ..., // *int64
                Offset:           ..., // *int64
                SortBy:           ..., // *string
                Tags:             ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'User Achievements'
    name: 'AdminResetAchievement'
    summary: 'Reset an achievement'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAchievementClient(configRepo)
        userAchievementsService := achievement.UserAchievementsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userAchievementsService.AdminResetAchievementShort(
            &user_achievements.AdminResetAchievementParams{
                AchievementCode: ..., // string
                Namespace:       ..., // string
                UserID:          ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'User Achievements'
    name: 'AdminUnlockAchievement'
    summary: 'Unlock an achievement'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAchievementClient(configRepo)
        userAchievementsService := achievement.UserAchievementsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userAchievementsService.AdminUnlockAchievementShort(
            &user_achievements.AdminUnlockAchievementParams{
                AchievementCode: ..., // string
                Namespace:       ..., // string
                UserID:          ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Achievements'
    name: 'AdminUpdateAchievement'
    summary: 'Update an achievement'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAchievementClient(configRepo)
        achievementsService := achievement.AchievementsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := achievementsService.AdminUpdateAchievementShort(
            &achievements.AdminUpdateAchievementParams{
                Body:            ..., // *achievementclientmodels.ModelsAchievementUpdateRequest
                AchievementCode: ..., // string
                Namespace:       ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Achievements'
    name: 'AdminUpdateAchievementListOrder'
    summary: 'Update achievements listOrder'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAchievementClient(configRepo)
        achievementsService := achievement.AchievementsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := achievementsService.AdminUpdateAchievementListOrderShort(
            &achievements.AdminUpdateAchievementListOrderParams{
                Body:            ..., // *achievementclientmodels.ModelsAchievementOrderUpdateRequest
                AchievementCode: ..., // string
                Namespace:       ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Platform Achievement'
    name: 'BulkCreatePSNEvent'
    summary: 'Create PSN UDS events'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAchievementClient(configRepo)
        platformAchievementService := achievement.PlatformAchievementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := platformAchievementService.BulkCreatePSNEventShort(
            &platform_achievement.BulkCreatePSNEventParams{
                Body:      ..., // *achievementclientmodels.ModelsBulkCreatePSNEvents
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Global Achievements'
    name: 'ClaimGlobalAchievementReward'
    summary: 'Claim global achievement reward'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAchievementClient(configRepo)
        globalAchievementsService := achievement.GlobalAchievementsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := globalAchievementsService.ClaimGlobalAchievementRewardShort(
            &global_achievements.ClaimGlobalAchievementRewardParams{
                AchievementCode: ..., // string
                Namespace:       ..., // string
                UserID:          ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Achievements'
    name: 'ExportAchievements'
    summary: 'Export achievements configuration into a json file'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAchievementClient(configRepo)
        achievementsService := achievement.AchievementsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := achievementsService.ExportAchievementsShort(
            &achievements.ExportAchievementsParams{
                Namespace: ..., // string
                Tags:      ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Achievements'
    name: 'ImportAchievements'
    summary: 'Import achievements from file'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAchievementClient(configRepo)
        achievementsService := achievement.AchievementsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := achievementsService.ImportAchievementsShort(
            &achievements.ImportAchievementsParams{
                File:      ..., // *[]byte
                Strategy:  ..., // *string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Global Achievements'
    name: 'ListGlobalAchievementContributors'
    summary: 'List contributors of global achievement'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAchievementClient(configRepo)
        globalAchievementsService := achievement.GlobalAchievementsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := globalAchievementsService.ListGlobalAchievementContributorsShort(
            &global_achievements.ListGlobalAchievementContributorsParams{
                AchievementCode: ..., // string
                Namespace:       ..., // string
                Limit:           ..., // *int64
                Offset:          ..., // *int64
                SortBy:          ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Global Achievements'
    name: 'ListUserContributions'
    summary: 'List of global achievements that has been contributed by the user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAchievementClient(configRepo)
        globalAchievementsService := achievement.GlobalAchievementsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := globalAchievementsService.ListUserContributionsShort(
            &global_achievements.ListUserContributionsParams{
                Namespace:        ..., // string
                UserID:           ..., // string
                AchievementCodes: ..., // *string
                Limit:            ..., // *int64
                Offset:           ..., // *int64
                SortBy:           ..., // *string
                Tags:             ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'User Achievements'
    name: 'PublicBulkUnlockAchievement'
    summary: 'Bulk Unlock achievements'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAchievementClient(configRepo)
        userAchievementsService := achievement.UserAchievementsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userAchievementsService.PublicBulkUnlockAchievementShort(
            &user_achievements.PublicBulkUnlockAchievementParams{
                Body:      ..., // *achievementclientmodels.ModelsBulkUnlockAchievementRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Achievements'
    name: 'PublicGetAchievement'
    summary: 'Get an achievement'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAchievementClient(configRepo)
        achievementsService := achievement.AchievementsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := achievementsService.PublicGetAchievementShort(
            &achievements.PublicGetAchievementParams{
                AchievementCode: ..., // string
                Namespace:       ..., // string
                Language:        ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Achievements'
    name: 'PublicListAchievements'
    summary: 'Query achievements'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAchievementClient(configRepo)
        achievementsService := achievement.AchievementsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := achievementsService.PublicListAchievementsShort(
            &achievements.PublicListAchievementsParams{
                Namespace: ..., // string
                Global:    ..., // *bool
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                SortBy:    ..., // *string
                Tags:      ..., // []string
                Language:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Global Achievements'
    name: 'PublicListGlobalAchievements'
    summary: 'Query global achievements [include achieved and in-progress]'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAchievementClient(configRepo)
        globalAchievementsService := achievement.GlobalAchievementsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := globalAchievementsService.PublicListGlobalAchievementsShort(
            &global_achievements.PublicListGlobalAchievementsParams{
                Namespace:        ..., // string
                AchievementCodes: ..., // *string
                Limit:            ..., // *int64
                Offset:           ..., // *int64
                SortBy:           ..., // *string
                Status:           ..., // *string
                Tags:             ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Tags'
    name: 'PublicListTags'
    summary: 'Query tags'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAchievementClient(configRepo)
        tagsService := achievement.TagsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := tagsService.PublicListTagsShort(
            &tags.PublicListTagsParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Name:      ..., // *string
                Offset:    ..., // *int64
                SortBy:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'User Achievements'
    name: 'PublicListUserAchievements'
    summary: 'Query user achievements [include achieved and in-progress]'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAchievementClient(configRepo)
        userAchievementsService := achievement.UserAchievementsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userAchievementsService.PublicListUserAchievementsShort(
            &user_achievements.PublicListUserAchievementsParams{
                Namespace:      ..., // string
                UserID:         ..., // string
                Limit:          ..., // *int64
                Offset:         ..., // *int64
                PreferUnlocked: ..., // *bool
                SortBy:         ..., // *string
                Tags:           ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'User Achievements'
    name: 'PublicUnlockAchievement'
    summary: 'Unlock an achievement'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAchievementClient(configRepo)
        userAchievementsService := achievement.UserAchievementsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userAchievementsService.PublicUnlockAchievementShort(
            &user_achievements.PublicUnlockAchievementParams{
                AchievementCode: ..., // string
                Namespace:       ..., // string
                UserID:          ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Global Achievements'
    name: 'ResetGlobalAchievement'
    summary: 'Reset global achievement'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAchievementClient(configRepo)
        globalAchievementsService := achievement.GlobalAchievementsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := globalAchievementsService.ResetGlobalAchievementShort(
            &global_achievements.ResetGlobalAchievementParams{
                AchievementCode: ..., // string
                Namespace:       ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }
# end achievement
# begin ams
  - ns: 'Account'
    name: 'AccountGet'
    summary: 'get the account associated with the namespace'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        accountService := ams.AccountService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := accountService.AccountGetShort(
            &account.AccountGetParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Account'
    name: 'AdminAccountCreate'
    summary: 'create a new AMS account'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        accountService := ams.AccountService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := accountService.AdminAccountCreateShort(
            &account.AdminAccountCreateParams{
                Body:      ..., // *amsclientmodels.APIAccountCreateRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Account'
    name: 'AdminAccountGet'
    summary: 'get the account associated with the namespace'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        accountService := ams.AccountService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := accountService.AdminAccountGetShort(
            &account.AdminAccountGetParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Account'
    name: 'AdminAccountLink'
    summary: 'link an account to a namespace'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        accountService := ams.AccountService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := accountService.AdminAccountLinkShort(
            &account.AdminAccountLinkParams{
                Body:      ..., // *amsclientmodels.APIAccountLinkRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Account'
    name: 'AdminAccountLinkTokenGet'
    summary: 'get a link to account token'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        accountService := ams.AccountService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := accountService.AdminAccountLinkTokenGetShort(
            &account.AdminAccountLinkTokenGetParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Artifacts'
    name: 'ArtifactBulkDelete'
    summary: 'delete artifacts that match criteria in bulk. all artifacts matching any one criteria will be deleted. at least 1 parameter is required.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        artifactsService := ams.ArtifactsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := artifactsService.ArtifactBulkDeleteShort(
            &artifacts.ArtifactBulkDeleteParams{
                Namespace:      ..., // string
                ArtifactType:   ..., // *string
                FleetID:        ..., // *string
                UploadedBefore: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Artifacts'
    name: 'ArtifactDelete'
    summary: 'delete a specified artifact'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        artifactsService := ams.ArtifactsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := artifactsService.ArtifactDeleteShort(
            &artifacts.ArtifactDeleteParams{
                ArtifactID: ..., // string
                Namespace:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Artifacts'
    name: 'ArtifactGet'
    summary: 'get all artifacts matching the provided criteria'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        artifactsService := ams.ArtifactsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := artifactsService.ArtifactGetShort(
            &artifacts.ArtifactGetParams{
                Namespace:     ..., // string
                ArtifactType:  ..., // *string
                Count:         ..., // *int64
                EndDate:       ..., // *string
                FleetID:       ..., // *string
                ImageID:       ..., // *string
                MaxSize:       ..., // *int64
                MinSize:       ..., // *int64
                Offset:        ..., // *int64
                Region:        ..., // *string
                ServerID:      ..., // *string
                SortBy:        ..., // *string
                SortDirection: ..., // *string
                StartDate:     ..., // *string
                Status:        ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Artifacts'
    name: 'ArtifactGetURL'
    summary: 'get a signed URL for a specific artifact'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        artifactsService := ams.ArtifactsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := artifactsService.ArtifactGetURLShort(
            &artifacts.ArtifactGetURLParams{
                ArtifactID: ..., // string
                Namespace:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Artifacts'
    name: 'ArtifactUsageGet'
    summary: 'retrieve artifact storage usage for the namespace'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        artifactsService := ams.ArtifactsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := artifactsService.ArtifactUsageGetShort(
            &artifacts.ArtifactUsageGetParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Auth'
    name: 'AuthCheck'
    summary: 'checks if fleet commander can auth with AMS'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        authService := ams.AuthService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := authService.AuthCheckShort(
            &auth.AuthCheckParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Fleet Commander'
    name: 'BasicHealthCheck'
    summary: 'Health check'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        fleetCommanderService := ams.FleetCommanderService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := fleetCommanderService.BasicHealthCheckShort(
            &fleet_commander.BasicHealthCheckParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Development'
    name: 'DevelopmentServerConfigurationCreate'
    summary: 'create a new development server configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        developmentService := ams.DevelopmentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := developmentService.DevelopmentServerConfigurationCreateShort(
            &development.DevelopmentServerConfigurationCreateParams{
                Body:      ..., // *amsclientmodels.APIDevelopmentServerConfigurationCreateRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Development'
    name: 'DevelopmentServerConfigurationDelete'
    summary: 'delete a development server configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        developmentService := ams.DevelopmentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := developmentService.DevelopmentServerConfigurationDeleteShort(
            &development.DevelopmentServerConfigurationDeleteParams{
                DevelopmentServerConfigID: ..., // string
                Namespace:                 ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Development'
    name: 'DevelopmentServerConfigurationGet'
    summary: 'get a development server configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        developmentService := ams.DevelopmentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := developmentService.DevelopmentServerConfigurationGetShort(
            &development.DevelopmentServerConfigurationGetParams{
                DevelopmentServerConfigID: ..., // string
                Namespace:                 ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Development'
    name: 'DevelopmentServerConfigurationList'
    summary: 'lists development server configurations with pagination'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        developmentService := ams.DevelopmentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := developmentService.DevelopmentServerConfigurationListShort(
            &development.DevelopmentServerConfigurationListParams{
                Namespace:     ..., // string
                Count:         ..., // *int64
                ImageID:       ..., // *string
                Name:          ..., // *string
                Offset:        ..., // *int64
                SortBy:        ..., // *string
                SortDirection: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Development'
    name: 'DevelopmentServerConfigurationPatch'
    summary: 'patch a development server configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        developmentService := ams.DevelopmentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := developmentService.DevelopmentServerConfigurationPatchShort(
            &development.DevelopmentServerConfigurationPatchParams{
                Body:                      ..., // *amsclientmodels.APIDevelopmentServerConfigurationUpdateRequest
                DevelopmentServerConfigID: ..., // string
                Namespace:                 ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Artifacts'
    name: 'FleetArtifactSamplingRulesGet'
    summary: 'get the sampling rules for a fleet'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        artifactsService := ams.ArtifactsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := artifactsService.FleetArtifactSamplingRulesGetShort(
            &artifacts.FleetArtifactSamplingRulesGetParams{
                FleetID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Artifacts'
    name: 'FleetArtifactSamplingRulesSet'
    summary: 'set sampling rules for a fleet'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        artifactsService := ams.ArtifactsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := artifactsService.FleetArtifactSamplingRulesSetShort(
            &artifacts.FleetArtifactSamplingRulesSetParams{
                Body:      ..., // *amsclientmodels.APIFleetArtifactsSampleRules
                FleetID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Fleets'
    name: 'FleetClaimByID'
    summary: 'claim a dedicated server from a fleet'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        fleetsService := ams.FleetsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := fleetsService.FleetClaimByIDShort(
            &fleets.FleetClaimByIDParams{
                Body:      ..., // *amsclientmodels.APIFleetClaimReq
                FleetID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Fleets'
    name: 'FleetClaimByKeys'
    summary: 'claim a dedicated server'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        fleetsService := ams.FleetsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := fleetsService.FleetClaimByKeysShort(
            &fleets.FleetClaimByKeysParams{
                Body:      ..., // *amsclientmodels.APIFleetClaimByKeysReq
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Fleets'
    name: 'FleetCreate'
    summary: 'create a fleet'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        fleetsService := ams.FleetsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := fleetsService.FleetCreateShort(
            &fleets.FleetCreateParams{
                Body:      ..., // *amsclientmodels.APIFleetParameters
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Fleets'
    name: 'FleetDelete'
    summary: 'delete a fleet'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        fleetsService := ams.FleetsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := fleetsService.FleetDeleteShort(
            &fleets.FleetDeleteParams{
                FleetID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Fleets'
    name: 'FleetGet'
    summary: 'get a fleet'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        fleetsService := ams.FleetsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := fleetsService.FleetGetShort(
            &fleets.FleetGetParams{
                FleetID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Fleets'
    name: 'FleetList'
    summary: 'list all fleets in a namespace'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        fleetsService := ams.FleetsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := fleetsService.FleetListShort(
            &fleets.FleetListParams{
                Namespace:     ..., // string
                Active:        ..., // *bool
                Count:         ..., // *int64
                Name:          ..., // *string
                Offset:        ..., // *int64
                Region:        ..., // *string
                SortBy:        ..., // *string
                SortDirection: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Servers'
    name: 'FleetServerConnectionInfo'
    summary: 'get connection info for a dedicated server'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        serversService := ams.ServersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := serversService.FleetServerConnectionInfoShort(
            &servers.FleetServerConnectionInfoParams{
                Namespace: ..., // string
                ServerID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Servers'
    name: 'FleetServerHistory'
    summary: 'get history records of a dedicated server in a fleet'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        serversService := ams.ServersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := serversService.FleetServerHistoryShort(
            &servers.FleetServerHistoryParams{
                FleetID:       ..., // string
                Namespace:     ..., // string
                Count:         ..., // *int64
                Offset:        ..., // *int64
                Reason:        ..., // *string
                Region:        ..., // *string
                ServerID:      ..., // *string
                SortDirection: ..., // *string
                Status:        ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Servers'
    name: 'FleetServerInfo'
    summary: 'get information about a dedicated server'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        serversService := ams.ServersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := serversService.FleetServerInfoShort(
            &servers.FleetServerInfoParams{
                Namespace: ..., // string
                ServerID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Fleets'
    name: 'FleetServers'
    summary: 'get server details & counts for a fleet'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        fleetsService := ams.FleetsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := fleetsService.FleetServersShort(
            &fleets.FleetServersParams{
                FleetID:       ..., // string
                Namespace:     ..., // string
                Count:         ..., // *int64
                Offset:        ..., // *int64
                Region:        ..., // *string
                ServerID:      ..., // *string
                SortBy:        ..., // *string
                SortDirection: ..., // *string
                Status:        ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Fleets'
    name: 'FleetUpdate'
    summary: 'update a fleet -– overrides current data'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        fleetsService := ams.FleetsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := fleetsService.FleetUpdateShort(
            &fleets.FleetUpdateParams{
                Body:      ..., // *amsclientmodels.APIFleetParameters
                FleetID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Fleet Commander'
    name: 'func1'
    summary: 'Version info'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        fleetCommanderService := ams.FleetCommanderService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := fleetCommanderService.Func1Short(
            &fleet_commander.Func1Params{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Images'
    name: 'ImageGet'
    summary: 'get image details.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        imagesService := ams.ImagesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := imagesService.ImageGetShort(
            &images.ImageGetParams{
                ImageID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Images'
    name: 'ImageList'
    summary: 'get a list of existing images'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        imagesService := ams.ImagesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := imagesService.ImageListShort(
            &images.ImageListParams{
                Namespace:     ..., // string
                Count:         ..., // *int64
                InUse:         ..., // *string
                IsProtected:   ..., // *bool
                Name:          ..., // *string
                Offset:        ..., // *int64
                SortBy:        ..., // *string
                SortDirection: ..., // *string
                Status:        ..., // *string
                Tag:           ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Images'
    name: 'ImageMarkForDeletion'
    summary: 'mark the image for deletion'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        imagesService := ams.ImagesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := imagesService.ImageMarkForDeletionShort(
            &images.ImageMarkForDeletionParams{
                ImageID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Images'
    name: 'ImagePatch'
    summary: 'edit the image'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        imagesService := ams.ImagesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := imagesService.ImagePatchShort(
            &images.ImagePatchParams{
                Body:      ..., // *amsclientmodels.APIImageUpdate
                ImageID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Images'
    name: 'ImagesStorage'
    summary: 'get current usage for images storage'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        imagesService := ams.ImagesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := imagesService.ImagesStorageShort(
            &images.ImagesStorageParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Images'
    name: 'ImageUnmarkForDeletion'
    summary: 'unmarks the image for deletion'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        imagesService := ams.ImagesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := imagesService.ImageUnmarkForDeletionShort(
            &images.ImageUnmarkForDeletionParams{
                ImageID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AMS Info'
    name: 'InfoRegions'
    summary: 'get a list of the available AMS regions'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        aMSInfoService := ams.AMSInfoService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := aMSInfoService.InfoRegionsShort(
            &a_m_s_info.InfoRegionsParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AMS Info'
    name: 'InfoSupportedInstances'
    summary: 'get a list of available instance types for the current account'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        aMSInfoService := ams.AMSInfoService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := aMSInfoService.InfoSupportedInstancesShort(
            &a_m_s_info.InfoSupportedInstancesParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Watchdogs'
    name: 'LocalWatchdogConnect'
    summary: 'connect a local watchdog'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        watchdogsService := ams.WatchdogsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := watchdogsService.LocalWatchdogConnectShort(
            &watchdogs.LocalWatchdogConnectParams{
                Namespace:  ..., // string
                WatchdogID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Fleet Commander'
    name: 'PortalHealthCheck'
    summary: 'Health check'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        fleetCommanderService := ams.FleetCommanderService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := fleetCommanderService.PortalHealthCheckShort(
            &fleet_commander.PortalHealthCheckParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AMS QoS'
    name: 'QoSRegionsGet'
    summary: 'get a list of available AMS QoS regions'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        aMSQoSService := ams.AMSQoSService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := aMSQoSService.QoSRegionsGetShort(
            &a_m_s_qo_s.QoSRegionsGetParams{
                Namespace: ..., // string
                Status:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AMS QoS'
    name: 'QoSRegionsUpdate'
    summary: 'update the status of a QoS region'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        aMSQoSService := ams.AMSQoSService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := aMSQoSService.QoSRegionsUpdateShort(
            &a_m_s_qo_s.QoSRegionsUpdateParams{
                Body:      ..., // *amsclientmodels.APIUpdateServerRequest
                Namespace: ..., // string
                Region:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Servers'
    name: 'ServerHistory'
    summary: 'get history records of a dedicated server'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        serversService := ams.ServersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := serversService.ServerHistoryShort(
            &servers.ServerHistoryParams{
                Namespace: ..., // string
                ServerID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AMS Info'
    name: 'UploadURLGet'
    summary: 'get an URL for uploading an image'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        aMSInfoService := ams.AMSInfoService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := aMSInfoService.UploadURLGetShort(
            &a_m_s_info.UploadURLGetParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Watchdogs'
    name: 'WatchdogConnect'
    summary: 'connect a watchdog'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewAmsClient(configRepo)
        watchdogsService := ams.WatchdogsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := watchdogsService.WatchdogConnectShort(
            &watchdogs.WatchdogConnectParams{
                Namespace:  ..., // string
                WatchdogID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }
# end ams
# begin basic
  - ns: 'Misc'
    name: 'addCountryGroup'
    summary: 'Add a country group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        miscService := basic.MiscService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := miscService.AddCountryGroupShort(
            &misc.AddCountryGroupParams{
                Body:      ..., // *basicclientmodels.AddCountryGroupRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserProfile'
    name: 'adminGetUserProfilePublicInfoByIds'
    summary: 'Admin get user profile public info by ids'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        userProfileService := basic.UserProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userProfileService.AdminGetUserProfilePublicInfoByIdsShort(
            &user_profile.AdminGetUserProfilePublicInfoByIdsParams{
                Body:      ..., // *basicclientmodels.UserProfileBulkRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Anonymization'
    name: 'anonymizeUserProfile'
    summary: 'anonymize user profile'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        anonymizationService := basic.AnonymizationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := anonymizationService.AnonymizeUserProfileShort(
            &anonymization.AnonymizeUserProfileParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Namespace'
    name: 'changeNamespaceStatus'
    summary: 'Change namespace status'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        namespaceService := basic.NamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := namespaceService.ChangeNamespaceStatusShort(
            &namespace.ChangeNamespaceStatusParams{
                Body:      ..., // *basicclientmodels.NamespaceStatusUpdate
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Config'
    name: 'createConfig'
    summary: 'Create a config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        configService := basic.ConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configService.CreateConfigShort(
            &config.CreateConfigParams{
                Body:      ..., // *basicclientmodels.ConfigCreate
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserProfile'
    name: 'createMyProfile'
    summary: 'Create my profile'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        userProfileService := basic.UserProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userProfileService.CreateMyProfileShort(
            &user_profile.CreateMyProfileParams{
                Body:      ..., // *basicclientmodels.UserProfilePrivateCreate
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Namespace'
    name: 'createNamespace'
    summary: 'Create a namespace'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        namespaceService := basic.NamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := namespaceService.CreateNamespaceShort(
            &namespace.CreateNamespaceParams{
                Body: ..., // *basicclientmodels.NamespaceCreate
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Config'
    name: 'deleteConfig'
    summary: 'Delete a config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        configService := basic.ConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configService.DeleteConfigShort(
            &config.DeleteConfigParams{
                ConfigKey: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Misc'
    name: 'deleteCountryGroup'
    summary: 'Delete a country group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        miscService := basic.MiscService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := miscService.DeleteCountryGroupShort(
            &misc.DeleteCountryGroupParams{
                CountryGroupCode: ..., // string
                Namespace:        ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Namespace'
    name: 'deleteNamespace'
    summary: 'Delete a namespace'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        namespaceService := basic.NamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := namespaceService.DeleteNamespaceShort(
            &namespace.DeleteNamespaceParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserProfile'
    name: 'deleteUserProfile'
    summary: 'Delete user profile'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        userProfileService := basic.UserProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userProfileService.DeleteUserProfileShort(
            &user_profile.DeleteUserProfileParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'FileUpload'
    name: 'generatedUploadUrl'
    summary: 'Generate Upload URL'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        fileUploadService := basic.FileUploadService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := fileUploadService.GeneratedUploadURLShort(
            &file_upload.GeneratedUploadURLParams{
                Folder:    ..., // string
                Namespace: ..., // string
                FileType:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'FileUpload'
    name: 'generatedUserUploadContentUrl'
    summary: 'Generate Upload URL For User Content'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        fileUploadService := basic.FileUploadService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := fileUploadService.GeneratedUserUploadContentURLShort(
            &file_upload.GeneratedUserUploadContentURLParams{
                Namespace: ..., // string
                UserID:    ..., // string
                Category:  ..., // *string
                FileType:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Namespace'
    name: 'getChildNamespaces'
    summary: 'Get child namespaces'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        namespaceService := basic.NamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := namespaceService.GetChildNamespacesShort(
            &namespace.GetChildNamespacesParams{
                Namespace:  ..., // string
                ActiveOnly: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Config'
    name: 'getConfig'
    summary: 'Get a config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        configService := basic.ConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configService.GetConfigShort(
            &config.GetConfigParams{
                ConfigKey: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Misc'
    name: 'getCountries'
    summary: 'List countries'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        miscService := basic.MiscService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := miscService.GetCountriesShort(
            &misc.GetCountriesParams{
                Namespace: ..., // string
                Lang:      ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Misc'
    name: 'getCountryGroups'
    summary: 'List country groups'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        miscService := basic.MiscService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := miscService.GetCountryGroupsShort(
            &misc.GetCountryGroupsParams{
                Namespace: ..., // string
                GroupCode: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserProfile'
    name: 'getCustomAttributesInfo'
    summary: 'Get user custom attributes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        userProfileService := basic.UserProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userProfileService.GetCustomAttributesInfoShort(
            &user_profile.GetCustomAttributesInfoParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Namespace'
    name: 'getGameNamespaces'
    summary: 'Get game namespaces'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        namespaceService := basic.NamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := namespaceService.GetGameNamespacesShort(
            &namespace.GetGameNamespacesParams{
                Namespace:  ..., // string
                ActiveOnly: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Misc'
    name: 'getLanguages'
    summary: 'List languages.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        miscService := basic.MiscService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := miscService.GetLanguagesShort(
            &misc.GetLanguagesParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserProfile'
    name: 'getMyPrivateCustomAttributesInfo'
    summary: 'Get my private custom attributes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        userProfileService := basic.UserProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userProfileService.GetMyPrivateCustomAttributesInfoShort(
            &user_profile.GetMyPrivateCustomAttributesInfoParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserProfile'
    name: 'getMyProfileInfo'
    summary: 'Get my profile'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        userProfileService := basic.UserProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userProfileService.GetMyProfileInfoShort(
            &user_profile.GetMyProfileInfoParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserProfile'
    name: 'getMyZipCode'
    summary: 'Get my zip code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        userProfileService := basic.UserProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userProfileService.GetMyZipCodeShort(
            &user_profile.GetMyZipCodeParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Namespace'
    name: 'getNamespace'
    summary: 'Get a namespace'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        namespaceService := basic.NamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := namespaceService.GetNamespaceShort(
            &namespace.GetNamespaceParams{
                Namespace:  ..., // string
                ActiveOnly: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Namespace'
    name: 'getNamespace_1'
    summary: 'Get a namespace info'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        namespaceService := basic.NamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := namespaceService.GetNamespace1Short(
            &namespace.GetNamespace1Params{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Namespace'
    name: 'getNamespaceContext'
    summary: 'Get context of namespace'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        namespaceService := basic.NamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := namespaceService.GetNamespaceContextShort(
            &namespace.GetNamespaceContextParams{
                Namespace:          ..., // string
                RefreshOnCacheMiss: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Namespace'
    name: 'getNamespacePublisher'
    summary: 'Get namespace info related publisher namespace'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        namespaceService := basic.NamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := namespaceService.GetNamespacePublisherShort(
            &namespace.GetNamespacePublisherParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Namespace'
    name: 'getNamespaces'
    summary: 'Get all namespaces'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        namespaceService := basic.NamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := namespaceService.GetNamespacesShort(
            &namespace.GetNamespacesParams{
                ActiveOnly: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserProfile'
    name: 'getPrivateCustomAttributesInfo'
    summary: 'Get user private custom attributes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        userProfileService := basic.UserProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userProfileService.GetPrivateCustomAttributesInfoShort(
            &user_profile.GetPrivateCustomAttributesInfoParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Config'
    name: 'getPublisherConfig'
    summary: 'Get a publisher config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        configService := basic.ConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configService.GetPublisherConfigShort(
            &config.GetPublisherConfigParams{
                ConfigKey: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Misc'
    name: 'getTimeZones'
    summary: 'List time zones'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        miscService := basic.MiscService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := miscService.GetTimeZonesShort(
            &misc.GetTimeZonesParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserProfile'
    name: 'getUserProfileInfo'
    summary: 'Get user profile'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        userProfileService := basic.UserProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userProfileService.GetUserProfileInfoShort(
            &user_profile.GetUserProfileInfoParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserProfile'
    name: 'getUserProfileInfoByPublicId'
    summary: 'Get user profile info by public id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        userProfileService := basic.UserProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userProfileService.GetUserProfileInfoByPublicIDShort(
            &user_profile.GetUserProfileInfoByPublicIDParams{
                Namespace: ..., // string
                PublicID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserProfile'
    name: 'publicBulkGetUserProfilePublicInfo'
    summary: 'Bulk get user profile public info by ids'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        userProfileService := basic.UserProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userProfileService.PublicBulkGetUserProfilePublicInfoShort(
            &user_profile.PublicBulkGetUserProfilePublicInfoParams{
                Body:      ..., // *basicclientmodels.UserProfileBulkRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserProfile'
    name: 'publicCreateUserProfile'
    summary: 'Create user profile'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        userProfileService := basic.UserProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userProfileService.PublicCreateUserProfileShort(
            &user_profile.PublicCreateUserProfileParams{
                Body:      ..., // *basicclientmodels.UserProfileCreate
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'FileUpload'
    name: 'publicGeneratedUploadUrl'
    summary: 'Generate Upload URL'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        fileUploadService := basic.FileUploadService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := fileUploadService.PublicGeneratedUploadURLShort(
            &file_upload.PublicGeneratedUploadURLParams{
                Folder:    ..., // string
                Namespace: ..., // string
                FileType:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'FileUpload'
    name: 'publicGeneratedUserUploadContentUrl'
    summary: 'Generate Upload URL For User Content'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        fileUploadService := basic.FileUploadService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := fileUploadService.PublicGeneratedUserUploadContentURLShort(
            &file_upload.PublicGeneratedUserUploadContentURLParams{
                Namespace: ..., // string
                UserID:    ..., // string
                Category:  ..., // *string
                FileType:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Misc'
    name: 'publicGetCountries'
    summary: 'List countries'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        miscService := basic.MiscService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := miscService.PublicGetCountriesShort(
            &misc.PublicGetCountriesParams{
                Namespace: ..., // string
                Lang:      ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserProfile'
    name: 'publicGetCustomAttributesInfo'
    summary: 'Get custom attributes info'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        userProfileService := basic.UserProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userProfileService.PublicGetCustomAttributesInfoShort(
            &user_profile.PublicGetCustomAttributesInfoParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Misc'
    name: 'publicGetLanguages'
    summary: 'List languages.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        miscService := basic.MiscService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := miscService.PublicGetLanguagesShort(
            &misc.PublicGetLanguagesParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Namespace'
    name: 'publicGetNamespacePublisher'
    summary: 'Get namespace info related publisher namespace'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        namespaceService := basic.NamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := namespaceService.PublicGetNamespacePublisherShort(
            &namespace.PublicGetNamespacePublisherParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Namespace'
    name: 'publicGetNamespaces'
    summary: 'Get all namespaces'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        namespaceService := basic.NamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := namespaceService.PublicGetNamespacesShort(
            &namespace.PublicGetNamespacesParams{
                ActiveOnly: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Misc'
    name: 'publicGetTime'
    summary: 'Get server time'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        miscService := basic.MiscService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := miscService.PublicGetTimeShort(
            &misc.PublicGetTimeParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Misc'
    name: 'publicGetTimeZones'
    summary: 'List time zones'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        miscService := basic.MiscService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := miscService.PublicGetTimeZonesShort(
            &misc.PublicGetTimeZonesParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserProfile'
    name: 'publicGetUserProfileInfo'
    summary: 'Get user profile'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        userProfileService := basic.UserProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userProfileService.PublicGetUserProfileInfoShort(
            &user_profile.PublicGetUserProfileInfoParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserProfile'
    name: 'publicGetUserProfileInfoByPublicId'
    summary: 'Get user profile public info by public id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        userProfileService := basic.UserProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userProfileService.PublicGetUserProfileInfoByPublicIDShort(
            &user_profile.PublicGetUserProfileInfoByPublicIDParams{
                Namespace: ..., // string
                PublicID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserProfile'
    name: 'publicGetUserProfilePublicInfo'
    summary: 'Get user profile public info'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        userProfileService := basic.UserProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userProfileService.PublicGetUserProfilePublicInfoShort(
            &user_profile.PublicGetUserProfilePublicInfoParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserProfile'
    name: 'publicGetUserProfilePublicInfoByIds'
    summary: 'Get user profile public info by ids'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        userProfileService := basic.UserProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userProfileService.PublicGetUserProfilePublicInfoByIdsShort(
            &user_profile.PublicGetUserProfilePublicInfoByIdsParams{
                Namespace: ..., // string
                UserIds:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserProfile'
    name: 'publicUpdateCustomAttributesPartially'
    summary: 'Update partially custom attributes tied to user id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        userProfileService := basic.UserProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userProfileService.PublicUpdateCustomAttributesPartiallyShort(
            &user_profile.PublicUpdateCustomAttributesPartiallyParams{
                Body:      ..., // map[string]interface{}
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserProfile'
    name: 'publicUpdateUserProfile'
    summary: 'Update user profile'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        userProfileService := basic.UserProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userProfileService.PublicUpdateUserProfileShort(
            &user_profile.PublicUpdateUserProfileParams{
                Body:      ..., // *basicclientmodels.UserProfileUpdate
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserProfile'
    name: 'publicUpdateUserProfileStatus'
    summary: 'Update user profile status'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        userProfileService := basic.UserProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userProfileService.PublicUpdateUserProfileStatusShort(
            &user_profile.PublicUpdateUserProfileStatusParams{
                Body:      ..., // *basicclientmodels.UserProfileStatusUpdate
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Config'
    name: 'updateConfig'
    summary: 'Update a config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        configService := basic.ConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configService.UpdateConfigShort(
            &config.UpdateConfigParams{
                Body:      ..., // *basicclientmodels.ConfigUpdate
                ConfigKey: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Misc'
    name: 'updateCountryGroup'
    summary: 'Update a country group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        miscService := basic.MiscService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := miscService.UpdateCountryGroupShort(
            &misc.UpdateCountryGroupParams{
                Body:             ..., // *basicclientmodels.UpdateCountryGroupRequest
                CountryGroupCode: ..., // string
                Namespace:        ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserProfile'
    name: 'updateCustomAttributesPartially'
    summary: 'Update partially custom attributes tied to the user id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        userProfileService := basic.UserProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userProfileService.UpdateCustomAttributesPartiallyShort(
            &user_profile.UpdateCustomAttributesPartiallyParams{
                Body:      ..., // map[string]interface{}
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserProfile'
    name: 'updateMyPrivateCustomAttributesPartially'
    summary: 'Update partially private custom attributes tied to me'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        userProfileService := basic.UserProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userProfileService.UpdateMyPrivateCustomAttributesPartiallyShort(
            &user_profile.UpdateMyPrivateCustomAttributesPartiallyParams{
                Body:      ..., // map[string]interface{}
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserProfile'
    name: 'updateMyProfile'
    summary: 'Update my profile'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        userProfileService := basic.UserProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userProfileService.UpdateMyProfileShort(
            &user_profile.UpdateMyProfileParams{
                Body:      ..., // *basicclientmodels.UserProfileUpdate
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserProfile'
    name: 'updateMyZipCode'
    summary: 'Update my zip code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        userProfileService := basic.UserProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userProfileService.UpdateMyZipCodeShort(
            &user_profile.UpdateMyZipCodeParams{
                UserZipCodeUpdate: ..., // *basicclientmodels.UserZipCodeUpdate
                Namespace:         ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Namespace'
    name: 'updateNamespace'
    summary: 'Update namespace basic info'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        namespaceService := basic.NamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := namespaceService.UpdateNamespaceShort(
            &namespace.UpdateNamespaceParams{
                Body:      ..., // *basicclientmodels.NamespaceUpdate
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserProfile'
    name: 'updatePrivateCustomAttributesPartially'
    summary: 'Update partially private custom attributes tied to the user id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        userProfileService := basic.UserProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userProfileService.UpdatePrivateCustomAttributesPartiallyShort(
            &user_profile.UpdatePrivateCustomAttributesPartiallyParams{
                Body:      ..., // map[string]interface{}
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserProfile'
    name: 'updateUserProfile'
    summary: 'Update user profile'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        userProfileService := basic.UserProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userProfileService.UpdateUserProfileShort(
            &user_profile.UpdateUserProfileParams{
                Body:      ..., // *basicclientmodels.UserProfileAdmin
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserProfile'
    name: 'updateUserProfileStatus'
    summary: 'Update user profile status'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewBasicClient(configRepo)
        userProfileService := basic.UserProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userProfileService.UpdateUserProfileStatusShort(
            &user_profile.UpdateUserProfileStatusParams{
                Body:      ..., // *basicclientmodels.UserProfileStatusUpdate
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }
# end basic
# begin challenge
  - ns: 'Player Reward'
    name: 'adminClaimUserRewards'
    summary: 'Claim rewards of a single user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        playerRewardService := challenge.PlayerRewardService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := playerRewardService.AdminClaimUserRewardsShort(
            &player_reward.AdminClaimUserRewardsParams{
                Body:      ..., // *challengeclientmodels.ModelClaimUserRewardsReq
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Player Reward'
    name: 'adminClaimUserRewardsByGoalCode'
    summary: 'Claim rewards of a single user by goal code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        playerRewardService := challenge.PlayerRewardService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := playerRewardService.AdminClaimUserRewardsByGoalCodeShort(
            &player_reward.AdminClaimUserRewardsByGoalCodeParams{
                Body:          ..., // *challengeclientmodels.ModelClaimUserRewardsByGoalCodeRequest
                ChallengeCode: ..., // string
                Namespace:     ..., // string
                UserID:        ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Player Reward'
    name: 'adminClaimUsersRewards'
    summary: 'Claim  rewards of multiple users'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        playerRewardService := challenge.PlayerRewardService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := playerRewardService.AdminClaimUsersRewardsShort(
            &player_reward.AdminClaimUsersRewardsParams{
                Body:      ..., // []*challengeclientmodels.ModelClaimUsersRewardsRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Plugins'
    name: 'adminCreateAssignmentPlugin'
    summary: 'Create assignment plugin'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        pluginsService := challenge.PluginsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := pluginsService.AdminCreateAssignmentPluginShort(
            &plugins.AdminCreateAssignmentPluginParams{
                Body:      ..., // *challengeclientmodels.ModelPluginAssignmentRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Challenge Configuration'
    name: 'adminCreateChallenge'
    summary: 'Create New Challenge'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        challengeConfigurationService := challenge.ChallengeConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := challengeConfigurationService.AdminCreateChallengeShort(
            &challenge_configuration.AdminCreateChallengeParams{
                Body:      ..., // *challengeclientmodels.ModelCreateChallengeRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Goal Configuration'
    name: 'adminCreateGoal'
    summary: 'Create New Goal'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        goalConfigurationService := challenge.GoalConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := goalConfigurationService.AdminCreateGoalShort(
            &goal_configuration.AdminCreateGoalParams{
                Body:          ..., // *challengeclientmodels.ModelCreateGoalRequest
                ChallengeCode: ..., // string
                Namespace:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Plugins'
    name: 'adminDeleteAssignmentPlugin'
    summary: 'Delete assignment plugin'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        pluginsService := challenge.PluginsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := pluginsService.AdminDeleteAssignmentPluginShort(
            &plugins.AdminDeleteAssignmentPluginParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Challenge Configuration'
    name: 'adminDeleteChallenge'
    summary: 'Delete a Challenge'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        challengeConfigurationService := challenge.ChallengeConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := challengeConfigurationService.AdminDeleteChallengeShort(
            &challenge_configuration.AdminDeleteChallengeParams{
                ChallengeCode: ..., // string
                Namespace:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Goal Configuration'
    name: 'adminDeleteGoal'
    summary: 'Delete Goal'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        goalConfigurationService := challenge.GoalConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := goalConfigurationService.AdminDeleteGoalShort(
            &goal_configuration.AdminDeleteGoalParams{
                ChallengeCode: ..., // string
                Code:          ..., // string
                Namespace:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Challenge Configuration'
    name: 'adminDeleteTiedChallenge'
    summary: 'Delete Tied Challenge'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        challengeConfigurationService := challenge.ChallengeConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := challengeConfigurationService.AdminDeleteTiedChallengeShort(
            &challenge_configuration.AdminDeleteTiedChallengeParams{
                ChallengeCode: ..., // string
                Namespace:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Challenge Progression'
    name: 'adminEvaluateProgress'
    summary: 'Evaluate User''s Progressions'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        challengeProgressionService := challenge.ChallengeProgressionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := challengeProgressionService.AdminEvaluateProgressShort(
            &challenge_progression.AdminEvaluateProgressParams{
                Body:      ..., // *challengeclientmodels.ModelEvaluatePlayerProgressionRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Challenge Configuration'
    name: 'adminGetActiveChallenges'
    summary: 'List User''s Active Challenges'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        challengeConfigurationService := challenge.ChallengeConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := challengeConfigurationService.AdminGetActiveChallengesShort(
            &challenge_configuration.AdminGetActiveChallengesParams{
                Namespace: ..., // string
                UserID:    ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Plugins'
    name: 'adminGetAssignmentPlugin'
    summary: 'Get assignment plugin'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        pluginsService := challenge.PluginsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := pluginsService.AdminGetAssignmentPluginShort(
            &plugins.AdminGetAssignmentPluginParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Challenge Configuration'
    name: 'adminGetChallenge'
    summary: 'Get a Challenge'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        challengeConfigurationService := challenge.ChallengeConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := challengeConfigurationService.AdminGetChallengeShort(
            &challenge_configuration.AdminGetChallengeParams{
                ChallengeCode: ..., // string
                Namespace:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Challenge Configuration'
    name: 'adminGetChallenges'
    summary: 'List Challenges'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        challengeConfigurationService := challenge.ChallengeConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := challengeConfigurationService.AdminGetChallengesShort(
            &challenge_configuration.AdminGetChallengesParams{
                Namespace: ..., // string
                Keyword:   ..., // *string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                SortBy:    ..., // *string
                Status:    ..., // *string
                Tags:      ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Goal Configuration'
    name: 'adminGetGoal'
    summary: 'Get Goal'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        goalConfigurationService := challenge.GoalConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := goalConfigurationService.AdminGetGoalShort(
            &goal_configuration.AdminGetGoalParams{
                ChallengeCode: ..., // string
                Code:          ..., // string
                Namespace:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Goal Configuration'
    name: 'adminGetGoals'
    summary: 'List Goals of a Challenge'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        goalConfigurationService := challenge.GoalConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := goalConfigurationService.AdminGetGoalsShort(
            &goal_configuration.AdminGetGoalsParams{
                ChallengeCode: ..., // string
                Namespace:     ..., // string
                Limit:         ..., // *int64
                Offset:        ..., // *int64
                SortBy:        ..., // *string
                Tags:          ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Utilities'
    name: 'adminGetItemReferences'
    summary: 'Get challenge ecommerce item references'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        utilitiesService := challenge.UtilitiesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := utilitiesService.AdminGetItemReferencesShort(
            &utilities.AdminGetItemReferencesParams{
                Namespace: ..., // string
                ItemID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Challenge Configuration'
    name: 'adminGetPeriods'
    summary: 'Get Challenge''s Periods'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        challengeConfigurationService := challenge.ChallengeConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := challengeConfigurationService.AdminGetPeriodsShort(
            &challenge_configuration.AdminGetPeriodsParams{
                ChallengeCode: ..., // string
                Namespace:     ..., // string
                Limit:         ..., // *int64
                Offset:        ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Challenge Progression'
    name: 'adminGetUserProgression'
    summary: 'List User''s Progressions'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        challengeProgressionService := challenge.ChallengeProgressionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := challengeProgressionService.AdminGetUserProgressionShort(
            &challenge_progression.AdminGetUserProgressionParams{
                ChallengeCode: ..., // string
                Namespace:     ..., // string
                UserID:        ..., // string
                DateTime:      ..., // *string
                GoalCode:      ..., // *string
                Limit:         ..., // *int64
                Offset:        ..., // *int64
                Tags:          ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Player Reward'
    name: 'adminGetUserRewards'
    summary: 'List User''s Rewards'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        playerRewardService := challenge.PlayerRewardService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := playerRewardService.AdminGetUserRewardsShort(
            &player_reward.AdminGetUserRewardsParams{
                Namespace:         ..., // string
                UserID:            ..., // string
                ChallengeCode:     ..., // *string
                GoalProgressionID: ..., // *string
                Limit:             ..., // *int64
                Offset:            ..., // *int64
                SortBy:            ..., // *string
                Status:            ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Schedules'
    name: 'adminListSchedules'
    summary: 'Get Challenge''s schedules'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        schedulesService := challenge.SchedulesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := schedulesService.AdminListSchedulesShort(
            &schedules.AdminListSchedulesParams{
                ChallengeCode: ..., // string
                Namespace:     ..., // string
                DateTime:      ..., // *string
                Limit:         ..., // *int64
                Offset:        ..., // *int64
                UserID:        ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Schedules'
    name: 'adminListSchedulesByGoal'
    summary: 'Get goal''s schedules'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        schedulesService := challenge.SchedulesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := schedulesService.AdminListSchedulesByGoalShort(
            &schedules.AdminListSchedulesByGoalParams{
                ChallengeCode: ..., // string
                Code:          ..., // string
                Namespace:     ..., // string
                Limit:         ..., // *int64
                Offset:        ..., // *int64
                UserID:        ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Challenge Configuration'
    name: 'adminRandomizeChallenge'
    summary: 'Randomize Goals of a Challenge'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        challengeConfigurationService := challenge.ChallengeConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := challengeConfigurationService.AdminRandomizeChallengeShort(
            &challenge_configuration.AdminRandomizeChallengeParams{
                ChallengeCode: ..., // string
                Namespace:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Plugins'
    name: 'adminUpdateAssignmentPlugin'
    summary: 'Update assignment plugin'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        pluginsService := challenge.PluginsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := pluginsService.AdminUpdateAssignmentPluginShort(
            &plugins.AdminUpdateAssignmentPluginParams{
                Body:      ..., // *challengeclientmodels.ModelPluginAssignmentRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Challenge Configuration'
    name: 'adminUpdateChallenge'
    summary: 'Update a Challenge'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        challengeConfigurationService := challenge.ChallengeConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := challengeConfigurationService.AdminUpdateChallengeShort(
            &challenge_configuration.AdminUpdateChallengeParams{
                Body:          ..., // *challengeclientmodels.ModelUpdateChallengeRequest
                ChallengeCode: ..., // string
                Namespace:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Goal Configuration'
    name: 'adminUpdateGoals'
    summary: 'Update Goal'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        goalConfigurationService := challenge.GoalConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := goalConfigurationService.AdminUpdateGoalsShort(
            &goal_configuration.AdminUpdateGoalsParams{
                Body:          ..., // *challengeclientmodels.ModelUpdateGoalRequest
                ChallengeCode: ..., // string
                Code:          ..., // string
                Namespace:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Challenge Configuration'
    name: 'adminUpdateTiedChallengeSchedule'
    summary: 'Update Tied Challenge Schedule'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        challengeConfigurationService := challenge.ChallengeConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := challengeConfigurationService.AdminUpdateTiedChallengeScheduleShort(
            &challenge_configuration.AdminUpdateTiedChallengeScheduleParams{
                Body:          ..., // *challengeclientmodels.ModelUpdateChallengeScheduleRequest
                ChallengeCode: ..., // string
                Namespace:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Challenge Progression'
    name: 'EvaluateMyProgress'
    summary: 'Evaluate User''s Challenge Progressions'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        challengeProgressionService := challenge.ChallengeProgressionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := challengeProgressionService.EvaluateMyProgressShort(
            &challenge_progression.EvaluateMyProgressParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Challenge List'
    name: 'GetChallenges'
    summary: 'List Challenges'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        challengeListService := challenge.ChallengeListService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := challengeListService.GetChallengesShort(
            &challenge_list.GetChallengesParams{
                Namespace: ..., // string
                Keyword:   ..., // *string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                SortBy:    ..., // *string
                Status:    ..., // *string
                Tags:      ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Player Reward'
    name: 'publicClaimUserRewards'
    summary: 'Claim User''s Rewards'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        playerRewardService := challenge.PlayerRewardService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := playerRewardService.PublicClaimUserRewardsShort(
            &player_reward.PublicClaimUserRewardsParams{
                Body:      ..., // *challengeclientmodels.ModelClaimUserRewardsReq
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Player Reward'
    name: 'publicClaimUserRewardsByGoalCode'
    summary: 'Claim User''s Rewards by Goal Code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        playerRewardService := challenge.PlayerRewardService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := playerRewardService.PublicClaimUserRewardsByGoalCodeShort(
            &player_reward.PublicClaimUserRewardsByGoalCodeParams{
                Body:          ..., // *challengeclientmodels.ModelClaimUserRewardsByGoalCodeRequest
                ChallengeCode: ..., // string
                Namespace:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Challenge Progression'
    name: 'publicGetPastUserProgression'
    summary: 'List User''s Progressions in the previous rotation.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        challengeProgressionService := challenge.ChallengeProgressionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := challengeProgressionService.PublicGetPastUserProgressionShort(
            &challenge_progression.PublicGetPastUserProgressionParams{
                ChallengeCode: ..., // string
                Index:         ..., // int64
                Namespace:     ..., // string
                GoalCode:      ..., // *string
                Limit:         ..., // *int64
                Offset:        ..., // *int64
                Tags:          ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Challenge List'
    name: 'publicGetScheduledGoals'
    summary: 'List Goals of a Challenge'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        challengeListService := challenge.ChallengeListService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := challengeListService.PublicGetScheduledGoalsShort(
            &challenge_list.PublicGetScheduledGoalsParams{
                ChallengeCode: ..., // string
                Namespace:     ..., // string
                Limit:         ..., // *int64
                Offset:        ..., // *int64
                SortBy:        ..., // *string
                Tags:          ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Challenge Progression'
    name: 'publicGetUserProgression'
    summary: 'List User''s Progressions'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        challengeProgressionService := challenge.ChallengeProgressionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := challengeProgressionService.PublicGetUserProgressionShort(
            &challenge_progression.PublicGetUserProgressionParams{
                ChallengeCode: ..., // string
                Namespace:     ..., // string
                DateTime:      ..., // *string
                GoalCode:      ..., // *string
                Limit:         ..., // *int64
                Offset:        ..., // *int64
                Tags:          ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Player Reward'
    name: 'publicGetUserRewards'
    summary: 'List User''s Rewards'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        playerRewardService := challenge.PlayerRewardService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := playerRewardService.PublicGetUserRewardsShort(
            &player_reward.PublicGetUserRewardsParams{
                Namespace:         ..., // string
                ChallengeCode:     ..., // *string
                GoalProgressionID: ..., // *string
                Limit:             ..., // *int64
                Offset:            ..., // *int64
                SortBy:            ..., // *string
                Status:            ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Schedules'
    name: 'publicListSchedules'
    summary: 'Get Challenge''s schedules'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        schedulesService := challenge.SchedulesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := schedulesService.PublicListSchedulesShort(
            &schedules.PublicListSchedulesParams{
                ChallengeCode: ..., // string
                Namespace:     ..., // string
                DateTime:      ..., // *string
                Limit:         ..., // *int64
                Offset:        ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Schedules'
    name: 'publicListSchedulesByGoal'
    summary: 'Get goal''s schedules'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChallengeClient(configRepo)
        schedulesService := challenge.SchedulesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := schedulesService.PublicListSchedulesByGoalShort(
            &schedules.PublicListSchedulesByGoalParams{
                ChallengeCode: ..., // string
                Code:          ..., // string
                Namespace:     ..., // string
                Limit:         ..., // *int64
                Offset:        ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }
# end challenge
# begin chat
  - ns: 'inbox'
    name: 'adminAddInboxCategory'
    summary: 'add inbox category'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        inboxService := chat.InboxService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := inboxService.AdminAddInboxCategoryShort(
            &inbox.AdminAddInboxCategoryParams{
                Body:      ..., // *chatclientmodels.ModelsAddInboxCategoryRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'topic'
    name: 'adminAddTopicMember'
    summary: 'admin add user to topic'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        topicService := chat.TopicService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := topicService.AdminAddTopicMemberShort(
            &topic.AdminAddTopicMemberParams{
                Body:      ..., // *chatclientmodels.APIAddMemberParams
                Namespace: ..., // string
                Topic:     ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'topic'
    name: 'adminBanTopicMembers'
    summary: 'admins ban user in group topic'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        topicService := chat.TopicService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := topicService.AdminBanTopicMembersShort(
            &topic.AdminBanTopicMembersParams{
                Body:      ..., // *chatclientmodels.ModelsBanTopicMemberParam
                Namespace: ..., // string
                Topic:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'topic'
    name: 'adminChannelTopicInfo'
    summary: 'admin get channel topic detail'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        topicService := chat.TopicService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := topicService.AdminChannelTopicInfoShort(
            &topic.AdminChannelTopicInfoParams{
                Namespace: ..., // string
                Topic:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'topic'
    name: 'adminChannelTopicList'
    summary: 'admin get list of channel topic'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        topicService := chat.TopicService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := topicService.AdminChannelTopicListShort(
            &topic.AdminChannelTopicListParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                TopicName: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'topic'
    name: 'adminChannelTopicSummary'
    summary: 'admin get channel topic summary'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        topicService := chat.TopicService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := topicService.AdminChannelTopicSummaryShort(
            &topic.AdminChannelTopicSummaryParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'topic'
    name: 'adminChatHistory'
    summary: 'admin get chat history'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        topicService := chat.TopicService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := topicService.AdminChatHistoryShort(
            &topic.AdminChatHistoryParams{
                Namespace:      ..., // string
                ChatID:         ..., // []string
                EndCreatedAt:   ..., // *int64
                Keyword:        ..., // *string
                Limit:          ..., // *int64
                Offset:         ..., // *int64
                Order:          ..., // *string
                SenderUserID:   ..., // *string
                ShardID:        ..., // *string
                StartCreatedAt: ..., // *int64
                Topic:          ..., // []string
                Unfiltered:     ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'topic'
    name: 'adminCreateNamespaceTopic'
    summary: 'Use to create namespace group. Message that send to this group send to connected user in a namespace'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        topicService := chat.TopicService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := topicService.AdminCreateNamespaceTopicShort(
            &topic.AdminCreateNamespaceTopicParams{
                Body:      ..., // *chatclientmodels.APICreateNamespaceTopicParams
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'topic'
    name: 'adminCreateTopic'
    summary: 'Use to create group. Only group admin can use this operation'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        topicService := chat.TopicService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := topicService.AdminCreateTopicShort(
            &topic.AdminCreateTopicParams{
                Body:      ..., // *chatclientmodels.APICreateTopicParams
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'topic'
    name: 'adminDeleteChat'
    summary: 'admins delete chat'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        topicService := chat.TopicService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := topicService.AdminDeleteChatShort(
            &topic.AdminDeleteChatParams{
                ChatID:    ..., // string
                Namespace: ..., // string
                Topic:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'moderation'
    name: 'adminDeleteChatSnapshot'
    summary: 'admin delete chat snapshot'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        moderationService := chat.ModerationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := moderationService.AdminDeleteChatSnapshotShort(
            &moderation.AdminDeleteChatSnapshotParams{
                ChatID:    ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'inbox'
    name: 'adminDeleteInboxCategory'
    summary: 'admin delete inbox category'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        inboxService := chat.InboxService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := inboxService.AdminDeleteInboxCategoryShort(
            &inbox.AdminDeleteInboxCategoryParams{
                Category:  ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'inbox'
    name: 'adminDeleteInboxMessage'
    summary: 'admin delete inbox message'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        inboxService := chat.InboxService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := inboxService.AdminDeleteInboxMessageShort(
            &inbox.AdminDeleteInboxMessageParams{
                MessageID: ..., // string
                Namespace: ..., // string
                Force:     ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'topic'
    name: 'adminDeleteTopic'
    summary: 'Use to delete group. Only group admin can use this operation'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        topicService := chat.TopicService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := topicService.AdminDeleteTopicShort(
            &topic.AdminDeleteTopicParams{
                Namespace: ..., // string
                Topic:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'topic'
    name: 'adminFilterChatMessage'
    summary: 'Return filtered chat message'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        topicService := chat.TopicService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := topicService.AdminFilterChatMessageShort(
            &topic.AdminFilterChatMessageParams{
                Body:      ..., // *chatclientmodels.ModelsMessageRequest
                Namespace: ..., // string
                Detail:    ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'config'
    name: 'adminGetAllConfigV1'
    summary: 'admin get all namespaces config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        configService := chat.ConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configService.AdminGetAllConfigV1Short(
            &config.AdminGetAllConfigV1Params{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'inbox'
    name: 'adminGetCategorySchema'
    summary: 'admin get category schema'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        inboxService := chat.InboxService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := inboxService.AdminGetCategorySchemaShort(
            &inbox.AdminGetCategorySchemaParams{
                Category:  ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'moderation'
    name: 'adminGetChatSnapshot'
    summary: 'admin get chat snapshot'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        moderationService := chat.ModerationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := moderationService.AdminGetChatSnapshotShort(
            &moderation.AdminGetChatSnapshotParams{
                ChatID:    ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'config'
    name: 'adminGetConfigV1'
    summary: 'admin get namespace config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        configService := chat.ConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configService.AdminGetConfigV1Short(
            &config.AdminGetConfigV1Params{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'inbox'
    name: 'adminGetInboxCategories'
    summary: 'admin get inbox categories'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        inboxService := chat.InboxService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := inboxService.AdminGetInboxCategoriesShort(
            &inbox.AdminGetInboxCategoriesParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'inbox'
    name: 'adminGetInboxMessages'
    summary: 'admin get inbox messages'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        inboxService := chat.InboxService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := inboxService.AdminGetInboxMessagesShort(
            &inbox.AdminGetInboxMessagesParams{
                Namespace:      ..., // string
                ActiveOnly:     ..., // *bool
                EndCreatedAt:   ..., // *int64
                Limit:          ..., // *int64
                MessageID:      ..., // []string
                Offset:         ..., // *int64
                Order:          ..., // *string
                Scope:          ..., // *string
                StartCreatedAt: ..., // *int64
                Status:         ..., // *string
                Transient:      ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'inbox'
    name: 'adminGetInboxStats'
    summary: 'admin get inbox stats'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        inboxService := chat.InboxService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := inboxService.AdminGetInboxStatsShort(
            &inbox.AdminGetInboxStatsParams{
                Namespace: ..., // string
                MessageID: ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'inbox'
    name: 'adminGetInboxUsers'
    summary: 'admin get inbox users'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        inboxService := chat.InboxService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := inboxService.AdminGetInboxUsersShort(
            &inbox.AdminGetInboxUsersParams{
                Inbox:     ..., // string
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                Status:    ..., // *string
                UserID:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'config'
    name: 'adminGetLogConfig'
    summary: 'Get Log Configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        configService := chat.ConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configService.AdminGetLogConfigShort(
            &config.AdminGetLogConfigParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'inbox'
    name: 'adminListKafkaTopic'
    summary: 'admin get list kafka topic'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        inboxService := chat.InboxService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := inboxService.AdminListKafkaTopicShort(
            &inbox.AdminListKafkaTopicParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'config'
    name: 'adminPatchUpdateLogConfig'
    summary: 'Patch Update Log Configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        configService := chat.ConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configService.AdminPatchUpdateLogConfigShort(
            &config.AdminPatchUpdateLogConfigParams{
                Body: ..., // *chatclientmodels.LogconfigConfiguration
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'profanity'
    name: 'adminProfanityCreate'
    summary: 'admin insert new profanity words'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        profanityService := chat.ProfanityService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := profanityService.AdminProfanityCreateShort(
            &profanity.AdminProfanityCreateParams{
                Body:      ..., // *chatclientmodels.ModelsDictionaryInsertRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'profanity'
    name: 'adminProfanityCreateBulk'
    summary: 'admin bulk insert new profanity words'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        profanityService := chat.ProfanityService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := profanityService.AdminProfanityCreateBulkShort(
            &profanity.AdminProfanityCreateBulkParams{
                Body:      ..., // *chatclientmodels.ModelsDictionaryInsertBulkRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'profanity'
    name: 'adminProfanityDelete'
    summary: 'admin delete profanity words'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        profanityService := chat.ProfanityService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := profanityService.AdminProfanityDeleteShort(
            &profanity.AdminProfanityDeleteParams{
                ID:        ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'profanity'
    name: 'adminProfanityExport'
    summary: 'admin export profanity words'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        profanityService := chat.ProfanityService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := profanityService.AdminProfanityExportShort(
            &profanity.AdminProfanityExportParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'profanity'
    name: 'adminProfanityGroup'
    summary: 'admin get profanity words group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        profanityService := chat.ProfanityService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := profanityService.AdminProfanityGroupShort(
            &profanity.AdminProfanityGroupParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'profanity'
    name: 'adminProfanityImport'
    summary: 'admin import profanity words'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        profanityService := chat.ProfanityService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := profanityService.AdminProfanityImportShort(
            &profanity.AdminProfanityImportParams{
                File:       ..., // []byte
                Namespace:  ..., // string
                Action:     ..., // *string
                ShowResult: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'profanity'
    name: 'adminProfanityQuery'
    summary: 'admin query profanity words'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        profanityService := chat.ProfanityService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := profanityService.AdminProfanityQueryShort(
            &profanity.AdminProfanityQueryParams{
                Namespace:       ..., // string
                FilterMask:      ..., // *string
                IncludeChildren: ..., // *bool
                Limit:           ..., // *int64
                Offset:          ..., // *int64
                ParentID:        ..., // *string
                StartWith:       ..., // *string
                WordType:        ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'profanity'
    name: 'adminProfanityUpdate'
    summary: 'admin update profanity word'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        profanityService := chat.ProfanityService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := profanityService.AdminProfanityUpdateShort(
            &profanity.AdminProfanityUpdateParams{
                Body:      ..., // *chatclientmodels.ModelsDictionaryUpdateRequest
                ID:        ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'topic'
    name: 'adminQueryTopic'
    summary: 'admin query topics'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        topicService := chat.TopicService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := topicService.AdminQueryTopicShort(
            &topic.AdminQueryTopicParams{
                Namespace:          ..., // string
                IncludeMembers:     ..., // *bool
                IncludePastMembers: ..., // *bool
                IncludePastTopics:  ..., // *bool
                Limit:              ..., // *int64
                Offset:             ..., // *int64
                Topic:              ..., // []string
                TopicSubType:       ..., // *string
                TopicType:          ..., // *string
                UserID:             ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'topic'
    name: 'adminQueryTopicLog'
    summary: 'admin query topic log'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        topicService := chat.TopicService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := topicService.AdminQueryTopicLogShort(
            &topic.AdminQueryTopicLogParams{
                Namespace:      ..., // string
                EndCreatedAt:   ..., // *int64
                Limit:          ..., // *int64
                Offset:         ..., // *int64
                SenderUserID:   ..., // *string
                StartCreatedAt: ..., // *int64
                TopicID:        ..., // *string
                TopicIds:       ..., // []string
                UserID:         ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'topic'
    name: 'adminQueryUsersTopic'
    summary: 'admin query user''s topics'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        topicService := chat.TopicService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := topicService.AdminQueryUsersTopicShort(
            &topic.AdminQueryUsersTopicParams{
                Namespace:         ..., // string
                UserID:            ..., // string
                IncludePastTopics: ..., // *bool
                Limit:             ..., // *int64
                Offset:            ..., // *int64
                TopicSubType:      ..., // *string
                TopicType:         ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'topic'
    name: 'adminRemoveTopicMember'
    summary: 'admin remove user from topic'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        topicService := chat.TopicService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := topicService.AdminRemoveTopicMemberShort(
            &topic.AdminRemoveTopicMemberParams{
                Namespace: ..., // string
                Topic:     ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'inbox'
    name: 'adminSaveInboxMessage'
    summary: 'admin save inbox message'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        inboxService := chat.InboxService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := inboxService.AdminSaveInboxMessageShort(
            &inbox.AdminSaveInboxMessageParams{
                Body:      ..., // *chatclientmodels.ModelsSaveInboxMessageRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'topic'
    name: 'adminSendChat'
    summary: 'admins send chat to group topic'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        topicService := chat.TopicService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := topicService.AdminSendChatShort(
            &topic.AdminSendChatParams{
                Body:      ..., // *chatclientmodels.APISendChatParams
                Namespace: ..., // string
                Topic:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'inbox'
    name: 'adminSendInboxMessage'
    summary: 'admin send inbox message'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        inboxService := chat.InboxService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := inboxService.AdminSendInboxMessageShort(
            &inbox.AdminSendInboxMessageParams{
                Body:      ..., // chatclientmodels.ModelsSendInboxMessageRequest
                MessageID: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'topic'
    name: 'adminTopicChatHistory'
    summary: 'admin get chat history'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        topicService := chat.TopicService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := topicService.AdminTopicChatHistoryShort(
            &topic.AdminTopicChatHistoryParams{
                Namespace:      ..., // string
                Topic:          ..., // string
                EndCreatedAt:   ..., // *int64
                Keyword:        ..., // *string
                Limit:          ..., // *int64
                Offset:         ..., // *int64
                Order:          ..., // *string
                SenderUserID:   ..., // *string
                ShardID:        ..., // *string
                StartCreatedAt: ..., // *int64
                Unfiltered:     ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'topic'
    name: 'adminTopicList'
    summary: 'admin get list of topic'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        topicService := chat.TopicService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := topicService.AdminTopicListShort(
            &topic.AdminTopicListParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                TopicType: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'topic'
    name: 'adminTopicMembers'
    summary: 'admin get topic member'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        topicService := chat.TopicService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := topicService.AdminTopicMembersShort(
            &topic.AdminTopicMembersParams{
                Namespace:   ..., // string
                Topic:       ..., // string
                IsBanned:    ..., // *bool
                IsModerator: ..., // *bool
                Limit:       ..., // *int64
                Offset:      ..., // *int64
                ShardID:     ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'topic'
    name: 'adminTopicShards'
    summary: 'admin get shard list of topic'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        topicService := chat.TopicService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := topicService.AdminTopicShardsShort(
            &topic.AdminTopicShardsParams{
                Namespace: ..., // string
                Topic:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'topic'
    name: 'adminUnbanTopicMembers'
    summary: 'admins unban user in group topic'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        topicService := chat.TopicService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := topicService.AdminUnbanTopicMembersShort(
            &topic.AdminUnbanTopicMembersParams{
                Body:      ..., // *chatclientmodels.ModelsUnbanTopicMemberParam
                Namespace: ..., // string
                Topic:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'inbox'
    name: 'adminUnsendInboxMessage'
    summary: 'admin unsend inbox message'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        inboxService := chat.InboxService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := inboxService.AdminUnsendInboxMessageShort(
            &inbox.AdminUnsendInboxMessageParams{
                Body:      ..., // *chatclientmodels.ModelsUnsendInboxMessageRequest
                Inbox:     ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'config'
    name: 'adminUpdateConfigV1'
    summary: 'admin update namespace config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        configService := chat.ConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configService.AdminUpdateConfigV1Short(
            &config.AdminUpdateConfigV1Params{
                Body:      ..., // *chatclientmodels.ModelsConfigResponse
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'inbox'
    name: 'adminUpdateInboxCategory'
    summary: 'admin update inbox category'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        inboxService := chat.InboxService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := inboxService.AdminUpdateInboxCategoryShort(
            &inbox.AdminUpdateInboxCategoryParams{
                Body:      ..., // *chatclientmodels.ModelsUpdateInboxCategoryRequest
                Category:  ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'inbox'
    name: 'adminUpdateInboxMessage'
    summary: 'admin update inbox message'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        inboxService := chat.InboxService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := inboxService.AdminUpdateInboxMessageShort(
            &inbox.AdminUpdateInboxMessageParams{
                Body:      ..., // *chatclientmodels.ModelsUpdateInboxMessageRequest
                MessageID: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'topic'
    name: 'adminUpdateTopic'
    summary: 'Use to update group information. Only group admin can use this operation'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        topicService := chat.TopicService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := topicService.AdminUpdateTopicShort(
            &topic.AdminUpdateTopicParams{
                Body:      ..., // *chatclientmodels.APIUpdateTopicParams
                Namespace: ..., // string
                Topic:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'config'
    name: 'ExportConfig'
    summary: 'Export chat config to a json file.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        configService := chat.ConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configService.ExportConfigShort(
            &config.ExportConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'config'
    name: 'ImportConfig'
    summary: 'Import chat config from a json file.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        configService := chat.ConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configService.ImportConfigShort(
            &config.ImportConfigParams{
                File:      ..., // *[]byte
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'topic'
    name: 'publicBanTopicMembers'
    summary: 'public ban topic members in a group topic'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        topicService := chat.TopicService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := topicService.PublicBanTopicMembersShort(
            &topic.PublicBanTopicMembersParams{
                Body:      ..., // *chatclientmodels.ModelsPublicBanTopicMembersRequest
                Namespace: ..., // string
                Topic:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'topic'
    name: 'publicChatHistory'
    summary: 'public get chat history'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        topicService := chat.TopicService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := topicService.PublicChatHistoryShort(
            &topic.PublicChatHistoryParams{
                Namespace:      ..., // string
                Topic:          ..., // string
                Limit:          ..., // *int64
                Order:          ..., // *string
                StartCreatedAt: ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'topic'
    name: 'publicDeleteChat'
    summary: 'public delete chat'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        topicService := chat.TopicService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := topicService.PublicDeleteChatShort(
            &topic.PublicDeleteChatParams{
                ChatID:    ..., // string
                Namespace: ..., // string
                Topic:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'moderation'
    name: 'publicGetChatSnapshot'
    summary: 'public get chat snapshot'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        moderationService := chat.ModerationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := moderationService.PublicGetChatSnapshotShort(
            &moderation.PublicGetChatSnapshotParams{
                ChatID:    ..., // string
                Namespace: ..., // string
                Topic:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'config'
    name: 'publicGetConfigV1'
    summary: 'public get namespace config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        configService := chat.ConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configService.PublicGetConfigV1Short(
            &config.PublicGetConfigV1Params{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: ''
    name: 'publicGetMessages'
    summary: 'get service messages'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        Service := chat.EmptyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := Service.PublicGetMessagesShort(
            &.PublicGetMessagesParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'topic'
    name: 'publicGetMutedTopics'
    summary: 'public get muted at topics'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        topicService := chat.TopicService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := topicService.PublicGetMutedTopicsShort(
            &topic.PublicGetMutedTopicsParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'topic'
    name: 'publicMuteUser'
    summary: 'public mute user in a topic'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        topicService := chat.TopicService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := topicService.PublicMuteUserShort(
            &topic.PublicMuteUserParams{
                Body:      ..., // *chatclientmodels.APIMuteUserRequest
                Namespace: ..., // string
                Topic:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'topic'
    name: 'publicTopicList'
    summary: 'public get list of topic'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        topicService := chat.TopicService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := topicService.PublicTopicListShort(
            &topic.PublicTopicListParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                TopicType: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'topic'
    name: 'publicUnbanTopicMembers'
    summary: 'public unban topic members in a group topic'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        topicService := chat.TopicService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := topicService.PublicUnbanTopicMembersShort(
            &topic.PublicUnbanTopicMembersParams{
                Body:      ..., // *chatclientmodels.ModelsPublicUnbanTopicMembersRequest
                Namespace: ..., // string
                Topic:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'topic'
    name: 'publicUnmuteUser'
    summary: 'public unmute user in a topic'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewChatClient(configRepo)
        topicService := chat.TopicService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := topicService.PublicUnmuteUserShort(
            &topic.PublicUnmuteUserParams{
                Body:      ..., // *chatclientmodels.APIUnmuteUserRequest
                Namespace: ..., // string
                Topic:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }
# end chat
# begin cloudsave
  - ns: 'AdminRecord'
    name: 'adminBulkGetAdminGameRecordV1'
    summary: 'Bulk get admin game records'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminRecordService := cloudsave.AdminRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminRecordService.AdminBulkGetAdminGameRecordV1Short(
            &admin_record.AdminBulkGetAdminGameRecordV1Params{
                Body:      ..., // *cloudsaveclientmodels.ModelsBulkGetAdminGameRecordRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminRecord'
    name: 'adminBulkGetAdminPlayerRecordV1'
    summary: 'Bulk get admin player records'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminRecordService := cloudsave.AdminRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminRecordService.AdminBulkGetAdminPlayerRecordV1Short(
            &admin_record.AdminBulkGetAdminPlayerRecordV1Params{
                Body:      ..., // *cloudsaveclientmodels.ModelsBulkGetAdminPlayerRecordRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminPlayerRecord'
    name: 'adminBulkGetPlayerRecordsByUserIDsHandlerV1'
    summary: 'Bulk get player records by multiple user ids'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminPlayerRecordService := cloudsave.AdminPlayerRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminPlayerRecordService.AdminBulkGetPlayerRecordsByUserIDsHandlerV1Short(
            &admin_player_record.AdminBulkGetPlayerRecordsByUserIDsHandlerV1Params{
                Body:      ..., // *cloudsaveclientmodels.ModelsBulkUserIDsRequest
                Key:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminPlayerRecord'
    name: 'adminBulkPutPlayerRecordsByKeyHandlerV1'
    summary: 'Bulk update player records by key'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminPlayerRecordService := cloudsave.AdminPlayerRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminPlayerRecordService.AdminBulkPutPlayerRecordsByKeyHandlerV1Short(
            &admin_player_record.AdminBulkPutPlayerRecordsByKeyHandlerV1Params{
                Body:      ..., // *cloudsaveclientmodels.ModelsBulkUpdatePlayerRecordsByKeyRequest
                Key:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminRecord'
    name: 'adminDeleteAdminGameRecordV1'
    summary: 'Delete admin game record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminRecordService := cloudsave.AdminRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminRecordService.AdminDeleteAdminGameRecordV1Short(
            &admin_record.AdminDeleteAdminGameRecordV1Params{
                Key:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminRecord'
    name: 'adminDeleteAdminPlayerRecordV1'
    summary: 'Delete admin player record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminRecordService := cloudsave.AdminRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminRecordService.AdminDeleteAdminPlayerRecordV1Short(
            &admin_record.AdminDeleteAdminPlayerRecordV1Params{
                Key:       ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminGameBinaryRecord'
    name: 'adminDeleteGameBinaryRecordV1'
    summary: 'Delete game binary record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminGameBinaryRecordService := cloudsave.AdminGameBinaryRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminGameBinaryRecordService.AdminDeleteGameBinaryRecordV1Short(
            &admin_game_binary_record.AdminDeleteGameBinaryRecordV1Params{
                Key:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminGameRecord'
    name: 'adminDeleteGameRecordHandlerV1'
    summary: 'Delete game record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminGameRecordService := cloudsave.AdminGameRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminGameRecordService.AdminDeleteGameRecordHandlerV1Short(
            &admin_game_record.AdminDeleteGameRecordHandlerV1Params{
                Key:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminPlayerBinaryRecord'
    name: 'adminDeletePlayerBinaryRecordV1'
    summary: 'Delete player binary record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminPlayerBinaryRecordService := cloudsave.AdminPlayerBinaryRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminPlayerBinaryRecordService.AdminDeletePlayerBinaryRecordV1Short(
            &admin_player_binary_record.AdminDeletePlayerBinaryRecordV1Params{
                Key:       ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminPlayerRecord'
    name: 'adminDeletePlayerPublicRecordHandlerV1'
    summary: 'Delete player public record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminPlayerRecordService := cloudsave.AdminPlayerRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminPlayerRecordService.AdminDeletePlayerPublicRecordHandlerV1Short(
            &admin_player_record.AdminDeletePlayerPublicRecordHandlerV1Params{
                Key:       ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminPlayerRecord'
    name: 'adminDeletePlayerRecordHandlerV1'
    summary: 'Delete player record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminPlayerRecordService := cloudsave.AdminPlayerRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminPlayerRecordService.AdminDeletePlayerRecordHandlerV1Short(
            &admin_player_record.AdminDeletePlayerRecordHandlerV1Params{
                Key:       ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminTags'
    name: 'adminDeleteTagHandlerV1'
    summary: 'Delete a tag'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminTagsService := cloudsave.AdminTagsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminTagsService.AdminDeleteTagHandlerV1Short(
            &admin_tags.AdminDeleteTagHandlerV1Params{
                Namespace: ..., // string
                Tag:       ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminRecord'
    name: 'adminGetAdminGameRecordV1'
    summary: 'Get admin game record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminRecordService := cloudsave.AdminRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminRecordService.AdminGetAdminGameRecordV1Short(
            &admin_record.AdminGetAdminGameRecordV1Params{
                Key:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminRecord'
    name: 'adminGetAdminPlayerRecordV1'
    summary: 'Get admin player record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminRecordService := cloudsave.AdminRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminRecordService.AdminGetAdminPlayerRecordV1Short(
            &admin_record.AdminGetAdminPlayerRecordV1Params{
                Key:       ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminGameBinaryRecord'
    name: 'adminGetGameBinaryRecordV1'
    summary: 'Get game binary record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminGameBinaryRecordService := cloudsave.AdminGameBinaryRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminGameBinaryRecordService.AdminGetGameBinaryRecordV1Short(
            &admin_game_binary_record.AdminGetGameBinaryRecordV1Params{
                Key:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminGameRecord'
    name: 'adminGetGameRecordHandlerV1'
    summary: 'Get game record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminGameRecordService := cloudsave.AdminGameRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminGameRecordService.AdminGetGameRecordHandlerV1Short(
            &admin_game_record.AdminGetGameRecordHandlerV1Params{
                Key:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminPlayerBinaryRecord'
    name: 'adminGetPlayerBinaryRecordV1'
    summary: 'Get player binary record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminPlayerBinaryRecordService := cloudsave.AdminPlayerBinaryRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminPlayerBinaryRecordService.AdminGetPlayerBinaryRecordV1Short(
            &admin_player_binary_record.AdminGetPlayerBinaryRecordV1Params{
                Key:       ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminPlayerRecord'
    name: 'adminGetPlayerPublicRecordHandlerV1'
    summary: 'Get player public record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminPlayerRecordService := cloudsave.AdminPlayerRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminPlayerRecordService.AdminGetPlayerPublicRecordHandlerV1Short(
            &admin_player_record.AdminGetPlayerPublicRecordHandlerV1Params{
                Key:       ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminPlayerRecord'
    name: 'adminGetPlayerRecordHandlerV1'
    summary: 'Get player record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminPlayerRecordService := cloudsave.AdminPlayerRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminPlayerRecordService.AdminGetPlayerRecordHandlerV1Short(
            &admin_player_record.AdminGetPlayerRecordHandlerV1Params{
                Key:       ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminPlayerRecord'
    name: 'adminGetPlayerRecordsHandlerV1'
    summary: 'Bulk get player records by multiple record keys'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminPlayerRecordService := cloudsave.AdminPlayerRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminPlayerRecordService.AdminGetPlayerRecordsHandlerV1Short(
            &admin_player_record.AdminGetPlayerRecordsHandlerV1Params{
                Body:      ..., // *cloudsaveclientmodels.ModelsBulkGetPlayerRecordsRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminPlayerRecord'
    name: 'adminGetPlayerRecordSizeHandlerV1'
    summary: 'Get player record size'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminPlayerRecordService := cloudsave.AdminPlayerRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminPlayerRecordService.AdminGetPlayerRecordSizeHandlerV1Short(
            &admin_player_record.AdminGetPlayerRecordSizeHandlerV1Params{
                Key:       ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminRecord'
    name: 'adminListAdminGameRecordV1'
    summary: 'List key of admin game record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminRecordService := cloudsave.AdminRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminRecordService.AdminListAdminGameRecordV1Short(
            &admin_record.AdminListAdminGameRecordV1Params{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                Query:     ..., // *string
                Tags:      ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminRecord'
    name: 'adminListAdminUserRecordsV1'
    summary: 'List key of admin player record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminRecordService := cloudsave.AdminRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminRecordService.AdminListAdminUserRecordsV1Short(
            &admin_record.AdminListAdminUserRecordsV1Params{
                Namespace: ..., // string
                UserID:    ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                Query:     ..., // *string
                Tags:      ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminGameBinaryRecord'
    name: 'adminListGameBinaryRecordsV1'
    summary: 'Query game binary records'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminGameBinaryRecordService := cloudsave.AdminGameBinaryRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminGameBinaryRecordService.AdminListGameBinaryRecordsV1Short(
            &admin_game_binary_record.AdminListGameBinaryRecordsV1Params{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                Query:     ..., // *string
                Tags:      ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminPlayerBinaryRecord'
    name: 'adminListPlayerBinaryRecordsV1'
    summary: 'Query player binary records'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminPlayerBinaryRecordService := cloudsave.AdminPlayerBinaryRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminPlayerBinaryRecordService.AdminListPlayerBinaryRecordsV1Short(
            &admin_player_binary_record.AdminListPlayerBinaryRecordsV1Params{
                Namespace: ..., // string
                UserID:    ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                Query:     ..., // *string
                Tags:      ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminTags'
    name: 'adminListTagsHandlerV1'
    summary: 'List tags'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminTagsService := cloudsave.AdminTagsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminTagsService.AdminListTagsHandlerV1Short(
            &admin_tags.AdminListTagsHandlerV1Params{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminRecord'
    name: 'adminPostAdminGameRecordV1'
    summary: 'Create or append admin game record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminRecordService := cloudsave.AdminRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminRecordService.AdminPostAdminGameRecordV1Short(
            &admin_record.AdminPostAdminGameRecordV1Params{
                Body:      ..., // cloudsaveclientmodels.ModelsAdminGameRecordRequest
                Key:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminGameBinaryRecord'
    name: 'adminPostGameBinaryPresignedURLV1'
    summary: 'Request presigned URL for upload game binary records'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminGameBinaryRecordService := cloudsave.AdminGameBinaryRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminGameBinaryRecordService.AdminPostGameBinaryPresignedURLV1Short(
            &admin_game_binary_record.AdminPostGameBinaryPresignedURLV1Params{
                Body:      ..., // *cloudsaveclientmodels.ModelsUploadBinaryRecordRequest
                Key:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminGameBinaryRecord'
    name: 'adminPostGameBinaryRecordV1'
    summary: 'Create game binary record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminGameBinaryRecordService := cloudsave.AdminGameBinaryRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminGameBinaryRecordService.AdminPostGameBinaryRecordV1Short(
            &admin_game_binary_record.AdminPostGameBinaryRecordV1Params{
                Body:      ..., // *cloudsaveclientmodels.ModelsGameBinaryRecordCreate
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminGameRecord'
    name: 'adminPostGameRecordHandlerV1'
    summary: 'Create or append game record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminGameRecordService := cloudsave.AdminGameRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminGameRecordService.AdminPostGameRecordHandlerV1Short(
            &admin_game_record.AdminPostGameRecordHandlerV1Params{
                Body:      ..., // cloudsaveclientmodels.ModelsGameRecordRequest
                Key:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminRecord'
    name: 'adminPostPlayerAdminRecordV1'
    summary: 'Create or append admin player record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminRecordService := cloudsave.AdminRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminRecordService.AdminPostPlayerAdminRecordV1Short(
            &admin_record.AdminPostPlayerAdminRecordV1Params{
                Body:      ..., // cloudsaveclientmodels.ModelsAdminPlayerRecordRequest
                Key:       ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminPlayerBinaryRecord'
    name: 'adminPostPlayerBinaryPresignedURLV1'
    summary: 'Request presigned URL for upload player binary records'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminPlayerBinaryRecordService := cloudsave.AdminPlayerBinaryRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminPlayerBinaryRecordService.AdminPostPlayerBinaryPresignedURLV1Short(
            &admin_player_binary_record.AdminPostPlayerBinaryPresignedURLV1Params{
                Body:      ..., // *cloudsaveclientmodels.ModelsUploadBinaryRecordRequest
                Key:       ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminPlayerBinaryRecord'
    name: 'adminPostPlayerBinaryRecordV1'
    summary: 'Create player binary record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminPlayerBinaryRecordService := cloudsave.AdminPlayerBinaryRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminPlayerBinaryRecordService.AdminPostPlayerBinaryRecordV1Short(
            &admin_player_binary_record.AdminPostPlayerBinaryRecordV1Params{
                Body:      ..., // *cloudsaveclientmodels.ModelsPlayerBinaryRecordCreate
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminPlayerRecord'
    name: 'adminPostPlayerPublicRecordHandlerV1'
    summary: 'Create or append player public record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminPlayerRecordService := cloudsave.AdminPlayerRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminPlayerRecordService.AdminPostPlayerPublicRecordHandlerV1Short(
            &admin_player_record.AdminPostPlayerPublicRecordHandlerV1Params{
                Body:      ..., // cloudsaveclientmodels.ModelsPlayerRecordRequest
                Key:       ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminPlayerRecord'
    name: 'adminPostPlayerRecordHandlerV1'
    summary: 'Create or append player record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminPlayerRecordService := cloudsave.AdminPlayerRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminPlayerRecordService.AdminPostPlayerRecordHandlerV1Short(
            &admin_player_record.AdminPostPlayerRecordHandlerV1Params{
                Body:      ..., // cloudsaveclientmodels.ModelsPlayerRecordRequest
                Key:       ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminTags'
    name: 'adminPostTagHandlerV1'
    summary: 'Create a tag'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminTagsService := cloudsave.AdminTagsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminTagsService.AdminPostTagHandlerV1Short(
            &admin_tags.AdminPostTagHandlerV1Params{
                Body:      ..., // *cloudsaveclientmodels.ModelsTagRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminConcurrentRecord'
    name: 'adminPutAdminGameRecordConcurrentHandlerV1'
    summary: 'Create or replace admin game record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminConcurrentRecordService := cloudsave.AdminConcurrentRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminConcurrentRecordService.AdminPutAdminGameRecordConcurrentHandlerV1Short(
            &admin_concurrent_record.AdminPutAdminGameRecordConcurrentHandlerV1Params{
                Body:      ..., // *cloudsaveclientmodels.ModelsAdminGameConcurrentRecordRequest
                Key:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminRecord'
    name: 'adminPutAdminGameRecordV1'
    summary: 'Create or replace admin game record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminRecordService := cloudsave.AdminRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminRecordService.AdminPutAdminGameRecordV1Short(
            &admin_record.AdminPutAdminGameRecordV1Params{
                Body:      ..., // cloudsaveclientmodels.ModelsAdminGameRecordRequest
                Key:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminConcurrentRecord'
    name: 'adminPutAdminPlayerRecordConcurrentHandlerV1'
    summary: 'Create or replace admin player record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminConcurrentRecordService := cloudsave.AdminConcurrentRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminConcurrentRecordService.AdminPutAdminPlayerRecordConcurrentHandlerV1Short(
            &admin_concurrent_record.AdminPutAdminPlayerRecordConcurrentHandlerV1Params{
                Body:         ..., // *cloudsaveclientmodels.ModelsAdminPlayerConcurrentRecordRequest
                Key:          ..., // string
                Namespace:    ..., // string
                UserID:       ..., // string
                ResponseBody: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminRecord'
    name: 'adminPutAdminPlayerRecordV1'
    summary: 'Create or replace admin player record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminRecordService := cloudsave.AdminRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminRecordService.AdminPutAdminPlayerRecordV1Short(
            &admin_record.AdminPutAdminPlayerRecordV1Params{
                Body:      ..., // cloudsaveclientmodels.ModelsAdminPlayerRecordRequest
                Key:       ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminGameBinaryRecord'
    name: 'adminPutGameBinaryRecordV1'
    summary: 'Update game binary record file'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminGameBinaryRecordService := cloudsave.AdminGameBinaryRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminGameBinaryRecordService.AdminPutGameBinaryRecordV1Short(
            &admin_game_binary_record.AdminPutGameBinaryRecordV1Params{
                Body:      ..., // *cloudsaveclientmodels.ModelsBinaryRecordRequest
                Key:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminGameBinaryRecord'
    name: 'adminPutGameBinaryRecorMetadataV1'
    summary: 'Update game binary record metadata'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminGameBinaryRecordService := cloudsave.AdminGameBinaryRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminGameBinaryRecordService.AdminPutGameBinaryRecorMetadataV1Short(
            &admin_game_binary_record.AdminPutGameBinaryRecorMetadataV1Params{
                Body:      ..., // *cloudsaveclientmodels.ModelsGameBinaryRecordMetadataRequest
                Key:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminConcurrentRecord'
    name: 'adminPutGameRecordConcurrentHandlerV1'
    summary: 'Create or replace game record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminConcurrentRecordService := cloudsave.AdminConcurrentRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminConcurrentRecordService.AdminPutGameRecordConcurrentHandlerV1Short(
            &admin_concurrent_record.AdminPutGameRecordConcurrentHandlerV1Params{
                Body:      ..., // *cloudsaveclientmodels.ModelsAdminConcurrentRecordRequest
                Key:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminGameRecord'
    name: 'adminPutGameRecordHandlerV1'
    summary: 'Create or replace game record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminGameRecordService := cloudsave.AdminGameRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminGameRecordService.AdminPutGameRecordHandlerV1Short(
            &admin_game_record.AdminPutGameRecordHandlerV1Params{
                Body:      ..., // cloudsaveclientmodels.ModelsGameRecordRequest
                Key:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminPlayerBinaryRecord'
    name: 'adminPutPlayerBinaryRecordV1'
    summary: 'Update player binary record file'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminPlayerBinaryRecordService := cloudsave.AdminPlayerBinaryRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminPlayerBinaryRecordService.AdminPutPlayerBinaryRecordV1Short(
            &admin_player_binary_record.AdminPutPlayerBinaryRecordV1Params{
                Body:      ..., // *cloudsaveclientmodels.ModelsBinaryRecordRequest
                Key:       ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminPlayerBinaryRecord'
    name: 'adminPutPlayerBinaryRecorMetadataV1'
    summary: 'Update player binary record metadata'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminPlayerBinaryRecordService := cloudsave.AdminPlayerBinaryRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminPlayerBinaryRecordService.AdminPutPlayerBinaryRecorMetadataV1Short(
            &admin_player_binary_record.AdminPutPlayerBinaryRecorMetadataV1Params{
                Body:      ..., // *cloudsaveclientmodels.ModelsPlayerBinaryRecordMetadataRequest
                Key:       ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminConcurrentRecord'
    name: 'adminPutPlayerPublicRecordConcurrentHandlerV1'
    summary: 'Create or replace player public record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminConcurrentRecordService := cloudsave.AdminConcurrentRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminConcurrentRecordService.AdminPutPlayerPublicRecordConcurrentHandlerV1Short(
            &admin_concurrent_record.AdminPutPlayerPublicRecordConcurrentHandlerV1Params{
                Body:         ..., // *cloudsaveclientmodels.ModelsAdminConcurrentRecordRequest
                Key:          ..., // string
                Namespace:    ..., // string
                UserID:       ..., // string
                ResponseBody: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminPlayerRecord'
    name: 'adminPutPlayerPublicRecordHandlerV1'
    summary: 'Create or replace player public record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminPlayerRecordService := cloudsave.AdminPlayerRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminPlayerRecordService.AdminPutPlayerPublicRecordHandlerV1Short(
            &admin_player_record.AdminPutPlayerPublicRecordHandlerV1Params{
                Body:      ..., // cloudsaveclientmodels.ModelsPlayerRecordRequest
                Key:       ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminConcurrentRecord'
    name: 'adminPutPlayerRecordConcurrentHandlerV1'
    summary: 'Create or replace player private record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminConcurrentRecordService := cloudsave.AdminConcurrentRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminConcurrentRecordService.AdminPutPlayerRecordConcurrentHandlerV1Short(
            &admin_concurrent_record.AdminPutPlayerRecordConcurrentHandlerV1Params{
                Body:         ..., // *cloudsaveclientmodels.ModelsAdminConcurrentRecordRequest
                Key:          ..., // string
                Namespace:    ..., // string
                UserID:       ..., // string
                ResponseBody: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminPlayerRecord'
    name: 'adminPutPlayerRecordHandlerV1'
    summary: 'Create or replace player record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminPlayerRecordService := cloudsave.AdminPlayerRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminPlayerRecordService.AdminPutPlayerRecordHandlerV1Short(
            &admin_player_record.AdminPutPlayerRecordHandlerV1Params{
                Body:      ..., // cloudsaveclientmodels.ModelsPlayerRecordRequest
                Key:       ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminPlayerRecord'
    name: 'adminPutPlayerRecordsHandlerV1'
    summary: 'Bulk update player records'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminPlayerRecordService := cloudsave.AdminPlayerRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminPlayerRecordService.AdminPutPlayerRecordsHandlerV1Short(
            &admin_player_record.AdminPutPlayerRecordsHandlerV1Params{
                Body:      ..., // *cloudsaveclientmodels.ModelsBulkUpdatePlayerRecordsRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminPlayerRecord'
    name: 'AdminRetrievePlayerRecords'
    summary: 'Query player records'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminPlayerRecordService := cloudsave.AdminPlayerRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminPlayerRecordService.AdminRetrievePlayerRecordsShort(
            &admin_player_record.AdminRetrievePlayerRecordsParams{
                Namespace: ..., // string
                UserID:    ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                Query:     ..., // *string
                Tags:      ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminRecord'
    name: 'bulkGetAdminPlayerRecordByUserIdsV1'
    summary: 'Bulk get admin player record by multiple user ID'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminRecordService := cloudsave.AdminRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminRecordService.BulkGetAdminPlayerRecordByUserIdsV1Short(
            &admin_record.BulkGetAdminPlayerRecordByUserIdsV1Params{
                Body:      ..., // *cloudsaveclientmodels.ModelsBulkUserIDsRequest
                Key:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicGameBinaryRecord'
    name: 'bulkGetGameBinaryRecordV1'
    summary: 'Bulk get game binary records'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicGameBinaryRecordService := cloudsave.PublicGameBinaryRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicGameBinaryRecordService.BulkGetGameBinaryRecordV1Short(
            &public_game_binary_record.BulkGetGameBinaryRecordV1Params{
                Body:      ..., // *cloudsaveclientmodels.ModelsBulkGetGameRecordRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicPlayerBinaryRecord'
    name: 'bulkGetMyBinaryRecordV1'
    summary: 'Get player records bulk'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicPlayerBinaryRecordService := cloudsave.PublicPlayerBinaryRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicPlayerBinaryRecordService.BulkGetMyBinaryRecordV1Short(
            &public_player_binary_record.BulkGetMyBinaryRecordV1Params{
                Body:      ..., // *cloudsaveclientmodels.ModelsBulkGetPlayerRecordsRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicPlayerBinaryRecord'
    name: 'bulkGetOtherPlayerPublicBinaryRecordsV1'
    summary: 'Bulk get other player public binary record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicPlayerBinaryRecordService := cloudsave.PublicPlayerBinaryRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicPlayerBinaryRecordService.BulkGetOtherPlayerPublicBinaryRecordsV1Short(
            &public_player_binary_record.BulkGetOtherPlayerPublicBinaryRecordsV1Params{
                Body:      ..., // *cloudsaveclientmodels.ModelsBulkGetPlayerRecordsRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicPlayerBinaryRecord'
    name: 'bulkGetPlayerPublicBinaryRecordsV1'
    summary: 'Bulk get player public records'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicPlayerBinaryRecordService := cloudsave.PublicPlayerBinaryRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicPlayerBinaryRecordService.BulkGetPlayerPublicBinaryRecordsV1Short(
            &public_player_binary_record.BulkGetPlayerPublicBinaryRecordsV1Params{
                Body:      ..., // *cloudsaveclientmodels.ModelsBulkUserIDsRequest
                Key:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicPlayerRecord'
    name: 'bulkGetPlayerPublicRecordHandlerV1'
    summary: 'Bulk get player public records'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicPlayerRecordService := cloudsave.PublicPlayerRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicPlayerRecordService.BulkGetPlayerPublicRecordHandlerV1Short(
            &public_player_record.BulkGetPlayerPublicRecordHandlerV1Params{
                Body:      ..., // *cloudsaveclientmodels.ModelsBulkUserIDsRequest
                Key:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminPlayerRecord'
    name: 'bulkGetPlayerRecordSizeHandlerV1'
    summary: 'Bulk get player records size'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminPlayerRecordService := cloudsave.AdminPlayerRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminPlayerRecordService.BulkGetPlayerRecordSizeHandlerV1Short(
            &admin_player_record.BulkGetPlayerRecordSizeHandlerV1Params{
                Body:      ..., // *cloudsaveclientmodels.ModelsBulkUserKeyRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PluginConfig'
    name: 'createPluginConfig'
    summary: 'Create plugin configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        pluginConfigService := cloudsave.PluginConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := pluginConfigService.CreatePluginConfigShort(
            &plugin_config.CreatePluginConfigParams{
                Body:      ..., // *cloudsaveclientmodels.ModelsPluginRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'TTLConfig'
    name: 'deleteAdminGameRecordTTLConfig'
    summary: 'Delete admin game record TTL config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        ttlConfigService := cloudsave.TTLConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := ttlConfigService.DeleteAdminGameRecordTTLConfigShort(
            &ttl_config.DeleteAdminGameRecordTTLConfigParams{
                Key:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'TTLConfig'
    name: 'deleteGameBinaryRecordTTLConfig'
    summary: 'Delete game binary record TTL config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        ttlConfigService := cloudsave.TTLConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := ttlConfigService.DeleteGameBinaryRecordTTLConfigShort(
            &ttl_config.DeleteGameBinaryRecordTTLConfigParams{
                Key:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicGameBinaryRecord'
    name: 'deleteGameBinaryRecordV1'
    summary: 'Delete game binary record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicGameBinaryRecordService := cloudsave.PublicGameBinaryRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicGameBinaryRecordService.DeleteGameBinaryRecordV1Short(
            &public_game_binary_record.DeleteGameBinaryRecordV1Params{
                Key:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicGameRecord'
    name: 'deleteGameRecordHandlerV1'
    summary: 'Delete game record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicGameRecordService := cloudsave.PublicGameRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicGameRecordService.DeleteGameRecordHandlerV1Short(
            &public_game_record.DeleteGameRecordHandlerV1Params{
                Key:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'TTLConfig'
    name: 'deleteGameRecordTTLConfig'
    summary: 'Delete game record TTL config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        ttlConfigService := cloudsave.TTLConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := ttlConfigService.DeleteGameRecordTTLConfigShort(
            &ttl_config.DeleteGameRecordTTLConfigParams{
                Key:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicPlayerBinaryRecord'
    name: 'deletePlayerBinaryRecordV1'
    summary: 'Delete player binary record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicPlayerBinaryRecordService := cloudsave.PublicPlayerBinaryRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicPlayerBinaryRecordService.DeletePlayerBinaryRecordV1Short(
            &public_player_binary_record.DeletePlayerBinaryRecordV1Params{
                Key:       ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicPlayerRecord'
    name: 'deletePlayerRecordHandlerV1'
    summary: 'Delete player record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicPlayerRecordService := cloudsave.PublicPlayerRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicPlayerRecordService.DeletePlayerRecordHandlerV1Short(
            &public_player_record.DeletePlayerRecordHandlerV1Params{
                Key:       ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PluginConfig'
    name: 'deletePluginConfig'
    summary: 'Delete plugin configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        pluginConfigService := cloudsave.PluginConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := pluginConfigService.DeletePluginConfigShort(
            &plugin_config.DeletePluginConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicGameBinaryRecord'
    name: 'getGameBinaryRecordV1'
    summary: 'Get game binary record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicGameBinaryRecordService := cloudsave.PublicGameBinaryRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicGameBinaryRecordService.GetGameBinaryRecordV1Short(
            &public_game_binary_record.GetGameBinaryRecordV1Params{
                Key:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicGameRecord'
    name: 'getGameRecordHandlerV1'
    summary: 'Get game record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicGameRecordService := cloudsave.PublicGameRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicGameRecordService.GetGameRecordHandlerV1Short(
            &public_game_record.GetGameRecordHandlerV1Params{
                Key:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicGameRecord'
    name: 'getGameRecordsBulk'
    summary: 'Bulk get game records'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicGameRecordService := cloudsave.PublicGameRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicGameRecordService.GetGameRecordsBulkShort(
            &public_game_record.GetGameRecordsBulkParams{
                Body:      ..., // *cloudsaveclientmodels.ModelsBulkGetGameRecordRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicPlayerRecord'
    name: 'getOtherPlayerPublicRecordHandlerV1'
    summary: 'Get other player public record bulk'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicPlayerRecordService := cloudsave.PublicPlayerRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicPlayerRecordService.GetOtherPlayerPublicRecordHandlerV1Short(
            &public_player_record.GetOtherPlayerPublicRecordHandlerV1Params{
                Body:      ..., // *cloudsaveclientmodels.ModelsBulkGetPlayerRecordsRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicPlayerRecord'
    name: 'getOtherPlayerPublicRecordKeyHandlerV1'
    summary: 'Query other player public record key'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicPlayerRecordService := cloudsave.PublicPlayerRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicPlayerRecordService.GetOtherPlayerPublicRecordKeyHandlerV1Short(
            &public_player_record.GetOtherPlayerPublicRecordKeyHandlerV1Params{
                Namespace: ..., // string
                UserID:    ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                Tags:      ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicPlayerBinaryRecord'
    name: 'getPlayerBinaryRecordV1'
    summary: 'Get player binary record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicPlayerBinaryRecordService := cloudsave.PublicPlayerBinaryRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicPlayerBinaryRecordService.GetPlayerBinaryRecordV1Short(
            &public_player_binary_record.GetPlayerBinaryRecordV1Params{
                Key:       ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicPlayerBinaryRecord'
    name: 'getPlayerPublicBinaryRecordsV1'
    summary: 'Get player public binary record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicPlayerBinaryRecordService := cloudsave.PublicPlayerBinaryRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicPlayerBinaryRecordService.GetPlayerPublicBinaryRecordsV1Short(
            &public_player_binary_record.GetPlayerPublicBinaryRecordsV1Params{
                Key:       ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicPlayerRecord'
    name: 'getPlayerPublicRecordHandlerV1'
    summary: 'Get player public record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicPlayerRecordService := cloudsave.PublicPlayerRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicPlayerRecordService.GetPlayerPublicRecordHandlerV1Short(
            &public_player_record.GetPlayerPublicRecordHandlerV1Params{
                Key:       ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicPlayerRecord'
    name: 'getPlayerRecordHandlerV1'
    summary: 'Get player record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicPlayerRecordService := cloudsave.PublicPlayerRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicPlayerRecordService.GetPlayerRecordHandlerV1Short(
            &public_player_record.GetPlayerRecordHandlerV1Params{
                Key:       ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicPlayerRecord'
    name: 'getPlayerRecordsBulkHandlerV1'
    summary: 'Get player records bulk'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicPlayerRecordService := cloudsave.PublicPlayerRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicPlayerRecordService.GetPlayerRecordsBulkHandlerV1Short(
            &public_player_record.GetPlayerRecordsBulkHandlerV1Params{
                Body:      ..., // *cloudsaveclientmodels.ModelsBulkGetPlayerRecordsRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PluginConfig'
    name: 'getPluginConfig'
    summary: 'Get plugin configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        pluginConfigService := cloudsave.PluginConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := pluginConfigService.GetPluginConfigShort(
            &plugin_config.GetPluginConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicGameBinaryRecord'
    name: 'listGameBinaryRecordsV1'
    summary: 'Query game binary records'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicGameBinaryRecordService := cloudsave.PublicGameBinaryRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicGameBinaryRecordService.ListGameBinaryRecordsV1Short(
            &public_game_binary_record.ListGameBinaryRecordsV1Params{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                Query:     ..., // *string
                Tags:      ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminGameRecord'
    name: 'listGameRecordsHandlerV1'
    summary: 'Query game records'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminGameRecordService := cloudsave.AdminGameRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminGameRecordService.ListGameRecordsHandlerV1Short(
            &admin_game_record.ListGameRecordsHandlerV1Params{
                Namespace: ..., // string
                Query:     ..., // *string
                Tags:      ..., // []string
                Limit:     ..., // int64
                Offset:    ..., // int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicPlayerBinaryRecord'
    name: 'listMyBinaryRecordsV1'
    summary: 'Query my binary records'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicPlayerBinaryRecordService := cloudsave.PublicPlayerBinaryRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicPlayerBinaryRecordService.ListMyBinaryRecordsV1Short(
            &public_player_binary_record.ListMyBinaryRecordsV1Params{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                Query:     ..., // *string
                Tags:      ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicPlayerBinaryRecord'
    name: 'listOtherPlayerPublicBinaryRecordsV1'
    summary: 'Query other player public binary record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicPlayerBinaryRecordService := cloudsave.PublicPlayerBinaryRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicPlayerBinaryRecordService.ListOtherPlayerPublicBinaryRecordsV1Short(
            &public_player_binary_record.ListOtherPlayerPublicBinaryRecordsV1Params{
                Namespace: ..., // string
                UserID:    ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                Tags:      ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'AdminPlayerRecord'
    name: 'listPlayerRecordHandlerV1'
    summary: 'Retrieve list of public player records'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        adminPlayerRecordService := cloudsave.AdminPlayerRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminPlayerRecordService.ListPlayerRecordHandlerV1Short(
            &admin_player_record.ListPlayerRecordHandlerV1Params{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                Query:     ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicGameBinaryRecord'
    name: 'postGameBinaryPresignedURLV1'
    summary: 'Request presigned URL for upload game binary records'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicGameBinaryRecordService := cloudsave.PublicGameBinaryRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicGameBinaryRecordService.PostGameBinaryPresignedURLV1Short(
            &public_game_binary_record.PostGameBinaryPresignedURLV1Params{
                Body:      ..., // *cloudsaveclientmodels.ModelsUploadBinaryRecordRequest
                Key:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicGameBinaryRecord'
    name: 'postGameBinaryRecordV1'
    summary: 'Create game binary record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicGameBinaryRecordService := cloudsave.PublicGameBinaryRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicGameBinaryRecordService.PostGameBinaryRecordV1Short(
            &public_game_binary_record.PostGameBinaryRecordV1Params{
                Body:      ..., // *cloudsaveclientmodels.ModelsPublicGameBinaryRecordCreate
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicGameRecord'
    name: 'postGameRecordHandlerV1'
    summary: 'Create or append game record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicGameRecordService := cloudsave.PublicGameRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicGameRecordService.PostGameRecordHandlerV1Short(
            &public_game_record.PostGameRecordHandlerV1Params{
                Body:      ..., // cloudsaveclientmodels.ModelsGameRecordRequest
                Key:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicPlayerBinaryRecord'
    name: 'postPlayerBinaryPresignedURLV1'
    summary: 'Request presigned URL for upload player binary records'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicPlayerBinaryRecordService := cloudsave.PublicPlayerBinaryRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicPlayerBinaryRecordService.PostPlayerBinaryPresignedURLV1Short(
            &public_player_binary_record.PostPlayerBinaryPresignedURLV1Params{
                Body:      ..., // *cloudsaveclientmodels.ModelsUploadBinaryRecordRequest
                Key:       ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicPlayerBinaryRecord'
    name: 'postPlayerBinaryRecordV1'
    summary: 'Create player binary record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicPlayerBinaryRecordService := cloudsave.PublicPlayerBinaryRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicPlayerBinaryRecordService.PostPlayerBinaryRecordV1Short(
            &public_player_binary_record.PostPlayerBinaryRecordV1Params{
                Body:      ..., // *cloudsaveclientmodels.ModelsPublicPlayerBinaryRecordCreate
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicPlayerRecord'
    name: 'postPlayerPublicRecordHandlerV1'
    summary: 'Create or append player public record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicPlayerRecordService := cloudsave.PublicPlayerRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicPlayerRecordService.PostPlayerPublicRecordHandlerV1Short(
            &public_player_record.PostPlayerPublicRecordHandlerV1Params{
                Body:      ..., // cloudsaveclientmodels.ModelsPlayerRecordRequest
                Key:       ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicPlayerRecord'
    name: 'postPlayerRecordHandlerV1'
    summary: 'Create or append player record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicPlayerRecordService := cloudsave.PublicPlayerRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicPlayerRecordService.PostPlayerRecordHandlerV1Short(
            &public_player_record.PostPlayerRecordHandlerV1Params{
                Body:      ..., // cloudsaveclientmodels.ModelsPlayerRecordRequest
                Key:       ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicPlayerRecord'
    name: 'publicDeletePlayerPublicRecordHandlerV1'
    summary: 'Delete player public record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicPlayerRecordService := cloudsave.PublicPlayerRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicPlayerRecordService.PublicDeletePlayerPublicRecordHandlerV1Short(
            &public_player_record.PublicDeletePlayerPublicRecordHandlerV1Params{
                Key:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicTags'
    name: 'publicListTagsHandlerV1'
    summary: 'List tags'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicTagsService := cloudsave.PublicTagsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicTagsService.PublicListTagsHandlerV1Short(
            &public_tags.PublicListTagsHandlerV1Params{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicGameBinaryRecord'
    name: 'putGameBinaryRecordV1'
    summary: 'Update game binary record file'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicGameBinaryRecordService := cloudsave.PublicGameBinaryRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicGameBinaryRecordService.PutGameBinaryRecordV1Short(
            &public_game_binary_record.PutGameBinaryRecordV1Params{
                Body:      ..., // *cloudsaveclientmodels.ModelsBinaryRecordRequest
                Key:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'ConcurrentRecord'
    name: 'putGameRecordConcurrentHandlerV1'
    summary: 'Create or replace game record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        concurrentRecordService := cloudsave.ConcurrentRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := concurrentRecordService.PutGameRecordConcurrentHandlerV1Short(
            &concurrent_record.PutGameRecordConcurrentHandlerV1Params{
                Body:      ..., // *cloudsaveclientmodels.ModelsConcurrentRecordRequest
                Key:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicGameRecord'
    name: 'putGameRecordHandlerV1'
    summary: 'Create or replace game record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicGameRecordService := cloudsave.PublicGameRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicGameRecordService.PutGameRecordHandlerV1Short(
            &public_game_record.PutGameRecordHandlerV1Params{
                Body:      ..., // cloudsaveclientmodels.ModelsGameRecordRequest
                Key:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicPlayerBinaryRecord'
    name: 'putPlayerBinaryRecordV1'
    summary: 'Update player binary record file'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicPlayerBinaryRecordService := cloudsave.PublicPlayerBinaryRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicPlayerBinaryRecordService.PutPlayerBinaryRecordV1Short(
            &public_player_binary_record.PutPlayerBinaryRecordV1Params{
                Body:      ..., // *cloudsaveclientmodels.ModelsBinaryRecordRequest
                Key:       ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicPlayerBinaryRecord'
    name: 'putPlayerBinaryRecorMetadataV1'
    summary: 'Update player binary record metadata'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicPlayerBinaryRecordService := cloudsave.PublicPlayerBinaryRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicPlayerBinaryRecordService.PutPlayerBinaryRecorMetadataV1Short(
            &public_player_binary_record.PutPlayerBinaryRecorMetadataV1Params{
                Body:      ..., // *cloudsaveclientmodels.ModelsPlayerBinaryRecordMetadataPublicRequest
                Key:       ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'ConcurrentRecord'
    name: 'putPlayerPublicRecordConcurrentHandlerV1'
    summary: 'Create or replace player public record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        concurrentRecordService := cloudsave.ConcurrentRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := concurrentRecordService.PutPlayerPublicRecordConcurrentHandlerV1Short(
            &concurrent_record.PutPlayerPublicRecordConcurrentHandlerV1Params{
                Body:         ..., // *cloudsaveclientmodels.ModelsConcurrentRecordRequest
                Key:          ..., // string
                Namespace:    ..., // string
                UserID:       ..., // string
                ResponseBody: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicPlayerRecord'
    name: 'putPlayerPublicRecordHandlerV1'
    summary: 'Create or replace player public record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicPlayerRecordService := cloudsave.PublicPlayerRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicPlayerRecordService.PutPlayerPublicRecordHandlerV1Short(
            &public_player_record.PutPlayerPublicRecordHandlerV1Params{
                Body:      ..., // cloudsaveclientmodels.ModelsPlayerRecordRequest
                Key:       ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'ConcurrentRecord'
    name: 'putPlayerRecordConcurrentHandlerV1'
    summary: 'Create or replace player private record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        concurrentRecordService := cloudsave.ConcurrentRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := concurrentRecordService.PutPlayerRecordConcurrentHandlerV1Short(
            &concurrent_record.PutPlayerRecordConcurrentHandlerV1Params{
                Body:         ..., // *cloudsaveclientmodels.ModelsConcurrentRecordRequest
                Key:          ..., // string
                Namespace:    ..., // string
                UserID:       ..., // string
                ResponseBody: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicPlayerRecord'
    name: 'putPlayerRecordHandlerV1'
    summary: 'Create or replace player record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicPlayerRecordService := cloudsave.PublicPlayerRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicPlayerRecordService.PutPlayerRecordHandlerV1Short(
            &public_player_record.PutPlayerRecordHandlerV1Params{
                Body:      ..., // cloudsaveclientmodels.ModelsPlayerRecordRequest
                Key:       ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PublicPlayerRecord'
    name: 'RetrievePlayerRecords'
    summary: 'Query player records key'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        publicPlayerRecordService := cloudsave.PublicPlayerRecordService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicPlayerRecordService.RetrievePlayerRecordsShort(
            &public_player_record.RetrievePlayerRecordsParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                Tags:      ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PluginConfig'
    name: 'updatePluginConfig'
    summary: 'Update plugin configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCloudsaveClient(configRepo)
        pluginConfigService := cloudsave.PluginConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := pluginConfigService.UpdatePluginConfigShort(
            &plugin_config.UpdatePluginConfigParams{
                Body:      ..., // *cloudsaveclientmodels.ModelsPluginRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }
# end cloudsave
# begin csm
  - ns: 'Notification Subscription'
    name: 'BulkSaveSubscriptionAppNotificationV2'
    summary: 'Bulk update of users subscribed to an app''s notifications'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        notificationSubscriptionService := csm.NotificationSubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationSubscriptionService.BulkSaveSubscriptionAppNotificationV2Short(
            &notification_subscription.BulkSaveSubscriptionAppNotificationV2Params{
                Body:      ..., // *csmclientmodels.ApimodelBulkSubscribeRequest
                App:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'App'
    name: 'CreateAppV1'
    summary: 'Creates new App for AB-Extend Customers'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        appService := csm.AppService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := appService.CreateAppV1Short(
            &app.CreateAppV1Params{
                Body:      ..., // *csmclientmodels.GeneratedCreateAppV1Request
                App:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'App V2'
    name: 'CreateAppV2'
    summary: 'Create new extend app'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        appV2Service := csm.AppV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := appV2Service.CreateAppV2Short(
            &app_v2.CreateAppV2Params{
                Body:      ..., // *csmclientmodels.ApimodelCreateAppV2Request
                App:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Deployment'
    name: 'CreateDeploymentV1'
    summary: 'Creates Deployment'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        deploymentService := csm.DeploymentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := deploymentService.CreateDeploymentV1Short(
            &deployment.CreateDeploymentV1Params{
                Body:      ..., // *csmclientmodels.GeneratedCreateDeploymentV1Request
                App:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Deployment V2'
    name: 'CreateDeploymentV2'
    summary: 'Creates Deployment'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        deploymentV2Service := csm.DeploymentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := deploymentV2Service.CreateDeploymentV2Short(
            &deployment_v2.CreateDeploymentV2Params{
                Body:      ..., // *csmclientmodels.ApimodelCreateDeploymentV2Request
                App:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Image'
    name: 'DeleteAppImagesV1'
    summary: 'Deletes the images'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        imageService := csm.ImageService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := imageService.DeleteAppImagesV1Short(
            &image.DeleteAppImagesV1Params{
                Body:      ..., // *csmclientmodels.GeneratedDeleteAppImagesV1Request
                App:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Image V2'
    name: 'DeleteAppImagesV2'
    summary: 'Delete app images'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        imageV2Service := csm.ImageV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := imageV2Service.DeleteAppImagesV2Short(
            &image_v2.DeleteAppImagesV2Params{
                Body:      ..., // *csmclientmodels.GeneratedDeleteAppImagesV1Request
                App:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'App'
    name: 'DeleteAppV1'
    summary: 'Delete App by Åpp Name'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        appService := csm.AppService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := appService.DeleteAppV1Short(
            &app.DeleteAppV1Params{
                App:       ..., // string
                Namespace: ..., // string
                Forced:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'App V2'
    name: 'DeleteAppV2'
    summary: 'Delete extend app by name'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        appV2Service := csm.AppV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := appV2Service.DeleteAppV2Short(
            &app_v2.DeleteAppV2Params{
                App:       ..., // string
                Namespace: ..., // string
                Forced:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Deployment'
    name: 'DeleteDeploymentV1'
    summary: 'Delete Deployment by Deployment ID'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        deploymentService := csm.DeploymentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := deploymentService.DeleteDeploymentV1Short(
            &deployment.DeleteDeploymentV1Params{
                DeploymentID: ..., // string
                Namespace:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Deployment V2'
    name: 'DeleteDeploymentV2'
    summary: 'Delete Deployment by Deployment ID'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        deploymentV2Service := csm.DeploymentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := deploymentV2Service.DeleteDeploymentV2Short(
            &deployment_v2.DeleteDeploymentV2Params{
                DeploymentID: ..., // string
                Namespace:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration'
    name: 'DeleteSecretV1'
    summary: 'Delete an environment secret'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        configurationService := csm.ConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationService.DeleteSecretV1Short(
            &configuration.DeleteSecretV1Params{
                App:       ..., // string
                ConfigID:  ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration V2'
    name: 'DeleteSecretV2'
    summary: 'Delete an environment secret'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        configurationV2Service := csm.ConfigurationV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationV2Service.DeleteSecretV2Short(
            &configuration_v2.DeleteSecretV2Params{
                App:       ..., // string
                ConfigID:  ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Notification Subscription'
    name: 'DeleteSubscriptionAppNotificationByUserIDV2'
    summary: 'Remove other person subscription by user ID'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        notificationSubscriptionService := csm.NotificationSubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationSubscriptionService.DeleteSubscriptionAppNotificationByUserIDV2Short(
            &notification_subscription.DeleteSubscriptionAppNotificationByUserIDV2Params{
                App:       ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Notification Subscription'
    name: 'DeleteSubscriptionAppNotificationV2'
    summary: 'Remove other person subscription'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        notificationSubscriptionService := csm.NotificationSubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationSubscriptionService.DeleteSubscriptionAppNotificationV2Short(
            &notification_subscription.DeleteSubscriptionAppNotificationV2Params{
                App:            ..., // string
                Namespace:      ..., // string
                SubscriptionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Notification Subscription V3'
    name: 'DeleteSubscriptionAppNotificationV3'
    summary: 'Remove other person subscription by user ID or email address'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        notificationSubscriptionV3Service := csm.NotificationSubscriptionV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationSubscriptionV3Service.DeleteSubscriptionAppNotificationV3Short(
            &notification_subscription_v3.DeleteSubscriptionAppNotificationV3Params{
                App:          ..., // string
                Namespace:    ..., // string
                EmailAddress: ..., // *string
                UserID:       ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration'
    name: 'DeleteVariableV1'
    summary: 'Delete an environment variable'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        configurationService := csm.ConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationService.DeleteVariableV1Short(
            &configuration.DeleteVariableV1Params{
                App:       ..., // string
                ConfigID:  ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration V2'
    name: 'DeleteVariableV2'
    summary: 'Delete an environment variable'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        configurationV2Service := csm.ConfigurationV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationV2Service.DeleteVariableV2Short(
            &configuration_v2.DeleteVariableV2Params{
                App:       ..., // string
                ConfigID:  ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Image'
    name: 'GetAppImageListV1'
    summary: 'Get a list of container images'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        imageService := csm.ImageService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := imageService.GetAppImageListV1Short(
            &image.GetAppImageListV1Params{
                App:       ..., // string
                Namespace: ..., // string
                Cached:    ..., // *string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Image V2'
    name: 'GetAppImageListV2'
    summary: 'Get a list of container images'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        imageV2Service := csm.ImageV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := imageV2Service.GetAppImageListV2Short(
            &image_v2.GetAppImageListV2Params{
                App:       ..., // string
                Namespace: ..., // string
                Cached:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'App'
    name: 'GetAppListV1'
    summary: 'Gets the List of Apps for AB-Extend Customer'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        appService := csm.AppService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := appService.GetAppListV1Short(
            &app.GetAppListV1Params{
                Body:      ..., // *csmclientmodels.GeneratedGetAppListV1Request
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'App V2'
    name: 'GetAppListV2'
    summary: 'Get list of extend apps on a given game namespace'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        appV2Service := csm.AppV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := appV2Service.GetAppListV2Short(
            &app_v2.GetAppListV2Params{
                Body:      ..., // *csmclientmodels.ApimodelGetAppListV2Request
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'App'
    name: 'GetAppReleaseV1'
    summary: 'Gets the Latest Release Version info of this App'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        appService := csm.AppService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := appService.GetAppReleaseV1Short(
            &app.GetAppReleaseV1Params{
                App:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'App'
    name: 'GetAppV1'
    summary: 'Gets the App By Name'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        appService := csm.AppService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := appService.GetAppV1Short(
            &app.GetAppV1Params{
                App:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'App V2'
    name: 'GetAppV2'
    summary: 'Get extend app by name'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        appV2Service := csm.AppV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := appV2Service.GetAppV2Short(
            &app_v2.GetAppV2Params{
                App:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Deployment'
    name: 'GetDeploymentV1'
    summary: 'Get Deployment by Deployment ID'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        deploymentService := csm.DeploymentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := deploymentService.GetDeploymentV1Short(
            &deployment.GetDeploymentV1Params{
                DeploymentID: ..., // string
                Namespace:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Deployment V2'
    name: 'GetDeploymentV2'
    summary: 'Get Deployment by Deployment ID'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        deploymentV2Service := csm.DeploymentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := deploymentV2Service.GetDeploymentV2Short(
            &deployment_v2.GetDeploymentV2Params{
                DeploymentID: ..., // string
                Namespace:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Deployment'
    name: 'GetListOfDeploymentV1'
    summary: 'Fetches the List of Deployments'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        deploymentService := csm.DeploymentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := deploymentService.GetListOfDeploymentV1Short(
            &deployment.GetListOfDeploymentV1Params{
                Body:      ..., // *csmclientmodels.GeneratedGetDeploymentListV1Request
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Deployment V2'
    name: 'GetListOfDeploymentV2'
    summary: 'Fetches the List of Deployments'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        deploymentV2Service := csm.DeploymentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := deploymentV2Service.GetListOfDeploymentV2Short(
            &deployment_v2.GetListOfDeploymentV2Params{
                Body:      ..., // *csmclientmodels.ApimodelGetDeploymentListV2Request
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration'
    name: 'GetListOfSecretsV1'
    summary: 'Get list of environment secrets'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        configurationService := csm.ConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationService.GetListOfSecretsV1Short(
            &configuration.GetListOfSecretsV1Params{
                App:       ..., // string
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration V2'
    name: 'GetListOfSecretsV2'
    summary: 'Get list of environment secrets'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        configurationV2Service := csm.ConfigurationV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationV2Service.GetListOfSecretsV2Short(
            &configuration_v2.GetListOfSecretsV2Params{
                App:       ..., // string
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration'
    name: 'GetListOfVariablesV1'
    summary: 'Get list of environment variables'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        configurationService := csm.ConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationService.GetListOfVariablesV1Short(
            &configuration.GetListOfVariablesV1Params{
                App:       ..., // string
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration V2'
    name: 'GetListOfVariablesV2'
    summary: 'Get list of environment variables'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        configurationV2Service := csm.ConfigurationV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationV2Service.GetListOfVariablesV2Short(
            &configuration_v2.GetListOfVariablesV2Params{
                App:       ..., // string
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Notification Subscription'
    name: 'GetNotificationSubscriberListV2'
    summary: 'Get a list of the app notification subscriber'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        notificationSubscriptionService := csm.NotificationSubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationSubscriptionService.GetNotificationSubscriberListV2Short(
            &notification_subscription.GetNotificationSubscriberListV2Params{
                App:              ..., // string
                Namespace:        ..., // string
                NotificationType: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Notification Subscription V3'
    name: 'GetNotificationSubscriberListV3'
    summary: 'Get a list of the app notification subscriber'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        notificationSubscriptionV3Service := csm.NotificationSubscriptionV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationSubscriptionV3Service.GetNotificationSubscriberListV3Short(
            &notification_subscription_v3.GetNotificationSubscriberListV3Params{
                App:              ..., // string
                Namespace:        ..., // string
                NotificationType: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Resources Limits'
    name: 'GetResourcesLimits'
    summary: 'Get Extend Apps Configurable Limits e.g. any kind of limits for front end validation use'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        resourcesLimitsService := csm.ResourcesLimitsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := resourcesLimitsService.GetResourcesLimitsShort(
            &resources_limits.GetResourcesLimitsParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Notification Subscription'
    name: 'GetSubscriptionV2Handler'
    summary: 'Get Subscription status of a user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        notificationSubscriptionService := csm.NotificationSubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationSubscriptionService.GetSubscriptionV2HandlerShort(
            &notification_subscription.GetSubscriptionV2HandlerParams{
                App:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Messages'
    name: 'publicGetMessages'
    summary: 'get service messages'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        messagesService := csm.MessagesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := messagesService.PublicGetMessagesShort(
            &messages.PublicGetMessagesParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration'
    name: 'SaveSecretV1'
    summary: 'Save an environment secret'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        configurationService := csm.ConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationService.SaveSecretV1Short(
            &configuration.SaveSecretV1Params{
                Body:      ..., // *csmclientmodels.GeneratedSaveConfigurationV1Request
                App:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration V2'
    name: 'SaveSecretV2'
    summary: 'Save an environment secret'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        configurationV2Service := csm.ConfigurationV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationV2Service.SaveSecretV2Short(
            &configuration_v2.SaveSecretV2Params{
                Body:      ..., // *csmclientmodels.ApimodelSaveSecretConfigurationV2Request
                App:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration'
    name: 'SaveVariableV1'
    summary: 'Save an environment variable'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        configurationService := csm.ConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationService.SaveVariableV1Short(
            &configuration.SaveVariableV1Params{
                Body:      ..., // *csmclientmodels.GeneratedSaveConfigurationV1Request
                App:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration V2'
    name: 'SaveVariableV2'
    summary: 'Save an environment variable'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        configurationV2Service := csm.ConfigurationV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationV2Service.SaveVariableV2Short(
            &configuration_v2.SaveVariableV2Params{
                Body:      ..., // *csmclientmodels.ApimodelSaveConfigurationV2Request
                App:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'App'
    name: 'StartAppV1'
    summary: 'Starts the Application'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        appService := csm.AppService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := appService.StartAppV1Short(
            &app.StartAppV1Params{
                Body:      ..., // *csmclientmodels.GeneratedStartAppV1Request
                App:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'App V2'
    name: 'StartAppV2'
    summary: 'Starts the Application'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        appV2Service := csm.AppV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := appV2Service.StartAppV2Short(
            &app_v2.StartAppV2Params{
                App:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'App'
    name: 'StopAppV1'
    summary: 'Stops the Application'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        appService := csm.AppService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := appService.StopAppV1Short(
            &app.StopAppV1Params{
                Body:      ..., // *csmclientmodels.GeneratedStopAppV1Request
                App:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'App V2'
    name: 'StopAppV2'
    summary: 'Stops the Application'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        appV2Service := csm.AppV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := appV2Service.StopAppV2Short(
            &app_v2.StopAppV2Params{
                App:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Notification Subscription'
    name: 'SubscribeAppNotificationV2'
    summary: 'Subscribe the user(s) an app notification'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        notificationSubscriptionService := csm.NotificationSubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationSubscriptionService.SubscribeAppNotificationV2Short(
            &notification_subscription.SubscribeAppNotificationV2Params{
                Body:      ..., // *csmclientmodels.ApimodelSubscribeNotificationRequest
                App:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Notification Subscription'
    name: 'SubscribeV2Handler'
    summary: 'Subscribe to app notification'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        notificationSubscriptionService := csm.NotificationSubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationSubscriptionService.SubscribeV2HandlerShort(
            &notification_subscription.SubscribeV2HandlerParams{
                Body:      ..., // *csmclientmodels.ApimodelSelfSubscribeNotificationRequest
                App:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Notification Subscription'
    name: 'UnsubscribeV2Handler'
    summary: 'Unsubscribe to app notification'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        notificationSubscriptionService := csm.NotificationSubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationSubscriptionService.UnsubscribeV2HandlerShort(
            &notification_subscription.UnsubscribeV2HandlerParams{
                App:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'App V2'
    name: 'UpdateAppResourcesResourceLimitFormV2'
    summary: 'Request Resource Limit to be increased'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        appV2Service := csm.AppV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := appV2Service.UpdateAppResourcesResourceLimitFormV2Short(
            &app_v2.UpdateAppResourcesResourceLimitFormV2Params{
                Body:      ..., // *csmclientmodels.ApimodelIncreaseLimitFormRequest
                App:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'App V2'
    name: 'UpdateAppResourcesV2'
    summary: 'Update app info'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        appV2Service := csm.AppV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := appV2Service.UpdateAppResourcesV2Short(
            &app_v2.UpdateAppResourcesV2Params{
                Body:      ..., // *csmclientmodels.ApimodelUpdateAppResourceRequest
                App:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'App'
    name: 'UpdateAppV1'
    summary: 'Update App Partially'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        appService := csm.AppService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := appService.UpdateAppV1Short(
            &app.UpdateAppV1Params{
                Body:      ..., // *csmclientmodels.GeneratedUpdateAppV1Request
                App:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'App V2'
    name: 'UpdateAppV2'
    summary: 'Update app info'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        appV2Service := csm.AppV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := appV2Service.UpdateAppV2Short(
            &app_v2.UpdateAppV2Params{
                Body:      ..., // *csmclientmodels.ApimodelUpdateAppV2Request
                App:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration'
    name: 'UpdateSecretV1'
    summary: 'Update an environment secret'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        configurationService := csm.ConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationService.UpdateSecretV1Short(
            &configuration.UpdateSecretV1Params{
                Body:      ..., // *csmclientmodels.GeneratedUpdateConfigurationV1Request
                App:       ..., // string
                ConfigID:  ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration V2'
    name: 'UpdateSecretV2'
    summary: 'Update an environment secret'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        configurationV2Service := csm.ConfigurationV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationV2Service.UpdateSecretV2Short(
            &configuration_v2.UpdateSecretV2Params{
                Body:      ..., // *csmclientmodels.ApimodelUpdateSecretConfigurationV2Request
                App:       ..., // string
                ConfigID:  ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration'
    name: 'UpdateVariableV1'
    summary: 'Update an environment variable'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        configurationService := csm.ConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationService.UpdateVariableV1Short(
            &configuration.UpdateVariableV1Params{
                Body:      ..., // *csmclientmodels.GeneratedUpdateConfigurationV1Request
                App:       ..., // string
                ConfigID:  ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration V2'
    name: 'UpdateVariableV2'
    summary: 'Update an environment variable'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewCsmClient(configRepo)
        configurationV2Service := csm.ConfigurationV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationV2Service.UpdateVariableV2Short(
            &configuration_v2.UpdateVariableV2Params{
                Body:      ..., // *csmclientmodels.ApimodelUpdateConfigurationV2Request
                App:       ..., // string
                ConfigID:  ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }
# end csm
# begin gametelemetry
  - ns: 'telemetry'
    name: 'get_events_game_telemetry_v1_admin_namespaces__namespace__events_get'
    summary: 'Get Events'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGametelemetryClient(configRepo)
        telemetryService := gametelemetry.TelemetryService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := telemetryService.GetEventsGameTelemetryV1AdminNamespacesNamespaceEventsGetShort(
            &telemetry.GetEventsGameTelemetryV1AdminNamespacesNamespaceEventsGetParams{
                Namespace:    ..., // string
                DeviceType:   ..., // *string
                EndTime:      ..., // *string
                EventID:      ..., // *string
                EventName:    ..., // *string
                EventPayload: ..., // *string
                FlightID:     ..., // *string
                Limit:        ..., // *int64
                Offset:       ..., // *int64
                StartTime:    ..., // *string
                UserID:       ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'telemetry'
    name: 'get_namespaces_game_telemetry_v1_admin_namespaces_get'
    summary: 'Get Namespaces'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGametelemetryClient(configRepo)
        telemetryService := gametelemetry.TelemetryService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := telemetryService.GetNamespacesGameTelemetryV1AdminNamespacesGetShort(
            &telemetry.GetNamespacesGameTelemetryV1AdminNamespacesGetParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Gametelemetry Operations'
    name: 'protected_get_playtime_game_telemetry_v1_protected_steamIds__steamId__playtime_get'
    summary: 'Protected Get Playtime'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGametelemetryClient(configRepo)
        gametelemetryOperationsService := gametelemetry.GametelemetryOperationsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gametelemetryOperationsService.ProtectedGetPlaytimeGameTelemetryV1ProtectedSteamIdsSteamIDPlaytimeGetShort(
            &gametelemetry_operations.ProtectedGetPlaytimeGameTelemetryV1ProtectedSteamIdsSteamIDPlaytimeGetParams{
                Cookie:  ..., // *string
                SteamID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Gametelemetry Operations'
    name: 'protected_save_events_game_telemetry_v1_protected_events_post'
    summary: 'Protected Save Events'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGametelemetryClient(configRepo)
        gametelemetryOperationsService := gametelemetry.GametelemetryOperationsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gametelemetryOperationsService.ProtectedSaveEventsGameTelemetryV1ProtectedEventsPostShort(
            &gametelemetry_operations.ProtectedSaveEventsGameTelemetryV1ProtectedEventsPostParams{
                Body:   ..., // []*gametelemetryclientmodels.TelemetryBody
                Cookie: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Gametelemetry Operations'
    name: 'protected_update_playtime_game_telemetry_v1_protected_steamIds__steamId__playtime__playtime__put'
    summary: 'Protected Update Playtime'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGametelemetryClient(configRepo)
        gametelemetryOperationsService := gametelemetry.GametelemetryOperationsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gametelemetryOperationsService.ProtectedUpdatePlaytimeGameTelemetryV1ProtectedSteamIdsSteamIDPlaytimePlaytimePutShort(
            &gametelemetry_operations.ProtectedUpdatePlaytimeGameTelemetryV1ProtectedSteamIdsSteamIDPlaytimePlaytimePutParams{
                Cookie:   ..., // *string
                Playtime: ..., // string
                SteamID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }
# end gametelemetry
# begin gdpr
  - ns: 'Data Deletion'
    name: 'AdminCancelUserAccountDeletionRequest'
    summary: 'Cancel user''s account deletion request'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        dataDeletionService := gdpr.DataDeletionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dataDeletionService.AdminCancelUserAccountDeletionRequestShort(
            &data_deletion.AdminCancelUserAccountDeletionRequestParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Data Retrieval'
    name: 'AdminCancelUserPersonalDataRequest'
    summary: 'Cancel user''s personal data requests'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        dataRetrievalService := gdpr.DataRetrievalService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dataRetrievalService.AdminCancelUserPersonalDataRequestShort(
            &data_retrieval.AdminCancelUserPersonalDataRequestParams{
                Namespace:   ..., // string
                RequestDate: ..., // string
                UserID:      ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Platform Account Closure Client'
    name: 'AdminDeletePlatformAccountClosureClient'
    summary: 'Delete Platform Account Closure Client'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        platformAccountClosureClientService := gdpr.PlatformAccountClosureClientService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := platformAccountClosureClientService.AdminDeletePlatformAccountClosureClientShort(
            &platform_account_closure_client.AdminDeletePlatformAccountClosureClientParams{
                Namespace: ..., // string
                Platform:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Data Retrieval'
    name: 'AdminGeneratePersonalDataURL'
    summary: 'Generate personal data download url'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        dataRetrievalService := gdpr.DataRetrievalService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dataRetrievalService.AdminGeneratePersonalDataURLShort(
            &data_retrieval.AdminGeneratePersonalDataURLParams{
                Password:    ..., // *string
                Namespace:   ..., // string
                RequestDate: ..., // string
                UserID:      ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Data Deletion'
    name: 'AdminGetListDeletionDataRequest'
    summary: 'Retrieve all user''s account deletion requests in specified date'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        dataDeletionService := gdpr.DataDeletionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dataDeletionService.AdminGetListDeletionDataRequestShort(
            &data_deletion.AdminGetListDeletionDataRequestParams{
                Namespace:   ..., // string
                After:       ..., // *string
                Before:      ..., // *string
                Limit:       ..., // *int64
                Offset:      ..., // *int64
                RequestDate: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Data Retrieval'
    name: 'AdminGetListPersonalDataRequest'
    summary: 'Get list personal data requests'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        dataRetrievalService := gdpr.DataRetrievalService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dataRetrievalService.AdminGetListPersonalDataRequestShort(
            &data_retrieval.AdminGetListPersonalDataRequestParams{
                Namespace:   ..., // string
                Limit:       ..., // *int64
                Offset:      ..., // *int64
                RequestDate: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Platform Account Closure Client'
    name: 'AdminGetPlatformAccountClosureClient'
    summary: 'Get Platform Account Closure Config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        platformAccountClosureClientService := gdpr.PlatformAccountClosureClientService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := platformAccountClosureClientService.AdminGetPlatformAccountClosureClientShort(
            &platform_account_closure_client.AdminGetPlatformAccountClosureClientParams{
                Namespace: ..., // string
                Platform:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Platform Account Closure Client'
    name: 'AdminGetPlatformAccountClosureClients'
    summary: 'Get Platform Account Closure Configs'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        platformAccountClosureClientService := gdpr.PlatformAccountClosureClientService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := platformAccountClosureClientService.AdminGetPlatformAccountClosureClientsShort(
            &platform_account_closure_client.AdminGetPlatformAccountClosureClientsParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration'
    name: 'AdminGetPlatformAccountClosureServicesConfiguration'
    summary: 'Get Registered Platform Account Closure Services Configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        configurationService := gdpr.ConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationService.AdminGetPlatformAccountClosureServicesConfigurationShort(
            &configuration.AdminGetPlatformAccountClosureServicesConfigurationParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration'
    name: 'AdminGetServicesConfiguration'
    summary: 'Get Registered Services Configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        configurationService := gdpr.ConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationService.AdminGetServicesConfigurationShort(
            &configuration.AdminGetServicesConfigurationParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Data Deletion'
    name: 'AdminGetUserAccountDeletionRequest'
    summary: 'Retrieve specific user''s account deletion request'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        dataDeletionService := gdpr.DataDeletionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dataDeletionService.AdminGetUserAccountDeletionRequestShort(
            &data_deletion.AdminGetUserAccountDeletionRequestParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Data Retrieval'
    name: 'AdminGetUserPersonalDataRequests'
    summary: 'Get user''s personal data requests'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        dataRetrievalService := gdpr.DataRetrievalService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dataRetrievalService.AdminGetUserPersonalDataRequestsShort(
            &data_retrieval.AdminGetUserPersonalDataRequestsParams{
                Namespace: ..., // string
                UserID:    ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Platform Account Closure History'
    name: 'AdminGetUserPlatformAccountClosureHistories'
    summary: 'Get User''s Platform Account Closure Histories'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        platformAccountClosureHistoryService := gdpr.PlatformAccountClosureHistoryService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := platformAccountClosureHistoryService.AdminGetUserPlatformAccountClosureHistoriesShort(
            &platform_account_closure_history.AdminGetUserPlatformAccountClosureHistoriesParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                Platform:  ..., // *string
                UserID:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Platform Account Closure Client'
    name: 'AdminMockPlatformAccountClosureData'
    summary: 'Mock Platform Account Closure Data'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        platformAccountClosureClientService := gdpr.PlatformAccountClosureClientService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := platformAccountClosureClientService.AdminMockPlatformAccountClosureDataShort(
            &platform_account_closure_client.AdminMockPlatformAccountClosureDataParams{
                Body:      ..., // *gdprclientmodels.DTOPlatformAccountClosureMockRequest
                Namespace: ..., // string
                Platform:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Data Retrieval'
    name: 'AdminRequestDataRetrieval'
    summary: 'Submit user personal data retrieval request'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        dataRetrievalService := gdpr.DataRetrievalService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dataRetrievalService.AdminRequestDataRetrievalShort(
            &data_retrieval.AdminRequestDataRetrievalParams{
                Password:  ..., // *string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration'
    name: 'AdminResetPlatformAccountClosureServicesConfiguration'
    summary: 'Reset Registered Platform Account Closure Services Configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        configurationService := gdpr.ConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationService.AdminResetPlatformAccountClosureServicesConfigurationShort(
            &configuration.AdminResetPlatformAccountClosureServicesConfigurationParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration'
    name: 'AdminResetServicesConfiguration'
    summary: 'Reset Registered Services Configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        configurationService := gdpr.ConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationService.AdminResetServicesConfigurationShort(
            &configuration.AdminResetServicesConfigurationParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Data Deletion'
    name: 'AdminSubmitUserAccountDeletionRequest'
    summary: 'Submit user''s account deletion request'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        dataDeletionService := gdpr.DataDeletionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dataDeletionService.AdminSubmitUserAccountDeletionRequestShort(
            &data_deletion.AdminSubmitUserAccountDeletionRequestParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Platform Account Closure Client'
    name: 'AdminUpdatePlatformAccountClosureClient'
    summary: 'Update Platform Account Closure Client'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        platformAccountClosureClientService := gdpr.PlatformAccountClosureClientService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := platformAccountClosureClientService.AdminUpdatePlatformAccountClosureClientShort(
            &platform_account_closure_client.AdminUpdatePlatformAccountClosureClientParams{
                Body:      ..., // *gdprclientmodels.DTOPlatformAccountClosureClientRequest
                Namespace: ..., // string
                Platform:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration'
    name: 'AdminUpdatePlatformAccountClosureServicesConfiguration'
    summary: 'Update Registered Platform Account Closure Services Configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        configurationService := gdpr.ConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationService.AdminUpdatePlatformAccountClosureServicesConfigurationShort(
            &configuration.AdminUpdatePlatformAccountClosureServicesConfigurationParams{
                Body:      ..., // *gdprclientmodels.DTOServiceConfigurationUpdateRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration'
    name: 'AdminUpdateServicesConfiguration'
    summary: 'Update Registered Services Configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        configurationService := gdpr.ConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationService.AdminUpdateServicesConfigurationShort(
            &configuration.AdminUpdateServicesConfigurationParams{
                Body:      ..., // *gdprclientmodels.DTOServiceConfigurationUpdateRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Platform Account Closure Client'
    name: 'AdminValidateXboxBPCertFile'
    summary: 'Validate Xbox BP cert file'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        platformAccountClosureClientService := gdpr.PlatformAccountClosureClientService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := platformAccountClosureClientService.AdminValidateXboxBPCertFileShort(
            &platform_account_closure_client.AdminValidateXboxBPCertFileParams{
                Body:      ..., // *gdprclientmodels.DTOXboxBPCertValidationRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration'
    name: 'DeleteAdminEmailConfiguration'
    summary: 'Delete admin emails configurations'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        configurationService := gdpr.ConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationService.DeleteAdminEmailConfigurationShort(
            &configuration.DeleteAdminEmailConfigurationParams{
                Namespace: ..., // string
                Emails:    ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration'
    name: 'GetAdminEmailConfiguration'
    summary: 'Get admin email addresses configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        configurationService := gdpr.ConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationService.GetAdminEmailConfigurationShort(
            &configuration.GetAdminEmailConfigurationParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Data Deletion'
    name: 'PublicCancelMyAccountDeletionRequest'
    summary: 'Cancel my account deletion request'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        dataDeletionService := gdpr.DataDeletionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dataDeletionService.PublicCancelMyAccountDeletionRequestShort(
            &data_deletion.PublicCancelMyAccountDeletionRequestParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Data Deletion'
    name: 'PublicCancelUserAccountDeletionRequest'
    summary: 'Cancel user''s account deletion request'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        dataDeletionService := gdpr.DataDeletionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dataDeletionService.PublicCancelUserAccountDeletionRequestShort(
            &data_deletion.PublicCancelUserAccountDeletionRequestParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Data Retrieval'
    name: 'PublicCancelUserPersonalDataRequest'
    summary: 'Cancel user''s personal data requests'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        dataRetrievalService := gdpr.DataRetrievalService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dataRetrievalService.PublicCancelUserPersonalDataRequestShort(
            &data_retrieval.PublicCancelUserPersonalDataRequestParams{
                Namespace:   ..., // string
                RequestDate: ..., // string
                UserID:      ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Data Retrieval'
    name: 'PublicGeneratePersonalDataURL'
    summary: 'Generate personal data download url'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        dataRetrievalService := gdpr.DataRetrievalService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dataRetrievalService.PublicGeneratePersonalDataURLShort(
            &data_retrieval.PublicGeneratePersonalDataURLParams{
                Password:    ..., // string
                Namespace:   ..., // string
                RequestDate: ..., // string
                UserID:      ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Data Deletion'
    name: 'PublicGetMyAccountDeletionStatus'
    summary: 'Retrieve my account deletion status'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        dataDeletionService := gdpr.DataDeletionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dataDeletionService.PublicGetMyAccountDeletionStatusShort(
            &data_deletion.PublicGetMyAccountDeletionStatusParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Data Deletion'
    name: 'PublicGetUserAccountDeletionStatus'
    summary: 'Retrieve specific user''s account deletion status'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        dataDeletionService := gdpr.DataDeletionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dataDeletionService.PublicGetUserAccountDeletionStatusShort(
            &data_deletion.PublicGetUserAccountDeletionStatusParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Data Retrieval'
    name: 'PublicGetUserPersonalDataRequests'
    summary: 'Get user''s personal data requests'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        dataRetrievalService := gdpr.DataRetrievalService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dataRetrievalService.PublicGetUserPersonalDataRequestsShort(
            &data_retrieval.PublicGetUserPersonalDataRequestsParams{
                Namespace: ..., // string
                UserID:    ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Data Retrieval'
    name: 'PublicRequestDataRetrieval'
    summary: 'Submit personal data retrieval request'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        dataRetrievalService := gdpr.DataRetrievalService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dataRetrievalService.PublicRequestDataRetrievalShort(
            &data_retrieval.PublicRequestDataRetrievalParams{
                LanguageTag: ..., // *string
                Password:    ..., // string
                Namespace:   ..., // string
                UserID:      ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Data Deletion'
    name: 'PublicSubmitMyAccountDeletionRequest'
    summary: 'Submit my account deletion requests.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        dataDeletionService := gdpr.DataDeletionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dataDeletionService.PublicSubmitMyAccountDeletionRequestShort(
            &data_deletion.PublicSubmitMyAccountDeletionRequestParams{
                PlatformID:    ..., // string
                PlatformToken: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Data Deletion'
    name: 'PublicSubmitUserAccountDeletionRequest'
    summary: 'Submit user''s account deletion request'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        dataDeletionService := gdpr.DataDeletionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dataDeletionService.PublicSubmitUserAccountDeletionRequestShort(
            &data_deletion.PublicSubmitUserAccountDeletionRequestParams{
                LanguageTag: ..., // *string
                Password:    ..., // string
                Namespace:   ..., // string
                UserID:      ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Data Retrieval (S2S)'
    name: 'S2SGeneratePersonalDataURL'
    summary: 'Generate personal data download url'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        dataRetrievalS2SService := gdpr.DataRetrievalS2SService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dataRetrievalS2SService.S2SGeneratePersonalDataURLShort(
            &data_retrieval_s2_s.S2SGeneratePersonalDataURLParams{
                Namespace:   ..., // string
                RequestDate: ..., // string
                UserID:      ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Data Retrieval (S2S)'
    name: 'S2SGetDataRequestByRequestID'
    summary: 'Get Personal Data Request by Request Id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        dataRetrievalS2SService := gdpr.DataRetrievalS2SService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dataRetrievalS2SService.S2SGetDataRequestByRequestIDShort(
            &data_retrieval_s2_s.S2SGetDataRequestByRequestIDParams{
                Namespace: ..., // string
                RequestID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Data Deletion (S2S)'
    name: 'S2SGetListFinishedAccountDeletionRequest'
    summary: 'Get list of finished account deletion requests'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        dataDeletionS2SService := gdpr.DataDeletionS2SService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dataDeletionS2SService.S2SGetListFinishedAccountDeletionRequestShort(
            &data_deletion_s2_s.S2SGetListFinishedAccountDeletionRequestParams{
                Namespace: ..., // string
                End:       ..., // string
                Start:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Data Retrieval (S2S)'
    name: 'S2SGetListFinishedPersonalDataRequest'
    summary: 'Get list of finished personal data requests'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        dataRetrievalS2SService := gdpr.DataRetrievalS2SService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dataRetrievalS2SService.S2SGetListFinishedPersonalDataRequestShort(
            &data_retrieval_s2_s.S2SGetListFinishedPersonalDataRequestParams{
                Namespace: ..., // string
                End:       ..., // string
                Start:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Data Retrieval (S2S)'
    name: 'S2SRequestDataRetrieval'
    summary: 'Submit user personal data retrieval request'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        dataRetrievalS2SService := gdpr.DataRetrievalS2SService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dataRetrievalS2SService.S2SRequestDataRetrievalShort(
            &data_retrieval_s2_s.S2SRequestDataRetrievalParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Data Deletion (S2S)'
    name: 'S2SSubmitUserAccountDeletionRequest'
    summary: 'Submit user''s account deletion request'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        dataDeletionS2SService := gdpr.DataDeletionS2SService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dataDeletionS2SService.S2SSubmitUserAccountDeletionRequestShort(
            &data_deletion_s2_s.S2SSubmitUserAccountDeletionRequestParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration'
    name: 'SaveAdminEmailConfiguration'
    summary: 'Add admin email address configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        configurationService := gdpr.ConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationService.SaveAdminEmailConfigurationShort(
            &configuration.SaveAdminEmailConfigurationParams{
                Body:      ..., // []string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration'
    name: 'UpdateAdminEmailConfiguration'
    summary: 'Update admin email address configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGdprClient(configRepo)
        configurationService := gdpr.ConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationService.UpdateAdminEmailConfigurationShort(
            &configuration.UpdateAdminEmailConfigurationParams{
                Body:      ..., // []string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }
# end gdpr
# begin group
  - ns: 'Group Member'
    name: 'acceptGroupInvitationPublicV1'
    summary: 'Accept Group Invitation'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupMemberService := group.GroupMemberService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupMemberService.AcceptGroupInvitationPublicV1Short(
            &group_member.AcceptGroupInvitationPublicV1Params{
                GroupID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group Member'
    name: 'acceptGroupInvitationPublicV2'
    summary: 'Accept Group Invitation'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupMemberService := group.GroupMemberService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupMemberService.AcceptGroupInvitationPublicV2Short(
            &group_member.AcceptGroupInvitationPublicV2Params{
                GroupID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group Member'
    name: 'acceptGroupJoinRequestPublicV1'
    summary: 'Accept Group Join Request'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupMemberService := group.GroupMemberService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupMemberService.AcceptGroupJoinRequestPublicV1Short(
            &group_member.AcceptGroupJoinRequestPublicV1Params{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group Member'
    name: 'acceptGroupJoinRequestPublicV2'
    summary: 'Accept Group Join Request'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupMemberService := group.GroupMemberService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupMemberService.AcceptGroupJoinRequestPublicV2Short(
            &group_member.AcceptGroupJoinRequestPublicV2Params{
                GroupID:   ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group Member'
    name: 'AdminGetUserGroupStatusInformationV2'
    summary: 'User Group Status Information'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupMemberService := group.GroupMemberService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupMemberService.AdminGetUserGroupStatusInformationV2Short(
            &group_member.AdminGetUserGroupStatusInformationV2Params{
                GroupID:   ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group Member'
    name: 'CancelGroupJoinRequestV1'
    summary: 'Cancel request to join group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupMemberService := group.GroupMemberService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupMemberService.CancelGroupJoinRequestV1Short(
            &group_member.CancelGroupJoinRequestV1Params{
                GroupID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group Member'
    name: 'CancelInvitationGroupMemberV2'
    summary: 'Cancel Invitation Group Member'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupMemberService := group.GroupMemberService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupMemberService.CancelInvitationGroupMemberV2Short(
            &group_member.CancelInvitationGroupMemberV2Params{
                GroupID:   ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration'
    name: 'createGroupConfigurationAdminV1'
    summary: 'create new configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        configurationService := group.ConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationService.CreateGroupConfigurationAdminV1Short(
            &configuration.CreateGroupConfigurationAdminV1Params{
                Body:      ..., // *groupclientmodels.ModelsCreateGroupConfigurationRequestV1
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group Roles'
    name: 'createMemberRoleAdminV1'
    summary: 'create new member role'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupRolesService := group.GroupRolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupRolesService.CreateMemberRoleAdminV1Short(
            &group_roles.CreateMemberRoleAdminV1Params{
                Body:      ..., // *groupclientmodels.ModelsCreateMemberRoleRequestV1
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group'
    name: 'createNewGroupPublicV1'
    summary: 'create new group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupService := group.GroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupService.CreateNewGroupPublicV1Short(
            &group.CreateNewGroupPublicV1Params{
                Body:      ..., // *groupclientmodels.ModelsPublicCreateNewGroupRequestV1
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group'
    name: 'createNewGroupPublicV2'
    summary: 'create new group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupService := group.GroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupService.CreateNewGroupPublicV2Short(
            &group.CreateNewGroupPublicV2Params{
                Body:      ..., // *groupclientmodels.ModelsPublicCreateNewGroupRequestV1
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group'
    name: 'deleteGroupAdminV1'
    summary: 'Delete existing group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupService := group.GroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupService.DeleteGroupAdminV1Short(
            &group.DeleteGroupAdminV1Params{
                GroupID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration'
    name: 'deleteGroupConfigurationGlobalRuleAdminV1'
    summary: 'delete existing configuration global rule based on allowed action'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        configurationService := group.ConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationService.DeleteGroupConfigurationGlobalRuleAdminV1Short(
            &configuration.DeleteGroupConfigurationGlobalRuleAdminV1Params{
                AllowedAction:     ..., // string
                ConfigurationCode: ..., // string
                Namespace:         ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration'
    name: 'deleteGroupConfigurationV1'
    summary: 'Delete group configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        configurationService := group.ConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationService.DeleteGroupConfigurationV1Short(
            &configuration.DeleteGroupConfigurationV1Params{
                ConfigurationCode: ..., // string
                Namespace:         ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group'
    name: 'deleteGroupPredefinedRulePublicV1'
    summary: 'Delete group predefined rule'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupService := group.GroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupService.DeleteGroupPredefinedRulePublicV1Short(
            &group.DeleteGroupPredefinedRulePublicV1Params{
                AllowedAction: ..., // string
                GroupID:       ..., // string
                Namespace:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group'
    name: 'deleteGroupPredefinedRulePublicV2'
    summary: 'Delete group predefined rule'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupService := group.GroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupService.DeleteGroupPredefinedRulePublicV2Short(
            &group.DeleteGroupPredefinedRulePublicV2Params{
                AllowedAction: ..., // string
                GroupID:       ..., // string
                Namespace:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group'
    name: 'deleteGroupPublicV1'
    summary: 'Delete existing group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupService := group.GroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupService.DeleteGroupPublicV1Short(
            &group.DeleteGroupPublicV1Params{
                GroupID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group'
    name: 'deleteGroupPublicV2'
    summary: 'Delete existing group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupService := group.GroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupService.DeleteGroupPublicV2Short(
            &group.DeleteGroupPublicV2Params{
                GroupID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group Roles'
    name: 'deleteMemberRoleAdminV1'
    summary: 'delete member role'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupRolesService := group.GroupRolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupRolesService.DeleteMemberRoleAdminV1Short(
            &group_roles.DeleteMemberRoleAdminV1Params{
                MemberRoleID: ..., // string
                Namespace:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group Roles'
    name: 'deleteMemberRolePublicV1'
    summary: 'Remove role from group member'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupRolesService := group.GroupRolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupRolesService.DeleteMemberRolePublicV1Short(
            &group_roles.DeleteMemberRolePublicV1Params{
                Body:         ..., // *groupclientmodels.ModelsRemoveRoleFromMemberRequestV1
                MemberRoleID: ..., // string
                Namespace:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group Roles'
    name: 'deleteMemberRolePublicV2'
    summary: 'Remove role from group member'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupRolesService := group.GroupRolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupRolesService.DeleteMemberRolePublicV2Short(
            &group_roles.DeleteMemberRolePublicV2Params{
                Body:         ..., // *groupclientmodels.ModelsRemoveRoleFromMemberRequestV1
                GroupID:      ..., // string
                MemberRoleID: ..., // string
                Namespace:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration'
    name: 'getGroupConfigurationAdminV1'
    summary: 'get existing configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        configurationService := group.ConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationService.GetGroupConfigurationAdminV1Short(
            &configuration.GetGroupConfigurationAdminV1Params{
                ConfigurationCode: ..., // string
                Namespace:         ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Member Request'
    name: 'getGroupInvitationRequestPublicV1'
    summary: 'Get Group Invitation Request List'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        memberRequestService := group.MemberRequestService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := memberRequestService.GetGroupInvitationRequestPublicV1Short(
            &member_request.GetGroupInvitationRequestPublicV1Params{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Member Request'
    name: 'getGroupInviteRequestPublicV2'
    summary: 'Get Group Invite Request List'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        memberRequestService := group.MemberRequestService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := memberRequestService.GetGroupInviteRequestPublicV2Short(
            &member_request.GetGroupInviteRequestPublicV2Params{
                GroupID:   ..., // string
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Member Request'
    name: 'getGroupJoinRequestPublicV1'
    summary: 'Get Group Join Request List'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        memberRequestService := group.MemberRequestService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := memberRequestService.GetGroupJoinRequestPublicV1Short(
            &member_request.GetGroupJoinRequestPublicV1Params{
                GroupID:   ..., // string
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Member Request'
    name: 'getGroupJoinRequestPublicV2'
    summary: 'Get Group Join Request List'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        memberRequestService := group.MemberRequestService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := memberRequestService.GetGroupJoinRequestPublicV2Short(
            &member_request.GetGroupJoinRequestPublicV2Params{
                GroupID:   ..., // string
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group'
    name: 'getGroupListAdminV1'
    summary: 'get list of groups'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupService := group.GroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupService.GetGroupListAdminV1Short(
            &group.GetGroupListAdminV1Params{
                Namespace:         ..., // string
                ConfigurationCode: ..., // *string
                GroupName:         ..., // *string
                GroupRegion:       ..., // *string
                Limit:             ..., // *int64
                Offset:            ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group'
    name: 'getGroupListPublicV1'
    summary: 'get list of groups'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupService := group.GroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupService.GetGroupListPublicV1Short(
            &group.GetGroupListPublicV1Params{
                Namespace:   ..., // string
                GroupName:   ..., // *string
                GroupRegion: ..., // *string
                Limit:       ..., // *int64
                Offset:      ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group Member'
    name: 'getGroupMembersListAdminV1'
    summary: 'Get list of group members'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupMemberService := group.GroupMemberService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupMemberService.GetGroupMembersListAdminV1Short(
            &group_member.GetGroupMembersListAdminV1Params{
                GroupID:   ..., // string
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                Order:     ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group Member'
    name: 'getGroupMembersListPublicV1'
    summary: 'Get list of group members'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupMemberService := group.GroupMemberService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupMemberService.GetGroupMembersListPublicV1Short(
            &group_member.GetGroupMembersListPublicV1Params{
                GroupID:   ..., // string
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                Order:     ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group'
    name: 'getListGroupByIDsAdminV2'
    summary: 'get list of groups by group Ids'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupService := group.GroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupService.GetListGroupByIDsAdminV2Short(
            &group.GetListGroupByIDsAdminV2Params{
                Body:      ..., // *groupclientmodels.ModelsGetGroupListRequestV2
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group'
    name: 'getListGroupByIDsV2'
    summary: 'get list of groups by group Ids'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupService := group.GroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupService.GetListGroupByIDsV2Short(
            &group.GetListGroupByIDsV2Params{
                Body:      ..., // *groupclientmodels.ModelsGetGroupListRequestV2
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group Roles'
    name: 'getMemberRolesListAdminV1'
    summary: 'get all list of member roles'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupRolesService := group.GroupRolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupRolesService.GetMemberRolesListAdminV1Short(
            &group_roles.GetMemberRolesListAdminV1Params{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group Roles'
    name: 'getMemberRolesListPublicV1'
    summary: 'get all list of member roles'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupRolesService := group.GroupRolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupRolesService.GetMemberRolesListPublicV1Short(
            &group_roles.GetMemberRolesListPublicV1Params{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group Roles'
    name: 'getMemberRolesListPublicV2'
    summary: 'get all list of member roles'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupRolesService := group.GroupRolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupRolesService.GetMemberRolesListPublicV2Short(
            &group_roles.GetMemberRolesListPublicV2Params{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Member Request'
    name: 'GetMyGroupJoinRequestV2'
    summary: 'Get My Join Request To The Groups'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        memberRequestService := group.MemberRequestService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := memberRequestService.GetMyGroupJoinRequestV2Short(
            &member_request.GetMyGroupJoinRequestV2Params{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group'
    name: 'getSingleGroupAdminV1'
    summary: 'get single group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupService := group.GroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupService.GetSingleGroupAdminV1Short(
            &group.GetSingleGroupAdminV1Params{
                GroupID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group'
    name: 'getSingleGroupPublicV1'
    summary: 'get single group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupService := group.GroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupService.GetSingleGroupPublicV1Short(
            &group.GetSingleGroupPublicV1Params{
                GroupID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group Roles'
    name: 'getSingleMemberRoleAdminV1'
    summary: 'get member role'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupRolesService := group.GroupRolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupRolesService.GetSingleMemberRoleAdminV1Short(
            &group_roles.GetSingleMemberRoleAdminV1Params{
                MemberRoleID: ..., // string
                Namespace:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group Member'
    name: 'getUserGroupInformationPublicV1'
    summary: 'Get User Group Information'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupMemberService := group.GroupMemberService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupMemberService.GetUserGroupInformationPublicV1Short(
            &group_member.GetUserGroupInformationPublicV1Params{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group Member'
    name: 'getUserGroupInformationPublicV2'
    summary: 'Get User Joined Group Information'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupMemberService := group.GroupMemberService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupMemberService.GetUserGroupInformationPublicV2Short(
            &group_member.GetUserGroupInformationPublicV2Params{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group Member'
    name: 'GetUserGroupStatusInformationV2'
    summary: 'User Group Status Information'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupMemberService := group.GroupMemberService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupMemberService.GetUserGroupStatusInformationV2Short(
            &group_member.GetUserGroupStatusInformationV2Params{
                GroupID:   ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group Member'
    name: 'getUserJoinedGroupInformationPublicV2'
    summary: 'Get User Joined Group Information'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupMemberService := group.GroupMemberService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupMemberService.GetUserJoinedGroupInformationPublicV2Short(
            &group_member.GetUserJoinedGroupInformationPublicV2Params{
                Namespace: ..., // string
                UserID:    ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration'
    name: 'initiateGroupConfigurationAdminV1'
    summary: 'initiate configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        configurationService := group.ConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationService.InitiateGroupConfigurationAdminV1Short(
            &configuration.InitiateGroupConfigurationAdminV1Params{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group Member'
    name: 'inviteGroupPublicV1'
    summary: 'Invite user to group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupMemberService := group.GroupMemberService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupMemberService.InviteGroupPublicV1Short(
            &group_member.InviteGroupPublicV1Params{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group Member'
    name: 'inviteGroupPublicV2'
    summary: 'Invite user to group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupMemberService := group.GroupMemberService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupMemberService.InviteGroupPublicV2Short(
            &group_member.InviteGroupPublicV2Params{
                GroupID:   ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group Member'
    name: 'JoinGroupV1'
    summary: 'Join to group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupMemberService := group.GroupMemberService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupMemberService.JoinGroupV1Short(
            &group_member.JoinGroupV1Params{
                GroupID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group Member'
    name: 'JoinGroupV2'
    summary: 'Join to group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupMemberService := group.GroupMemberService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupMemberService.JoinGroupV2Short(
            &group_member.JoinGroupV2Params{
                GroupID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group Member'
    name: 'kickGroupMemberPublicV1'
    summary: 'Kick Group Member'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupMemberService := group.GroupMemberService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupMemberService.KickGroupMemberPublicV1Short(
            &group_member.KickGroupMemberPublicV1Params{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group Member'
    name: 'kickGroupMemberPublicV2'
    summary: 'Kick Group Member'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupMemberService := group.GroupMemberService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupMemberService.KickGroupMemberPublicV2Short(
            &group_member.KickGroupMemberPublicV2Params{
                GroupID:   ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group Member'
    name: 'leaveGroupPublicV1'
    summary: 'Leave group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupMemberService := group.GroupMemberService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupMemberService.LeaveGroupPublicV1Short(
            &group_member.LeaveGroupPublicV1Params{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group Member'
    name: 'leaveGroupPublicV2'
    summary: 'Leave group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupMemberService := group.GroupMemberService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupMemberService.LeaveGroupPublicV2Short(
            &group_member.LeaveGroupPublicV2Params{
                GroupID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration'
    name: 'listGroupConfigurationAdminV1'
    summary: 'List Group Configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        configurationService := group.ConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationService.ListGroupConfigurationAdminV1Short(
            &configuration.ListGroupConfigurationAdminV1Params{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group Member'
    name: 'rejectGroupInvitationPublicV1'
    summary: 'Reject Group Invitation'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupMemberService := group.GroupMemberService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupMemberService.RejectGroupInvitationPublicV1Short(
            &group_member.RejectGroupInvitationPublicV1Params{
                GroupID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group Member'
    name: 'rejectGroupInvitationPublicV2'
    summary: 'Reject Group Invitation'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupMemberService := group.GroupMemberService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupMemberService.RejectGroupInvitationPublicV2Short(
            &group_member.RejectGroupInvitationPublicV2Params{
                GroupID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group Member'
    name: 'rejectGroupJoinRequestPublicV1'
    summary: 'Reject Group Join Request'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupMemberService := group.GroupMemberService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupMemberService.RejectGroupJoinRequestPublicV1Short(
            &group_member.RejectGroupJoinRequestPublicV1Params{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group Member'
    name: 'rejectGroupJoinRequestPublicV2'
    summary: 'Reject Group Join Request'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupMemberService := group.GroupMemberService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupMemberService.RejectGroupJoinRequestPublicV2Short(
            &group_member.RejectGroupJoinRequestPublicV2Params{
                GroupID:   ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration'
    name: 'updateGroupConfigurationAdminV1'
    summary: 'update existing configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        configurationService := group.ConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationService.UpdateGroupConfigurationAdminV1Short(
            &configuration.UpdateGroupConfigurationAdminV1Params{
                Body:              ..., // *groupclientmodels.ModelsUpdateGroupConfigurationRequestV1
                ConfigurationCode: ..., // string
                Namespace:         ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration'
    name: 'updateGroupConfigurationGlobalRuleAdminV1'
    summary: 'update existing configuration global rule'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        configurationService := group.ConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationService.UpdateGroupConfigurationGlobalRuleAdminV1Short(
            &configuration.UpdateGroupConfigurationGlobalRuleAdminV1Params{
                Body:              ..., // *groupclientmodels.ModelsUpdateGroupConfigurationGlobalRulesRequestV1
                AllowedAction:     ..., // string
                ConfigurationCode: ..., // string
                Namespace:         ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group'
    name: 'updateGroupCustomAttributesPublicV1'
    summary: 'Update group custom attributes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupService := group.GroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupService.UpdateGroupCustomAttributesPublicV1Short(
            &group.UpdateGroupCustomAttributesPublicV1Params{
                Body:      ..., // *groupclientmodels.ModelsUpdateGroupCustomAttributesRequestV1
                GroupID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group'
    name: 'updateGroupCustomAttributesPublicV2'
    summary: 'Update group custom attributes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupService := group.GroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupService.UpdateGroupCustomAttributesPublicV2Short(
            &group.UpdateGroupCustomAttributesPublicV2Params{
                Body:      ..., // *groupclientmodels.ModelsUpdateGroupCustomAttributesRequestV1
                GroupID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group'
    name: 'updateGroupCustomRulePublicV1'
    summary: 'update group custom rule'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupService := group.GroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupService.UpdateGroupCustomRulePublicV1Short(
            &group.UpdateGroupCustomRulePublicV1Params{
                Body:      ..., // *groupclientmodels.ModelsUpdateGroupCustomRuleRequestV1
                GroupID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group'
    name: 'updateGroupCustomRulePublicV2'
    summary: 'update group custom rule'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupService := group.GroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupService.UpdateGroupCustomRulePublicV2Short(
            &group.UpdateGroupCustomRulePublicV2Params{
                Body:      ..., // *groupclientmodels.ModelsUpdateGroupCustomRuleRequestV1
                GroupID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group'
    name: 'updateGroupPredefinedRulePublicV1'
    summary: 'update predefined group rule'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupService := group.GroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupService.UpdateGroupPredefinedRulePublicV1Short(
            &group.UpdateGroupPredefinedRulePublicV1Params{
                Body:          ..., // *groupclientmodels.ModelsUpdateGroupPredefinedRuleRequestV1
                AllowedAction: ..., // string
                GroupID:       ..., // string
                Namespace:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group'
    name: 'updateGroupPredefinedRulePublicV2'
    summary: 'update predefined group rule'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupService := group.GroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupService.UpdateGroupPredefinedRulePublicV2Short(
            &group.UpdateGroupPredefinedRulePublicV2Params{
                Body:          ..., // *groupclientmodels.ModelsUpdateGroupPredefinedRuleRequestV1
                AllowedAction: ..., // string
                GroupID:       ..., // string
                Namespace:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group Roles'
    name: 'updateMemberRoleAdminV1'
    summary: 'update member role'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupRolesService := group.GroupRolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupRolesService.UpdateMemberRoleAdminV1Short(
            &group_roles.UpdateMemberRoleAdminV1Params{
                Body:         ..., // *groupclientmodels.ModelsUpdateMemberRoleRequestV1
                MemberRoleID: ..., // string
                Namespace:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group Roles'
    name: 'updateMemberRolePermissionAdminV1'
    summary: 'update member role permission'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupRolesService := group.GroupRolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupRolesService.UpdateMemberRolePermissionAdminV1Short(
            &group_roles.UpdateMemberRolePermissionAdminV1Params{
                Body:         ..., // *groupclientmodels.ModelsUpdateMemberRolePermissionsRequestV1
                MemberRoleID: ..., // string
                Namespace:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group Roles'
    name: 'updateMemberRolePublicV1'
    summary: 'Assign role to group member'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupRolesService := group.GroupRolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupRolesService.UpdateMemberRolePublicV1Short(
            &group_roles.UpdateMemberRolePublicV1Params{
                Body:         ..., // *groupclientmodels.ModelsAssignRoleToMemberRequestV1
                MemberRoleID: ..., // string
                Namespace:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group Roles'
    name: 'updateMemberRolePublicV2'
    summary: 'Assign role to group member'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupRolesService := group.GroupRolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupRolesService.UpdateMemberRolePublicV2Short(
            &group_roles.UpdateMemberRolePublicV2Params{
                Body:         ..., // *groupclientmodels.ModelsAssignRoleToMemberRequestV1
                GroupID:      ..., // string
                MemberRoleID: ..., // string
                Namespace:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group'
    name: 'updatePatchSingleGroupPublicV1'
    summary: 'update existing group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupService := group.GroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupService.UpdatePatchSingleGroupPublicV1Short(
            &group.UpdatePatchSingleGroupPublicV1Params{
                Body:      ..., // *groupclientmodels.ModelsUpdateGroupRequestV1
                GroupID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group'
    name: 'updatePatchSingleGroupPublicV2'
    summary: 'update existing group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupService := group.GroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupService.UpdatePatchSingleGroupPublicV2Short(
            &group.UpdatePatchSingleGroupPublicV2Params{
                Body:      ..., // *groupclientmodels.ModelsUpdateGroupRequestV1
                GroupID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group'
    name: 'updatePutSingleGroupPublicV2'
    summary: 'update existing group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupService := group.GroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupService.UpdatePutSingleGroupPublicV2Short(
            &group.UpdatePutSingleGroupPublicV2Params{
                Body:      ..., // *groupclientmodels.ModelsUpdateGroupRequestV1
                GroupID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Group'
    name: 'updateSingleGroupV1'
    summary: 'update existing group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewGroupClient(configRepo)
        groupService := group.GroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := groupService.UpdateSingleGroupV1Short(
            &group.UpdateSingleGroupV1Params{
                Body:      ..., // *groupclientmodels.ModelsUpdateGroupRequestV1
                GroupID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }
# end group
# begin iam
  - ns: 'Clients'
    name: 'AddClientPermission'
    summary: 'Add Client Permission'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        clientsService := iam.ClientsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := clientsService.AddClientPermissionShort(
            &clients.AddClientPermissionParams{
                Action:   ..., // int64
                ClientID: ..., // string
                Resource: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'AddRoleManagers'
    summary: 'Add Role Managers'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.AddRoleManagersShort(
            &roles.AddRoleManagersParams{
                Body:   ..., // *iamclientmodels.ModelRoleManagersRequest
                RoleID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'AddRoleMembers'
    summary: 'Add Role Members'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.AddRoleMembersShort(
            &roles.AddRoleMembersParams{
                Body:   ..., // *iamclientmodels.ModelRoleMembersRequest
                RoleID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'AddRolePermission'
    summary: 'Add Role Permission'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.AddRolePermissionShort(
            &roles.AddRolePermissionParams{
                Body:     ..., // *iamclientmodels.ModelUpdatePermissionScheduleRequest
                Action:   ..., // int64
                Resource: ..., // string
                RoleID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'SSO Credential'
    name: 'AddSSOLoginPlatformCredential'
    summary: 'Add SSO Platform Credential'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        ssoCredentialService := iam.SSOCredentialService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := ssoCredentialService.AddSSOLoginPlatformCredentialShort(
            &sso_credential.AddSSOLoginPlatformCredentialParams{
                Body:       ..., // *iamclientmodels.ModelSSOPlatformCredentialRequest
                Namespace:  ..., // string
                PlatformID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Third Party Credential'
    name: 'AddThirdPartyLoginPlatformCredentialV3'
    summary: 'Add Third Party Platform Credential'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        thirdPartyCredentialService := iam.ThirdPartyCredentialService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := thirdPartyCredentialService.AddThirdPartyLoginPlatformCredentialV3Short(
            &third_party_credential.AddThirdPartyLoginPlatformCredentialV3Params{
                Body:       ..., // *iamclientmodels.ModelThirdPartyLoginPlatformCredentialRequest
                Namespace:  ..., // string
                PlatformID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AddUserPermission'
    summary: 'Add User Permission'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AddUserPermissionShort(
            &users.AddUserPermissionParams{
                Body:      ..., // *iamclientmodels.ModelUpdatePermissionScheduleRequest
                Action:    ..., // int64
                Namespace: ..., // string
                Resource:  ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AddUserRole'
    summary: 'Add User Role'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AddUserRoleShort(
            &users.AddUserRoleParams{
                Namespace: ..., // string
                RoleID:    ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Clients'
    name: 'AdminAddClientPermissionsV3'
    summary: 'Add Client Permissions'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        clientsService := iam.ClientsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := clientsService.AdminAddClientPermissionsV3Short(
            &clients.AdminAddClientPermissionsV3Params{
                Body:      ..., // *iamclientmodels.AccountcommonClientPermissionsV3
                ClientID:  ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Country'
    name: 'AdminAddCountryBlacklistV3'
    summary: 'Update Country Blacklist'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        countryService := iam.CountryService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := countryService.AdminAddCountryBlacklistV3Short(
            &country.AdminAddCountryBlacklistV3Params{
                Body:      ..., // *iamclientmodels.ModelCountryBlacklistRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'AdminAddRoleManagersV3'
    summary: 'Add Role Managers'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.AdminAddRoleManagersV3Short(
            &roles.AdminAddRoleManagersV3Params{
                Body:   ..., // *iamclientmodels.ModelRoleManagersRequestV3
                RoleID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'AdminAddRoleMembersV3'
    summary: 'Add Role Members'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.AdminAddRoleMembersV3Short(
            &roles.AdminAddRoleMembersV3Params{
                Body:   ..., // *iamclientmodels.ModelRoleMembersRequestV3
                RoleID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'AdminAddRolePermissionsV3'
    summary: 'Add Role Permissions'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.AdminAddRolePermissionsV3Short(
            &roles.AdminAddRolePermissionsV3Params{
                Body:   ..., // *iamclientmodels.AccountcommonPermissionsV3
                RoleID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'AdminAddRolePermissionsV4'
    summary: 'Add Role Permissions V4'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.AdminAddRolePermissionsV4Short(
            &roles.AdminAddRolePermissionsV4Params{
                Body:   ..., // *iamclientmodels.AccountcommonPermissionsV3
                RoleID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminAddUserPermissionsV3'
    summary: 'Add User Permissions'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminAddUserPermissionsV3Short(
            &users.AdminAddUserPermissionsV3Params{
                Body:      ..., // *iamclientmodels.AccountcommonPermissions
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminAddUserRoleV3'
    summary: 'Add User Role'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminAddUserRoleV3Short(
            &users.AdminAddUserRoleV3Params{
                Namespace: ..., // string
                RoleID:    ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminAddUserRoleV4'
    summary: 'Admin Add User''s Role V4'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminAddUserRoleV4Short(
            &users_v4.AdminAddUserRoleV4Params{
                Body:      ..., // *iamclientmodels.ModelAddUserRoleV4Request
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'AdminAssignUserToRoleV4'
    summary: 'Assign User to Role'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.AdminAssignUserToRoleV4Short(
            &roles.AdminAssignUserToRoleV4Params{
                Body:   ..., // *iamclientmodels.ModelAssignUserV4Request
                RoleID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Devices V4'
    name: 'AdminBanDeviceV4'
    summary: 'Admin Ban a device'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        devicesV4Service := iam.DevicesV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := devicesV4Service.AdminBanDeviceV4Short(
            &devices_v4.AdminBanDeviceV4Params{
                Body:      ..., // *iamclientmodels.ModelDeviceBanRequestV4
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Bans'
    name: 'AdminBanUserBulkV3'
    summary: 'Bulk ban user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        bansService := iam.BansService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := bansService.AdminBanUserBulkV3Short(
            &bans.AdminBanUserBulkV3Params{
                Body:      ..., // *iamclientmodels.ModelBulkBanCreateRequestV3
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminBanUserV2'
    summary: 'Ban a single user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminBanUserV2Short(
            &users.AdminBanUserV2Params{
                Body:      ..., // *iamclientmodels.ModelBanCreateRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminBanUserV3'
    summary: 'Ban a single user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminBanUserV3Short(
            &users.AdminBanUserV3Params{
                Body:      ..., // *iamclientmodels.ModelBanCreateRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminBulkCheckValidUserIDV4'
    summary: 'Admin Check Valid User ID'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminBulkCheckValidUserIDV4Short(
            &users_v4.AdminBulkCheckValidUserIDV4Params{
                Body:      ..., // *iamclientmodels.ModelCheckValidUserIDRequestV4
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminBulkGetUsersPlatform'
    summary: 'Admin bulk get users'' platform info by user Ids'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminBulkGetUsersPlatformShort(
            &users.AdminBulkGetUsersPlatformParams{
                Body:      ..., // *iamclientmodels.ModelUserIDsRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Clients'
    name: 'AdminBulkUpdateClientsV3'
    summary: 'Bulk Update Clients'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        clientsService := iam.ClientsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := clientsService.AdminBulkUpdateClientsV3Short(
            &clients.AdminBulkUpdateClientsV3Params{
                Body:      ..., // *iamclientmodels.ClientmodelClientsUpdateRequestV3
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminBulkUpdateUserAccountTypeV4'
    summary: 'Admin Bulk Update User Account Type V4'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminBulkUpdateUserAccountTypeV4Short(
            &users_v4.AdminBulkUpdateUserAccountTypeV4Params{
                Body:      ..., // *iamclientmodels.ModelBulkAccountTypeUpdateRequestV4
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminBulkUpdateUsersV3'
    summary: 'Update Users'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminBulkUpdateUsersV3Short(
            &users.AdminBulkUpdateUsersV3Params{
                Body:      ..., // *iamclientmodels.ModelUsersUpdateRequestV3
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminChallengeMyMFAV4'
    summary: 'Challenge User MFA'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminChallengeMyMFAV4Short(
            &users_v4.AdminChallengeMyMFAV4Params{
                Code:   ..., // *string
                Factor: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Override Role Config v3'
    name: 'AdminChangeRoleOverrideConfigStatusV3'
    summary: 'Change role override config active status'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        overrideRoleConfigV3Service := iam.OverrideRoleConfigV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := overrideRoleConfigV3Service.AdminChangeRoleOverrideConfigStatusV3Short(
            &override_role_config_v3.AdminChangeRoleOverrideConfigStatusV3Params{
                Body:      ..., // *iamclientmodels.ModelRoleOverrideStatsUpdateRequest
                Namespace: ..., // string
                Identity:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Third Party Credential'
    name: 'AdminCheckThirdPartyLoginPlatformAvailabilityV3'
    summary: 'Check 3rd party platform availability'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        thirdPartyCredentialService := iam.ThirdPartyCredentialService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := thirdPartyCredentialService.AdminCheckThirdPartyLoginPlatformAvailabilityV3Short(
            &third_party_credential.AdminCheckThirdPartyLoginPlatformAvailabilityV3Params{
                PlatformID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Clients'
    name: 'AdminCreateClientV3'
    summary: 'Create Client'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        clientsService := iam.ClientsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := clientsService.AdminCreateClientV3Short(
            &clients.AdminCreateClientV3Params{
                Body:      ..., // *iamclientmodels.ClientmodelClientCreationV3Request
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminCreateJusticeUser'
    summary: 'Create Justice User from Publisher User'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminCreateJusticeUserShort(
            &users.AdminCreateJusticeUserParams{
                Namespace:       ..., // string
                TargetNamespace: ..., // string
                UserID:          ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'AdminCreateRoleV3'
    summary: 'Create Role'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.AdminCreateRoleV3Short(
            &roles.AdminCreateRoleV3Params{
                Body: ..., // *iamclientmodels.ModelRoleCreateV3Request
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'AdminCreateRoleV4'
    summary: 'Create Role'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.AdminCreateRoleV4Short(
            &roles.AdminCreateRoleV4Params{
                Body: ..., // *iamclientmodels.ModelRoleV4Request
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Account Identifier Tag'
    name: 'AdminCreateTagV3'
    summary: 'Create Account Identifier Tag'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        accountIdentifierTagService := iam.AccountIdentifierTagService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := accountIdentifierTagService.AdminCreateTagV3Short(
            &account_identifier_tag.AdminCreateTagV3Params{
                Body:      ..., // *iamclientmodels.ModelTagCreateRequestV3
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminCreateTestUsersV4'
    summary: '[TEST FACILITY ONLY]Create test Users'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminCreateTestUsersV4Short(
            &users_v4.AdminCreateTestUsersV4Params{
                Body:      ..., // *iamclientmodels.AccountCreateTestUsersRequestV4
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminCreateUserRolesV2'
    summary: 'Save User Roles'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminCreateUserRolesV2Short(
            &users.AdminCreateUserRolesV2Params{
                Body:      ..., // []string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminCreateUserV4'
    summary: 'Admin Create User'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminCreateUserV4Short(
            &users_v4.AdminCreateUserV4Params{
                Body:      ..., // *iamclientmodels.AccountCreateUserRequestV4
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminCursorGetUserV3'
    summary: 'Admin Cursor-Based User Retrieval'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminCursorGetUserV3Short(
            &users.AdminCursorGetUserV3Params{
                Body:      ..., // *iamclientmodels.ModelCursorGetUserRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Devices V4'
    name: 'AdminDecryptDeviceV4'
    summary: 'Admin decrypt device id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        devicesV4Service := iam.DevicesV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := devicesV4Service.AdminDecryptDeviceV4Short(
            &devices_v4.AdminDecryptDeviceV4Params{
                DeviceID:  ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Clients'
    name: 'AdminDeleteClientPermissionV3'
    summary: 'Delete Client Permission'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        clientsService := iam.ClientsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := clientsService.AdminDeleteClientPermissionV3Short(
            &clients.AdminDeleteClientPermissionV3Params{
                Action:    ..., // int64
                ClientID:  ..., // string
                Namespace: ..., // string
                Resource:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Clients'
    name: 'AdminDeleteClientV3'
    summary: 'Delete Client'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        clientsService := iam.ClientsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := clientsService.AdminDeleteClientV3Short(
            &clients.AdminDeleteClientV3Params{
                ClientID:  ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Clients Config V3'
    name: 'AdminDeleteConfigPermissionsByGroup'
    summary: 'Delete Client config permissions by module and group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        clientsConfigV3Service := iam.ClientsConfigV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := clientsConfigV3Service.AdminDeleteConfigPermissionsByGroupShort(
            &clients_config_v3.AdminDeleteConfigPermissionsByGroupParams{
                Body:        ..., // *iamclientmodels.ClientmodelPermissionSetDeleteGroupRequest
                ForceDelete: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminDeletePlatformLinkV2'
    summary: 'Delete the link of user''s account with platform'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminDeletePlatformLinkV2Short(
            &users.AdminDeletePlatformLinkV2Params{
                PlatformNamespace: ..., // *string
                Namespace:         ..., // string
                PlatformID:        ..., // string
                UserID:            ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'AdminDeleteRolePermissionsV3'
    summary: 'Delete Role'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.AdminDeleteRolePermissionsV3Short(
            &roles.AdminDeleteRolePermissionsV3Params{
                Body:   ..., // []string
                RoleID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'AdminDeleteRolePermissionsV4'
    summary: 'Delete Role Permission V4'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.AdminDeleteRolePermissionsV4Short(
            &roles.AdminDeleteRolePermissionsV4Params{
                Body:   ..., // []string
                RoleID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'AdminDeleteRolePermissionV3'
    summary: 'Delete Role Permission'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.AdminDeleteRolePermissionV3Short(
            &roles.AdminDeleteRolePermissionV3Params{
                Action:   ..., // int64
                Resource: ..., // string
                RoleID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'AdminDeleteRoleV3'
    summary: 'Delete Role'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.AdminDeleteRoleV3Short(
            &roles.AdminDeleteRoleV3Params{
                RoleID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'AdminDeleteRoleV4'
    summary: 'Delete Role'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.AdminDeleteRoleV4Short(
            &roles.AdminDeleteRoleV4Params{
                RoleID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Account Identifier Tag'
    name: 'AdminDeleteTagV3'
    summary: 'Delete Account Identifier Tag'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        accountIdentifierTagService := iam.AccountIdentifierTagService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := accountIdentifierTagService.AdminDeleteTagV3Short(
            &account_identifier_tag.AdminDeleteTagV3Params{
                Namespace: ..., // string
                TagID:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminDeleteUserInformationV3'
    summary: 'Delete user''s information'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminDeleteUserInformationV3Short(
            &users.AdminDeleteUserInformationV3Params{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminDeleteUserLinkingHistoryByPlatformIDV3'
    summary: 'Admin remove user''s platform linking history.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminDeleteUserLinkingHistoryByPlatformIDV3Short(
            &users.AdminDeleteUserLinkingHistoryByPlatformIDV3Params{
                Namespace:  ..., // string
                PlatformID: ..., // string
                UserID:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminDeleteUserLinkingRestrictionByPlatformIDV3'
    summary: 'Admin remove user''s platform linking restriction.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminDeleteUserLinkingRestrictionByPlatformIDV3Short(
            &users.AdminDeleteUserLinkingRestrictionByPlatformIDV3Params{
                Namespace:  ..., // string
                PlatformID: ..., // string
                UserID:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminDeleteUserPermissionBulkV3'
    summary: 'Delete User Permission'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminDeleteUserPermissionBulkV3Short(
            &users.AdminDeleteUserPermissionBulkV3Params{
                Body:      ..., // []*iamclientmodels.ModelPermissionDeleteRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminDeleteUserPermissionV3'
    summary: 'Delete User Permission'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminDeleteUserPermissionV3Short(
            &users.AdminDeleteUserPermissionV3Params{
                Action:    ..., // int64
                Namespace: ..., // string
                Resource:  ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminDeleteUserRolesV3'
    summary: 'Delete User Roles'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminDeleteUserRolesV3Short(
            &users.AdminDeleteUserRolesV3Params{
                Body:      ..., // []string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminDeleteUserRoleV3'
    summary: 'Delete User Role'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminDeleteUserRoleV3Short(
            &users.AdminDeleteUserRoleV3Params{
                Namespace: ..., // string
                RoleID:    ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminDisableMyAuthenticatorV4'
    summary: 'Disable 2FA authenticator'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminDisableMyAuthenticatorV4Short(
            &users_v4.AdminDisableMyAuthenticatorV4Params{
                Body: ..., // *iamclientmodels.ModelDisableMFARequest
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminDisableMyBackupCodesV4'
    summary: 'Disable 2FA backup codes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminDisableMyBackupCodesV4Short(
            &users_v4.AdminDisableMyBackupCodesV4Params{
                Body: ..., // *iamclientmodels.ModelDisableMFARequest
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminDisableMyEmailV4'
    summary: 'Disable 2FA email'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminDisableMyEmailV4Short(
            &users_v4.AdminDisableMyEmailV4Params{
                Body: ..., // *iamclientmodels.ModelDisableMFARequest
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminDisableUserMFAV4'
    summary: 'Disable User 2FA'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminDisableUserMFAV4Short(
            &users_v4.AdminDisableUserMFAV4Params{
                Body:      ..., // *iamclientmodels.ModelDisableMFARequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminDisableUserV2'
    summary: 'Disable a user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminDisableUserV2Short(
            &users.AdminDisableUserV2Params{
                Body:      ..., // *iamclientmodels.ModelDisableUserRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminDownloadMyBackupCodesV4'
    summary: 'Download user backup codes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminDownloadMyBackupCodesV4Short(
            &users_v4.AdminDownloadMyBackupCodesV4Params{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminEnableBackupCodesV4'
    summary: 'Enable 2FA backup codes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminEnableBackupCodesV4Short(
            &users_v4.AdminEnableBackupCodesV4Params{
                LanguageTag: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminEnableMyAuthenticatorV4'
    summary: 'Enable 2FA authenticator'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminEnableMyAuthenticatorV4Short(
            &users_v4.AdminEnableMyAuthenticatorV4Params{
                Code: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminEnableMyBackupCodesV4'
    summary: 'Enable 2FA backup codes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminEnableMyBackupCodesV4Short(
            &users_v4.AdminEnableMyBackupCodesV4Params{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminEnableMyEmailV4'
    summary: 'Enable 2FA email'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminEnableMyEmailV4Short(
            &users_v4.AdminEnableMyEmailV4Params{
                Code: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminEnableUserV2'
    summary: 'Enable a user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminEnableUserV2Short(
            &users.AdminEnableUserV2Params{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminGenerateBackupCodesV4'
    summary: 'Generate backup codes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminGenerateBackupCodesV4Short(
            &users_v4.AdminGenerateBackupCodesV4Params{
                LanguageTag: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminGenerateMyAuthenticatorKeyV4'
    summary: 'Generate secret key for 3rd-party authenticate app'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminGenerateMyAuthenticatorKeyV4Short(
            &users_v4.AdminGenerateMyAuthenticatorKeyV4Params{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminGenerateMyBackupCodesV4'
    summary: 'Generate backup codes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminGenerateMyBackupCodesV4Short(
            &users_v4.AdminGenerateMyBackupCodesV4Params{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Devices V4'
    name: 'AdminGenerateReportV4'
    summary: 'Admin generate device report'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        devicesV4Service := iam.DevicesV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := devicesV4Service.AdminGenerateReportV4Short(
            &devices_v4.AdminGenerateReportV4Params{
                Namespace:  ..., // string
                EndDate:    ..., // *string
                StartDate:  ..., // *string
                DeviceType: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminGetAgeRestrictionStatusV2'
    summary: 'Get age restriction status'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminGetAgeRestrictionStatusV2Short(
            &users.AdminGetAgeRestrictionStatusV2Params{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminGetAgeRestrictionStatusV3'
    summary: 'Get age restriction status'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminGetAgeRestrictionStatusV3Short(
            &users.AdminGetAgeRestrictionStatusV3Params{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminGetBackupCodesV4'
    summary: 'Get backup codes and send to email'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminGetBackupCodesV4Short(
            &users_v4.AdminGetBackupCodesV4Params{
                LanguageTag: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Devices V4'
    name: 'AdminGetBannedDevicesV4'
    summary: 'Admin get banned devices'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        devicesV4Service := iam.DevicesV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := devicesV4Service.AdminGetBannedDevicesV4Short(
            &devices_v4.AdminGetBannedDevicesV4Params{
                Namespace:  ..., // string
                DeviceType: ..., // *string
                EndDate:    ..., // *string
                Limit:      ..., // *int64
                Offset:     ..., // *int64
                StartDate:  ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Bans'
    name: 'AdminGetBannedUsersV3'
    summary: 'Get list of user filtered by ban types'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        bansService := iam.BansService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := bansService.AdminGetBannedUsersV3Short(
            &bans.AdminGetBannedUsersV3Params{
                Namespace:  ..., // string
                ActiveOnly: ..., // *bool
                BanType:    ..., // *string
                Limit:      ..., // *int64
                Offset:     ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Bans'
    name: 'AdminGetBansTypeV3'
    summary: 'Get list of ban types'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        bansService := iam.BansService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := bansService.AdminGetBansTypeV3Short(
            &bans.AdminGetBansTypeV3Params{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Bans'
    name: 'AdminGetBansTypeWithNamespaceV3'
    summary: 'Get list of ban types'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        bansService := iam.BansService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := bansService.AdminGetBansTypeWithNamespaceV3Short(
            &bans.AdminGetBansTypeWithNamespaceV3Params{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminGetBulkUserBanV3'
    summary: 'Get bulk user bans'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminGetBulkUserBanV3Short(
            &users.AdminGetBulkUserBanV3Params{
                Body:       ..., // *iamclientmodels.ModelGetBulkUserBansRequest
                Namespace:  ..., // string
                ActiveOnly: ..., // *bool
                BanType:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminGetBulkUserByEmailAddressV3'
    summary: 'Get Bulk User By Email Address'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminGetBulkUserByEmailAddressV3Short(
            &users.AdminGetBulkUserByEmailAddressV3Params{
                Body:      ..., // *iamclientmodels.ModelListEmailAddressRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Clients'
    name: 'AdminGetClientsbyNamespacebyIDV3'
    summary: 'Get clients by namespace and client id. Multi tenant mode allows admin portal client to be accessible regardless of namespace path.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        clientsService := iam.ClientsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := clientsService.AdminGetClientsbyNamespacebyIDV3Short(
            &clients.AdminGetClientsbyNamespacebyIDV3Params{
                ClientID:  ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Clients'
    name: 'AdminGetClientsByNamespaceV3'
    summary: 'Get clients by namespace'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        clientsService := iam.ClientsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := clientsService.AdminGetClientsByNamespaceV3Short(
            &clients.AdminGetClientsByNamespaceV3Params{
                Namespace:      ..., // string
                ClientID:       ..., // *string
                ClientName:     ..., // *string
                ClientType:     ..., // *string
                Limit:          ..., // *int64
                Offset:         ..., // *int64
                SkipLoginQueue: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Config'
    name: 'AdminGetConfigValueV3'
    summary: 'Get Config Value'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        configService := iam.ConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configService.AdminGetConfigValueV3Short(
            &config.AdminGetConfigValueV3Params{
                ConfigKey: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Country'
    name: 'AdminGetCountryBlacklistV3'
    summary: 'Get Country Blacklist'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        countryService := iam.CountryService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := countryService.AdminGetCountryBlacklistV3Short(
            &country.AdminGetCountryBlacklistV3Params{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Country'
    name: 'AdminGetCountryListV3'
    summary: 'Get Country List'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        countryService := iam.CountryService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := countryService.AdminGetCountryListV3Short(
            &country.AdminGetCountryListV3Params{
                Namespace:       ..., // string
                FilterBlacklist: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Devices V4'
    name: 'AdminGetDeviceBansV4'
    summary: 'Admin get device ban list'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        devicesV4Service := iam.DevicesV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := devicesV4Service.AdminGetDeviceBansV4Short(
            &devices_v4.AdminGetDeviceBansV4Params{
                DeviceID:  ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Devices V4'
    name: 'AdminGetDeviceBanV4'
    summary: 'Admin get device ban config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        devicesV4Service := iam.DevicesV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := devicesV4Service.AdminGetDeviceBanV4Short(
            &devices_v4.AdminGetDeviceBanV4Params{
                BanID:     ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Devices V4'
    name: 'AdminGetDevicesByUserV4'
    summary: 'Admin get devices by user id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        devicesV4Service := iam.DevicesV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := devicesV4Service.AdminGetDevicesByUserV4Short(
            &devices_v4.AdminGetDevicesByUserV4Params{
                Namespace: ..., // string
                UserID:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Devices V4'
    name: 'AdminGetDeviceTypesV4'
    summary: 'Admin get device types'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        devicesV4Service := iam.DevicesV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := devicesV4Service.AdminGetDeviceTypesV4Short(
            &devices_v4.AdminGetDeviceTypesV4Params{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'InputValidations'
    name: 'AdminGetInputValidations'
    summary: 'Admin Get Input Validations'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        inputValidationsService := iam.InputValidationsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := inputValidationsService.AdminGetInputValidationsShort(
            &input_validations.AdminGetInputValidationsParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Bans'
    name: 'AdminGetListBanReasonV3'
    summary: 'Get list of ban reasons'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        bansService := iam.BansService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := bansService.AdminGetListBanReasonV3Short(
            &bans.AdminGetListBanReasonV3Params{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminGetListCountryAgeRestrictionV3'
    summary: 'Get List Country age restriction'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminGetListCountryAgeRestrictionV3Short(
            &users.AdminGetListCountryAgeRestrictionV3Params{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminGetListJusticePlatformAccounts'
    summary: 'Get User Justice Platform Accounts'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminGetListJusticePlatformAccountsShort(
            &users.AdminGetListJusticePlatformAccountsParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Login Allowlist'
    name: 'AdminGetLoginAllowlistV3'
    summary: 'Admin get login allowlist'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        loginAllowlistService := iam.LoginAllowlistService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := loginAllowlistService.AdminGetLoginAllowlistV3Short(
            &login_allowlist.AdminGetLoginAllowlistV3Params{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminGetMyBackupCodesV4'
    summary: 'Get backup codes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminGetMyBackupCodesV4Short(
            &users_v4.AdminGetMyBackupCodesV4Params{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminGetMyEnabledFactorsV4'
    summary: 'Get user enabled factors'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminGetMyEnabledFactorsV4Short(
            &users_v4.AdminGetMyEnabledFactorsV4Params{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminGetMyMFAStatusV4'
    summary: 'Get User MFA Status'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminGetMyMFAStatusV4Short(
            &users_v4.AdminGetMyMFAStatusV4Params{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminGetMyOwnMFAStatusV4'
    summary: 'Get Admin Own MFA Status'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminGetMyOwnMFAStatusV4Short(
            &users_v4.AdminGetMyOwnMFAStatusV4Params{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminGetMyUserV3'
    summary: 'Get My User'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminGetMyUserV3Short(
            &users.AdminGetMyUserV3Params{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminGetNamespaceInvitationHistoryV4'
    summary: 'Admin Get Invitation History for specific namespace V4'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminGetNamespaceInvitationHistoryV4Short(
            &users_v4.AdminGetNamespaceInvitationHistoryV4Params{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminGetNamespaceUserInvitationHistoryV4'
    summary: 'Admin Get Users Invitation History for specific namespace V4'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminGetNamespaceUserInvitationHistoryV4Short(
            &users_v4.AdminGetNamespaceUserInvitationHistoryV4Params{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Profile Update Strategy'
    name: 'AdminGetProfileUpdateStrategyV3'
    summary: 'Admin get profile update strategy by namespace and field.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        profileUpdateStrategyService := iam.ProfileUpdateStrategyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := profileUpdateStrategyService.AdminGetProfileUpdateStrategyV3Short(
            &profile_update_strategy.AdminGetProfileUpdateStrategyV3Params{
                Namespace: ..., // string
                Field:     ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'AdminGetRoleAdminStatusV3'
    summary: 'Get Role Admin Status'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.AdminGetRoleAdminStatusV3Short(
            &roles.AdminGetRoleAdminStatusV3Params{
                RoleID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'AdminGetRoleManagersV3'
    summary: 'Get Role Managers'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.AdminGetRoleManagersV3Short(
            &roles.AdminGetRoleManagersV3Params{
                RoleID: ..., // string
                After:  ..., // *string
                Before: ..., // *string
                Limit:  ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'AdminGetRoleMembersV3'
    summary: 'Get Role Members'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.AdminGetRoleMembersV3Short(
            &roles.AdminGetRoleMembersV3Params{
                RoleID: ..., // string
                After:  ..., // *string
                Before: ..., // *string
                Limit:  ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Override Role Config v3'
    name: 'AdminGetRoleNamespacePermissionV3'
    summary: 'Get role namespace permission'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        overrideRoleConfigV3Service := iam.OverrideRoleConfigV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := overrideRoleConfigV3Service.AdminGetRoleNamespacePermissionV3Short(
            &override_role_config_v3.AdminGetRoleNamespacePermissionV3Params{
                Namespace: ..., // string
                RoleID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Override Role Config v3'
    name: 'AdminGetRoleOverrideConfigV3'
    summary: 'Get role override config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        overrideRoleConfigV3Service := iam.OverrideRoleConfigV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := overrideRoleConfigV3Service.AdminGetRoleOverrideConfigV3Short(
            &override_role_config_v3.AdminGetRoleOverrideConfigV3Params{
                Namespace: ..., // string
                Identity:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Override Role Config v3'
    name: 'AdminGetRoleSourceV3'
    summary: 'Get role source permission set'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        overrideRoleConfigV3Service := iam.OverrideRoleConfigV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := overrideRoleConfigV3Service.AdminGetRoleSourceV3Short(
            &override_role_config_v3.AdminGetRoleSourceV3Params{
                Namespace: ..., // string
                Identity:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'AdminGetRolesV3'
    summary: 'Get Roles'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.AdminGetRolesV3Short(
            &roles.AdminGetRolesV3Params{
                After:      ..., // *string
                Before:     ..., // *string
                IsWildcard: ..., // *bool
                Limit:      ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'AdminGetRolesV4'
    summary: 'Get Roles'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.AdminGetRolesV4Short(
            &roles.AdminGetRolesV4Params{
                AdminRole:  ..., // *bool
                IsWildcard: ..., // *bool
                Limit:      ..., // *int64
                Offset:     ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'AdminGetRoleV3'
    summary: 'Get Role'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.AdminGetRoleV3Short(
            &roles.AdminGetRoleV3Params{
                RoleID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'AdminGetRoleV4'
    summary: 'Get Role'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.AdminGetRoleV4Short(
            &roles.AdminGetRoleV4Params{
                RoleID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminGetThirdPartyPlatformTokenLinkStatusV3'
    summary: 'Admin get the link status of the third party platform token with user id.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminGetThirdPartyPlatformTokenLinkStatusV3Short(
            &users.AdminGetThirdPartyPlatformTokenLinkStatusV3Params{
                PlatformToken: ..., // string
                Namespace:     ..., // string
                PlatformID:    ..., // string
                UserID:        ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminGetUserBanSummaryV3'
    summary: 'Get user''s bans summary'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminGetUserBanSummaryV3Short(
            &users.AdminGetUserBanSummaryV3Params{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminGetUserBanV2'
    summary: 'Get user''s bans'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminGetUserBanV2Short(
            &users.AdminGetUserBanV2Params{
                Namespace:  ..., // string
                UserID:     ..., // string
                ActiveOnly: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminGetUserBanV3'
    summary: 'Get user''s bans'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminGetUserBanV3Short(
            &users.AdminGetUserBanV3Params{
                Namespace:  ..., // string
                UserID:     ..., // string
                ActiveOnly: ..., // *bool
                After:      ..., // *string
                Before:     ..., // *string
                Limit:      ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminGetUserByEmailAddressV3'
    summary: 'Get User By Email Address'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminGetUserByEmailAddressV3Short(
            &users.AdminGetUserByEmailAddressV3Params{
                Namespace:    ..., // string
                EmailAddress: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminGetUserByPlatformUserIDV3'
    summary: 'Admin Get User By Platform User ID'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminGetUserByPlatformUserIDV3Short(
            &users.AdminGetUserByPlatformUserIDV3Params{
                Namespace:      ..., // string
                PlatformID:     ..., // string
                PlatformUserID: ..., // string
                PidType:        ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminGetUserByUserIdV2'
    summary: 'Get User By User Id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminGetUserByUserIDV2Short(
            &users.AdminGetUserByUserIDV2Params{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminGetUserByUserIdV3'
    summary: 'Admin Get User By User Id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminGetUserByUserIDV3Short(
            &users.AdminGetUserByUserIDV3Params{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminGetUserDeletionStatusV3'
    summary: 'Get User Deletion Status'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminGetUserDeletionStatusV3Short(
            &users.AdminGetUserDeletionStatusV3Params{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Devices V4'
    name: 'AdminGetUserDeviceBansV4'
    summary: 'Admin get device bans of user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        devicesV4Service := iam.DevicesV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := devicesV4Service.AdminGetUserDeviceBansV4Short(
            &devices_v4.AdminGetUserDeviceBansV4Params{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminGetUserLinkHistoriesV3'
    summary: 'Admin get user''s platform link histories.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminGetUserLinkHistoriesV3Short(
            &users.AdminGetUserLinkHistoriesV3Params{
                Namespace:  ..., // string
                UserID:     ..., // string
                PlatformID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminGetUserLoginHistoriesV3'
    summary: 'Get User''s Login Histories'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminGetUserLoginHistoriesV3Short(
            &users.AdminGetUserLoginHistoriesV3Params{
                Namespace: ..., // string
                UserID:    ..., // string
                After:     ..., // *float64
                Before:    ..., // *float64
                Limit:     ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminGetUserMapping'
    summary: 'Get user mapping'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminGetUserMappingShort(
            &users.AdminGetUserMappingParams{
                Namespace:        ..., // string
                TargetNamespace:  ..., // string
                UserID:           ..., // string
                CreateIfNotFound: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminGetUserMFAStatusV4'
    summary: 'Get User 2FA Status'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminGetUserMFAStatusV4Short(
            &users_v4.AdminGetUserMFAStatusV4Params{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminGetUserPlatformAccountsV3'
    summary: 'Get platform accounts linked to the user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminGetUserPlatformAccountsV3Short(
            &users.AdminGetUserPlatformAccountsV3Params{
                Namespace:       ..., // string
                UserID:          ..., // string
                After:           ..., // *string
                Before:          ..., // *string
                Limit:           ..., // *int64
                PlatformID:      ..., // *string
                TargetNamespace: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Devices V4'
    name: 'AdminGetUsersByDeviceV4'
    summary: 'Admin get users by device id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        devicesV4Service := iam.DevicesV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := devicesV4Service.AdminGetUsersByDeviceV4Short(
            &devices_v4.AdminGetUsersByDeviceV4Params{
                DeviceID:  ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminGetUserSinglePlatformAccount'
    summary: 'Admin get user single platform account metadata'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminGetUserSinglePlatformAccountShort(
            &users.AdminGetUserSinglePlatformAccountParams{
                Namespace:      ..., // string
                PlatformID:     ..., // string
                UserID:         ..., // string
                CrossNamespace: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminGetUserStateByUserIdV3'
    summary: 'Admin Get User State By User Id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminGetUserStateByUserIDV3Short(
            &users.AdminGetUserStateByUserIDV3Params{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminInviteUserNewV4'
    summary: 'Admin invite User v4'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminInviteUserNewV4Short(
            &users_v4.AdminInviteUserNewV4Params{
                Body: ..., // *iamclientmodels.ModelInviteUserRequestV4
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminInviteUserV3'
    summary: 'Invite User'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminInviteUserV3Short(
            &users.AdminInviteUserV3Params{
                Body:      ..., // *iamclientmodels.ModelInviteUserRequestV3
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminInviteUserV4'
    summary: 'Admin invite User v4'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminInviteUserV4Short(
            &users_v4.AdminInviteUserV4Params{
                Body: ..., // *iamclientmodels.ModelInviteUserRequestV4
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminLinkPlatformAccount'
    summary: 'Link a Platform User Account to User Account'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminLinkPlatformAccountShort(
            &users.AdminLinkPlatformAccountParams{
                Body:         ..., // *iamclientmodels.ModelLinkPlatformAccountRequest
                Namespace:    ..., // string
                UserID:       ..., // string
                SkipConflict: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminListAllDistinctPlatformAccountsV3'
    summary: 'Admin get distinct platforms linked to the user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminListAllDistinctPlatformAccountsV3Short(
            &users.AdminListAllDistinctPlatformAccountsV3Params{
                Namespace: ..., // string
                UserID:    ..., // string
                Status:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'AdminListAssignedUsersV4'
    summary: 'Get Role''s Assigned Users'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.AdminListAssignedUsersV4Short(
            &roles.AdminListAssignedUsersV4Params{
                RoleID: ..., // string
                After:  ..., // *string
                Before: ..., // *string
                Limit:  ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Clients Config V3'
    name: 'AdminListClientAvailablePermissions'
    summary: 'List Client available permissions'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        clientsConfigV3Service := iam.ClientsConfigV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := clientsConfigV3Service.AdminListClientAvailablePermissionsShort(
            &clients_config_v3.AdminListClientAvailablePermissionsParams{
                ExcludePermissions: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Clients Config V3'
    name: 'AdminListClientTemplates'
    summary: 'List Client templates'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        clientsConfigV3Service := iam.ClientsConfigV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := clientsConfigV3Service.AdminListClientTemplatesShort(
            &clients_config_v3.AdminListClientTemplatesParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminListInvitationHistoriesV4'
    summary: 'Admin List Invitation Histories V4'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminListInvitationHistoriesV4Short(
            &users_v4.AdminListInvitationHistoriesV4Params{
                Limit:     ..., // *int64
                Namespace: ..., // *string
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminListUserAllPlatformAccountsDistinctV3'
    summary: 'Admin get distinct platform accounts linked to the user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminListUserAllPlatformAccountsDistinctV3Short(
            &users.AdminListUserAllPlatformAccountsDistinctV3Params{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminListUserIDByPlatformUserIDsV3'
    summary: 'Admin List User ID By Platform User ID'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminListUserIDByPlatformUserIDsV3Short(
            &users.AdminListUserIDByPlatformUserIDsV3Params{
                Body:       ..., // *iamclientmodels.ModelPlatformUserIDRequest
                Namespace:  ..., // string
                PlatformID: ..., // string
                RawPID:     ..., // *bool
                RawPUID:    ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminListUserIDByUserIDsV3'
    summary: 'List User By User ID'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminListUserIDByUserIDsV3Short(
            &users.AdminListUserIDByUserIDsV3Params{
                Body:      ..., // *iamclientmodels.ModelAdminBulkUserRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminListUserRolesV4'
    summary: 'Admin List User''s Roles V4'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminListUserRolesV4Short(
            &users_v4.AdminListUserRolesV4Params{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminListUsersV3'
    summary: 'Admin List Users V3'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminListUsersV3Short(
            &users.AdminListUsersV3Params{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminMakeFactorMyDefaultV4'
    summary: 'Make 2FA factor default'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminMakeFactorMyDefaultV4Short(
            &users_v4.AdminMakeFactorMyDefaultV4Params{
                Factor: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminPlatformLinkV3'
    summary: 'Link user''s account with platform'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminPlatformLinkV3Short(
            &users.AdminPlatformLinkV3Params{
                Ticket:     ..., // string
                Namespace:  ..., // string
                PlatformID: ..., // string
                UserID:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminPlatformUnlinkAllV3'
    summary: 'Admin unlink user''s account from specific platform for all namespaces'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminPlatformUnlinkAllV3Short(
            &users.AdminPlatformUnlinkAllV3Params{
                Namespace:  ..., // string
                PlatformID: ..., // string
                UserID:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminPlatformUnlinkV3'
    summary: 'Admin unlink user''s account from specific platform'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminPlatformUnlinkV3Short(
            &users.AdminPlatformUnlinkV3Params{
                Body:       ..., // *iamclientmodels.ModelUnlinkUserPlatformRequest
                Namespace:  ..., // string
                PlatformID: ..., // string
                UserID:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminPutUserRolesV2'
    summary: 'Update User Roles, will replace all the existing roles'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminPutUserRolesV2Short(
            &users.AdminPutUserRolesV2Params{
                Body:      ..., // []string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Account Identifier Tag'
    name: 'AdminQueryTagV3'
    summary: 'Query Account Identifier Tag'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        accountIdentifierTagService := iam.AccountIdentifierTagService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := accountIdentifierTagService.AdminQueryTagV3Short(
            &account_identifier_tag.AdminQueryTagV3Params{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                TagName:   ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminQueryThirdPlatformLinkHistoryV3'
    summary: 'Search linking history of the query platform with platform user id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminQueryThirdPlatformLinkHistoryV3Short(
            &users.AdminQueryThirdPlatformLinkHistoryV3Params{
                Namespace:      ..., // string
                Limit:          ..., // *int64
                Offset:         ..., // *int64
                PlatformUserID: ..., // *string
                PlatformID:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'AdminRemoveRoleAdminV3'
    summary: 'Remove Role Admin Status'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.AdminRemoveRoleAdminV3Short(
            &roles.AdminRemoveRoleAdminV3Params{
                RoleID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'AdminRemoveRoleManagersV3'
    summary: 'Remove Role Managers'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.AdminRemoveRoleManagersV3Short(
            &roles.AdminRemoveRoleManagersV3Params{
                Body:   ..., // *iamclientmodels.ModelRoleManagersRequestV3
                RoleID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'AdminRemoveRoleMembersV3'
    summary: 'Remove Role Members'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.AdminRemoveRoleMembersV3Short(
            &roles.AdminRemoveRoleMembersV3Params{
                Body:   ..., // *iamclientmodels.ModelRoleMembersRequestV3
                RoleID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminRemoveUserRoleV4'
    summary: 'Admin Remove User Role v4'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminRemoveUserRoleV4Short(
            &users_v4.AdminRemoveUserRoleV4Params{
                Body:      ..., // *iamclientmodels.ModelRemoveUserRoleV4Request
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'InputValidations'
    name: 'AdminResetInputValidations'
    summary: 'Admin Reset Input Validations'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        inputValidationsService := iam.InputValidationsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := inputValidationsService.AdminResetInputValidationsShort(
            &input_validations.AdminResetInputValidationsParams{
                Field: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminResetPasswordV2'
    summary: 'Update User Password'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminResetPasswordV2Short(
            &users.AdminResetPasswordV2Params{
                Body:      ..., // *iamclientmodels.ModelUserPasswordUpdateRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminResetPasswordV3'
    summary: 'Update User Password'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminResetPasswordV3Short(
            &users.AdminResetPasswordV3Params{
                Body:      ..., // *iamclientmodels.ModelUserPasswordUpdateV3Request
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0'
    name: 'AdminRetrieveUserThirdPartyPlatformTokenV3'
    summary: 'Admin Retrieve User Third Party Platform Token'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20Service := iam.OAuth20Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20Service.AdminRetrieveUserThirdPartyPlatformTokenV3Short(
            &o_auth2_0.AdminRetrieveUserThirdPartyPlatformTokenV3Params{
                Namespace:      ..., // string
                PlatformID:     ..., // string
                UserID:         ..., // string
                PlatformUserID: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'AdminRevokeUserFromRoleV4'
    summary: 'Revoke User from Role'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.AdminRevokeUserFromRoleV4Short(
            &roles.AdminRevokeUserFromRoleV4Params{
                Body:   ..., // *iamclientmodels.ModelRevokeUserV4Request
                RoleID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminSaveUserRoleV3'
    summary: 'Admin Save User Role V3'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminSaveUserRoleV3Short(
            &users.AdminSaveUserRoleV3Params{
                Body:      ..., // []*iamclientmodels.ModelNamespaceRoleRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminSearchUsersV2'
    summary: 'Search Users'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminSearchUsersV2Short(
            &users.AdminSearchUsersV2Params{
                Namespace:      ..., // string
                After:          ..., // *string
                Before:         ..., // *string
                DisplayName:    ..., // *string
                Limit:          ..., // *int64
                LoginID:        ..., // *string
                PlatformUserID: ..., // *string
                RoleID:         ..., // *string
                UserID:         ..., // *string
                PlatformID:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminSearchUserV3'
    summary: 'Search User'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminSearchUserV3Short(
            &users.AdminSearchUserV3Params{
                Namespace:      ..., // string
                By:             ..., // *string
                EndDate:        ..., // *string
                IncludeTotal:   ..., // *bool
                Limit:          ..., // *int64
                Offset:         ..., // *int64
                PlatformBy:     ..., // *string
                PlatformID:     ..., // *string
                Query:          ..., // *string
                RoleIds:        ..., // *string
                SelectedFields: ..., // *string
                SkipLoginQueue: ..., // *bool
                StartDate:      ..., // *string
                TagIds:         ..., // *string
                TestAccount:    ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminSendMyMFAEmailCodeV4'
    summary: 'Send code for MFA email'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminSendMyMFAEmailCodeV4Short(
            &users_v4.AdminSendMyMFAEmailCodeV4Params{
                Action:      ..., // *string
                LanguageTag: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminSendVerificationCodeV3'
    summary: 'Send verification code to user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminSendVerificationCodeV3Short(
            &users.AdminSendVerificationCodeV3Params{
                Body:      ..., // *iamclientmodels.ModelSendVerificationCodeRequestV3
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminTrustlyUpdateUserIdentity'
    summary: 'Update User Identity'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminTrustlyUpdateUserIdentityShort(
            &users.AdminTrustlyUpdateUserIdentityParams{
                Body:      ..., // *iamclientmodels.ModelUserIdentityUpdateRequestV3
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Devices V4'
    name: 'AdminUnbanDeviceV4'
    summary: 'Admin unban device'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        devicesV4Service := iam.DevicesV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := devicesV4Service.AdminUnbanDeviceV4Short(
            &devices_v4.AdminUnbanDeviceV4Params{
                DeviceID:  ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Bans'
    name: 'AdminUnbanUserBulkV3'
    summary: 'Bulk unban user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        bansService := iam.BansService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := bansService.AdminUnbanUserBulkV3Short(
            &bans.AdminUnbanUserBulkV3Params{
                Body:      ..., // *iamclientmodels.ModelBulkUnbanCreateRequestV3
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'AdminUpdateAdminRoleStatusV3'
    summary: 'Set Role as Admin Role'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.AdminUpdateAdminRoleStatusV3Short(
            &roles.AdminUpdateAdminRoleStatusV3Params{
                RoleID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminUpdateAgeRestrictionConfigV2'
    summary: 'Update age restriction config value'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminUpdateAgeRestrictionConfigV2Short(
            &users.AdminUpdateAgeRestrictionConfigV2Params{
                Body:      ..., // *iamclientmodels.ModelAgeRestrictionRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminUpdateAgeRestrictionConfigV3'
    summary: 'Update age restriction config value'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminUpdateAgeRestrictionConfigV3Short(
            &users.AdminUpdateAgeRestrictionConfigV3Params{
                Body:      ..., // *iamclientmodels.ModelAgeRestrictionRequestV3
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Clients Config V3'
    name: 'AdminUpdateAvailablePermissionsByModule'
    summary: 'Update or create Client permissions module'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        clientsConfigV3Service := iam.ClientsConfigV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := clientsConfigV3Service.AdminUpdateAvailablePermissionsByModuleShort(
            &clients_config_v3.AdminUpdateAvailablePermissionsByModuleParams{
                Body:        ..., // *iamclientmodels.ClientmodelListUpsertModulesRequest
                ForceDelete: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Clients'
    name: 'AdminUpdateClientPermissionV3'
    summary: 'Update Client Permissions'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        clientsService := iam.ClientsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := clientsService.AdminUpdateClientPermissionV3Short(
            &clients.AdminUpdateClientPermissionV3Params{
                Body:      ..., // *iamclientmodels.AccountcommonClientPermissionsV3
                ClientID:  ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Clients'
    name: 'AdminUpdateClientSecretV3'
    summary: 'Update Client Secret'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        clientsService := iam.ClientsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := clientsService.AdminUpdateClientSecretV3Short(
            &clients.AdminUpdateClientSecretV3Params{
                Body:      ..., // *iamclientmodels.ClientmodelV3ClientUpdateSecretRequest
                ClientID:  ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Clients'
    name: 'AdminUpdateClientV3'
    summary: 'Update Client'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        clientsService := iam.ClientsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := clientsService.AdminUpdateClientV3Short(
            &clients.AdminUpdateClientV3Params{
                Body:      ..., // *iamclientmodels.ClientmodelClientUpdateV3Request
                ClientID:  ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminUpdateCountryAgeRestrictionV3'
    summary: 'Update country''s age restriction'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminUpdateCountryAgeRestrictionV3Short(
            &users.AdminUpdateCountryAgeRestrictionV3Params{
                Body:        ..., // *iamclientmodels.ModelCountryAgeRestrictionV3Request
                CountryCode: ..., // string
                Namespace:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Devices V4'
    name: 'AdminUpdateDeviceBanV4'
    summary: 'Admin update device ban config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        devicesV4Service := iam.DevicesV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := devicesV4Service.AdminUpdateDeviceBanV4Short(
            &devices_v4.AdminUpdateDeviceBanV4Params{
                Body:      ..., // *iamclientmodels.ModelDeviceBanUpdateRequestV4
                BanID:     ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'InputValidations'
    name: 'AdminUpdateInputValidations'
    summary: 'Admin Update Input Validations'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        inputValidationsService := iam.InputValidationsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := inputValidationsService.AdminUpdateInputValidationsShort(
            &input_validations.AdminUpdateInputValidationsParams{
                Body: ..., // []*iamclientmodels.ModelInputValidationUpdatePayload
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Login Allowlist'
    name: 'AdminUpdateLoginAllowlistV3'
    summary: 'Admin update login allowlist'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        loginAllowlistService := iam.LoginAllowlistService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := loginAllowlistService.AdminUpdateLoginAllowlistV3Short(
            &login_allowlist.AdminUpdateLoginAllowlistV3Params{
                Body:      ..., // *iamclientmodels.ModelLoginAllowlistRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminUpdateMyUserV4'
    summary: 'Admin Update My User'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminUpdateMyUserV4Short(
            &users_v4.AdminUpdateMyUserV4Params{
                Body: ..., // *iamclientmodels.ModelUserUpdateRequestV3
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Profile Update Strategy'
    name: 'AdminUpdateProfileUpdateStrategyV3'
    summary: 'Admin update profile update strategy by namespace and field.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        profileUpdateStrategyService := iam.ProfileUpdateStrategyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := profileUpdateStrategyService.AdminUpdateProfileUpdateStrategyV3Short(
            &profile_update_strategy.AdminUpdateProfileUpdateStrategyV3Params{
                Body:      ..., // *iamclientmodels.ModelUpdateProfileUpdateStrategyConfigRequest
                Namespace: ..., // string
                Field:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Override Role Config v3'
    name: 'AdminUpdateRoleOverrideConfigV3'
    summary: 'Update role override config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        overrideRoleConfigV3Service := iam.OverrideRoleConfigV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := overrideRoleConfigV3Service.AdminUpdateRoleOverrideConfigV3Short(
            &override_role_config_v3.AdminUpdateRoleOverrideConfigV3Params{
                Body:      ..., // *iamclientmodels.ModelRoleOverrideUpdateRequest
                Namespace: ..., // string
                Identity:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'AdminUpdateRolePermissionsV3'
    summary: 'Update Role Permissions'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.AdminUpdateRolePermissionsV3Short(
            &roles.AdminUpdateRolePermissionsV3Params{
                Body:   ..., // *iamclientmodels.AccountcommonPermissionsV3
                RoleID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'AdminUpdateRolePermissionsV4'
    summary: 'Update Role Permissions'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.AdminUpdateRolePermissionsV4Short(
            &roles.AdminUpdateRolePermissionsV4Params{
                Body:   ..., // *iamclientmodels.AccountcommonPermissionsV3
                RoleID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'AdminUpdateRoleV3'
    summary: 'Update Role'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.AdminUpdateRoleV3Short(
            &roles.AdminUpdateRoleV3Params{
                Body:   ..., // *iamclientmodels.ModelRoleUpdateRequestV3
                RoleID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'AdminUpdateRoleV4'
    summary: 'Update Role'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.AdminUpdateRoleV4Short(
            &roles.AdminUpdateRoleV4Params{
                Body:   ..., // *iamclientmodels.ModelRoleV4Request
                RoleID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Account Identifier Tag'
    name: 'AdminUpdateTagV3'
    summary: 'Update Account Identifier Tag'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        accountIdentifierTagService := iam.AccountIdentifierTagService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := accountIdentifierTagService.AdminUpdateTagV3Short(
            &account_identifier_tag.AdminUpdateTagV3Params{
                Body:      ..., // *iamclientmodels.ModelTagUpdateRequestV3
                Namespace: ..., // string
                TagID:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminUpdateUserBanV3'
    summary: 'Enable or disable ban for a single user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminUpdateUserBanV3Short(
            &users.AdminUpdateUserBanV3Params{
                Body:      ..., // *iamclientmodels.ModelBanUpdateRequest
                BanID:     ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminUpdateUserDeletionStatusV3'
    summary: 'Update User Deletion Status'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminUpdateUserDeletionStatusV3Short(
            &users.AdminUpdateUserDeletionStatusV3Params{
                Body:      ..., // *iamclientmodels.ModelUpdateUserDeletionStatusRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminUpdateUserEmailAddressV4'
    summary: 'Update a User Email Address'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminUpdateUserEmailAddressV4Short(
            &users_v4.AdminUpdateUserEmailAddressV4Params{
                Body:      ..., // *iamclientmodels.ModelEmailUpdateRequestV4
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminUpdateUserPermissionV3'
    summary: 'Update User Permissions'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminUpdateUserPermissionV3Short(
            &users.AdminUpdateUserPermissionV3Params{
                Body:      ..., // *iamclientmodels.AccountcommonPermissions
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminUpdateUserRoleV4'
    summary: 'Admin Update User''s Role V4'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminUpdateUserRoleV4Short(
            &users_v4.AdminUpdateUserRoleV4Params{
                Body:      ..., // *iamclientmodels.ModelAddUserRoleV4Request
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminUpdateUserStatusV3'
    summary: 'Update user status'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminUpdateUserStatusV3Short(
            &users.AdminUpdateUserStatusV3Params{
                Body:      ..., // *iamclientmodels.ModelUpdateUserStatusRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminUpdateUserV2'
    summary: 'Update User'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminUpdateUserV2Short(
            &users.AdminUpdateUserV2Params{
                Body:      ..., // *iamclientmodels.ModelUserUpdateRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminUpdateUserV3'
    summary: 'Update User'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminUpdateUserV3Short(
            &users.AdminUpdateUserV3Params{
                Body:      ..., // *iamclientmodels.ModelUserUpdateRequestV3
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'AdminUpdateUserV4'
    summary: 'Update User'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.AdminUpdateUserV4Short(
            &users_v4.AdminUpdateUserV4Params{
                Body:      ..., // *iamclientmodels.ModelUserUpdateRequestV3
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminUpgradeHeadlessAccountV3'
    summary: 'Verify or consume verification code. '
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminUpgradeHeadlessAccountV3Short(
            &users.AdminUpgradeHeadlessAccountV3Params{
                Body:      ..., // *iamclientmodels.ModelUpgradeHeadlessAccountWithVerificationCodeRequestV3
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminVerifyAccountV3'
    summary: 'Verify or consume verification code sent to user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminVerifyAccountV3Short(
            &users.AdminVerifyAccountV3Params{
                Body:      ..., // *iamclientmodels.ModelUserVerificationRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'AdminVerifyUserWithoutVerificationCodeV3'
    summary: 'Verify user without verification code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.AdminVerifyUserWithoutVerificationCodeV3Short(
            &users.AdminVerifyUserWithoutVerificationCodeV3Params{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0'
    name: 'AuthCodeRequestV3'
    summary: 'Generate url to request auth code from third party platform.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20Service := iam.OAuth20Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20Service.AuthCodeRequestV3Short(
            &o_auth2_0.AuthCodeRequestV3Params{
                PlatformID:  ..., // string
                ClientID:    ..., // *string
                RedirectURI: ..., // *string
                RequestID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0 - Extension'
    name: 'AuthenticateAndLinkForwardV3'
    summary: 'Authentication with platform link, the response will be a forward'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20ExtensionService := iam.OAuth20ExtensionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20ExtensionService.AuthenticateAndLinkForwardV3Short(
            &o_auth2_0_extension.AuthenticateAndLinkForwardV3Params{
                ExtendExp:    ..., // *bool
                ClientID:     ..., // string
                LinkingToken: ..., // string
                Password:     ..., // string
                Username:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0 - Extension'
    name: 'AuthenticationWithPlatformLinkV3'
    summary: 'Authentication with platform link'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20ExtensionService := iam.OAuth20ExtensionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20ExtensionService.AuthenticationWithPlatformLinkV3Short(
            &o_auth2_0_extension.AuthenticationWithPlatformLinkV3Params{
                ExtendExp:    ..., // *bool
                ClientID:     ..., // string
                LinkingToken: ..., // string
                Password:     ..., // string
                Username:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0 V4'
    name: 'AuthenticationWithPlatformLinkV4'
    summary: 'Authentication with platform link'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20V4Service := iam.OAuth20V4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20V4Service.AuthenticationWithPlatformLinkV4Short(
            &o_auth2_0_v4.AuthenticationWithPlatformLinkV4Params{
                ExtendExp:    ..., // *bool
                ClientID:     ..., // string
                LinkingToken: ..., // string
                Password:     ..., // string
                Username:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth'
    name: 'Authorization'
    summary: 'OAuth2 authorize API'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuthService := iam.OAuthService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuthService.AuthorizationShort(
            &o_auth.AuthorizationParams{
                Login:        ..., // *string
                Password:     ..., // *string
                Scope:        ..., // *string
                State:        ..., // *string
                ClientID:     ..., // string
                RedirectURI:  ..., // string
                ResponseType: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0'
    name: 'AuthorizeV3'
    summary: 'OAuth2 authorize API'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20Service := iam.OAuth20Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20Service.AuthorizeV3Short(
            &o_auth2_0.AuthorizeV3Params{
                BlockedPlatformID:                  ..., // *string
                CodeChallenge:                      ..., // *string
                CodeChallengeMethod:                ..., // *string
                CreateHeadless:                     ..., // *bool
                LoginWebBased:                      ..., // *bool
                Nonce:                              ..., // *string
                OneTimeLinkCode:                    ..., // *string
                RedirectURI:                        ..., // *string
                Scope:                              ..., // *string
                State:                              ..., // *string
                TargetAuthPage:                     ..., // *string
                UseRedirectURIAsLoginURLWhenLocked: ..., // *bool
                ClientID:                           ..., // string
                ResponseType:                       ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'BanUser'
    summary: 'Ban a single user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.BanUserShort(
            &users.BanUserParams{
                Body:      ..., // *iamclientmodels.ModelBanCreateRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0'
    name: 'Change2FAMethod'
    summary: 'Change 2FA method'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20Service := iam.OAuth20Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20Service.Change2FAMethodShort(
            &o_auth2_0.Change2FAMethodParams{
                Factor:   ..., // string
                MFAToken: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'CheckUserAvailability'
    summary: 'Check user''s account availability'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.CheckUserAvailabilityShort(
            &users.CheckUserAvailabilityParams{
                Namespace: ..., // string
                Field:     ..., // string
                Query:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Clients'
    name: 'CreateClient'
    summary: 'Create Client'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        clientsService := iam.ClientsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := clientsService.CreateClientShort(
            &clients.CreateClientParams{
                Body: ..., // *iamclientmodels.ClientmodelClientCreateRequest
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Clients'
    name: 'CreateClientByNamespace'
    summary: 'Create Client'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        clientsService := iam.ClientsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := clientsService.CreateClientByNamespaceShort(
            &clients.CreateClientByNamespaceParams{
                Body:      ..., // *iamclientmodels.ClientmodelClientCreateRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'CreateRole'
    summary: 'Create Role'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.CreateRoleShort(
            &roles.CreateRoleParams{
                Body: ..., // *iamclientmodels.ModelRoleCreateRequest
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'CreateUser'
    summary: 'Create User'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.CreateUserShort(
            &users.CreateUserParams{
                Body:      ..., // *iamclientmodels.ModelUserCreateRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'CreateUserFromInvitationV3'
    summary: 'Create User From Invitation'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.CreateUserFromInvitationV3Short(
            &users.CreateUserFromInvitationV3Params{
                Body:         ..., // *iamclientmodels.ModelUserCreateRequestV3
                InvitationID: ..., // string
                Namespace:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'CreateUserFromInvitationV4'
    summary: 'Create User From Invitation'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.CreateUserFromInvitationV4Short(
            &users_v4.CreateUserFromInvitationV4Params{
                Body:         ..., // *iamclientmodels.AccountCreateUserRequestV4
                InvitationID: ..., // string
                Namespace:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Clients'
    name: 'DeleteClient'
    summary: 'Delete Client'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        clientsService := iam.ClientsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := clientsService.DeleteClientShort(
            &clients.DeleteClientParams{
                ClientID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Clients'
    name: 'DeleteClientByNamespace'
    summary: 'Delete Client'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        clientsService := iam.ClientsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := clientsService.DeleteClientByNamespaceShort(
            &clients.DeleteClientByNamespaceParams{
                ClientID:  ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Clients'
    name: 'DeleteClientPermission'
    summary: 'Delete Client Permission'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        clientsService := iam.ClientsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := clientsService.DeleteClientPermissionShort(
            &clients.DeleteClientPermissionParams{
                Action:   ..., // int64
                ClientID: ..., // string
                Resource: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'DeleteRole'
    summary: 'Delete Role'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.DeleteRoleShort(
            &roles.DeleteRoleParams{
                RoleID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'DeleteRolePermission'
    summary: 'Delete Role Permission'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.DeleteRolePermissionShort(
            &roles.DeleteRolePermissionParams{
                Action:   ..., // int64
                Resource: ..., // string
                RoleID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'SSO Credential'
    name: 'DeleteSSOLoginPlatformCredentialV3'
    summary: 'Delete SSO Platform Credential'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        ssoCredentialService := iam.SSOCredentialService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := ssoCredentialService.DeleteSSOLoginPlatformCredentialV3Short(
            &sso_credential.DeleteSSOLoginPlatformCredentialV3Params{
                Namespace:  ..., // string
                PlatformID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Third Party Credential'
    name: 'DeleteThirdPartyLoginPlatformCredentialV3'
    summary: 'Delete Third Party Platform Credential'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        thirdPartyCredentialService := iam.ThirdPartyCredentialService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := thirdPartyCredentialService.DeleteThirdPartyLoginPlatformCredentialV3Short(
            &third_party_credential.DeleteThirdPartyLoginPlatformCredentialV3Params{
                Namespace:  ..., // string
                PlatformID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Third Party Credential'
    name: 'DeleteThirdPartyLoginPlatformDomainV3'
    summary: 'Unregister Third Party Platform Credential''s domain'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        thirdPartyCredentialService := iam.ThirdPartyCredentialService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := thirdPartyCredentialService.DeleteThirdPartyLoginPlatformDomainV3Short(
            &third_party_credential.DeleteThirdPartyLoginPlatformDomainV3Params{
                Body:       ..., // *iamclientmodels.ModelPlatformDomainDeleteRequest
                Namespace:  ..., // string
                PlatformID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'DeleteUser'
    summary: 'Delete User'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.DeleteUserShort(
            &users.DeleteUserParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'DeleteUserInformation'
    summary: 'Delete user''s information'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.DeleteUserInformationShort(
            &users.DeleteUserInformationParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'DeleteUserPermission'
    summary: 'Delete User Permission'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.DeleteUserPermissionShort(
            &users.DeleteUserPermissionParams{
                Action:    ..., // int64
                Namespace: ..., // string
                Resource:  ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'DeleteUserRole'
    summary: 'Delete User Role'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.DeleteUserRoleShort(
            &users.DeleteUserRoleParams{
                Namespace: ..., // string
                RoleID:    ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'DisableUser'
    summary: 'Disable a user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.DisableUserShort(
            &users.DisableUserParams{
                Body:      ..., // *iamclientmodels.ModelDisableUserRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'DisableUserBan'
    summary: 'Disable ban for a single user.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.DisableUserBanShort(
            &users.DisableUserBanParams{
                BanID:     ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'EnableUser'
    summary: 'Enable a user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.EnableUserShort(
            &users.EnableUserParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'EnableUserBan'
    summary: 'Enable ban for a single user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.EnableUserBanShort(
            &users.EnableUserBanParams{
                BanID:     ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'ForgotPassword'
    summary: 'Request Password Reset Code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.ForgotPasswordShort(
            &users.ForgotPasswordParams{
                Body:      ..., // *iamclientmodels.ModelSendVerificationCodeRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0 - Extension'
    name: 'GenerateTokenByNewHeadlessAccountV3'
    summary: 'Create headless account and response token'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20ExtensionService := iam.OAuth20ExtensionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20ExtensionService.GenerateTokenByNewHeadlessAccountV3Short(
            &o_auth2_0_extension.GenerateTokenByNewHeadlessAccountV3Params{
                AdditionalData: ..., // *string
                ExtendExp:      ..., // *bool
                LinkingToken:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0 V4'
    name: 'GenerateTokenByNewHeadlessAccountV4'
    summary: 'Create headless account and response token'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20V4Service := iam.OAuth20V4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20V4Service.GenerateTokenByNewHeadlessAccountV4Short(
            &o_auth2_0_v4.GenerateTokenByNewHeadlessAccountV4Params{
                AdditionalData: ..., // *string
                ExtendExp:      ..., // *bool
                LinkingToken:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'GetAdminInvitationV3'
    summary: 'Get User Invitation'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.GetAdminInvitationV3Short(
            &users.GetAdminInvitationV3Params{
                InvitationID: ..., // string
                Namespace:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'GetAdminUsersByRoleID'
    summary: 'Get Admin Users By RoleId'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.GetAdminUsersByRoleIDShort(
            &users.GetAdminUsersByRoleIDParams{
                Namespace: ..., // string
                After:     ..., // *int64
                Before:    ..., // *int64
                Limit:     ..., // *int64
                RoleID:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'GetAdminUsersByRoleIdV3'
    summary: 'Get Admin Users By RoleId'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.GetAdminUsersByRoleIDV3Short(
            &users.GetAdminUsersByRoleIDV3Params{
                Namespace: ..., // string
                RoleID:    ..., // string
                After:     ..., // *int64
                Before:    ..., // *int64
                Limit:     ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Bans'
    name: 'GetBansType'
    summary: 'Get list of ban types'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        bansService := iam.BansService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := bansService.GetBansTypeShort(
            &bans.GetBansTypeParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Clients'
    name: 'GetClient'
    summary: 'Get Client'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        clientsService := iam.ClientsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := clientsService.GetClientShort(
            &clients.GetClientParams{
                ClientID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Clients'
    name: 'GetClients'
    summary: 'Get All Clients'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        clientsService := iam.ClientsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := clientsService.GetClientsShort(
            &clients.GetClientsParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Clients'
    name: 'GetClientsbyNamespace'
    summary: 'Get clients by namespace'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        clientsService := iam.ClientsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := clientsService.GetClientsbyNamespaceShort(
            &clients.GetClientsbyNamespaceParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0 - Extension'
    name: 'GetCountryLocationV3'
    summary: 'Get Country Location'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20ExtensionService := iam.OAuth20ExtensionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20ExtensionService.GetCountryLocationV3Short(
            &o_auth2_0_extension.GetCountryLocationV3Params{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth'
    name: 'GetJWKS'
    summary: 'JSON Web Key Set for verifying JWT'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuthService := iam.OAuthService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuthService.GetJWKSShort(
            &o_auth.GetJWKSParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0'
    name: 'GetJWKSV3'
    summary: 'JSON Web Key Set for verifying JWT'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20Service := iam.OAuth20Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20Service.GetJWKSV3Short(
            &o_auth2_0.GetJWKSV3Params{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Bans'
    name: 'GetListBanReason'
    summary: 'Get list of ban reasons'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        bansService := iam.BansService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := bansService.GetListBanReasonShort(
            &bans.GetListBanReasonParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'GetListCountryAgeRestriction'
    summary: 'Get List Country age restriction'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.GetListCountryAgeRestrictionShort(
            &users.GetListCountryAgeRestrictionParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'GetListJusticePlatformAccounts'
    summary: 'Get User Justice Platform Accounts'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.GetListJusticePlatformAccountsShort(
            &users.GetListJusticePlatformAccountsParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'GetPublisherUser'
    summary: 'Get Publisher User'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.GetPublisherUserShort(
            &users.GetPublisherUserParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth'
    name: 'GetRevocationList'
    summary: 'OAuth2 revocation list API'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuthService := iam.OAuthService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuthService.GetRevocationListShort(
            &o_auth.GetRevocationListParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0'
    name: 'GetRevocationListV3'
    summary: 'OAuth2 revocation list API'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20Service := iam.OAuth20Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20Service.GetRevocationListV3Short(
            &o_auth2_0.GetRevocationListV3Params{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'GetRole'
    summary: 'Get Role'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.GetRoleShort(
            &roles.GetRoleParams{
                RoleID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'GetRoleAdminStatus'
    summary: 'Get Role Admin Status'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.GetRoleAdminStatusShort(
            &roles.GetRoleAdminStatusParams{
                RoleID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'GetRoleManagers'
    summary: 'Get Role Managers'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.GetRoleManagersShort(
            &roles.GetRoleManagersParams{
                RoleID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'GetRoleMembers'
    summary: 'Get Role Members'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.GetRoleMembersShort(
            &roles.GetRoleMembersParams{
                RoleID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'GetRoles'
    summary: 'Get Roles'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.GetRolesShort(
            &roles.GetRolesParams{
                IsWildcard: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'GetUserBanHistory'
    summary: 'Get user''s bans history'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.GetUserBanHistoryShort(
            &users.GetUserBanHistoryParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'GetUserByLoginID'
    summary: 'Get User By Login Id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.GetUserByLoginIDShort(
            &users.GetUserByLoginIDParams{
                Namespace: ..., // string
                LoginID:   ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'GetUserByPlatformUserID'
    summary: 'Get User By Platform User ID'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.GetUserByPlatformUserIDShort(
            &users.GetUserByPlatformUserIDParams{
                Namespace:      ..., // string
                PlatformID:     ..., // string
                PlatformUserID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'GetUserByUserID'
    summary: 'Get User By User Id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.GetUserByUserIDShort(
            &users.GetUserByUserIDParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'GetUserInformation'
    summary: 'Get user''s information'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.GetUserInformationShort(
            &users.GetUserInformationParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'GetUserJusticePlatformAccount'
    summary: 'Get the Justice linked accounts on the designated namespace'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.GetUserJusticePlatformAccountShort(
            &users.GetUserJusticePlatformAccountParams{
                Namespace:       ..., // string
                TargetNamespace: ..., // string
                UserID:          ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'GetUserLoginHistories'
    summary: 'Get User''s Login Histories'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.GetUserLoginHistoriesShort(
            &users.GetUserLoginHistoriesParams{
                Namespace: ..., // string
                UserID:    ..., // string
                After:     ..., // *float64
                Before:    ..., // *float64
                Limit:     ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'GetUserMapping'
    summary: 'Get user mapping'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.GetUserMappingShort(
            &users.GetUserMappingParams{
                Namespace:       ..., // string
                TargetNamespace: ..., // string
                UserID:          ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'GetUserPlatformAccounts'
    summary: 'Get platform accounts linked to the user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.GetUserPlatformAccountsShort(
            &users.GetUserPlatformAccountsParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'GetUsersByLoginIds'
    summary: 'Get a List of Users By Their Login Id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.GetUsersByLoginIdsShort(
            &users.GetUsersByLoginIdsParams{
                Namespace: ..., // string
                LoginIds:  ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'getUserVerificationCode'
    summary: 'Get verification code sent to user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.GetUserVerificationCodeShort(
            &users.GetUserVerificationCodeParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'LinkHeadlessAccountToMyAccountV3'
    summary: 'Link headless account to current full account'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.LinkHeadlessAccountToMyAccountV3Short(
            &users.LinkHeadlessAccountToMyAccountV3Params{
                Body: ..., // *iamclientmodels.ModelLinkHeadlessAccountRequest
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'ListAdminsV3'
    summary: 'List User Admins'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.ListAdminsV3Short(
            &users.ListAdminsV3Params{
                Namespace: ..., // string
                After:     ..., // *string
                Before:    ..., // *string
                EndDate:   ..., // *string
                Limit:     ..., // *int64
                Query:     ..., // *string
                RoleID:    ..., // *string
                StartDate: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'ListCrossNamespaceAccountLink'
    summary: 'Links existing account with another account in different namespace. '
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.ListCrossNamespaceAccountLinkShort(
            &users.ListCrossNamespaceAccountLinkParams{
                PlatformID:   ..., // *string
                LinkingToken: ..., // string
                Namespace:    ..., // string
                UserID:       ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'SSO'
    name: 'LoginSSOClient'
    summary: 'Login to SSO client with provided platformId'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        ssoService := iam.SSOService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := ssoService.LoginSSOClientShort(
            &sso.LoginSSOClientParams{
                PlatformID: ..., // string
                Payload:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0 - Extension'
    name: 'Logout'
    summary: 'Logout'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20ExtensionService := iam.OAuth20ExtensionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20ExtensionService.LogoutShort(
            &o_auth2_0_extension.LogoutParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'SSO'
    name: 'LogoutSSOClient'
    summary: 'Logout'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        ssoService := iam.SSOService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := ssoService.LogoutSSOClientShort(
            &sso.LogoutSSOClientParams{
                PlatformID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Third Party Credential'
    name: 'PartialUpdateThirdPartyLoginPlatformDomainV3'
    summary: 'Partial update third Party Platform Credential''s domain'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        thirdPartyCredentialService := iam.ThirdPartyCredentialService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := thirdPartyCredentialService.PartialUpdateThirdPartyLoginPlatformDomainV3Short(
            &third_party_credential.PartialUpdateThirdPartyLoginPlatformDomainV3Params{
                Body:       ..., // *iamclientmodels.ModelPlatformDomainPatchRequest
                Namespace:  ..., // string
                PlatformID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'SSO SAML 2.0'
    name: 'platformAuthenticateSAMLV3Handler'
    summary: 'SAML platform Authentication API'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        ssoSaml20Service := iam.SSOSAML20Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := ssoSaml20Service.PlatformAuthenticateSAMLV3HandlerShort(
            &sso_saml_2_0.PlatformAuthenticateSAMLV3HandlerParams{
                PlatformID: ..., // string
                Code:       ..., // *string
                Error:      ..., // *string
                State:      ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0 - Extension'
    name: 'PlatformAuthenticationV3'
    summary: 'Platform Authentication API'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20ExtensionService := iam.OAuth20ExtensionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20ExtensionService.PlatformAuthenticationV3Short(
            &o_auth2_0_extension.PlatformAuthenticationV3Params{
                PlatformID:          ..., // string
                Code:                ..., // *string
                Error:               ..., // *string
                OpenidAssocHandle:   ..., // *string
                OpenidClaimedID:     ..., // *string
                OpenidIdentity:      ..., // *string
                OpenidMode:          ..., // *string
                OpenidNs:            ..., // *string
                OpenidOpEndpoint:    ..., // *string
                OpenidResponseNonce: ..., // *string
                OpenidReturnTo:      ..., // *string
                OpenidSig:           ..., // *string
                OpenidSigned:        ..., // *string
                State:               ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PlatformLink'
    summary: 'Link user''s account with platform'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PlatformLinkShort(
            &users.PlatformLinkParams{
                Ticket:     ..., // string
                Namespace:  ..., // string
                PlatformID: ..., // string
                UserID:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0'
    name: 'PlatformTokenGrantV3'
    summary: 'OAuth2 access token generation specific to platform'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20Service := iam.OAuth20Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20Service.PlatformTokenGrantV3Short(
            &o_auth2_0.PlatformTokenGrantV3Params{
                AdditionalData: ..., // *string
                ClientID:       ..., // *string
                CreateHeadless: ..., // *bool
                DeviceID:       ..., // *string
                MacAddress:     ..., // *string
                PlatformToken:  ..., // *string
                ServiceLabel:   ..., // *float64
                SkipSetCookie:  ..., // *bool
                PlatformID:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0 V4'
    name: 'PlatformTokenGrantV4'
    summary: 'OAuth2 access token generation specific to platform'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20V4Service := iam.OAuth20V4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20V4Service.PlatformTokenGrantV4Short(
            &o_auth2_0_v4.PlatformTokenGrantV4Params{
                AdditionalData:      ..., // *string
                ClientID:            ..., // *string
                CreateHeadless:      ..., // *bool
                DeviceID:            ..., // *string
                MacAddress:          ..., // *string
                PlatformToken:       ..., // *string
                ServiceLabel:        ..., // *float64
                SkipSetCookie:       ..., // *bool
                PlatformID:          ..., // string
                CodeChallenge:       ..., // *string
                CodeChallengeMethod: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0 - Extension'
    name: 'PlatformTokenRefreshV3'
    summary: 'Platform Token Validation'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20ExtensionService := iam.OAuth20ExtensionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20ExtensionService.PlatformTokenRefreshV3Short(
            &o_auth2_0_extension.PlatformTokenRefreshV3Params{
                PlatformToken: ..., // string
                PlatformID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth'
    name: 'PlatformTokenRequestHandler'
    summary: 'OAuth2 access token generation specific to platform'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuthService := iam.OAuthService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuthService.PlatformTokenRequestHandlerShort(
            &o_auth.PlatformTokenRequestHandlerParams{
                DeviceID:      ..., // *string
                MacAddress:    ..., // *string
                PlatformToken: ..., // *string
                Namespace:     ..., // string
                PlatformID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PlatformUnlink'
    summary: 'Unlink user''s account with platform'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PlatformUnlinkShort(
            &users.PlatformUnlinkParams{
                PlatformNamespace: ..., // *string
                Namespace:         ..., // string
                PlatformID:        ..., // string
                UserID:            ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicBulkGetUsers'
    summary: 'Bulk get users'' basic info by user Id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicBulkGetUsersShort(
            &users.PublicBulkGetUsersParams{
                Body:      ..., // *iamclientmodels.ModelUserIDsRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'PublicChallengeMyMFAV4'
    summary: 'Challenge User MFA'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.PublicChallengeMyMFAV4Short(
            &users_v4.PublicChallengeMyMFAV4Params{
                Code:      ..., // *string
                Factor:    ..., // *string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicCreateJusticeUser'
    summary: 'Create Justice User from Publisher User'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicCreateJusticeUserShort(
            &users.PublicCreateJusticeUserParams{
                Namespace:       ..., // string
                TargetNamespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'PublicCreateTestUserV4'
    summary: '[TEST FACILITY ONLY]Create test User'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.PublicCreateTestUserV4Short(
            &users_v4.PublicCreateTestUserV4Params{
                Body:      ..., // *iamclientmodels.AccountCreateTestUserRequestV4
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicCreateUserV2'
    summary: 'Create User'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicCreateUserV2Short(
            &users.PublicCreateUserV2Params{
                Body:      ..., // *iamclientmodels.ModelUserCreateRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicCreateUserV3'
    summary: 'Create User'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicCreateUserV3Short(
            &users.PublicCreateUserV3Params{
                Body:      ..., // *iamclientmodels.ModelUserCreateRequestV3
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'PublicCreateUserV4'
    summary: 'Create User'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.PublicCreateUserV4Short(
            &users_v4.PublicCreateUserV4Params{
                Body:      ..., // *iamclientmodels.AccountCreateUserRequestV4
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicDeletePlatformLinkV2'
    summary: 'Delete the link of user''s account with platform'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicDeletePlatformLinkV2Short(
            &users.PublicDeletePlatformLinkV2Params{
                PlatformNamespace: ..., // *string
                Namespace:         ..., // string
                PlatformID:        ..., // string
                UserID:            ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'PublicDisableMyAuthenticatorV4'
    summary: 'Disable 2FA authenticator'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.PublicDisableMyAuthenticatorV4Short(
            &users_v4.PublicDisableMyAuthenticatorV4Params{
                Body:      ..., // *iamclientmodels.ModelDisableMFARequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'PublicDisableMyBackupCodesV4'
    summary: 'Disable 2FA backup codes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.PublicDisableMyBackupCodesV4Short(
            &users_v4.PublicDisableMyBackupCodesV4Params{
                Body:      ..., // *iamclientmodels.ModelDisableMFARequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'PublicDisableMyEmailV4'
    summary: 'Disable 2FA email'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.PublicDisableMyEmailV4Short(
            &users_v4.PublicDisableMyEmailV4Params{
                Body:      ..., // *iamclientmodels.ModelDisableMFARequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'PublicDownloadMyBackupCodesV4'
    summary: 'Download user backup codes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.PublicDownloadMyBackupCodesV4Short(
            &users_v4.PublicDownloadMyBackupCodesV4Params{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'PublicEnableBackupCodesV4'
    summary: 'Enable 2FA backup codes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.PublicEnableBackupCodesV4Short(
            &users_v4.PublicEnableBackupCodesV4Params{
                Namespace:   ..., // string
                LanguageTag: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'PublicEnableMyAuthenticatorV4'
    summary: 'Enable 2FA authenticator'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.PublicEnableMyAuthenticatorV4Short(
            &users_v4.PublicEnableMyAuthenticatorV4Params{
                Code:      ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'PublicEnableMyBackupCodesV4'
    summary: 'Enable 2FA backup codes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.PublicEnableMyBackupCodesV4Short(
            &users_v4.PublicEnableMyBackupCodesV4Params{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'PublicEnableMyEmailV4'
    summary: 'Enable 2FA email'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.PublicEnableMyEmailV4Short(
            &users_v4.PublicEnableMyEmailV4Params{
                Code:      ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicForceLinkPlatformWithProgression'
    summary: 'Force link 3rd platform account and transfer progression'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicForceLinkPlatformWithProgressionShort(
            &users.PublicForceLinkPlatformWithProgressionParams{
                Body:      ..., // *iamclientmodels.ModelLinkPlatformAccountWithProgressionRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicForcePlatformLinkV3'
    summary: 'Force linking user''s account with platform'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicForcePlatformLinkV3Short(
            &users.PublicForcePlatformLinkV3Params{
                Ticket:     ..., // string
                Namespace:  ..., // string
                PlatformID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicForgotPasswordV2'
    summary: 'Request Password Reset Code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicForgotPasswordV2Short(
            &users.PublicForgotPasswordV2Params{
                Body:      ..., // *iamclientmodels.ModelSendVerificationCodeRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicForgotPasswordV3'
    summary: 'Request Password Reset Code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicForgotPasswordV3Short(
            &users.PublicForgotPasswordV3Params{
                Body:      ..., // *iamclientmodels.ModelForgotPasswordRequestV3
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicForgotPasswordWithoutNamespaceV3'
    summary: 'Request Password Reset Code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicForgotPasswordWithoutNamespaceV3Short(
            &users.PublicForgotPasswordWithoutNamespaceV3Params{
                Body: ..., // *iamclientmodels.ModelForgotPasswordWithoutNamespaceRequestV3
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'PublicGenerateBackupCodesV4'
    summary: 'Generate backup codes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.PublicGenerateBackupCodesV4Short(
            &users_v4.PublicGenerateBackupCodesV4Params{
                Namespace:   ..., // string
                LanguageTag: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'PublicGenerateMyAuthenticatorKeyV4'
    summary: 'Generate secret key for 3rd-party authenticate app'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.PublicGenerateMyAuthenticatorKeyV4Short(
            &users_v4.PublicGenerateMyAuthenticatorKeyV4Params{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'PublicGenerateMyBackupCodesV4'
    summary: 'Generate backup codes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.PublicGenerateMyBackupCodesV4Short(
            &users_v4.PublicGenerateMyBackupCodesV4Params{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicGetAsyncStatus'
    summary: 'Get Linking Progress Status'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicGetAsyncStatusShort(
            &users.PublicGetAsyncStatusParams{
                Namespace: ..., // string
                RequestID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'PublicGetBackupCodesV4'
    summary: 'Get backup codes and send to email'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.PublicGetBackupCodesV4Short(
            &users_v4.PublicGetBackupCodesV4Params{
                Namespace:   ..., // string
                LanguageTag: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Config'
    name: 'PublicGetConfigValueV3'
    summary: 'Get Config Value'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        configService := iam.ConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configService.PublicGetConfigValueV3Short(
            &config.PublicGetConfigValueV3Params{
                ConfigKey: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicGetCountryAgeRestriction'
    summary: 'Public get age restriction by country code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicGetCountryAgeRestrictionShort(
            &users.PublicGetCountryAgeRestrictionParams{
                CountryCode: ..., // string
                Namespace:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicGetCountryAgeRestrictionV3'
    summary: 'Public get age restriction by country code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicGetCountryAgeRestrictionV3Short(
            &users.PublicGetCountryAgeRestrictionV3Params{
                CountryCode: ..., // string
                Namespace:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Country'
    name: 'PublicGetCountryListV3'
    summary: 'Get Country List'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        countryService := iam.CountryService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := countryService.PublicGetCountryListV3Short(
            &country.PublicGetCountryListV3Params{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'InputValidations'
    name: 'PublicGetInputValidationByField'
    summary: 'Public Get Input Validation by field'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        inputValidationsService := iam.InputValidationsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := inputValidationsService.PublicGetInputValidationByFieldShort(
            &input_validations.PublicGetInputValidationByFieldParams{
                Field: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'InputValidations'
    name: 'PublicGetInputValidations'
    summary: 'Public Get Input Validations'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        inputValidationsService := iam.InputValidationsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := inputValidationsService.PublicGetInputValidationsShort(
            &input_validations.PublicGetInputValidationsParams{
                DefaultOnEmpty: ..., // *bool
                LanguageCode:   ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicGetLinkHeadlessAccountToMyAccountConflictV3'
    summary: 'Get conflict result when link headless account to current full account by one time code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicGetLinkHeadlessAccountToMyAccountConflictV3Short(
            &users.PublicGetLinkHeadlessAccountToMyAccountConflictV3Params{
                OneTimeLinkCode: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'PublicGetMyBackupCodesV4'
    summary: 'Get backup codes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.PublicGetMyBackupCodesV4Short(
            &users_v4.PublicGetMyBackupCodesV4Params{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'PublicGetMyEnabledFactorsV4'
    summary: 'Get user enabled factors'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.PublicGetMyEnabledFactorsV4Short(
            &users_v4.PublicGetMyEnabledFactorsV4Params{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'PublicGetMyMFAStatusV4'
    summary: 'Get User MFA Status'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.PublicGetMyMFAStatusV4Short(
            &users_v4.PublicGetMyMFAStatusV4Params{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'PublicGetMyOwnMFAStatusV4'
    summary: 'Get My Own MFA Status'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.PublicGetMyOwnMFAStatusV4Short(
            &users_v4.PublicGetMyOwnMFAStatusV4Params{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicGetMyProfileAllowUpdateStatusV3'
    summary: 'Public get my profile allowed update status.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicGetMyProfileAllowUpdateStatusV3Short(
            &users.PublicGetMyProfileAllowUpdateStatusV3Params{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicGetMyRedirectionAfterLinkV3'
    summary: 'Get My Forward URI Link'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicGetMyRedirectionAfterLinkV3Short(
            &users.PublicGetMyRedirectionAfterLinkV3Params{
                OneTimeLinkCode: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicGetMyUserV3'
    summary: 'Get My User'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicGetMyUserV3Short(
            &users.PublicGetMyUserV3Params{
                IncludeAllPlatforms: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicGetOpenidUserInfoV3'
    summary: 'Get my user info'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicGetOpenidUserInfoV3Short(
            &users.PublicGetOpenidUserInfoV3Params{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Profile Update Strategy'
    name: 'PublicGetProfileUpdateStrategyV3'
    summary: 'Public get profile update strategy by namespace and field.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        profileUpdateStrategyService := iam.ProfileUpdateStrategyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := profileUpdateStrategyService.PublicGetProfileUpdateStrategyV3Short(
            &profile_update_strategy.PublicGetProfileUpdateStrategyV3Params{
                Namespace: ..., // string
                Field:     ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicGetPublisherUserV3'
    summary: 'Get Publisher User'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicGetPublisherUserV3Short(
            &users.PublicGetPublisherUserV3Params{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'PublicGetRolesV3'
    summary: 'Get Roles'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.PublicGetRolesV3Short(
            &roles.PublicGetRolesV3Params{
                After:      ..., // *string
                Before:     ..., // *string
                IsWildcard: ..., // *bool
                Limit:      ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'PublicGetRoleV3'
    summary: 'Get Public Role'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.PublicGetRoleV3Short(
            &roles.PublicGetRoleV3Params{
                RoleID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Config'
    name: 'PublicGetSystemConfigV3'
    summary: 'Get Public System Config Value'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        configService := iam.ConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configService.PublicGetSystemConfigV3Short(
            &config.PublicGetSystemConfigV3Params{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicGetUserBan'
    summary: 'Get user''s bans'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicGetUserBanShort(
            &users.PublicGetUserBanParams{
                Namespace:  ..., // string
                UserID:     ..., // string
                ActiveOnly: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicGetUserBanHistoryV3'
    summary: 'Get user''s bans'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicGetUserBanHistoryV3Short(
            &users.PublicGetUserBanHistoryV3Params{
                Namespace:  ..., // string
                UserID:     ..., // string
                ActiveOnly: ..., // *bool
                After:      ..., // *string
                Before:     ..., // *string
                Limit:      ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicGetUserByPlatformUserIDV3'
    summary: 'Get User By Platform User ID'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicGetUserByPlatformUserIDV3Short(
            &users.PublicGetUserByPlatformUserIDV3Params{
                Namespace:      ..., // string
                PlatformID:     ..., // string
                PlatformUserID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'PublicGetUserByPlatformUserIDV4'
    summary: 'Get User By Platform User ID'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.PublicGetUserByPlatformUserIDV4Short(
            &users_v4.PublicGetUserByPlatformUserIDV4Params{
                Namespace:      ..., // string
                PlatformID:     ..., // string
                PlatformUserID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicGetUserByUserIDV2'
    summary: 'Get User By User ID'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicGetUserByUserIDV2Short(
            &users.PublicGetUserByUserIDV2Params{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicGetUserByUserIdV3'
    summary: 'Get User By User ID'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicGetUserByUserIDV3Short(
            &users.PublicGetUserByUserIDV3Params{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicGetUserInformationV3'
    summary: 'Get user''s information v3'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicGetUserInformationV3Short(
            &users.PublicGetUserInformationV3Params{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicGetUserLoginHistoriesV3'
    summary: 'Get User''s Login Histories'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicGetUserLoginHistoriesV3Short(
            &users.PublicGetUserLoginHistoriesV3Params{
                Namespace: ..., // string
                UserID:    ..., // string
                After:     ..., // *float64
                Before:    ..., // *float64
                Limit:     ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicGetUserPlatformAccountsV3'
    summary: 'Get platform accounts linked to the user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicGetUserPlatformAccountsV3Short(
            &users.PublicGetUserPlatformAccountsV3Params{
                Namespace:  ..., // string
                UserID:     ..., // string
                After:      ..., // *string
                Before:     ..., // *string
                Limit:      ..., // *int64
                PlatformID: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'PublicGetUserPublicInfoByUserIdV4'
    summary: 'Get User Public Info By User ID'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.PublicGetUserPublicInfoByUserIDV4Short(
            &users_v4.PublicGetUserPublicInfoByUserIDV4Params{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicGetUsersPlatformInfosV3'
    summary: 'Get user''s basic info and public info of 3rd party account'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicGetUsersPlatformInfosV3Short(
            &users.PublicGetUsersPlatformInfosV3Params{
                Body:      ..., // *iamclientmodels.ModelUsersPlatformInfosRequestV3
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'PublicInviteUserV4'
    summary: 'Public invite admin user v4'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.PublicInviteUserV4Short(
            &users_v4.PublicInviteUserV4Params{
                Body: ..., // *iamclientmodels.ModelPublicInviteUserRequestV4
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicLinkPlatformAccount'
    summary: 'Link a Platform User Account to User Account'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicLinkPlatformAccountShort(
            &users.PublicLinkPlatformAccountParams{
                Body:      ..., // *iamclientmodels.ModelLinkPlatformAccountRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicListJusticePlatformAccountsV3'
    summary: 'Get User Justice Platform Accounts'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicListJusticePlatformAccountsV3Short(
            &users.PublicListJusticePlatformAccountsV3Params{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicListUserAllPlatformAccountsDistinctV3'
    summary: 'Get distinct platform accounts linked to the user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicListUserAllPlatformAccountsDistinctV3Short(
            &users.PublicListUserAllPlatformAccountsDistinctV3Params{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicListUserIDByPlatformUserIDsV3'
    summary: 'List User ID By Platform User ID'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicListUserIDByPlatformUserIDsV3Short(
            &users.PublicListUserIDByPlatformUserIDsV3Params{
                Body:       ..., // *iamclientmodels.ModelPlatformUserIDRequest
                Namespace:  ..., // string
                PlatformID: ..., // string
                RawPID:     ..., // *bool
                RawPUID:    ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'PublicListUserIDByPlatformUserIDsV4'
    summary: 'List User ID By Platform User ID'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.PublicListUserIDByPlatformUserIDsV4Short(
            &users_v4.PublicListUserIDByPlatformUserIDsV4Params{
                Body:       ..., // *iamclientmodels.ModelPlatformUserIDRequestV4
                Namespace:  ..., // string
                PlatformID: ..., // string
                RawPUID:    ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'PublicMakeFactorMyDefaultV4'
    summary: 'Make 2FA factor default'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.PublicMakeFactorMyDefaultV4Short(
            &users_v4.PublicMakeFactorMyDefaultV4Params{
                Factor:    ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicPartialUpdateUserV3'
    summary: 'Update User'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicPartialUpdateUserV3Short(
            &users.PublicPartialUpdateUserV3Params{
                Body:      ..., // *iamclientmodels.ModelPublicUserUpdateRequestV3
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicPlatformLinkV2'
    summary: 'Link user''s account with platform'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicPlatformLinkV2Short(
            &users.PublicPlatformLinkV2Params{
                Ticket:     ..., // string
                Namespace:  ..., // string
                PlatformID: ..., // string
                UserID:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicPlatformLinkV3'
    summary: 'Link user''s account with platform'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicPlatformLinkV3Short(
            &users.PublicPlatformLinkV3Params{
                RedirectURI: ..., // *string
                Ticket:      ..., // string
                Namespace:   ..., // string
                PlatformID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicPlatformUnlinkAllV3'
    summary: 'Public unlink user''s account from specific platform for all namespaces'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicPlatformUnlinkAllV3Short(
            &users.PublicPlatformUnlinkAllV3Params{
                Namespace:  ..., // string
                PlatformID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicPlatformUnlinkV3'
    summary: 'Public unlink user''s account from specific platform'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicPlatformUnlinkV3Short(
            &users.PublicPlatformUnlinkV3Params{
                Body:       ..., // *iamclientmodels.ModelUnlinkUserPlatformRequest
                Namespace:  ..., // string
                PlatformID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicProcessWebLinkPlatformV3'
    summary: 'Process Link Progress '
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicProcessWebLinkPlatformV3Short(
            &users.PublicProcessWebLinkPlatformV3Params{
                Code:       ..., // *string
                State:      ..., // string
                Namespace:  ..., // string
                PlatformID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'PublicRemoveTrustedDeviceV4'
    summary: 'Remove Trusted Device'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.PublicRemoveTrustedDeviceV4Short(
            &users_v4.PublicRemoveTrustedDeviceV4Params{
                Cookie:    ..., // *string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicResetPasswordV2'
    summary: 'Reset User Password'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicResetPasswordV2Short(
            &users.PublicResetPasswordV2Params{
                Body:      ..., // *iamclientmodels.ModelResetPasswordRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicSearchUserV3'
    summary: 'Search User'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicSearchUserV3Short(
            &users.PublicSearchUserV3Params{
                Namespace:  ..., // string
                By:         ..., // *string
                Limit:      ..., // *int64
                Offset:     ..., // *int64
                PlatformBy: ..., // *string
                PlatformID: ..., // *string
                Query:      ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicSendCodeForwardV3'
    summary: 'Send verification code to user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicSendCodeForwardV3Short(
            &users.PublicSendCodeForwardV3Params{
                Body: ..., // *iamclientmodels.ModelSendVerificationCodeRequestV3
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'PublicSendMyMFAEmailCodeV4'
    summary: 'Send code for MFA email'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.PublicSendMyMFAEmailCodeV4Short(
            &users_v4.PublicSendMyMFAEmailCodeV4Params{
                Action:      ..., // *string
                LanguageTag: ..., // *string
                Namespace:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicSendRegistrationCode'
    summary: 'Send verification code to new unregistered account''s email address'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicSendRegistrationCodeShort(
            &users.PublicSendRegistrationCodeParams{
                Body:      ..., // *iamclientmodels.ModelSendRegisterVerificationCodeRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicSendVerificationCodeV3'
    summary: 'Send verification code to user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicSendVerificationCodeV3Short(
            &users.PublicSendVerificationCodeV3Params{
                Body:      ..., // *iamclientmodels.ModelSendVerificationCodeRequestV3
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicSendVerificationLinkV3'
    summary: 'Send verification link to user, this link will point to ''/iam/v3/public/users/verify_link/verify'''
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicSendVerificationLinkV3Short(
            &users.PublicSendVerificationLinkV3Params{
                Body: ..., // *iamclientmodels.ModelSendVerificationLinkRequest
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicUpdatePasswordV2'
    summary: 'Update User Password'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicUpdatePasswordV2Short(
            &users.PublicUpdatePasswordV2Params{
                Body:      ..., // *iamclientmodels.ModelUserPasswordUpdateRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicUpdatePasswordV3'
    summary: 'Update User Password'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicUpdatePasswordV3Short(
            &users.PublicUpdatePasswordV3Params{
                Body:      ..., // *iamclientmodels.ModelUserPasswordUpdateV3Request
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'PublicUpdateUserEmailAddressV4'
    summary: 'Update My Email Address'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.PublicUpdateUserEmailAddressV4Short(
            &users_v4.PublicUpdateUserEmailAddressV4Params{
                Body:      ..., // *iamclientmodels.ModelEmailUpdateRequestV4
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicUpdateUserV2'
    summary: 'Update User'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicUpdateUserV2Short(
            &users.PublicUpdateUserV2Params{
                Body:      ..., // *iamclientmodels.ModelUserUpdateRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'PublicUpdateUserV4'
    summary: 'Update User'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.PublicUpdateUserV4Short(
            &users_v4.PublicUpdateUserV4Params{
                Body:      ..., // *iamclientmodels.ModelPublicUserUpdateRequestV3
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicUpgradeHeadlessAccountV3'
    summary: 'Verify or consume verification code.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicUpgradeHeadlessAccountV3Short(
            &users.PublicUpgradeHeadlessAccountV3Params{
                Body:      ..., // *iamclientmodels.ModelUpgradeHeadlessAccountWithVerificationCodeRequestV3
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'PublicUpgradeHeadlessAccountV4'
    summary: 'Upgrade user account to full account'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.PublicUpgradeHeadlessAccountV4Short(
            &users_v4.PublicUpgradeHeadlessAccountV4Params{
                Body:      ..., // *iamclientmodels.AccountUpgradeHeadlessAccountRequestV4
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'PublicUpgradeHeadlessAccountWithVerificationCodeV4'
    summary: 'Upgrade headless account and automatically verified the email address if it is succeeded'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.PublicUpgradeHeadlessAccountWithVerificationCodeV4Short(
            &users_v4.PublicUpgradeHeadlessAccountWithVerificationCodeV4Params{
                Body:      ..., // *iamclientmodels.AccountUpgradeHeadlessAccountWithVerificationCodeRequestV4
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users V4'
    name: 'PublicUpgradeHeadlessWithCodeV4Forward'
    summary: 'Upgrade the headless account and automatically verify the email address if the upgrade succeeds.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersV4Service := iam.UsersV4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersV4Service.PublicUpgradeHeadlessWithCodeV4ForwardShort(
            &users_v4.PublicUpgradeHeadlessWithCodeV4ForwardParams{
                Body: ..., // *iamclientmodels.AccountUpgradeHeadlessAccountWithVerificationCodeForwardRequestV4
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicUserVerificationV3'
    summary: 'Validate or consume verification code sent to user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicUserVerificationV3Short(
            &users.PublicUserVerificationV3Params{
                Body:      ..., // *iamclientmodels.ModelUserVerificationRequestV3
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicValidateUserByUserIDAndPasswordV3'
    summary: 'Validate user password by user ID and password'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicValidateUserByUserIDAndPasswordV3Short(
            &users.PublicValidateUserByUserIDAndPasswordV3Params{
                Password:  ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicValidateUserInput'
    summary: 'Validate user''s input.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicValidateUserInputShort(
            &users.PublicValidateUserInputParams{
                Body:      ..., // *iamclientmodels.ModelUserInputValidationRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicVerifyHeadlessAccountV3'
    summary: 'Upgrade user headless account to full account (with email)'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicVerifyHeadlessAccountV3Short(
            &users.PublicVerifyHeadlessAccountV3Params{
                Body:                 ..., // *iamclientmodels.ModelUpgradeHeadlessAccountV3Request
                Namespace:            ..., // string
                NeedVerificationCode: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicVerifyRegistrationCode'
    summary: 'Verify the registration code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicVerifyRegistrationCodeShort(
            &users.PublicVerifyRegistrationCodeParams{
                Body:      ..., // *iamclientmodels.ModelVerifyRegistrationCode
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicVerifyUserByLinkV3'
    summary: 'Verify user email by the verification link code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicVerifyUserByLinkV3Short(
            &users.PublicVerifyUserByLinkV3Params{
                Code: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicWebLinkPlatform'
    summary: 'Create Public Web Linking'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicWebLinkPlatformShort(
            &users.PublicWebLinkPlatformParams{
                Namespace:   ..., // string
                PlatformID:  ..., // string
                ClientID:    ..., // *string
                RedirectURI: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'PublicWebLinkPlatformEstablish'
    summary: 'Establish Link Progress '
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.PublicWebLinkPlatformEstablishShort(
            &users.PublicWebLinkPlatformEstablishParams{
                Namespace:  ..., // string
                PlatformID: ..., // string
                Code:       ..., // *string
                State:      ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'RemoveRoleAdmin'
    summary: 'Remove Role Admin Status'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.RemoveRoleAdminShort(
            &roles.RemoveRoleAdminParams{
                RoleID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'RemoveRoleManagers'
    summary: 'Remove Role Managers'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.RemoveRoleManagersShort(
            &roles.RemoveRoleManagersParams{
                Body:   ..., // *iamclientmodels.ModelRoleManagersRequest
                RoleID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'RemoveRoleMembers'
    summary: 'Remove Role Members'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.RemoveRoleMembersShort(
            &roles.RemoveRoleMembersParams{
                Body:   ..., // *iamclientmodels.ModelRoleMembersRequest
                RoleID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0 - Extension'
    name: 'RequestOneTimeLinkingCodeV3'
    summary: 'Generate one time linking code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20ExtensionService := iam.OAuth20ExtensionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20ExtensionService.RequestOneTimeLinkingCodeV3Short(
            &o_auth2_0_extension.RequestOneTimeLinkingCodeV3Params{
                RedirectURI: ..., // *string
                State:       ..., // *string
                PlatformID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0 - Extension'
    name: 'RequestTargetTokenResponseV3'
    summary: 'Generate target token by code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20ExtensionService := iam.OAuth20ExtensionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20ExtensionService.RequestTargetTokenResponseV3Short(
            &o_auth2_0_extension.RequestTargetTokenResponseV3Params{
                AdditionalData: ..., // *string
                Code:           ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0 V4'
    name: 'RequestTargetTokenResponseV4'
    summary: 'Generate target token by code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20V4Service := iam.OAuth20V4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20V4Service.RequestTargetTokenResponseV4Short(
            &o_auth2_0_v4.RequestTargetTokenResponseV4Params{
                AdditionalData: ..., // *string
                Code:           ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0 - Extension'
    name: 'RequestTokenByOneTimeLinkCodeResponseV3'
    summary: 'Generate token by headless account''s one time link code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20ExtensionService := iam.OAuth20ExtensionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20ExtensionService.RequestTokenByOneTimeLinkCodeResponseV3Short(
            &o_auth2_0_extension.RequestTokenByOneTimeLinkCodeResponseV3Params{
                AdditionalData:  ..., // *string
                IsTransient:     ..., // *bool
                ClientID:        ..., // string
                OneTimeLinkCode: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0 - Extension'
    name: 'RequestTokenExchangeCodeV3'
    summary: 'Request code to get a new token'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20ExtensionService := iam.OAuth20ExtensionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20ExtensionService.RequestTokenExchangeCodeV3Short(
            &o_auth2_0_extension.RequestTokenExchangeCodeV3Params{
                ClientID:            ..., // string
                Namespace:           ..., // string
                CodeChallenge:       ..., // *string
                CodeChallengeMethod: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'ResetPassword'
    summary: 'Reset User Password'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.ResetPasswordShort(
            &users.ResetPasswordParams{
                Body:      ..., // *iamclientmodels.ModelResetPasswordRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'ResetPasswordV3'
    summary: 'Reset User Password'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.ResetPasswordV3Short(
            &users.ResetPasswordV3Params{
                Body:      ..., // *iamclientmodels.ModelResetPasswordRequestV3
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Third Party Credential'
    name: 'RetrieveActiveOIDCClientsPublicV3'
    summary: 'Get Active OIDC Platform Credential By Client ID'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        thirdPartyCredentialService := iam.ThirdPartyCredentialService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := thirdPartyCredentialService.RetrieveActiveOIDCClientsPublicV3Short(
            &third_party_credential.RetrieveActiveOIDCClientsPublicV3Params{
                Namespace: ..., // string
                ClientID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Third Party Credential'
    name: 'RetrieveAllActiveThirdPartyLoginPlatformCredentialPublicV3'
    summary: 'Get All Active Third Party Platform Active Credential for Public Usage'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        thirdPartyCredentialService := iam.ThirdPartyCredentialService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := thirdPartyCredentialService.RetrieveAllActiveThirdPartyLoginPlatformCredentialPublicV3Short(
            &third_party_credential.RetrieveAllActiveThirdPartyLoginPlatformCredentialPublicV3Params{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Third Party Credential'
    name: 'RetrieveAllActiveThirdPartyLoginPlatformCredentialV3'
    summary: 'Get All Active Third Party Platform Active Credential'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        thirdPartyCredentialService := iam.ThirdPartyCredentialService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := thirdPartyCredentialService.RetrieveAllActiveThirdPartyLoginPlatformCredentialV3Short(
            &third_party_credential.RetrieveAllActiveThirdPartyLoginPlatformCredentialV3Params{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'SSO Credential'
    name: 'RetrieveAllSSOLoginPlatformCredentialV3'
    summary: 'Get All SSO Platform Credential'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        ssoCredentialService := iam.SSOCredentialService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := ssoCredentialService.RetrieveAllSSOLoginPlatformCredentialV3Short(
            &sso_credential.RetrieveAllSSOLoginPlatformCredentialV3Params{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Third Party Credential'
    name: 'RetrieveAllThirdPartyLoginPlatformCredentialV3'
    summary: 'Get All Third Party Platform Credential'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        thirdPartyCredentialService := iam.ThirdPartyCredentialService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := thirdPartyCredentialService.RetrieveAllThirdPartyLoginPlatformCredentialV3Short(
            &third_party_credential.RetrieveAllThirdPartyLoginPlatformCredentialV3Params{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'SSO Credential'
    name: 'RetrieveSSOLoginPlatformCredential'
    summary: 'Retrieve SSO Platform Credential'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        ssoCredentialService := iam.SSOCredentialService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := ssoCredentialService.RetrieveSSOLoginPlatformCredentialShort(
            &sso_credential.RetrieveSSOLoginPlatformCredentialParams{
                Namespace:  ..., // string
                PlatformID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Third Party Credential'
    name: 'RetrieveThirdPartyLoginPlatformCredentialV3'
    summary: 'Retrieve Third Party Platform Credential'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        thirdPartyCredentialService := iam.ThirdPartyCredentialService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := thirdPartyCredentialService.RetrieveThirdPartyLoginPlatformCredentialV3Short(
            &third_party_credential.RetrieveThirdPartyLoginPlatformCredentialV3Params{
                Namespace:  ..., // string
                PlatformID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0'
    name: 'RetrieveUserThirdPartyPlatformTokenV3'
    summary: 'Retrieve User Third Party Platform Token'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20Service := iam.OAuth20Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20Service.RetrieveUserThirdPartyPlatformTokenV3Short(
            &o_auth2_0.RetrieveUserThirdPartyPlatformTokenV3Params{
                Namespace:  ..., // string
                PlatformID: ..., // string
                UserID:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth'
    name: 'RevokeAUser'
    summary: 'OAuth2 user revocation API'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuthService := iam.OAuthService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuthService.RevokeAUserShort(
            &o_auth.RevokeAUserParams{
                UserID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth'
    name: 'RevokeToken'
    summary: 'OAuth2 token revocation API'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuthService := iam.OAuthService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuthService.RevokeTokenShort(
            &o_auth.RevokeTokenParams{
                Token: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth'
    name: 'RevokeUser'
    summary: 'Revokes user''s tokens'''
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuthService := iam.OAuthService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuthService.RevokeUserShort(
            &o_auth.RevokeUserParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0'
    name: 'RevokeUserV3'
    summary: 'Revokes user''s tokens'''
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20Service := iam.OAuth20Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20Service.RevokeUserV3Short(
            &o_auth2_0.RevokeUserV3Params{
                Namespace:            ..., // string
                UserID:               ..., // string
                IncludeGameNamespace: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'SaveUserPermission'
    summary: 'Save User Permissions'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.SaveUserPermissionShort(
            &users.SaveUserPermissionParams{
                Body:      ..., // *iamclientmodels.AccountcommonPermissions
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'SaveUserRoles'
    summary: 'Save User Roles'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.SaveUserRolesShort(
            &users.SaveUserRolesParams{
                Body:      ..., // []string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'SearchUser'
    summary: 'Search users'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.SearchUserShort(
            &users.SearchUserParams{
                Namespace: ..., // string
                Query:     ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0'
    name: 'SendMFAAuthenticationCode'
    summary: 'Send 2FA code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20Service := iam.OAuth20Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20Service.SendMFAAuthenticationCodeShort(
            &o_auth2_0.SendMFAAuthenticationCodeParams{
                ClientID: ..., // string
                Factor:   ..., // string
                MFAToken: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'SendVerificationCode'
    summary: 'Send verification code to user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.SendVerificationCodeShort(
            &users.SendVerificationCodeParams{
                Body:      ..., // *iamclientmodels.ModelSendVerificationCodeRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'SetRoleAsAdmin'
    summary: 'Set Role as Admin Role'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.SetRoleAsAdminShort(
            &roles.SetRoleAsAdminParams{
                RoleID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0'
    name: 'SimultaneousLoginV3'
    summary: 'Simultaneous login'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20Service := iam.OAuth20Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20Service.SimultaneousLoginV3Short(
            &o_auth2_0.SimultaneousLoginV3Params{
                SimultaneousPlatform: ..., // *string
                SimultaneousTicket:   ..., // *string
                NativePlatform:       ..., // string
                NativePlatformTicket: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0 V4'
    name: 'SimultaneousLoginV4'
    summary: 'Simultaneous login'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20V4Service := iam.OAuth20V4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20V4Service.SimultaneousLoginV4Short(
            &o_auth2_0_v4.SimultaneousLoginV4Params{
                SimultaneousPlatform: ..., // *string
                SimultaneousTicket:   ..., // *string
                NativePlatform:       ..., // string
                NativePlatformTicket: ..., // string
                CodeChallenge:        ..., // *string
                CodeChallengeMethod:  ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth'
    name: 'TokenGrant'
    summary: 'OAuth2 access token generation endpoint'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuthService := iam.OAuthService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuthService.TokenGrantShort(
            &o_auth.TokenGrantParams{
                DeviceID:     ..., // *string
                Code:         ..., // *string
                ExtendExp:    ..., // *bool
                Namespace:    ..., // *string
                Password:     ..., // *string
                RedirectURI:  ..., // *string
                RefreshToken: ..., // *string
                Username:     ..., // *string
                GrantType:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0'
    name: 'TokenGrantV3'
    summary: 'OAuth2 access token generation endpoint'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20Service := iam.OAuth20Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20Service.TokenGrantV3Short(
            &o_auth2_0.TokenGrantV3Params{
                AuthTrustID:     ..., // *string
                DeviceID:        ..., // *string
                AdditionalData:  ..., // *string
                ClientID:        ..., // *string
                ClientSecret:    ..., // *string
                Code:            ..., // *string
                CodeVerifier:    ..., // *string
                ExtendNamespace: ..., // *string
                ExtendExp:       ..., // *bool
                Password:        ..., // *string
                RedirectURI:     ..., // *string
                RefreshToken:    ..., // *string
                Scope:           ..., // *string
                Username:        ..., // *string
                GrantType:       ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0 V4'
    name: 'TokenGrantV4'
    summary: 'OAuth2 access token generation endpoint V4'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20V4Service := iam.OAuth20V4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20V4Service.TokenGrantV4Short(
            &o_auth2_0_v4.TokenGrantV4Params{
                AuthTrustID:         ..., // *string
                DeviceID:            ..., // *string
                AdditionalData:      ..., // *string
                ClientID:            ..., // *string
                ClientSecret:        ..., // *string
                Code:                ..., // *string
                CodeVerifier:        ..., // *string
                ExtendNamespace:     ..., // *string
                ExtendExp:           ..., // *bool
                LoginQueueTicket:    ..., // *string
                Password:            ..., // *string
                RedirectURI:         ..., // *string
                RefreshToken:        ..., // *string
                Scope:               ..., // *string
                Username:            ..., // *string
                GrantType:           ..., // string
                CodeChallenge:       ..., // *string
                CodeChallengeMethod: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0'
    name: 'TokenIntrospectionV3'
    summary: 'OAuth2 token introspection API'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20Service := iam.OAuth20Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20Service.TokenIntrospectionV3Short(
            &o_auth2_0.TokenIntrospectionV3Params{
                Token: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0'
    name: 'TokenRevocationV3'
    summary: 'OAuth2 token revocation API'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20Service := iam.OAuth20Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20Service.TokenRevocationV3Short(
            &o_auth2_0.TokenRevocationV3Params{
                Token: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Clients'
    name: 'UpdateClient'
    summary: 'Update Client'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        clientsService := iam.ClientsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := clientsService.UpdateClientShort(
            &clients.UpdateClientParams{
                Body:     ..., // *iamclientmodels.ClientmodelClientUpdateRequest
                ClientID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Clients'
    name: 'UpdateClientPermission'
    summary: 'Update Client Permissions'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        clientsService := iam.ClientsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := clientsService.UpdateClientPermissionShort(
            &clients.UpdateClientPermissionParams{
                Body:     ..., // *iamclientmodels.AccountcommonClientPermissions
                ClientID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Clients'
    name: 'UpdateClientSecret'
    summary: 'Update Client Secret'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        clientsService := iam.ClientsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := clientsService.UpdateClientSecretShort(
            &clients.UpdateClientSecretParams{
                Body:     ..., // *iamclientmodels.ClientmodelClientUpdateSecretRequest
                ClientID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'UpdateCountryAgeRestriction'
    summary: 'Update country''s age restriction'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.UpdateCountryAgeRestrictionShort(
            &users.UpdateCountryAgeRestrictionParams{
                Body:        ..., // *iamclientmodels.ModelCountryAgeRestrictionRequest
                CountryCode: ..., // string
                Namespace:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'UpdatePassword'
    summary: 'Update User Password'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.UpdatePasswordShort(
            &users.UpdatePasswordParams{
                Body:      ..., // *iamclientmodels.ModelUserPasswordUpdateRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'UpdateRole'
    summary: 'Update Role'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.UpdateRoleShort(
            &roles.UpdateRoleParams{
                Body:   ..., // *iamclientmodels.ModelRoleUpdateRequest
                RoleID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Roles'
    name: 'UpdateRolePermissions'
    summary: 'Update Role Permissions'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        rolesService := iam.RolesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rolesService.UpdateRolePermissionsShort(
            &roles.UpdateRolePermissionsParams{
                Body:   ..., // *iamclientmodels.AccountcommonPermissions
                RoleID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'SSO Credential'
    name: 'UpdateSSOPlatformCredential'
    summary: 'Update SSO Platform Credential'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        ssoCredentialService := iam.SSOCredentialService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := ssoCredentialService.UpdateSSOPlatformCredentialShort(
            &sso_credential.UpdateSSOPlatformCredentialParams{
                Body:       ..., // *iamclientmodels.ModelSSOPlatformCredentialRequest
                Namespace:  ..., // string
                PlatformID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Third Party Credential'
    name: 'UpdateThirdPartyLoginPlatformCredentialV3'
    summary: 'Update Third Party Platform Credential'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        thirdPartyCredentialService := iam.ThirdPartyCredentialService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := thirdPartyCredentialService.UpdateThirdPartyLoginPlatformCredentialV3Short(
            &third_party_credential.UpdateThirdPartyLoginPlatformCredentialV3Params{
                Body:       ..., // *iamclientmodels.ModelThirdPartyLoginPlatformCredentialRequest
                Namespace:  ..., // string
                PlatformID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Third Party Credential'
    name: 'UpdateThirdPartyLoginPlatformDomainV3'
    summary: 'Set Third Party Platform Credential''s domain'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        thirdPartyCredentialService := iam.ThirdPartyCredentialService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := thirdPartyCredentialService.UpdateThirdPartyLoginPlatformDomainV3Short(
            &third_party_credential.UpdateThirdPartyLoginPlatformDomainV3Params{
                Body:       ..., // *iamclientmodels.ModelPlatformDomainUpdateRequest
                Namespace:  ..., // string
                PlatformID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'UpdateUser'
    summary: 'Update User'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.UpdateUserShort(
            &users.UpdateUserParams{
                Body:      ..., // *iamclientmodels.ModelUserUpdateRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'UpdateUserV3'
    summary: 'Update User'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.UpdateUserV3Short(
            &users.UpdateUserV3Params{
                Body:      ..., // *iamclientmodels.ModelPublicUserUpdateRequestV3
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0 - Extension'
    name: 'UpgradeAndAuthenticateForwardV3'
    summary: 'Handle the forward for account upgrade'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20ExtensionService := iam.OAuth20ExtensionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20ExtensionService.UpgradeAndAuthenticateForwardV3Short(
            &o_auth2_0_extension.UpgradeAndAuthenticateForwardV3Params{
                ClientID:            ..., // string
                UpgradeSuccessToken: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'upgradeHeadlessAccount'
    summary: 'Upgrade user account to full account (with email)'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.UpgradeHeadlessAccountShort(
            &users.UpgradeHeadlessAccountParams{
                Body:      ..., // *iamclientmodels.ModelUpgradeHeadlessAccountRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'UpgradeHeadlessAccountWithVerificationCode'
    summary: 'Upgrade headless account and automatically verified the email address if it is succeeded'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.UpgradeHeadlessAccountWithVerificationCodeShort(
            &users.UpgradeHeadlessAccountWithVerificationCodeParams{
                Body:      ..., // *iamclientmodels.ModelUpgradeHeadlessAccountWithVerificationCodeRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0 - Extension'
    name: 'UserAuthenticationV3'
    summary: 'Authentication API'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20ExtensionService := iam.OAuth20ExtensionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20ExtensionService.UserAuthenticationV3Short(
            &o_auth2_0_extension.UserAuthenticationV3Params{
                ClientID:    ..., // *string
                ExtendExp:   ..., // *bool
                RedirectURI: ..., // *string
                Password:    ..., // string
                RequestID:   ..., // string
                UserName:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Users'
    name: 'UserVerification'
    summary: 'Redeem verification code sent to user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        usersService := iam.UsersService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := usersService.UserVerificationShort(
            &users.UserVerificationParams{
                Body:      ..., // *iamclientmodels.ModelUserVerificationRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0 - Extension'
    name: 'ValidateOneTimeLinkingCodeV3'
    summary: 'Validate one time linking code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20ExtensionService := iam.OAuth20ExtensionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20ExtensionService.ValidateOneTimeLinkingCodeV3Short(
            &o_auth2_0_extension.ValidateOneTimeLinkingCodeV3Params{
                OneTimeLinkCode: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0'
    name: 'Verify2FACode'
    summary: 'Verify 2FA code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20Service := iam.OAuth20Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20Service.Verify2FACodeShort(
            &o_auth2_0.Verify2FACodeParams{
                Code:           ..., // string
                Factor:         ..., // string
                MFAToken:       ..., // string
                RememberDevice: ..., // bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0'
    name: 'Verify2FACodeForward'
    summary: 'Verify 2FA code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20Service := iam.OAuth20Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20Service.Verify2FACodeForwardShort(
            &o_auth2_0.Verify2FACodeForwardParams{
                DefaultFactor:  ..., // *string
                Factors:        ..., // *string
                RememberDevice: ..., // *bool
                ClientID:       ..., // string
                Code:           ..., // string
                Factor:         ..., // string
                MFAToken:       ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0 V4'
    name: 'Verify2FACodeV4'
    summary: 'Verify 2FA code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20V4Service := iam.OAuth20V4Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20V4Service.Verify2FACodeV4Short(
            &o_auth2_0_v4.Verify2FACodeV4Params{
                Code:           ..., // string
                Factor:         ..., // string
                MFAToken:       ..., // string
                RememberDevice: ..., // bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth'
    name: 'VerifyToken'
    summary: 'OAuth2 token verification API'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuthService := iam.OAuthService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuthService.VerifyTokenShort(
            &o_auth.VerifyTokenParams{
                Token: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'OAuth2.0'
    name: 'VerifyTokenV3'
    summary: 'OAuth2 token verification API'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewIamClient(configRepo)
        oAuth20Service := iam.OAuth20Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := oAuth20Service.VerifyTokenV3Short(
            &o_auth2_0.VerifyTokenV3Params{
                Token: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }
# end iam
# begin inventory
  - ns: 'Admin Items'
    name: 'AdminBulkRemoveItems'
    summary: 'To bulk remove items'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        adminItemsService := inventory.AdminItemsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminItemsService.AdminBulkRemoveItemsShort(
            &admin_items.AdminBulkRemoveItemsParams{
                Body:        ..., // []*inventoryclientmodels.ApimodelsRemoveInventoryItemReq
                InventoryID: ..., // string
                Namespace:   ..., // string
                UserID:      ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Items'
    name: 'AdminBulkSaveItem'
    summary: 'Bulk save items to inventory'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        adminItemsService := inventory.AdminItemsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminItemsService.AdminBulkSaveItemShort(
            &admin_items.AdminBulkSaveItemParams{
                Body:      ..., // []*inventoryclientmodels.ApimodelsSaveItemReq
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Items'
    name: 'AdminBulkSaveItemToInventory'
    summary: 'Bulk save items to specific inventory'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        adminItemsService := inventory.AdminItemsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminItemsService.AdminBulkSaveItemToInventoryShort(
            &admin_items.AdminBulkSaveItemToInventoryParams{
                Body:        ..., // []*inventoryclientmodels.ApimodelsSaveItemToInventoryReq
                InventoryID: ..., // string
                Namespace:   ..., // string
                UserID:      ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Items'
    name: 'AdminBulkUpdateMyItems'
    summary: 'To bulk update items'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        adminItemsService := inventory.AdminItemsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminItemsService.AdminBulkUpdateMyItemsShort(
            &admin_items.AdminBulkUpdateMyItemsParams{
                Body:        ..., // []*inventoryclientmodels.ApimodelsAdminUpdateItemReq
                InventoryID: ..., // string
                Namespace:   ..., // string
                UserID:      ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Items'
    name: 'AdminConsumeUserItem'
    summary: 'To consume item'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        adminItemsService := inventory.AdminItemsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminItemsService.AdminConsumeUserItemShort(
            &admin_items.AdminConsumeUserItemParams{
                Body:                ..., // *inventoryclientmodels.ApimodelsConsumeItemReq
                InventoryID:         ..., // string
                Namespace:           ..., // string
                UserID:              ..., // string
                DateRangeValidation: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Chaining Operations'
    name: 'AdminCreateChainingOperations'
    summary: 'To Create Chaining Operation'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        adminChainingOperationsService := inventory.AdminChainingOperationsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminChainingOperationsService.AdminCreateChainingOperationsShort(
            &admin_chaining_operations.AdminCreateChainingOperationsParams{
                Body:      ..., // *inventoryclientmodels.ApimodelsChainingOperationReq
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Integration Configurations'
    name: 'AdminCreateIntegrationConfiguration'
    summary: 'To create integration configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        adminIntegrationConfigurationsService := inventory.AdminIntegrationConfigurationsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminIntegrationConfigurationsService.AdminCreateIntegrationConfigurationShort(
            &admin_integration_configurations.AdminCreateIntegrationConfigurationParams{
                Body:      ..., // *inventoryclientmodels.ApimodelsCreateIntegrationConfigurationReq
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Inventories'
    name: 'AdminCreateInventory'
    summary: 'To create inventory'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        adminInventoriesService := inventory.AdminInventoriesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminInventoriesService.AdminCreateInventoryShort(
            &admin_inventories.AdminCreateInventoryParams{
                Body:      ..., // *inventoryclientmodels.ApimodelsCreateInventoryReq
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Inventory Configurations'
    name: 'AdminCreateInventoryConfiguration'
    summary: 'To create inventory configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        adminInventoryConfigurationsService := inventory.AdminInventoryConfigurationsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminInventoryConfigurationsService.AdminCreateInventoryConfigurationShort(
            &admin_inventory_configurations.AdminCreateInventoryConfigurationParams{
                Body:      ..., // *inventoryclientmodels.ApimodelsCreateInventoryConfigurationReq
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Item Types'
    name: 'AdminCreateItemType'
    summary: 'To create an itemtype'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        adminItemTypesService := inventory.AdminItemTypesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminItemTypesService.AdminCreateItemTypeShort(
            &admin_item_types.AdminCreateItemTypeParams{
                Body:      ..., // *inventoryclientmodels.ApimodelsCreateItemTypeReq
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Tags'
    name: 'AdminCreateTag'
    summary: 'To create a tag'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        adminTagsService := inventory.AdminTagsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminTagsService.AdminCreateTagShort(
            &admin_tags.AdminCreateTagParams{
                Body:      ..., // *inventoryclientmodels.ApimodelsCreateTagReq
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Inventory Configurations'
    name: 'AdminDeleteInventoryConfiguration'
    summary: 'To delete inventory configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        adminInventoryConfigurationsService := inventory.AdminInventoryConfigurationsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminInventoryConfigurationsService.AdminDeleteInventoryConfigurationShort(
            &admin_inventory_configurations.AdminDeleteInventoryConfigurationParams{
                InventoryConfigurationID: ..., // string
                Namespace:                ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Item Types'
    name: 'AdminDeleteItemType'
    summary: 'To delete an item type'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        adminItemTypesService := inventory.AdminItemTypesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminItemTypesService.AdminDeleteItemTypeShort(
            &admin_item_types.AdminDeleteItemTypeParams{
                ItemTypeName: ..., // string
                Namespace:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Tags'
    name: 'AdminDeleteTag'
    summary: 'To delete a tag'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        adminTagsService := inventory.AdminTagsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminTagsService.AdminDeleteTagShort(
            &admin_tags.AdminDeleteTagParams{
                Namespace: ..., // string
                TagName:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Inventories'
    name: 'AdminGetInventory'
    summary: 'To get an inventory'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        adminInventoriesService := inventory.AdminInventoriesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminInventoriesService.AdminGetInventoryShort(
            &admin_inventories.AdminGetInventoryParams{
                InventoryID: ..., // string
                Namespace:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Inventory Configurations'
    name: 'AdminGetInventoryConfiguration'
    summary: 'To get inventory configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        adminInventoryConfigurationsService := inventory.AdminInventoryConfigurationsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminInventoryConfigurationsService.AdminGetInventoryConfigurationShort(
            &admin_inventory_configurations.AdminGetInventoryConfigurationParams{
                InventoryConfigurationID: ..., // string
                Namespace:                ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Items'
    name: 'AdminGetInventoryItem'
    summary: 'To get an item'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        adminItemsService := inventory.AdminItemsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminItemsService.AdminGetInventoryItemShort(
            &admin_items.AdminGetInventoryItemParams{
                InventoryID:  ..., // string
                Namespace:    ..., // string
                SlotID:       ..., // string
                SourceItemID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Integration Configurations'
    name: 'AdminListIntegrationConfigurations'
    summary: 'To list integration configurations'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        adminIntegrationConfigurationsService := inventory.AdminIntegrationConfigurationsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminIntegrationConfigurationsService.AdminListIntegrationConfigurationsShort(
            &admin_integration_configurations.AdminListIntegrationConfigurationsParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                SortBy:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Inventories'
    name: 'AdminListInventories'
    summary: 'To list all inventories'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        adminInventoriesService := inventory.AdminInventoriesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminInventoriesService.AdminListInventoriesShort(
            &admin_inventories.AdminListInventoriesParams{
                Namespace:                  ..., // string
                InventoryConfigurationCode: ..., // *string
                Limit:                      ..., // *int64
                Offset:                     ..., // *int64
                SortBy:                     ..., // *string
                UserID:                     ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Inventory Configurations'
    name: 'AdminListInventoryConfigurations'
    summary: 'To list inventory configurations'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        adminInventoryConfigurationsService := inventory.AdminInventoryConfigurationsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminInventoryConfigurationsService.AdminListInventoryConfigurationsShort(
            &admin_inventory_configurations.AdminListInventoryConfigurationsParams{
                Namespace: ..., // string
                Code:      ..., // *string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                SortBy:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Items'
    name: 'AdminListItems'
    summary: 'To list all items'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        adminItemsService := inventory.AdminItemsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminItemsService.AdminListItemsShort(
            &admin_items.AdminListItemsParams{
                InventoryID:  ..., // string
                Namespace:    ..., // string
                Limit:        ..., // *int64
                Offset:       ..., // *int64
                SortBy:       ..., // *string
                SourceItemID: ..., // *string
                Tags:         ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Item Types'
    name: 'AdminListItemTypes'
    summary: 'To list itemtypes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        adminItemTypesService := inventory.AdminItemTypesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminItemTypesService.AdminListItemTypesShort(
            &admin_item_types.AdminListItemTypesParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                SortBy:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Tags'
    name: 'AdminListTags'
    summary: 'To list tags'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        adminTagsService := inventory.AdminTagsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminTagsService.AdminListTagsShort(
            &admin_tags.AdminListTagsParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                Owner:     ..., // *string
                SortBy:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Inventories'
    name: 'AdminPurchasable'
    summary: 'To validate user inventory capacity when purchase ecommerce item'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        adminInventoriesService := inventory.AdminInventoriesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminInventoriesService.AdminPurchasableShort(
            &admin_inventories.AdminPurchasableParams{
                Body:      ..., // *inventoryclientmodels.ApimodelsPurchaseValidationReq
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Items'
    name: 'AdminSaveItem'
    summary: 'To save item'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        adminItemsService := inventory.AdminItemsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminItemsService.AdminSaveItemShort(
            &admin_items.AdminSaveItemParams{
                Body:      ..., // *inventoryclientmodels.ApimodelsSaveItemReq
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Items'
    name: 'AdminSaveItemToInventory'
    summary: 'To save item to specific inventory'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        adminItemsService := inventory.AdminItemsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminItemsService.AdminSaveItemToInventoryShort(
            &admin_items.AdminSaveItemToInventoryParams{
                Body:        ..., // *inventoryclientmodels.ApimodelsSaveItemToInventoryReq
                InventoryID: ..., // string
                Namespace:   ..., // string
                UserID:      ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Items'
    name: 'AdminSyncUserEntitlements'
    summary: 'To sync user''s entitlements to e-commerce'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        adminItemsService := inventory.AdminItemsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminItemsService.AdminSyncUserEntitlementsShort(
            &admin_items.AdminSyncUserEntitlementsParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Integration Configurations'
    name: 'AdminUpdateIntegrationConfiguration'
    summary: 'To update integration configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        adminIntegrationConfigurationsService := inventory.AdminIntegrationConfigurationsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminIntegrationConfigurationsService.AdminUpdateIntegrationConfigurationShort(
            &admin_integration_configurations.AdminUpdateIntegrationConfigurationParams{
                Body:                       ..., // *inventoryclientmodels.ApimodelsUpdateIntegrationConfigurationReq
                IntegrationConfigurationID: ..., // string
                Namespace:                  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Inventories'
    name: 'AdminUpdateInventory'
    summary: 'To update inventory'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        adminInventoriesService := inventory.AdminInventoriesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminInventoriesService.AdminUpdateInventoryShort(
            &admin_inventories.AdminUpdateInventoryParams{
                Body:        ..., // *inventoryclientmodels.ApimodelsUpdateInventoryReq
                InventoryID: ..., // string
                Namespace:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Inventory Configurations'
    name: 'AdminUpdateInventoryConfiguration'
    summary: 'To update inventory configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        adminInventoryConfigurationsService := inventory.AdminInventoryConfigurationsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminInventoryConfigurationsService.AdminUpdateInventoryConfigurationShort(
            &admin_inventory_configurations.AdminUpdateInventoryConfigurationParams{
                Body:                     ..., // *inventoryclientmodels.ApimodelsInventoryConfigurationReq
                InventoryConfigurationID: ..., // string
                Namespace:                ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Integration Configurations'
    name: 'AdminUpdateStatusIntegrationConfiguration'
    summary: 'To update status integration configuration to be active / not active '
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        adminIntegrationConfigurationsService := inventory.AdminIntegrationConfigurationsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminIntegrationConfigurationsService.AdminUpdateStatusIntegrationConfigurationShort(
            &admin_integration_configurations.AdminUpdateStatusIntegrationConfigurationParams{
                Body:                       ..., // *inventoryclientmodels.ApimodelsUpdateStatusIntegrationConfigurationReq
                IntegrationConfigurationID: ..., // string
                Namespace:                  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Inventories'
    name: 'AdminUpdateUserInventoriesByInventoryCode'
    summary: 'To update user inventories by inventory code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        adminInventoriesService := inventory.AdminInventoriesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminInventoriesService.AdminUpdateUserInventoriesByInventoryCodeShort(
            &admin_inventories.AdminUpdateUserInventoriesByInventoryCodeParams{
                Body:                       ..., // *inventoryclientmodels.ApimodelsUpdateInventoryReq
                InventoryConfigurationCode: ..., // string
                Namespace:                  ..., // string
                UserID:                     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Inventories'
    name: 'deleteInventory'
    summary: 'To delete inventory'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        adminInventoriesService := inventory.AdminInventoriesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminInventoriesService.DeleteInventoryShort(
            &admin_inventories.DeleteInventoryParams{
                Body:        ..., // *inventoryclientmodels.ApimodelsDeleteInventoryReq
                InventoryID: ..., // string
                Namespace:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Items'
    name: 'PublicBulkRemoveMyItems'
    summary: 'To bulk remove items'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        publicItemsService := inventory.PublicItemsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicItemsService.PublicBulkRemoveMyItemsShort(
            &public_items.PublicBulkRemoveMyItemsParams{
                Body:        ..., // []*inventoryclientmodels.ApimodelsRemoveInventoryItemReq
                InventoryID: ..., // string
                Namespace:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Items'
    name: 'PublicBulkUpdateMyItems'
    summary: 'To bulk update items'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        publicItemsService := inventory.PublicItemsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicItemsService.PublicBulkUpdateMyItemsShort(
            &public_items.PublicBulkUpdateMyItemsParams{
                Body:        ..., // []*inventoryclientmodels.ApimodelsUpdateItemReq
                InventoryID: ..., // string
                Namespace:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Items'
    name: 'PublicConsumeMyItem'
    summary: 'To consume item'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        publicItemsService := inventory.PublicItemsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicItemsService.PublicConsumeMyItemShort(
            &public_items.PublicConsumeMyItemParams{
                Body:        ..., // *inventoryclientmodels.ApimodelsConsumeItemReq
                InventoryID: ..., // string
                Namespace:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Items'
    name: 'PublicGetItem'
    summary: 'To get an item'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        publicItemsService := inventory.PublicItemsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicItemsService.PublicGetItemShort(
            &public_items.PublicGetItemParams{
                InventoryID:  ..., // string
                Namespace:    ..., // string
                SlotID:       ..., // string
                SourceItemID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Inventories'
    name: 'PublicListInventories'
    summary: 'To list all my inventories'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        publicInventoriesService := inventory.PublicInventoriesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicInventoriesService.PublicListInventoriesShort(
            &public_inventories.PublicListInventoriesParams{
                Namespace:                  ..., // string
                InventoryConfigurationCode: ..., // *string
                Limit:                      ..., // *int64
                Offset:                     ..., // *int64
                SortBy:                     ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Inventory Configurations'
    name: 'PublicListInventoryConfigurations'
    summary: 'To list inventory configurations'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        publicInventoryConfigurationsService := inventory.PublicInventoryConfigurationsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicInventoryConfigurationsService.PublicListInventoryConfigurationsShort(
            &public_inventory_configurations.PublicListInventoryConfigurationsParams{
                Namespace: ..., // string
                Code:      ..., // *string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                SortBy:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Items'
    name: 'PublicListItems'
    summary: 'To list all items'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        publicItemsService := inventory.PublicItemsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicItemsService.PublicListItemsShort(
            &public_items.PublicListItemsParams{
                InventoryID:  ..., // string
                Namespace:    ..., // string
                Limit:        ..., // *int64
                Offset:       ..., // *int64
                SortBy:       ..., // *string
                SourceItemID: ..., // *string
                Tags:         ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Item Types'
    name: 'PublicListItemTypes'
    summary: 'To list item types'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        publicItemTypesService := inventory.PublicItemTypesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicItemTypesService.PublicListItemTypesShort(
            &public_item_types.PublicListItemTypesParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                SortBy:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Tags'
    name: 'PublicListTags'
    summary: 'To list tags'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        publicTagsService := inventory.PublicTagsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicTagsService.PublicListTagsShort(
            &public_tags.PublicListTagsParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                SortBy:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Items'
    name: 'PublicMoveMyItems'
    summary: 'To move items between my inventories'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewInventoryClient(configRepo)
        publicItemsService := inventory.PublicItemsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicItemsService.PublicMoveMyItemsShort(
            &public_items.PublicMoveMyItemsParams{
                Body:        ..., // *inventoryclientmodels.ApimodelsMoveItemsReq
                InventoryID: ..., // string
                Namespace:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }
# end inventory
# begin leaderboard
  - ns: 'Anonymization'
    name: 'adminAnonymizeUserLeaderboardAdminV1'
    summary: 'Anonymize user''s leaderboard'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        anonymizationService := leaderboard.AnonymizationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := anonymizationService.AdminAnonymizeUserLeaderboardAdminV1Short(
            &anonymization.AdminAnonymizeUserLeaderboardAdminV1Params{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardData'
    name: 'AdminGetArchivedLeaderboardRankingDataV1Handler'
    summary: 'Admin Get signed url for archive all time leaderboard ranking data'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardDataService := leaderboard.LeaderboardDataService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardDataService.AdminGetArchivedLeaderboardRankingDataV1HandlerShort(
            &leaderboard_data.AdminGetArchivedLeaderboardRankingDataV1HandlerParams{
                Namespace:        ..., // string
                Slug:             ..., // *string
                LeaderboardCodes: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardDataV3'
    name: 'bulkGetUsersRankingPublicV3'
    summary: 'Bulk get users ranking'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardDataV3Service := leaderboard.LeaderboardDataV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardDataV3Service.BulkGetUsersRankingPublicV3Short(
            &leaderboard_data_v3.BulkGetUsersRankingPublicV3Params{
                Body:            ..., // *leaderboardclientmodels.ModelsBulkUserIDsRequest
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
                PreviousVersion: ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardData'
    name: 'CreateArchivedLeaderboardRankingDataV1Handler'
    summary: 'Archive a leadeboard data ranking'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardDataService := leaderboard.LeaderboardDataService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardDataService.CreateArchivedLeaderboardRankingDataV1HandlerShort(
            &leaderboard_data.CreateArchivedLeaderboardRankingDataV1HandlerParams{
                Body:      ..., // *leaderboardclientmodels.ModelsArchiveLeaderboardReq
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardConfiguration'
    name: 'createLeaderboardConfigurationAdminV1'
    summary: 'create new leaderboard'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardConfigurationService := leaderboard.LeaderboardConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardConfigurationService.CreateLeaderboardConfigurationAdminV1Short(
            &leaderboard_configuration.CreateLeaderboardConfigurationAdminV1Params{
                Body:      ..., // *leaderboardclientmodels.ModelsLeaderboardConfigReq
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardConfigurationV3'
    name: 'createLeaderboardConfigurationAdminV3'
    summary: 'create new leaderboard'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardConfigurationV3Service := leaderboard.LeaderboardConfigurationV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardConfigurationV3Service.CreateLeaderboardConfigurationAdminV3Short(
            &leaderboard_configuration_v3.CreateLeaderboardConfigurationAdminV3Params{
                Body:      ..., // *leaderboardclientmodels.ModelsLeaderboardConfigReqV3
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardConfiguration'
    name: 'createLeaderboardConfigurationPublicV1'
    summary: 'create new leaderboard'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardConfigurationService := leaderboard.LeaderboardConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardConfigurationService.CreateLeaderboardConfigurationPublicV1Short(
            &leaderboard_configuration.CreateLeaderboardConfigurationPublicV1Params{
                Body:      ..., // *leaderboardclientmodels.ModelsLeaderboardConfigReq
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardDataV3'
    name: 'deleteAllUserRankingByCycleIdAdminV3'
    summary: 'delete all user ranking by cycle id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardDataV3Service := leaderboard.LeaderboardDataV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardDataV3Service.DeleteAllUserRankingByCycleIDAdminV3Short(
            &leaderboard_data_v3.DeleteAllUserRankingByCycleIDAdminV3Params{
                CycleID:         ..., // string
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardConfiguration'
    name: 'deleteBulkLeaderboardConfigurationAdminV1'
    summary: 'delete bulk leaderboards. response body will contains failed leaderboard with the reason why it''s failed to delete'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardConfigurationService := leaderboard.LeaderboardConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardConfigurationService.DeleteBulkLeaderboardConfigurationAdminV1Short(
            &leaderboard_configuration.DeleteBulkLeaderboardConfigurationAdminV1Params{
                Body:      ..., // *leaderboardclientmodels.ModelsDeleteBulkLeaderboardsReq
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardConfigurationV3'
    name: 'deleteBulkLeaderboardConfigurationAdminV3'
    summary: 'delete bulk leaderboards. response body will contains failed leaderboard with the reason why it''s failed to delete'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardConfigurationV3Service := leaderboard.LeaderboardConfigurationV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardConfigurationV3Service.DeleteBulkLeaderboardConfigurationAdminV3Short(
            &leaderboard_configuration_v3.DeleteBulkLeaderboardConfigurationAdminV3Params{
                Body:      ..., // *leaderboardclientmodels.ModelsDeleteBulkLeaderboardsReq
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardConfiguration'
    name: 'deleteLeaderboardConfigurationAdminV1'
    summary: 'delete leaderboard by leaderboardCode'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardConfigurationService := leaderboard.LeaderboardConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardConfigurationService.DeleteLeaderboardConfigurationAdminV1Short(
            &leaderboard_configuration.DeleteLeaderboardConfigurationAdminV1Params{
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardConfigurationV3'
    name: 'deleteLeaderboardConfigurationAdminV3'
    summary: 'delete leaderboard by leaderboardCode'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardConfigurationV3Service := leaderboard.LeaderboardConfigurationV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardConfigurationV3Service.DeleteLeaderboardConfigurationAdminV3Short(
            &leaderboard_configuration_v3.DeleteLeaderboardConfigurationAdminV3Params{
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardData'
    name: 'deleteUserRankingAdminV1'
    summary: 'Delete user ranking'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardDataService := leaderboard.LeaderboardDataService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardDataService.DeleteUserRankingAdminV1Short(
            &leaderboard_data.DeleteUserRankingAdminV1Params{
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
                UserID:          ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardDataV3'
    name: 'deleteUserRankingAdminV3'
    summary: 'Delete user ranking'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardDataV3Service := leaderboard.LeaderboardDataV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardDataV3Service.DeleteUserRankingAdminV3Short(
            &leaderboard_data_v3.DeleteUserRankingAdminV3Params{
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
                UserID:          ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardDataV3'
    name: 'deleteUserRankingByCycleIdAdminV3'
    summary: 'Delete user ranking by cycle id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardDataV3Service := leaderboard.LeaderboardDataV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardDataV3Service.DeleteUserRankingByCycleIDAdminV3Short(
            &leaderboard_data_v3.DeleteUserRankingByCycleIDAdminV3Params{
                CycleID:         ..., // string
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
                UserID:          ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardData'
    name: 'deleteUserRankingByLeaderboardCodeAdminV1'
    summary: 'delete all user ranking by leaderboard code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardDataService := leaderboard.LeaderboardDataService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardDataService.DeleteUserRankingByLeaderboardCodeAdminV1Short(
            &leaderboard_data.DeleteUserRankingByLeaderboardCodeAdminV1Params{
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardDataV3'
    name: 'deleteUserRankingByLeaderboardCodeAdminV3'
    summary: 'delete all user ranking by leaderboard code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardDataV3Service := leaderboard.LeaderboardDataV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardDataV3Service.DeleteUserRankingByLeaderboardCodeAdminV3Short(
            &leaderboard_data_v3.DeleteUserRankingByLeaderboardCodeAdminV3Params{
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardData'
    name: 'deleteUserRankingPublicV1'
    summary: 'Delete user ranking'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardDataService := leaderboard.LeaderboardDataService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardDataService.DeleteUserRankingPublicV1Short(
            &leaderboard_data.DeleteUserRankingPublicV1Params{
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
                UserID:          ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardData'
    name: 'deleteUserRankingsAdminV1'
    summary: 'Delete user ranking across leaderboard(s)'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardDataService := leaderboard.LeaderboardDataService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardDataService.DeleteUserRankingsAdminV1Short(
            &leaderboard_data.DeleteUserRankingsAdminV1Params{
                Namespace:       ..., // string
                UserID:          ..., // string
                LeaderboardCode: ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardDataV3'
    name: 'deleteUserRankingsAdminV3'
    summary: 'Delete user ranking across leaderboard(s)'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardDataV3Service := leaderboard.LeaderboardDataV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardDataV3Service.DeleteUserRankingsAdminV3Short(
            &leaderboard_data_v3.DeleteUserRankingsAdminV3Params{
                Namespace:       ..., // string
                UserID:          ..., // string
                LeaderboardCode: ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardData'
    name: 'GetAllTimeLeaderboardRankingAdminV1'
    summary: 'Get all time leaderboard ranking data'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardDataService := leaderboard.LeaderboardDataService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardDataService.GetAllTimeLeaderboardRankingAdminV1Short(
            &leaderboard_data.GetAllTimeLeaderboardRankingAdminV1Params{
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
                Limit:           ..., // *int64
                Offset:          ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardDataV3'
    name: 'GetAllTimeLeaderboardRankingAdminV3'
    summary: 'Get all time leaderboard ranking data'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardDataV3Service := leaderboard.LeaderboardDataV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardDataV3Service.GetAllTimeLeaderboardRankingAdminV3Short(
            &leaderboard_data_v3.GetAllTimeLeaderboardRankingAdminV3Params{
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
                Limit:           ..., // *int64
                Offset:          ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardData'
    name: 'GetAllTimeLeaderboardRankingPublicV1'
    summary: 'Get all time leaderboard ranking data'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardDataService := leaderboard.LeaderboardDataService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardDataService.GetAllTimeLeaderboardRankingPublicV1Short(
            &leaderboard_data.GetAllTimeLeaderboardRankingPublicV1Params{
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
                Limit:           ..., // *int64
                Offset:          ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardData'
    name: 'GetAllTimeLeaderboardRankingPublicV2'
    summary: 'Get all time leaderboard ranking data'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardDataService := leaderboard.LeaderboardDataService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardDataService.GetAllTimeLeaderboardRankingPublicV2Short(
            &leaderboard_data.GetAllTimeLeaderboardRankingPublicV2Params{
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
                Limit:           ..., // *int64
                Offset:          ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardDataV3'
    name: 'GetAllTimeLeaderboardRankingPublicV3'
    summary: 'Get all time leaderboard ranking data'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardDataV3Service := leaderboard.LeaderboardDataV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardDataV3Service.GetAllTimeLeaderboardRankingPublicV3Short(
            &leaderboard_data_v3.GetAllTimeLeaderboardRankingPublicV3Params{
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
                Limit:           ..., // *int64
                Offset:          ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardData'
    name: 'GetArchivedLeaderboardRankingDataV1Handler'
    summary: 'Get signed url for archive all time leaderboard ranking data'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardDataService := leaderboard.LeaderboardDataService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardDataService.GetArchivedLeaderboardRankingDataV1HandlerShort(
            &leaderboard_data.GetArchivedLeaderboardRankingDataV1HandlerParams{
                LeaderboardCode:  ..., // string
                Namespace:        ..., // string
                Slug:             ..., // *string
                LeaderboardCodes: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardDataV3'
    name: 'GetCurrentCycleLeaderboardRankingAdminV3'
    summary: 'Get cycle leaderboard ranking data'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardDataV3Service := leaderboard.LeaderboardDataV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardDataV3Service.GetCurrentCycleLeaderboardRankingAdminV3Short(
            &leaderboard_data_v3.GetCurrentCycleLeaderboardRankingAdminV3Params{
                CycleID:         ..., // string
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
                Limit:           ..., // *int64
                Offset:          ..., // *int64
                PreviousVersion: ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardDataV3'
    name: 'GetCurrentCycleLeaderboardRankingPublicV3'
    summary: 'Get cycle leaderboard ranking data'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardDataV3Service := leaderboard.LeaderboardDataV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardDataV3Service.GetCurrentCycleLeaderboardRankingPublicV3Short(
            &leaderboard_data_v3.GetCurrentCycleLeaderboardRankingPublicV3Params{
                CycleID:         ..., // string
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
                Limit:           ..., // *int64
                Offset:          ..., // *int64
                PreviousVersion: ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardData'
    name: 'GetCurrentMonthLeaderboardRankingAdminV1'
    summary: 'Get current month leaderboard ranking data'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardDataService := leaderboard.LeaderboardDataService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardDataService.GetCurrentMonthLeaderboardRankingAdminV1Short(
            &leaderboard_data.GetCurrentMonthLeaderboardRankingAdminV1Params{
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
                Limit:           ..., // *int64
                Offset:          ..., // *int64
                PreviousVersion: ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardData'
    name: 'GetCurrentMonthLeaderboardRankingPublicV1'
    summary: 'Get current month leaderboard ranking data'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardDataService := leaderboard.LeaderboardDataService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardDataService.GetCurrentMonthLeaderboardRankingPublicV1Short(
            &leaderboard_data.GetCurrentMonthLeaderboardRankingPublicV1Params{
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
                Limit:           ..., // *int64
                Offset:          ..., // *int64
                PreviousVersion: ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardData'
    name: 'GetCurrentSeasonLeaderboardRankingAdminV1'
    summary: 'Get current season leaderboard ranking data'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardDataService := leaderboard.LeaderboardDataService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardDataService.GetCurrentSeasonLeaderboardRankingAdminV1Short(
            &leaderboard_data.GetCurrentSeasonLeaderboardRankingAdminV1Params{
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
                Limit:           ..., // *int64
                Offset:          ..., // *int64
                PreviousVersion: ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardData'
    name: 'GetCurrentSeasonLeaderboardRankingPublicV1'
    summary: 'Get current season leaderboard ranking data'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardDataService := leaderboard.LeaderboardDataService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardDataService.GetCurrentSeasonLeaderboardRankingPublicV1Short(
            &leaderboard_data.GetCurrentSeasonLeaderboardRankingPublicV1Params{
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
                Limit:           ..., // *int64
                Offset:          ..., // *int64
                PreviousVersion: ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardData'
    name: 'GetCurrentWeekLeaderboardRankingAdminV1'
    summary: 'Get current week leaderboard ranking data'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardDataService := leaderboard.LeaderboardDataService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardDataService.GetCurrentWeekLeaderboardRankingAdminV1Short(
            &leaderboard_data.GetCurrentWeekLeaderboardRankingAdminV1Params{
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
                Limit:           ..., // *int64
                Offset:          ..., // *int64
                PreviousVersion: ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardData'
    name: 'GetCurrentWeekLeaderboardRankingPublicV1'
    summary: 'Get current week leaderboard ranking data'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardDataService := leaderboard.LeaderboardDataService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardDataService.GetCurrentWeekLeaderboardRankingPublicV1Short(
            &leaderboard_data.GetCurrentWeekLeaderboardRankingPublicV1Params{
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
                Limit:           ..., // *int64
                Offset:          ..., // *int64
                PreviousVersion: ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserVisibility'
    name: 'GetHiddenUsersV2'
    summary: 'Get hidden users on a leaderboard'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        userVisibilityService := leaderboard.UserVisibilityService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userVisibilityService.GetHiddenUsersV2Short(
            &user_visibility.GetHiddenUsersV2Params{
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
                Limit:           ..., // *int64
                Offset:          ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserVisibilityV3'
    name: 'GetHiddenUsersV3'
    summary: 'Get hidden users on a leaderboard'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        userVisibilityV3Service := leaderboard.UserVisibilityV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userVisibilityV3Service.GetHiddenUsersV3Short(
            &user_visibility_v3.GetHiddenUsersV3Params{
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
                Limit:           ..., // *int64
                Offset:          ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardConfiguration'
    name: 'getLeaderboardConfigurationAdminV1'
    summary: 'get leaderboard by leaderboardCode'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardConfigurationService := leaderboard.LeaderboardConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardConfigurationService.GetLeaderboardConfigurationAdminV1Short(
            &leaderboard_configuration.GetLeaderboardConfigurationAdminV1Params{
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardConfigurationV3'
    name: 'getLeaderboardConfigurationAdminV3'
    summary: 'get leaderboard by leaderboardCode'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardConfigurationV3Service := leaderboard.LeaderboardConfigurationV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardConfigurationV3Service.GetLeaderboardConfigurationAdminV3Short(
            &leaderboard_configuration_v3.GetLeaderboardConfigurationAdminV3Params{
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardConfigurationV3'
    name: 'getLeaderboardConfigurationPublicV3'
    summary: 'get leaderboard by leaderboardCode'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardConfigurationV3Service := leaderboard.LeaderboardConfigurationV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardConfigurationV3Service.GetLeaderboardConfigurationPublicV3Short(
            &leaderboard_configuration_v3.GetLeaderboardConfigurationPublicV3Params{
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardConfiguration'
    name: 'getLeaderboardConfigurationsAdminV1'
    summary: 'list all leaderboards by given namespace'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardConfigurationService := leaderboard.LeaderboardConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardConfigurationService.GetLeaderboardConfigurationsAdminV1Short(
            &leaderboard_configuration.GetLeaderboardConfigurationsAdminV1Params{
                Namespace:  ..., // string
                IsArchived: ..., // *bool
                IsDeleted:  ..., // *bool
                Limit:      ..., // *int64
                Offset:     ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardConfigurationV3'
    name: 'getLeaderboardConfigurationsAdminV3'
    summary: 'list all leaderboards by given namespace'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardConfigurationV3Service := leaderboard.LeaderboardConfigurationV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardConfigurationV3Service.GetLeaderboardConfigurationsAdminV3Short(
            &leaderboard_configuration_v3.GetLeaderboardConfigurationsAdminV3Params{
                Namespace: ..., // string
                IsDeleted: ..., // *bool
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardConfiguration'
    name: 'getLeaderboardConfigurationsPublicV1'
    summary: 'list all leaderboards by given namespace'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardConfigurationService := leaderboard.LeaderboardConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardConfigurationService.GetLeaderboardConfigurationsPublicV1Short(
            &leaderboard_configuration.GetLeaderboardConfigurationsPublicV1Params{
                Namespace:  ..., // string
                IsArchived: ..., // *bool
                IsDeleted:  ..., // *bool
                Limit:      ..., // *int64
                Offset:     ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardConfiguration'
    name: 'GetLeaderboardConfigurationsPublicV2'
    summary: 'list all leaderboards by given namespace'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardConfigurationService := leaderboard.LeaderboardConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardConfigurationService.GetLeaderboardConfigurationsPublicV2Short(
            &leaderboard_configuration.GetLeaderboardConfigurationsPublicV2Params{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardConfigurationV3'
    name: 'getLeaderboardConfigurationsPublicV3'
    summary: 'list all leaderboards by given namespace'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardConfigurationV3Service := leaderboard.LeaderboardConfigurationV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardConfigurationV3Service.GetLeaderboardConfigurationsPublicV3Short(
            &leaderboard_configuration_v3.GetLeaderboardConfigurationsPublicV3Params{
                Namespace: ..., // string
                IsDeleted: ..., // *bool
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardData'
    name: 'GetTodayLeaderboardRankingAdminV1'
    summary: 'Get today leaderboard ranking data'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardDataService := leaderboard.LeaderboardDataService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardDataService.GetTodayLeaderboardRankingAdminV1Short(
            &leaderboard_data.GetTodayLeaderboardRankingAdminV1Params{
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
                Limit:           ..., // *int64
                Offset:          ..., // *int64
                PreviousVersion: ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardData'
    name: 'GetTodayLeaderboardRankingPublicV1'
    summary: 'Get today leaderboard ranking data'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardDataService := leaderboard.LeaderboardDataService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardDataService.GetTodayLeaderboardRankingPublicV1Short(
            &leaderboard_data.GetTodayLeaderboardRankingPublicV1Params{
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
                Limit:           ..., // *int64
                Offset:          ..., // *int64
                PreviousVersion: ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserData'
    name: 'getUserLeaderboardRankingsAdminV1'
    summary: 'Get user rankings'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        userDataService := leaderboard.UserDataService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userDataService.GetUserLeaderboardRankingsAdminV1Short(
            &user_data.GetUserLeaderboardRankingsAdminV1Params{
                Namespace:       ..., // string
                UserID:          ..., // string
                Limit:           ..., // *int64
                Offset:          ..., // *int64
                PreviousVersion: ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserDataV3'
    name: 'getUserLeaderboardRankingsAdminV3'
    summary: 'Get user rankings'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        userDataV3Service := leaderboard.UserDataV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userDataV3Service.GetUserLeaderboardRankingsAdminV3Short(
            &user_data_v3.GetUserLeaderboardRankingsAdminV3Params{
                Namespace:       ..., // string
                UserID:          ..., // string
                Limit:           ..., // *int64
                Offset:          ..., // *int64
                PreviousVersion: ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardData'
    name: 'getUserRankingAdminV1'
    summary: 'Get user ranking'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardDataService := leaderboard.LeaderboardDataService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardDataService.GetUserRankingAdminV1Short(
            &leaderboard_data.GetUserRankingAdminV1Params{
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
                UserID:          ..., // string
                PreviousVersion: ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardDataV3'
    name: 'getUserRankingAdminV3'
    summary: 'Get user ranking'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardDataV3Service := leaderboard.LeaderboardDataV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardDataV3Service.GetUserRankingAdminV3Short(
            &leaderboard_data_v3.GetUserRankingAdminV3Params{
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
                UserID:          ..., // string
                PreviousVersion: ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardData'
    name: 'getUserRankingPublicV1'
    summary: 'Get user ranking'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardDataService := leaderboard.LeaderboardDataService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardDataService.GetUserRankingPublicV1Short(
            &leaderboard_data.GetUserRankingPublicV1Params{
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
                UserID:          ..., // string
                PreviousVersion: ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardDataV3'
    name: 'getUserRankingPublicV3'
    summary: 'Get user ranking'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardDataV3Service := leaderboard.LeaderboardDataV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardDataV3Service.GetUserRankingPublicV3Short(
            &leaderboard_data_v3.GetUserRankingPublicV3Params{
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
                UserID:          ..., // string
                PreviousVersion: ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserVisibility'
    name: 'GetUserVisibilityStatusV2'
    summary: 'Get user visibility status'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        userVisibilityService := leaderboard.UserVisibilityService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userVisibilityService.GetUserVisibilityStatusV2Short(
            &user_visibility.GetUserVisibilityStatusV2Params{
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
                UserID:          ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserVisibilityV3'
    name: 'GetUserVisibilityStatusV3'
    summary: 'Get user visibility status'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        userVisibilityV3Service := leaderboard.UserVisibilityV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userVisibilityV3Service.GetUserVisibilityStatusV3Short(
            &user_visibility_v3.GetUserVisibilityStatusV3Params{
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
                UserID:          ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardConfiguration'
    name: 'hardDeleteLeaderboardAdminV1'
    summary: 'hard delete leaderboard config and data by leaderboard code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardConfigurationService := leaderboard.LeaderboardConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardConfigurationService.HardDeleteLeaderboardAdminV1Short(
            &leaderboard_configuration.HardDeleteLeaderboardAdminV1Params{
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardConfigurationV3'
    name: 'hardDeleteLeaderboardAdminV3'
    summary: 'hard delete leaderboard config and data by leaderboard code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardConfigurationV3Service := leaderboard.LeaderboardConfigurationV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardConfigurationV3Service.HardDeleteLeaderboardAdminV3Short(
            &leaderboard_configuration_v3.HardDeleteLeaderboardAdminV3Params{
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserVisibility'
    name: 'SetUserLeaderboardVisibilityStatusV2'
    summary: 'Set user visibility status on a specific leaderboard code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        userVisibilityService := leaderboard.UserVisibilityService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userVisibilityService.SetUserLeaderboardVisibilityStatusV2Short(
            &user_visibility.SetUserLeaderboardVisibilityStatusV2Params{
                Body:            ..., // *leaderboardclientmodels.ModelsSetUserVisibilityRequest
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
                UserID:          ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserVisibilityV3'
    name: 'SetUserLeaderboardVisibilityV3'
    summary: 'Set user visibility status on a specific leaderboard code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        userVisibilityV3Service := leaderboard.UserVisibilityV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userVisibilityV3Service.SetUserLeaderboardVisibilityV3Short(
            &user_visibility_v3.SetUserLeaderboardVisibilityV3Params{
                Body:            ..., // *leaderboardclientmodels.ModelsSetUserVisibilityRequest
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
                UserID:          ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserVisibility'
    name: 'SetUserVisibilityStatusV2'
    summary: 'Set user visibility status on a all current leaderboard'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        userVisibilityService := leaderboard.UserVisibilityService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userVisibilityService.SetUserVisibilityStatusV2Short(
            &user_visibility.SetUserVisibilityStatusV2Params{
                Body:      ..., // *leaderboardclientmodels.ModelsSetUserVisibilityRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserVisibilityV3'
    name: 'SetUserVisibilityV3'
    summary: 'Set user visibility status on a all current leaderboard'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        userVisibilityV3Service := leaderboard.UserVisibilityV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userVisibilityV3Service.SetUserVisibilityV3Short(
            &user_visibility_v3.SetUserVisibilityV3Params{
                Body:      ..., // *leaderboardclientmodels.ModelsSetUserVisibilityRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardConfiguration'
    name: 'updateLeaderboardConfigurationAdminV1'
    summary: 'update leaderboard by leaderboardCode'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardConfigurationService := leaderboard.LeaderboardConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardConfigurationService.UpdateLeaderboardConfigurationAdminV1Short(
            &leaderboard_configuration.UpdateLeaderboardConfigurationAdminV1Params{
                Body:            ..., // *leaderboardclientmodels.ModelsUpdateLeaderboardConfigReq
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardConfigurationV3'
    name: 'updateLeaderboardConfigurationAdminV3'
    summary: 'update leaderboard by leaderboardCode'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardConfigurationV3Service := leaderboard.LeaderboardConfigurationV3Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardConfigurationV3Service.UpdateLeaderboardConfigurationAdminV3Short(
            &leaderboard_configuration_v3.UpdateLeaderboardConfigurationAdminV3Params{
                Body:            ..., // *leaderboardclientmodels.ModelsUpdateLeaderboardConfigReqV3
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'LeaderboardData'
    name: 'updateUserPointAdminV1'
    summary: 'Update user point'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLeaderboardClient(configRepo)
        leaderboardDataService := leaderboard.LeaderboardDataService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := leaderboardDataService.UpdateUserPointAdminV1Short(
            &leaderboard_data.UpdateUserPointAdminV1Params{
                Body:            ..., // *leaderboardclientmodels.ModelsUpdateUserPointAdminV1Request
                LeaderboardCode: ..., // string
                Namespace:       ..., // string
                UserID:          ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }
# end leaderboard
# begin legal
  - ns: 'Agreement'
    name: 'acceptVersionedPolicy'
    summary: 'Accept a Policy Version'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        agreementService := legal.AgreementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := agreementService.AcceptVersionedPolicyShort(
            &agreement.AcceptVersionedPolicyParams{
                LocalizedPolicyVersionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin User Eligibilities'
    name: 'adminRetrieveEligibilities'
    summary: 'Check User Legal Eligibility'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        adminUserEligibilitiesService := legal.AdminUserEligibilitiesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminUserEligibilitiesService.AdminRetrieveEligibilitiesShort(
            &admin_user_eligibilities.AdminRetrieveEligibilitiesParams{
                Namespace:       ..., // string
                UserID:          ..., // string
                PublisherUserID: ..., // *string
                ClientID:        ..., // string
                CountryCode:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Anonymization'
    name: 'anonymizeUserAgreement'
    summary: 'Anonymize user''s agreement record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        anonymizationService := legal.AnonymizationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := anonymizationService.AnonymizeUserAgreementShort(
            &anonymization.AnonymizeUserAgreementParams{
                UserID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Agreement'
    name: 'bulkAcceptVersionedPolicy'
    summary: 'Bulk Accept Policy Versions'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        agreementService := legal.AgreementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := agreementService.BulkAcceptVersionedPolicyShort(
            &agreement.BulkAcceptVersionedPolicyParams{
                Body: ..., // []*legalclientmodels.AcceptAgreementRequest
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Agreement'
    name: 'changePreferenceConsent'
    summary: 'Change Preference Consent'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        agreementService := legal.AgreementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := agreementService.ChangePreferenceConsentShort(
            &agreement.ChangePreferenceConsentParams{
                Body:      ..., // []*legalclientmodels.AcceptAgreementRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Agreement'
    name: 'changePreferenceConsent_1'
    summary: 'Accept/Revoke Marketing Preference Consent'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        agreementService := legal.AgreementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := agreementService.ChangePreferenceConsent1Short(
            &agreement.ChangePreferenceConsent1Params{
                Body: ..., // []*legalclientmodels.AcceptAgreementRequest
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Utility'
    name: 'checkReadiness'
    summary: 'Check Legal Data Readiness'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        utilityService := legal.UtilityService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := utilityService.CheckReadinessShort(
            &utility.CheckReadinessParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Localized Policy Versions'
    name: 'createLocalizedPolicyVersion'
    summary: 'Create a Localized Version from Country-Specific Policy'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        localizedPolicyVersionsService := legal.LocalizedPolicyVersionsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := localizedPolicyVersionsService.CreateLocalizedPolicyVersionShort(
            &localized_policy_versions.CreateLocalizedPolicyVersionParams{
                Body:            ..., // *legalclientmodels.CreateLocalizedPolicyVersionRequest
                PolicyVersionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Localized Policy Versions With Namespace'
    name: 'createLocalizedPolicyVersion_1'
    summary: 'Create a Localized Version from Country-Specific Policy'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        localizedPolicyVersionsWithNamespaceService := legal.LocalizedPolicyVersionsWithNamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := localizedPolicyVersionsWithNamespaceService.CreateLocalizedPolicyVersion1Short(
            &localized_policy_versions_with_namespace.CreateLocalizedPolicyVersion1Params{
                Body:            ..., // *legalclientmodels.CreateLocalizedPolicyVersionRequest
                Namespace:       ..., // string
                PolicyVersionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Base Legal Policies'
    name: 'createPolicy'
    summary: 'Create a Base Legal Policy'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        baseLegalPoliciesService := legal.BaseLegalPoliciesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := baseLegalPoliciesService.CreatePolicyShort(
            &base_legal_policies.CreatePolicyParams{
                Body: ..., // *legalclientmodels.CreateBasePolicyRequest
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Base Legal Policies With Namespace'
    name: 'createPolicy_1'
    summary: 'Create a Base Legal Policy'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        baseLegalPoliciesWithNamespaceService := legal.BaseLegalPoliciesWithNamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := baseLegalPoliciesWithNamespaceService.CreatePolicy1Short(
            &base_legal_policies_with_namespace.CreatePolicy1Params{
                Body:      ..., // *legalclientmodels.CreateBasePolicyRequestV2
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Base Legal Policies With Namespace'
    name: 'createPolicyUnderBasePolicy'
    summary: 'Create policy under base policy'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        baseLegalPoliciesWithNamespaceService := legal.BaseLegalPoliciesWithNamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := baseLegalPoliciesWithNamespaceService.CreatePolicyUnderBasePolicyShort(
            &base_legal_policies_with_namespace.CreatePolicyUnderBasePolicyParams{
                Body:         ..., // *legalclientmodels.CreatePolicyRequest
                BasePolicyID: ..., // string
                Namespace:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Policy Versions'
    name: 'createPolicyVersion'
    summary: 'Create a Version from Country-Specific Policy'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        policyVersionsService := legal.PolicyVersionsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := policyVersionsService.CreatePolicyVersionShort(
            &policy_versions.CreatePolicyVersionParams{
                Body:     ..., // *legalclientmodels.CreatePolicyVersionRequest
                PolicyID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Policy Versions With Namespace'
    name: 'createPolicyVersion_1'
    summary: 'Create a Version from Country-Specific Policy'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        policyVersionsWithNamespaceService := legal.PolicyVersionsWithNamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := policyVersionsWithNamespaceService.CreatePolicyVersion1Short(
            &policy_versions_with_namespace.CreatePolicyVersion1Params{
                Body:      ..., // *legalclientmodels.CreatePolicyVersionRequest
                Namespace: ..., // string
                PolicyID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Base Legal Policies With Namespace'
    name: 'deleteBasePolicy'
    summary: 'Delete Base Legal Policy'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        baseLegalPoliciesWithNamespaceService := legal.BaseLegalPoliciesWithNamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := baseLegalPoliciesWithNamespaceService.DeleteBasePolicyShort(
            &base_legal_policies_with_namespace.DeleteBasePolicyParams{
                BasePolicyID: ..., // string
                Namespace:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Localized Policy Versions With Namespace'
    name: 'deleteLocalizedPolicy'
    summary: 'Delete Localized Policy'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        localizedPolicyVersionsWithNamespaceService := legal.LocalizedPolicyVersionsWithNamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := localizedPolicyVersionsWithNamespaceService.DeleteLocalizedPolicyShort(
            &localized_policy_versions_with_namespace.DeleteLocalizedPolicyParams{
                LocalizedPolicyVersionID: ..., // string
                Namespace:                ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Policies With Namespace'
    name: 'deletePolicy'
    summary: 'Delete Policy'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        policiesWithNamespaceService := legal.PoliciesWithNamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := policiesWithNamespaceService.DeletePolicyShort(
            &policies_with_namespace.DeletePolicyParams{
                Namespace: ..., // string
                PolicyID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Policy Versions With Namespace'
    name: 'deletePolicyVersion'
    summary: 'Delete a Version of Policy'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        policyVersionsWithNamespaceService := legal.PolicyVersionsWithNamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := policyVersionsWithNamespaceService.DeletePolicyVersionShort(
            &policy_versions_with_namespace.DeletePolicyVersionParams{
                Namespace:       ..., // string
                PolicyVersionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Agreement With Namespace'
    name: 'downloadExportedAgreementsInCSV'
    summary: 'Download Exported Users Accepted Agreements in CSV'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        agreementWithNamespaceService := legal.AgreementWithNamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := agreementWithNamespaceService.DownloadExportedAgreementsInCSVShort(
            &agreement_with_namespace.DownloadExportedAgreementsInCSVParams{
                Namespace: ..., // string
                ExportID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserInfo'
    name: 'getUserInfoStatus'
    summary: 'Get user info cache status'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        userInfoService := legal.UserInfoService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userInfoService.GetUserInfoStatusShort(
            &user_info.GetUserInfoStatusParams{
                Namespaces: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin User Agreement'
    name: 'indirectBulkAcceptVersionedPolicy'
    summary: 'Admin bulk accept Policy Versions'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        adminUserAgreementService := legal.AdminUserAgreementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminUserAgreementService.IndirectBulkAcceptVersionedPolicyShort(
            &admin_user_agreement.IndirectBulkAcceptVersionedPolicyParams{
                Body:            ..., // []*legalclientmodels.AcceptAgreementRequest
                Namespace:       ..., // string
                UserID:          ..., // string
                PublisherUserID: ..., // *string
                ClientID:        ..., // string
                CountryCode:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Agreement'
    name: 'indirectBulkAcceptVersionedPolicy_1'
    summary: 'Bulk Accept Policy Versions (Indirect)'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        agreementService := legal.AgreementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := agreementService.IndirectBulkAcceptVersionedPolicy1Short(
            &agreement.IndirectBulkAcceptVersionedPolicy1Params{
                Body:   ..., // []*legalclientmodels.AcceptAgreementRequest
                UserID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Agreement'
    name: 'indirectBulkAcceptVersionedPolicyV2'
    summary: 'Bulk Accept Policy Versions (Indirect)'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        agreementService := legal.AgreementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := agreementService.IndirectBulkAcceptVersionedPolicyV2Short(
            &agreement.IndirectBulkAcceptVersionedPolicyV2Params{
                Body:        ..., // []*legalclientmodels.AcceptAgreementRequest
                ClientID:    ..., // string
                CountryCode: ..., // string
                Namespace:   ..., // string
                UserID:      ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Agreement With Namespace'
    name: 'initiateExportAgreementsToCSV'
    summary: 'Initiate Export Users Accepted Agreements to CSV'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        agreementWithNamespaceService := legal.AgreementWithNamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := agreementWithNamespaceService.InitiateExportAgreementsToCSVShort(
            &agreement_with_namespace.InitiateExportAgreementsToCSVParams{
                Namespace:       ..., // string
                End:             ..., // *string
                PolicyVersionID: ..., // string
                Start:           ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserInfo'
    name: 'invalidateUserInfoCache'
    summary: 'Invalidate user info cache'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        userInfoService := legal.UserInfoService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userInfoService.InvalidateUserInfoCacheShort(
            &user_info.InvalidateUserInfoCacheParams{
                Namespace: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Base Legal Policies'
    name: 'partialUpdatePolicy'
    summary: 'Update Base Legal Policy'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        baseLegalPoliciesService := legal.BaseLegalPoliciesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := baseLegalPoliciesService.PartialUpdatePolicyShort(
            &base_legal_policies.PartialUpdatePolicyParams{
                Body:         ..., // *legalclientmodels.UpdateBasePolicyRequest
                BasePolicyID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Base Legal Policies With Namespace'
    name: 'partialUpdatePolicy_1'
    summary: 'Update Base Legal Policy'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        baseLegalPoliciesWithNamespaceService := legal.BaseLegalPoliciesWithNamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := baseLegalPoliciesWithNamespaceService.PartialUpdatePolicy1Short(
            &base_legal_policies_with_namespace.PartialUpdatePolicy1Params{
                Body:         ..., // *legalclientmodels.UpdateBasePolicyRequestV2
                BasePolicyID: ..., // string
                Namespace:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Policy Versions'
    name: 'publishPolicyVersion'
    summary: 'Manually Publish a Version from Country-Specific Policy'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        policyVersionsService := legal.PolicyVersionsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := policyVersionsService.PublishPolicyVersionShort(
            &policy_versions.PublishPolicyVersionParams{
                PolicyVersionID: ..., // string
                ShouldNotify:    ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Policy Versions With Namespace'
    name: 'publishPolicyVersion_1'
    summary: 'Manually Publish a Version from Country-Specific Policy'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        policyVersionsWithNamespaceService := legal.PolicyVersionsWithNamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := policyVersionsWithNamespaceService.PublishPolicyVersion1Short(
            &policy_versions_with_namespace.PublishPolicyVersion1Params{
                Namespace:       ..., // string
                PolicyVersionID: ..., // string
                ShouldNotify:    ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Localized Policy Versions'
    name: 'requestPresignedURL'
    summary: 'Request Presigned URL for Upload Document'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        localizedPolicyVersionsService := legal.LocalizedPolicyVersionsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := localizedPolicyVersionsService.RequestPresignedURLShort(
            &localized_policy_versions.RequestPresignedURLParams{
                Body:                     ..., // *legalclientmodels.UploadPolicyVersionAttachmentRequest
                LocalizedPolicyVersionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Localized Policy Versions With Namespace'
    name: 'requestPresignedURL_1'
    summary: 'Request Presigned URL for Upload Document'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        localizedPolicyVersionsWithNamespaceService := legal.LocalizedPolicyVersionsWithNamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := localizedPolicyVersionsWithNamespaceService.RequestPresignedURL1Short(
            &localized_policy_versions_with_namespace.RequestPresignedURL1Params{
                Body:                     ..., // *legalclientmodels.UploadPolicyVersionAttachmentRequest
                LocalizedPolicyVersionID: ..., // string
                Namespace:                ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Agreement'
    name: 'retrieveAcceptedAgreements'
    summary: 'Retrieve Accepted Legal Agreements'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        agreementService := legal.AgreementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := agreementService.RetrieveAcceptedAgreementsShort(
            &agreement.RetrieveAcceptedAgreementsParams{
                UserID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Agreement With Namespace'
    name: 'retrieveAcceptedAgreements_1'
    summary: 'Retrieve Accepted Legal Agreements'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        agreementWithNamespaceService := legal.AgreementWithNamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := agreementWithNamespaceService.RetrieveAcceptedAgreements1Short(
            &agreement_with_namespace.RetrieveAcceptedAgreements1Params{
                Namespace:                      ..., // string
                UserID:                         ..., // string
                ExcludeOtherNamespacesPolicies: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Agreement With Namespace'
    name: 'retrieveAcceptedAgreementsForMultiUsers'
    summary: 'Retrieve Accepted Legal Agreements For Multi Users'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        agreementWithNamespaceService := legal.AgreementWithNamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := agreementWithNamespaceService.RetrieveAcceptedAgreementsForMultiUsersShort(
            &agreement_with_namespace.RetrieveAcceptedAgreementsForMultiUsersParams{
                Body:      ..., // *legalclientmodels.UsersAgreementsRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Agreement'
    name: 'retrieveAgreementsPublic'
    summary: 'Retrieve the accepted Legal Agreements'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        agreementService := legal.AgreementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := agreementService.RetrieveAgreementsPublicShort(
            &agreement.RetrieveAgreementsPublicParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Base Legal Policies'
    name: 'retrieveAllLegalPolicies'
    summary: 'Retrieve All Base Legal Policy'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        baseLegalPoliciesService := legal.BaseLegalPoliciesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := baseLegalPoliciesService.RetrieveAllLegalPoliciesShort(
            &base_legal_policies.RetrieveAllLegalPoliciesParams{
                VisibleOnly: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Base Legal Policies With Namespace'
    name: 'retrieveAllLegalPoliciesByNamespace'
    summary: 'Retrieve All Base Legal Policy in the namespace'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        baseLegalPoliciesWithNamespaceService := legal.BaseLegalPoliciesWithNamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := baseLegalPoliciesWithNamespaceService.RetrieveAllLegalPoliciesByNamespaceShort(
            &base_legal_policies_with_namespace.RetrieveAllLegalPoliciesByNamespaceParams{
                Namespace:   ..., // string
                VisibleOnly: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Base Legal Policies With Namespace'
    name: 'retrieveAllPoliciesFromBasePolicy'
    summary: 'Retrieve all policies from Base Legal Policy'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        baseLegalPoliciesWithNamespaceService := legal.BaseLegalPoliciesWithNamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := baseLegalPoliciesWithNamespaceService.RetrieveAllPoliciesFromBasePolicyShort(
            &base_legal_policies_with_namespace.RetrieveAllPoliciesFromBasePolicyParams{
                BasePolicyID: ..., // string
                Namespace:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Base Legal Policies'
    name: 'retrieveAllPolicyTypes'
    summary: 'Retrieve All Policy Type'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        baseLegalPoliciesService := legal.BaseLegalPoliciesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := baseLegalPoliciesService.RetrieveAllPolicyTypesShort(
            &base_legal_policies.RetrieveAllPolicyTypesParams{
                Offset: ..., // *int32
                Limit:  ..., // int32
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Base Legal Policies With Namespace'
    name: 'retrieveAllPolicyTypes_1'
    summary: 'Retrieve All Policy Type'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        baseLegalPoliciesWithNamespaceService := legal.BaseLegalPoliciesWithNamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := baseLegalPoliciesWithNamespaceService.RetrieveAllPolicyTypes1Short(
            &base_legal_policies_with_namespace.RetrieveAllPolicyTypes1Params{
                Namespace: ..., // string
                Offset:    ..., // *int32
                Limit:     ..., // int32
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Agreement'
    name: 'retrieveAllUsersByPolicyVersion'
    summary: 'Retrieve Users Accepting Legal Agreements'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        agreementService := legal.AgreementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := agreementService.RetrieveAllUsersByPolicyVersionShort(
            &agreement.RetrieveAllUsersByPolicyVersionParams{
                Keyword:         ..., // *string
                Limit:           ..., // *int32
                Offset:          ..., // *int32
                PolicyVersionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Agreement With Namespace'
    name: 'retrieveAllUsersByPolicyVersion_1'
    summary: 'Retrieve Users Accepting Legal Agreements'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        agreementWithNamespaceService := legal.AgreementWithNamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := agreementWithNamespaceService.RetrieveAllUsersByPolicyVersion1Short(
            &agreement_with_namespace.RetrieveAllUsersByPolicyVersion1Params{
                Namespace:         ..., // string
                ConvertGameUserID: ..., // *bool
                Keyword:           ..., // *string
                Limit:             ..., // *int32
                Offset:            ..., // *int32
                PolicyVersionID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Policies'
    name: 'retrieveCountryListWithPolicies'
    summary: 'Retrieve List of Countries that have Active Legal Policies'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        policiesService := legal.PoliciesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := policiesService.RetrieveCountryListWithPoliciesShort(
            &policies.RetrieveCountryListWithPoliciesParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Eligibilities'
    name: 'retrieveEligibilitiesPublic'
    summary: 'Check User Legal Eligibility'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        eligibilitiesService := legal.EligibilitiesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := eligibilitiesService.RetrieveEligibilitiesPublicShort(
            &eligibilities.RetrieveEligibilitiesPublicParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Eligibilities'
    name: 'retrieveEligibilitiesPublicIndirect'
    summary: 'Check User Legal Eligibility'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        eligibilitiesService := legal.EligibilitiesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := eligibilitiesService.RetrieveEligibilitiesPublicIndirectShort(
            &eligibilities.RetrieveEligibilitiesPublicIndirectParams{
                ClientID:    ..., // string
                CountryCode: ..., // string
                Namespace:   ..., // string
                UserID:      ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Policies'
    name: 'retrieveLatestPolicies'
    summary: 'Retrieve Latest Policies by Country'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        policiesService := legal.PoliciesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := policiesService.RetrieveLatestPoliciesShort(
            &policies.RetrieveLatestPoliciesParams{
                CountryCode:    ..., // string
                DefaultOnEmpty: ..., // *bool
                PolicyType:     ..., // *string
                Tags:           ..., // *string
                VisibleOnly:    ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Policies'
    name: 'retrieveLatestPoliciesByNamespaceAndCountryPublic'
    summary: 'Retrieve Latest Policies by Namespace and Country'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        policiesService := legal.PoliciesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := policiesService.RetrieveLatestPoliciesByNamespaceAndCountryPublicShort(
            &policies.RetrieveLatestPoliciesByNamespaceAndCountryPublicParams{
                CountryCode:          ..., // string
                Namespace:            ..., // string
                AlwaysIncludeDefault: ..., // *bool
                DefaultOnEmpty:       ..., // *bool
                PolicyType:           ..., // *string
                Tags:                 ..., // *string
                VisibleOnly:          ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Policies With Namespace V2'
    name: 'retrieveLatestPoliciesByNamespaceAndCountryPublic_1'
    summary: 'Retrieve Latest Policies by Namespace and Country'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        policiesWithNamespaceV2Service := legal.PoliciesWithNamespaceV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := policiesWithNamespaceV2Service.RetrieveLatestPoliciesByNamespaceAndCountryPublic1Short(
            &policies_with_namespace_v2.RetrieveLatestPoliciesByNamespaceAndCountryPublic1Params{
                CountryCode: ..., // string
                Namespace:   ..., // string
                ClientID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Policies'
    name: 'retrieveLatestPoliciesPublic'
    summary: 'Retrieve Latest Policies by Namespace and Country'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        policiesService := legal.PoliciesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := policiesService.RetrieveLatestPoliciesPublicShort(
            &policies.RetrieveLatestPoliciesPublicParams{
                Namespace:            ..., // string
                AlwaysIncludeDefault: ..., // *bool
                DefaultOnEmpty:       ..., // *bool
                PolicyType:           ..., // *string
                Tags:                 ..., // *string
                VisibleOnly:          ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Localized Policy Versions'
    name: 'retrieveLocalizedPolicyVersions'
    summary: 'Retrieve Versions from Country-Specific Policy'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        localizedPolicyVersionsService := legal.LocalizedPolicyVersionsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := localizedPolicyVersionsService.RetrieveLocalizedPolicyVersionsShort(
            &localized_policy_versions.RetrieveLocalizedPolicyVersionsParams{
                PolicyVersionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Localized Policy Versions With Namespace'
    name: 'retrieveLocalizedPolicyVersions_1'
    summary: 'Retrieve Versions from Country-Specific Policy'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        localizedPolicyVersionsWithNamespaceService := legal.LocalizedPolicyVersionsWithNamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := localizedPolicyVersionsWithNamespaceService.RetrieveLocalizedPolicyVersions1Short(
            &localized_policy_versions_with_namespace.RetrieveLocalizedPolicyVersions1Params{
                Namespace:       ..., // string
                PolicyVersionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Policies'
    name: 'retrievePolicies'
    summary: 'Retrieve Policies by Country'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        policiesService := legal.PoliciesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := policiesService.RetrievePoliciesShort(
            &policies.RetrievePoliciesParams{
                CountryCode: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Base Legal Policies'
    name: 'retrievePolicyCountry'
    summary: 'Retrieve a Base Legal Policy based on a Particular Country'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        baseLegalPoliciesService := legal.BaseLegalPoliciesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := baseLegalPoliciesService.RetrievePolicyCountryShort(
            &base_legal_policies.RetrievePolicyCountryParams{
                BasePolicyID: ..., // string
                CountryCode:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Base Legal Policies With Namespace'
    name: 'retrievePolicyCountry_1'
    summary: 'Retrieve a Base Legal Policy based on a Particular Country'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        baseLegalPoliciesWithNamespaceService := legal.BaseLegalPoliciesWithNamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := baseLegalPoliciesWithNamespaceService.RetrievePolicyCountry1Short(
            &base_legal_policies_with_namespace.RetrievePolicyCountry1Params{
                BasePolicyID: ..., // string
                CountryCode:  ..., // string
                Namespace:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Localized Policy Versions'
    name: 'retrieveSingleLocalizedPolicyVersion'
    summary: 'Retrieve a Localized Version from Country-Specific Policy'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        localizedPolicyVersionsService := legal.LocalizedPolicyVersionsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := localizedPolicyVersionsService.RetrieveSingleLocalizedPolicyVersionShort(
            &localized_policy_versions.RetrieveSingleLocalizedPolicyVersionParams{
                LocalizedPolicyVersionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Localized Policy Versions With Namespace'
    name: 'retrieveSingleLocalizedPolicyVersion_1'
    summary: 'Retrieve a Localized Version from Country-Specific Policy'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        localizedPolicyVersionsWithNamespaceService := legal.LocalizedPolicyVersionsWithNamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := localizedPolicyVersionsWithNamespaceService.RetrieveSingleLocalizedPolicyVersion1Short(
            &localized_policy_versions_with_namespace.RetrieveSingleLocalizedPolicyVersion1Params{
                LocalizedPolicyVersionID: ..., // string
                Namespace:                ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Localized Policy Versions'
    name: 'retrieveSingleLocalizedPolicyVersion_2'
    summary: 'Retrieve a Localized Version'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        localizedPolicyVersionsService := legal.LocalizedPolicyVersionsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := localizedPolicyVersionsService.RetrieveSingleLocalizedPolicyVersion2Short(
            &localized_policy_versions.RetrieveSingleLocalizedPolicyVersion2Params{
                LocalizedPolicyVersionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Localized Policy Versions With Namespace'
    name: 'retrieveSingleLocalizedPolicyVersion_3'
    summary: 'Retrieve a Localized Version'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        localizedPolicyVersionsWithNamespaceService := legal.LocalizedPolicyVersionsWithNamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := localizedPolicyVersionsWithNamespaceService.RetrieveSingleLocalizedPolicyVersion3Short(
            &localized_policy_versions_with_namespace.RetrieveSingleLocalizedPolicyVersion3Params{
                LocalizedPolicyVersionID: ..., // string
                Namespace:                ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Base Legal Policies'
    name: 'retrieveSinglePolicy'
    summary: 'Retrieve a Base Legal Policy'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        baseLegalPoliciesService := legal.BaseLegalPoliciesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := baseLegalPoliciesService.RetrieveSinglePolicyShort(
            &base_legal_policies.RetrieveSinglePolicyParams{
                BasePolicyID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Base Legal Policies With Namespace'
    name: 'retrieveSinglePolicy_1'
    summary: 'Retrieve a Base Legal Policy'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        baseLegalPoliciesWithNamespaceService := legal.BaseLegalPoliciesWithNamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := baseLegalPoliciesWithNamespaceService.RetrieveSinglePolicy1Short(
            &base_legal_policies_with_namespace.RetrieveSinglePolicy1Params{
                BasePolicyID: ..., // string
                Namespace:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Policy Versions'
    name: 'retrieveSinglePolicyVersion'
    summary: 'Retrieve a Version from Country-Specific Policy'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        policyVersionsService := legal.PolicyVersionsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := policyVersionsService.RetrieveSinglePolicyVersionShort(
            &policy_versions.RetrieveSinglePolicyVersionParams{
                PolicyID:  ..., // string
                VersionID: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Policy Versions With Namespace'
    name: 'retrieveSinglePolicyVersion_1'
    summary: 'Retrieve a Version from Country-Specific Policy'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        policyVersionsWithNamespaceService := legal.PolicyVersionsWithNamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := policyVersionsWithNamespaceService.RetrieveSinglePolicyVersion1Short(
            &policy_versions_with_namespace.RetrieveSinglePolicyVersion1Params{
                Namespace: ..., // string
                PolicyID:  ..., // string
                VersionID: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Localized Policy Versions'
    name: 'setDefaultPolicy'
    summary: 'Set Default Localized Policy'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        localizedPolicyVersionsService := legal.LocalizedPolicyVersionsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := localizedPolicyVersionsService.SetDefaultPolicyShort(
            &localized_policy_versions.SetDefaultPolicyParams{
                LocalizedPolicyVersionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Localized Policy Versions With Namespace'
    name: 'setDefaultPolicy_1'
    summary: 'Set Default Localized Policy'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        localizedPolicyVersionsWithNamespaceService := legal.LocalizedPolicyVersionsWithNamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := localizedPolicyVersionsWithNamespaceService.SetDefaultPolicy1Short(
            &localized_policy_versions_with_namespace.SetDefaultPolicy1Params{
                LocalizedPolicyVersionID: ..., // string
                Namespace:                ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Policies'
    name: 'setDefaultPolicy_2'
    summary: 'Set Default Policy'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        policiesService := legal.PoliciesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := policiesService.SetDefaultPolicy2Short(
            &policies.SetDefaultPolicy2Params{
                PolicyID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Policies With Namespace'
    name: 'setDefaultPolicy_3'
    summary: 'Set Default Policy'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        policiesWithNamespaceService := legal.PoliciesWithNamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := policiesWithNamespaceService.SetDefaultPolicy3Short(
            &policies_with_namespace.SetDefaultPolicy3Params{
                Namespace: ..., // string
                PolicyID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserInfo'
    name: 'syncUserInfo'
    summary: 'Sync user info with iam service '
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        userInfoService := legal.UserInfoService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userInfoService.SyncUserInfoShort(
            &user_info.SyncUserInfoParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Policy Versions With Namespace'
    name: 'unpublishPolicyVersion'
    summary: 'Un-publish Version from Policy'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        policyVersionsWithNamespaceService := legal.PolicyVersionsWithNamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := policyVersionsWithNamespaceService.UnpublishPolicyVersionShort(
            &policy_versions_with_namespace.UnpublishPolicyVersionParams{
                Namespace:       ..., // string
                PolicyVersionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Localized Policy Versions'
    name: 'updateLocalizedPolicyVersion'
    summary: 'Update a Localized Version from Country-Specific Policy'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        localizedPolicyVersionsService := legal.LocalizedPolicyVersionsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := localizedPolicyVersionsService.UpdateLocalizedPolicyVersionShort(
            &localized_policy_versions.UpdateLocalizedPolicyVersionParams{
                Body:                     ..., // *legalclientmodels.UpdateLocalizedPolicyVersionRequest
                LocalizedPolicyVersionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Localized Policy Versions With Namespace'
    name: 'updateLocalizedPolicyVersion_1'
    summary: 'Update a Localized Version from Country-Specific Policy'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        localizedPolicyVersionsWithNamespaceService := legal.LocalizedPolicyVersionsWithNamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := localizedPolicyVersionsWithNamespaceService.UpdateLocalizedPolicyVersion1Short(
            &localized_policy_versions_with_namespace.UpdateLocalizedPolicyVersion1Params{
                Body:                     ..., // *legalclientmodels.UpdateLocalizedPolicyVersionRequest
                LocalizedPolicyVersionID: ..., // string
                Namespace:                ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Policies'
    name: 'updatePolicy'
    summary: 'Update Country-Specific Policy'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        policiesService := legal.PoliciesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := policiesService.UpdatePolicyShort(
            &policies.UpdatePolicyParams{
                Body:     ..., // *legalclientmodels.UpdatePolicyRequest
                PolicyID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Policies With Namespace'
    name: 'updatePolicy_1'
    summary: 'Update Country-Specific Policy'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        policiesWithNamespaceService := legal.PoliciesWithNamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := policiesWithNamespaceService.UpdatePolicy1Short(
            &policies_with_namespace.UpdatePolicy1Params{
                Body:      ..., // *legalclientmodels.UpdatePolicyRequest
                Namespace: ..., // string
                PolicyID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Policy Versions'
    name: 'updatePolicyVersion'
    summary: 'Update a Version of Policy'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        policyVersionsService := legal.PolicyVersionsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := policyVersionsService.UpdatePolicyVersionShort(
            &policy_versions.UpdatePolicyVersionParams{
                Body:            ..., // *legalclientmodels.UpdatePolicyVersionRequest
                PolicyVersionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Policy Versions With Namespace'
    name: 'updatePolicyVersion_1'
    summary: 'Update a Version of Policy'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLegalClient(configRepo)
        policyVersionsWithNamespaceService := legal.PolicyVersionsWithNamespaceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := policyVersionsWithNamespaceService.UpdatePolicyVersion1Short(
            &policy_versions_with_namespace.UpdatePolicyVersion1Params{
                Body:            ..., // *legalclientmodels.UpdatePolicyVersionRequest
                Namespace:       ..., // string
                PolicyVersionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }
# end legal
# begin lobby
  - ns: 'friends'
    name: 'addFriendsWithoutConfirmation'
    summary: 'Add friends without confirmation'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        friendsService := lobby.FriendsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := friendsService.AddFriendsWithoutConfirmationShort(
            &friends.AddFriendsWithoutConfirmationParams{
                Body:      ..., // *lobbyclientmodels.ModelBulkFriendsRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'profanity'
    name: 'adminAddProfanityFilterIntoList'
    summary: 'add a single filter into the list'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        profanityService := lobby.ProfanityService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := profanityService.AdminAddProfanityFilterIntoListShort(
            &profanity.AdminAddProfanityFilterIntoListParams{
                Body:      ..., // *lobbyclientmodels.ModelsAdminAddProfanityFilterIntoListRequest
                List:      ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'profanity'
    name: 'adminAddProfanityFilters'
    summary: 'add multiple filters into the list'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        profanityService := lobby.ProfanityService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := profanityService.AdminAddProfanityFiltersShort(
            &profanity.AdminAddProfanityFiltersParams{
                Body:      ..., // *lobbyclientmodels.ModelsAdminAddProfanityFiltersRequest
                List:      ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'player'
    name: 'adminBulkBlockPlayersV1'
    summary: 'Admin bulk blocks player by list user id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        playerService := lobby.PlayerService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := playerService.AdminBulkBlockPlayersV1Short(
            &player.AdminBulkBlockPlayersV1Params{
                Body:      ..., // *lobbyclientmodels.ModelsListBlockedPlayerRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'player'
    name: 'adminBulkUnblockPlayersV1'
    summary: 'Admin bulk unblock players by list user id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        playerService := lobby.PlayerService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := playerService.AdminBulkUnblockPlayersV1Short(
            &player.AdminBulkUnblockPlayersV1Params{
                Body:      ..., // *lobbyclientmodels.ModelsListUnblockPlayerRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'profanity'
    name: 'adminCreateProfanityList'
    summary: 'create a new list'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        profanityService := lobby.ProfanityService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := profanityService.AdminCreateProfanityListShort(
            &profanity.AdminCreateProfanityListParams{
                Body:      ..., // *lobbyclientmodels.ModelsAdminCreateProfanityListRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'thirdParty'
    name: 'adminCreateThirdPartyConfig'
    summary: 'Create Third Party Steam Config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        thirdPartyService := lobby.ThirdPartyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := thirdPartyService.AdminCreateThirdPartyConfigShort(
            &third_party.AdminCreateThirdPartyConfigParams{
                Body:      ..., // *lobbyclientmodels.ModelsCreateConfigRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'profanity'
    name: 'adminDebugProfanityFilters'
    summary: 'get the list of filters that would modify the phrase'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        profanityService := lobby.ProfanityService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := profanityService.AdminDebugProfanityFiltersShort(
            &profanity.AdminDebugProfanityFiltersParams{
                Body:      ..., // *lobbyclientmodels.ModelsDebugProfanityFilterRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'admin'
    name: 'adminDeleteGlobalConfig'
    summary: 'Delete of global configuration data.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        adminService := lobby.AdminService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminService.AdminDeleteGlobalConfigShort(
            &admin.AdminDeleteGlobalConfigParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'profanity'
    name: 'adminDeleteProfanityFilter'
    summary: 'delete the filter from the list'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        profanityService := lobby.ProfanityService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := profanityService.AdminDeleteProfanityFilterShort(
            &profanity.AdminDeleteProfanityFilterParams{
                Body:      ..., // *lobbyclientmodels.ModelsAdminDeleteProfanityFilterRequest
                List:      ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'profanity'
    name: 'adminDeleteProfanityList'
    summary: 'delete a list include all filters inside of it'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        profanityService := lobby.ProfanityService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := profanityService.AdminDeleteProfanityListShort(
            &profanity.AdminDeleteProfanityListParams{
                List:      ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'thirdParty'
    name: 'adminDeleteThirdPartyConfig'
    summary: 'Delete Third Party Steam Config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        thirdPartyService := lobby.ThirdPartyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := thirdPartyService.AdminDeleteThirdPartyConfigShort(
            &third_party.AdminDeleteThirdPartyConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'config'
    name: 'adminExportConfigV1'
    summary: 'Export lobby config to a json file.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        configService := lobby.ConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configService.AdminExportConfigV1Short(
            &config.AdminExportConfigV1Params{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'config'
    name: 'adminGetAllConfigV1'
    summary: 'admin get all namespaces config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        configService := lobby.ConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configService.AdminGetAllConfigV1Short(
            &config.AdminGetAllConfigV1Params{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'player'
    name: 'adminGetAllPlayerSessionAttribute'
    summary: 'admin get all player''s session attribute'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        playerService := lobby.PlayerService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := playerService.AdminGetAllPlayerSessionAttributeShort(
            &player.AdminGetAllPlayerSessionAttributeParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'player'
    name: 'adminGetBulkPlayerBlockedPlayersV1'
    summary: 'admin get blocked players by bulk user ids'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        playerService := lobby.PlayerService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := playerService.AdminGetBulkPlayerBlockedPlayersV1Short(
            &player.AdminGetBulkPlayerBlockedPlayersV1Params{
                Body:      ..., // *lobbyclientmodels.ModelsGetBulkAllPlayerBlockedUsersRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'config'
    name: 'adminGetConfigV1'
    summary: 'admin get namespace config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        configService := lobby.ConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configService.AdminGetConfigV1Short(
            &config.AdminGetConfigV1Params{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'admin'
    name: 'adminGetGlobalConfig'
    summary: 'Record of global configuration dsmc.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        adminService := lobby.AdminService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminService.AdminGetGlobalConfigShort(
            &admin.AdminGetGlobalConfigParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'player'
    name: 'adminGetLobbyCCU'
    summary: 'admin get number of players currently connected to the lobby.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        playerService := lobby.PlayerService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := playerService.AdminGetLobbyCCUShort(
            &player.AdminGetLobbyCCUParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'config'
    name: 'adminGetLogConfig'
    summary: 'Get Log Configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        configService := lobby.ConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configService.AdminGetLogConfigShort(
            &config.AdminGetLogConfigParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'party'
    name: 'adminGetPartyDataV1'
    summary: 'admin get party data'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        partyService := lobby.PartyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := partyService.AdminGetPartyDataV1Short(
            &party.AdminGetPartyDataV1Params{
                Namespace: ..., // string
                PartyID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'player'
    name: 'adminGetPlayerBlockedByPlayersV1'
    summary: 'get players who blocked this player by user id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        playerService := lobby.PlayerService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := playerService.AdminGetPlayerBlockedByPlayersV1Short(
            &player.AdminGetPlayerBlockedByPlayersV1Params{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'player'
    name: 'adminGetPlayerBlockedPlayersV1'
    summary: 'admin get blocked players by user id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        playerService := lobby.PlayerService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := playerService.AdminGetPlayerBlockedPlayersV1Short(
            &player.AdminGetPlayerBlockedPlayersV1Params{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'player'
    name: 'adminGetPlayerSessionAttribute'
    summary: 'admin get player''s session attribute'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        playerService := lobby.PlayerService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := playerService.AdminGetPlayerSessionAttributeShort(
            &player.AdminGetPlayerSessionAttributeParams{
                Attribute: ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'profanity'
    name: 'adminGetProfanityListFiltersV1'
    summary: 'get the list of filters inside the list'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        profanityService := lobby.ProfanityService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := profanityService.AdminGetProfanityListFiltersV1Short(
            &profanity.AdminGetProfanityListFiltersV1Params{
                List:      ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'profanity'
    name: 'adminGetProfanityLists'
    summary: 'get lists'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        profanityService := lobby.ProfanityService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := profanityService.AdminGetProfanityListsShort(
            &profanity.AdminGetProfanityListsParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'profanity'
    name: 'adminGetProfanityRule'
    summary: 'get current profanity rule'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        profanityService := lobby.ProfanityService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := profanityService.AdminGetProfanityRuleShort(
            &profanity.AdminGetProfanityRuleParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'thirdParty'
    name: 'adminGetThirdPartyConfig'
    summary: 'Get Third Party Steam Config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        thirdPartyService := lobby.ThirdPartyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := thirdPartyService.AdminGetThirdPartyConfigShort(
            &third_party.AdminGetThirdPartyConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'party'
    name: 'adminGetUserPartyV1'
    summary: 'admin get user party data'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        partyService := lobby.PartyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := partyService.AdminGetUserPartyV1Short(
            &party.AdminGetUserPartyV1Params{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'config'
    name: 'adminImportConfigV1'
    summary: 'Import lobby config from a json file.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        configService := lobby.ConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configService.AdminImportConfigV1Short(
            &config.AdminImportConfigV1Params{
                File:      ..., // *[]byte
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'profanity'
    name: 'adminImportProfanityFiltersFromFile'
    summary: 'import a file with filters'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        profanityService := lobby.ProfanityService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := profanityService.AdminImportProfanityFiltersFromFileShort(
            &profanity.AdminImportProfanityFiltersFromFileParams{
                Body:      ..., // []int64
                List:      ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Lobby Operations'
    name: 'adminJoinPartyV1'
    summary: 'admin join a player into a party'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        lobbyOperationsService := lobby.LobbyOperationsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := lobbyOperationsService.AdminJoinPartyV1Short(
            &lobby_operations.AdminJoinPartyV1Params{
                Namespace: ..., // string
                PartyID:   ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'friends'
    name: 'adminListFriendsOfFriends'
    summary: 'Load list friends of friends'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        friendsService := lobby.FriendsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := friendsService.AdminListFriendsOfFriendsShort(
            &friends.AdminListFriendsOfFriendsParams{
                Namespace: ..., // string
                UserID:    ..., // string
                FriendID:  ..., // *string
                Limit:     ..., // *int64
                Nopaging:  ..., // *bool
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'config'
    name: 'adminPatchUpdateLogConfig'
    summary: 'Patch Update Log Configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        configService := lobby.ConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configService.AdminPatchUpdateLogConfigShort(
            &config.AdminPatchUpdateLogConfigParams{
                Body: ..., // *lobbyclientmodels.LogconfigConfiguration
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'player'
    name: 'adminSetPlayerSessionAttribute'
    summary: 'admin set player''s session attribute'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        playerService := lobby.PlayerService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := playerService.AdminSetPlayerSessionAttributeShort(
            &player.AdminSetPlayerSessionAttributeParams{
                Body:      ..., // *lobbyclientmodels.ModelsSetPlayerSessionAttributeRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'profanity'
    name: 'adminSetProfanityRuleForNamespace'
    summary: 'set current profanity rule'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        profanityService := lobby.ProfanityService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := profanityService.AdminSetProfanityRuleForNamespaceShort(
            &profanity.AdminSetProfanityRuleForNamespaceParams{
                Body:      ..., // *lobbyclientmodels.ModelsAdminSetProfanityRuleForNamespaceRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'config'
    name: 'adminUpdateConfigV1'
    summary: 'admin update namespace config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        configService := lobby.ConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configService.AdminUpdateConfigV1Short(
            &config.AdminUpdateConfigV1Params{
                Body:      ..., // *lobbyclientmodels.ModelsConfigReq
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'admin'
    name: 'adminUpdateGlobalConfig'
    summary: 'Upsert global configuration data.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        adminService := lobby.AdminService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminService.AdminUpdateGlobalConfigShort(
            &admin.AdminUpdateGlobalConfigParams{
                Body: ..., // *lobbyclientmodels.ModelPutGlobalConfigurationRequest
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Lobby Operations'
    name: 'adminUpdatePartyAttributesV1'
    summary: 'admin update party attributes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        lobbyOperationsService := lobby.LobbyOperationsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := lobbyOperationsService.AdminUpdatePartyAttributesV1Short(
            &lobby_operations.AdminUpdatePartyAttributesV1Params{
                Body:      ..., // *lobbyclientmodels.ModelsPartyPUTCustomAttributesRequest
                Namespace: ..., // string
                PartyID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'profanity'
    name: 'adminUpdateProfanityList'
    summary: 'update the list'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        profanityService := lobby.ProfanityService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := profanityService.AdminUpdateProfanityListShort(
            &profanity.AdminUpdateProfanityListParams{
                Body:      ..., // *lobbyclientmodels.ModelsAdminUpdateProfanityList
                List:      ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'thirdParty'
    name: 'adminUpdateThirdPartyConfig'
    summary: 'Update Third Party Steam Config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        thirdPartyService := lobby.ThirdPartyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := thirdPartyService.AdminUpdateThirdPartyConfigShort(
            &third_party.AdminUpdateThirdPartyConfigParams{
                Body:      ..., // *lobbyclientmodels.ModelsUpdateConfigRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'profanity'
    name: 'adminVerifyMessageProfanityResponse'
    summary: 'verify a message directly from the UI or other services'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        profanityService := lobby.ProfanityService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := profanityService.AdminVerifyMessageProfanityResponseShort(
            &profanity.AdminVerifyMessageProfanityResponseParams{
                Body:      ..., // *lobbyclientmodels.ModelsAdminVerifyMessageProfanityRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'friends'
    name: 'bulkDeleteFriends'
    summary: 'Delete friends, and incoming/outgoing friend requests'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        friendsService := lobby.FriendsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := friendsService.BulkDeleteFriendsShort(
            &friends.BulkDeleteFriendsParams{
                Body:      ..., // *lobbyclientmodels.ModelBulkFriendsRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'notification'
    name: 'createNotificationTemplateV1Admin'
    summary: 'create new notification template'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        notificationService := lobby.NotificationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationService.CreateNotificationTemplateV1AdminShort(
            &notification.CreateNotificationTemplateV1AdminParams{
                Body:      ..., // *lobbyclientmodels.ModelCreateTemplateRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'notification'
    name: 'createNotificationTopicV1Admin'
    summary: 'create new notification topic'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        notificationService := lobby.NotificationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationService.CreateNotificationTopicV1AdminShort(
            &notification.CreateNotificationTopicV1AdminParams{
                Body:      ..., // *lobbyclientmodels.ModelCreateTopicRequestV1
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'admin'
    name: 'createTemplate'
    summary: 'create new notification template'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        adminService := lobby.AdminService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminService.CreateTemplateShort(
            &admin.CreateTemplateParams{
                Body:      ..., // *lobbyclientmodels.ModelCreateTemplateRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'notification'
    name: 'createTopic'
    summary: 'create new notification topic'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        notificationService := lobby.NotificationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationService.CreateTopicShort(
            &notification.CreateTopicParams{
                Body:      ..., // *lobbyclientmodels.ModelCreateTopicRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'notification'
    name: 'deleteNotificationTemplateSlugV1Admin'
    summary: 'delete template slug in notification template'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        notificationService := lobby.NotificationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationService.DeleteNotificationTemplateSlugV1AdminShort(
            &notification.DeleteNotificationTemplateSlugV1AdminParams{
                Namespace:    ..., // string
                TemplateSlug: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'notification'
    name: 'deleteNotificationTopicV1Admin'
    summary: 'delete topic information by topic name'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        notificationService := lobby.NotificationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationService.DeleteNotificationTopicV1AdminShort(
            &notification.DeleteNotificationTopicV1AdminParams{
                Namespace: ..., // string
                TopicName: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'admin'
    name: 'deleteTemplateLocalization'
    summary: 'delete template localization'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        adminService := lobby.AdminService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminService.DeleteTemplateLocalizationShort(
            &admin.DeleteTemplateLocalizationParams{
                Namespace:        ..., // string
                TemplateLanguage: ..., // string
                TemplateSlug:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'notification'
    name: 'deleteTemplateLocalizationV1Admin'
    summary: 'delete template localization'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        notificationService := lobby.NotificationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationService.DeleteTemplateLocalizationV1AdminShort(
            &notification.DeleteTemplateLocalizationV1AdminParams{
                Namespace:        ..., // string
                TemplateLanguage: ..., // string
                TemplateSlug:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'admin'
    name: 'deleteTemplateSlug'
    summary: 'delete template localization'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        adminService := lobby.AdminService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminService.DeleteTemplateSlugShort(
            &admin.DeleteTemplateSlugParams{
                Namespace:    ..., // string
                TemplateSlug: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'notification'
    name: 'deleteTopicByTopicName'
    summary: 'delete topic information by topic name'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        notificationService := lobby.NotificationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationService.DeleteTopicByTopicNameShort(
            &notification.DeleteTopicByTopicNameParams{
                Namespace: ..., // string
                Topic:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'admin'
    name: 'freeFormNotification'
    summary: 'send freeform notification to a user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        adminService := lobby.AdminService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminService.FreeFormNotificationShort(
            &admin.FreeFormNotificationParams{
                Body:      ..., // *lobbyclientmodels.ModelFreeFormNotificationRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'notification'
    name: 'freeFormNotificationByUserID'
    summary: 'send freeform notification to a user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        notificationService := lobby.NotificationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationService.FreeFormNotificationByUserIDShort(
            &notification.FreeFormNotificationByUserIDParams{
                Body:      ..., // *lobbyclientmodels.ModelFreeFormNotificationRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'friends'
    name: 'get incoming friend requests'
    summary: 'get incoming friend requests'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        friendsService := lobby.FriendsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := friendsService.GetIncomingFriendRequestsShort(
            &friends.GetIncomingFriendRequestsParams{
                Namespace: ..., // string
                UserID:    ..., // string
                FriendID:  ..., // *string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'friends'
    name: 'get list of friends'
    summary: 'get list of friends'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        friendsService := lobby.FriendsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := friendsService.GetListOfFriendsShort(
            &friends.GetListOfFriendsParams{
                Namespace: ..., // string
                UserID:    ..., // string
                FriendID:  ..., // *string
                FriendIds: ..., // []string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'friends'
    name: 'get outgoing friend requests'
    summary: 'get list of outgoing friend requests'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        friendsService := lobby.FriendsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := friendsService.GetOutgoingFriendRequestsShort(
            &friends.GetOutgoingFriendRequestsParams{
                Namespace: ..., // string
                UserID:    ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'notification'
    name: 'getAllNotificationTemplatesV1Admin'
    summary: 'get all notification template in a namespace'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        notificationService := lobby.NotificationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationService.GetAllNotificationTemplatesV1AdminShort(
            &notification.GetAllNotificationTemplatesV1AdminParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'notification'
    name: 'getAllNotificationTopicsV1Admin'
    summary: 'get topic by namespace'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        notificationService := lobby.NotificationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationService.GetAllNotificationTopicsV1AdminShort(
            &notification.GetAllNotificationTopicsV1AdminParams{
                Namespace: ..., // string
                After:     ..., // *string
                Before:    ..., // *string
                Limit:     ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'admin'
    name: 'getGameTemplate'
    summary: 'get all notification template in a namespace'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        adminService := lobby.AdminService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminService.GetGameTemplateShort(
            &admin.GetGameTemplateParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'admin'
    name: 'getLocalizationTemplate'
    summary: 'get a template localization'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        adminService := lobby.AdminService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminService.GetLocalizationTemplateShort(
            &admin.GetLocalizationTemplateParams{
                Namespace:        ..., // string
                TemplateLanguage: ..., // string
                TemplateSlug:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'notification'
    name: 'getMyNotifications'
    summary: 'Get list of notifications'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        notificationService := lobby.NotificationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationService.GetMyNotificationsShort(
            &notification.GetMyNotificationsParams{
                Namespace: ..., // string
                EndTime:   ..., // *int64
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                StartTime: ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'notification'
    name: 'getMyOfflineNotifications'
    summary: 'Get list of offline notifications'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        notificationService := lobby.NotificationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationService.GetMyOfflineNotificationsShort(
            &notification.GetMyOfflineNotificationsParams{
                Namespace: ..., // string
                EndTime:   ..., // *int64
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                StartTime: ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'notification'
    name: 'getNotificationTopicV1Admin'
    summary: 'get topic information by topic name'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        notificationService := lobby.NotificationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationService.GetNotificationTopicV1AdminShort(
            &notification.GetNotificationTopicV1AdminParams{
                Namespace: ..., // string
                TopicName: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'notification'
    name: 'getSingleTemplateLocalizationV1Admin'
    summary: 'get a template localization'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        notificationService := lobby.NotificationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationService.GetSingleTemplateLocalizationV1AdminShort(
            &notification.GetSingleTemplateLocalizationV1AdminParams{
                Namespace:        ..., // string
                TemplateLanguage: ..., // string
                TemplateSlug:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'admin'
    name: 'getSlugTemplate'
    summary: 'get all notification template in a slug'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        adminService := lobby.AdminService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminService.GetSlugTemplateShort(
            &admin.GetSlugTemplateParams{
                Namespace:    ..., // string
                TemplateSlug: ..., // string
                After:        ..., // *string
                Before:       ..., // *string
                Limit:        ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'notification'
    name: 'getTemplateSlugLocalizationsTemplateV1Admin'
    summary: 'get all notification template localization in a slug'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        notificationService := lobby.NotificationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationService.GetTemplateSlugLocalizationsTemplateV1AdminShort(
            &notification.GetTemplateSlugLocalizationsTemplateV1AdminParams{
                Namespace:    ..., // string
                TemplateSlug: ..., // string
                After:        ..., // *string
                Before:       ..., // *string
                Limit:        ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'notification'
    name: 'getTopicByNamespace'
    summary: 'get topic by namespace'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        notificationService := lobby.NotificationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationService.GetTopicByNamespaceShort(
            &notification.GetTopicByNamespaceParams{
                Namespace: ..., // string
                After:     ..., // *string
                Before:    ..., // *string
                Limit:     ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'notification'
    name: 'getTopicByTopicName'
    summary: 'get topic information by topic name'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        notificationService := lobby.NotificationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationService.GetTopicByTopicNameShort(
            &notification.GetTopicByTopicNameParams{
                Namespace: ..., // string
                Topic:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'friends'
    name: 'getUserFriendsUpdated'
    summary: 'get list of friends'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        friendsService := lobby.FriendsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := friendsService.GetUserFriendsUpdatedShort(
            &friends.GetUserFriendsUpdatedParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'friends'
    name: 'getUserFriendsWithPlatform'
    summary: 'get list of friends with platform data'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        friendsService := lobby.FriendsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := friendsService.GetUserFriendsWithPlatformShort(
            &friends.GetUserFriendsWithPlatformParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'friends'
    name: 'getUserIncomingFriends'
    summary: 'get list of incoming friends'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        friendsService := lobby.FriendsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := friendsService.GetUserIncomingFriendsShort(
            &friends.GetUserIncomingFriendsParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'friends'
    name: 'getUserIncomingFriendsWithTime'
    summary: 'get list of incoming friends with requested time info'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        friendsService := lobby.FriendsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := friendsService.GetUserIncomingFriendsWithTimeShort(
            &friends.GetUserIncomingFriendsWithTimeParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'friends'
    name: 'getUserOutgoingFriends'
    summary: 'get list of outgoing friends'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        friendsService := lobby.FriendsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := friendsService.GetUserOutgoingFriendsShort(
            &friends.GetUserOutgoingFriendsParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'friends'
    name: 'getUserOutgoingFriendsWithTime'
    summary: 'get list of outgoing friends with requested time info'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        friendsService := lobby.FriendsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := friendsService.GetUserOutgoingFriendsWithTimeShort(
            &friends.GetUserOutgoingFriendsWithTimeParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'admin'
    name: 'notificationWithTemplate'
    summary: 'send notification to a user with template'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        adminService := lobby.AdminService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminService.NotificationWithTemplateShort(
            &admin.NotificationWithTemplateParams{
                Body:      ..., // *lobbyclientmodels.ModelNotificationWithTemplateRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'notification'
    name: 'notificationWithTemplateByUserID'
    summary: 'send notification to a user with template'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        notificationService := lobby.NotificationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationService.NotificationWithTemplateByUserIDShort(
            &notification.NotificationWithTemplateByUserIDParams{
                Body:      ..., // *lobbyclientmodels.ModelNotificationWithTemplateRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Lobby Operations'
    name: 'publicGetMessages'
    summary: 'get service messages'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        lobbyOperationsService := lobby.LobbyOperationsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := lobbyOperationsService.PublicGetMessagesShort(
            &lobby_operations.PublicGetMessagesParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'party'
    name: 'publicGetPartyDataV1'
    summary: 'get party data by party id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        partyService := lobby.PartyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := partyService.PublicGetPartyDataV1Short(
            &party.PublicGetPartyDataV1Params{
                Namespace: ..., // string
                PartyID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'player'
    name: 'publicGetPlayerBlockedByPlayersV1'
    summary: 'get players who blocked this player by user id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        playerService := lobby.PlayerService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := playerService.PublicGetPlayerBlockedByPlayersV1Short(
            &player.PublicGetPlayerBlockedByPlayersV1Params{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'player'
    name: 'publicGetPlayerBlockedPlayersV1'
    summary: 'get blocked players by user id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        playerService := lobby.PlayerService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := playerService.PublicGetPlayerBlockedPlayersV1Short(
            &player.PublicGetPlayerBlockedPlayersV1Params{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'player'
    name: 'publicPlayerBlockPlayersV1'
    summary: 'block player by user id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        playerService := lobby.PlayerService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := playerService.PublicPlayerBlockPlayersV1Short(
            &player.PublicPlayerBlockPlayersV1Params{
                Body:      ..., // *lobbyclientmodels.ModelsBlockPlayerRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'party'
    name: 'publicSetPartyLimitV1'
    summary: 'set party limit'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        partyService := lobby.PartyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := partyService.PublicSetPartyLimitV1Short(
            &party.PublicSetPartyLimitV1Params{
                Body:      ..., // *lobbyclientmodels.ModelsPartyPUTLimitSizeRequest
                Namespace: ..., // string
                PartyID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'player'
    name: 'publicUnblockPlayerV1'
    summary: 'unblock player by user id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        playerService := lobby.PlayerService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := playerService.PublicUnblockPlayerV1Short(
            &player.PublicUnblockPlayerV1Params{
                Body:      ..., // *lobbyclientmodels.ModelsUnblockPlayerRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'party'
    name: 'publicUpdatePartyAttributesV1'
    summary: 'update party attributes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        partyService := lobby.PartyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := partyService.PublicUpdatePartyAttributesV1Short(
            &party.PublicUpdatePartyAttributesV1Params{
                Body:      ..., // *lobbyclientmodels.ModelsPartyPUTCustomAttributesRequest
                Namespace: ..., // string
                PartyID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'admin'
    name: 'publishTemplate'
    summary: 'publish draft template'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        adminService := lobby.AdminService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminService.PublishTemplateShort(
            &admin.PublishTemplateParams{
                Namespace:        ..., // string
                TemplateLanguage: ..., // string
                TemplateSlug:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'notification'
    name: 'publishTemplateLocalizationV1Admin'
    summary: 'publish template localization draft'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        notificationService := lobby.NotificationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationService.PublishTemplateLocalizationV1AdminShort(
            &notification.PublishTemplateLocalizationV1AdminParams{
                Namespace:        ..., // string
                TemplateLanguage: ..., // string
                TemplateSlug:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'notification'
    name: 'sendMultipleUsersFreeformNotificationV1Admin'
    summary: 'send freeform notification to multiple users'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        notificationService := lobby.NotificationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationService.SendMultipleUsersFreeformNotificationV1AdminShort(
            &notification.SendMultipleUsersFreeformNotificationV1AdminParams{
                Body:      ..., // *lobbyclientmodels.ModelBulkUsersFreeFormNotificationRequestV1
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'notification'
    name: 'sendPartyFreeformNotificationV1Admin'
    summary: 'send freeform notification to a party'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        notificationService := lobby.NotificationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationService.SendPartyFreeformNotificationV1AdminShort(
            &notification.SendPartyFreeformNotificationV1AdminParams{
                Body:      ..., // *lobbyclientmodels.ModelFreeFormNotificationRequestV1
                Namespace: ..., // string
                PartyID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'notification'
    name: 'sendPartyTemplatedNotificationV1Admin'
    summary: 'send templated notification to a party'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        notificationService := lobby.NotificationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationService.SendPartyTemplatedNotificationV1AdminShort(
            &notification.SendPartyTemplatedNotificationV1AdminParams{
                Body:      ..., // *lobbyclientmodels.ModelNotificationWithTemplateRequestV1
                Namespace: ..., // string
                PartyID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'notification'
    name: 'sendSpecificUserFreeformNotificationV1Admin'
    summary: 'send freeform notification to a user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        notificationService := lobby.NotificationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationService.SendSpecificUserFreeformNotificationV1AdminShort(
            &notification.SendSpecificUserFreeformNotificationV1AdminParams{
                Body:      ..., // *lobbyclientmodels.ModelFreeFormNotificationRequestV1
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'notification'
    name: 'sendSpecificUserTemplatedNotificationV1Admin'
    summary: 'send templated notification to specific user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        notificationService := lobby.NotificationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationService.SendSpecificUserTemplatedNotificationV1AdminShort(
            &notification.SendSpecificUserTemplatedNotificationV1AdminParams{
                Body:      ..., // *lobbyclientmodels.ModelNotificationWithTemplateRequestV1
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'notification'
    name: 'sendUsersFreeformNotificationV1Admin'
    summary: 'send freeform notification to connected users'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        notificationService := lobby.NotificationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationService.SendUsersFreeformNotificationV1AdminShort(
            &notification.SendUsersFreeformNotificationV1AdminParams{
                Body:      ..., // *lobbyclientmodels.ModelFreeFormNotificationRequestV1
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'notification'
    name: 'sendUsersTemplatedNotificationV1Admin'
    summary: 'send notification to connected users with template'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        notificationService := lobby.NotificationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationService.SendUsersTemplatedNotificationV1AdminShort(
            &notification.SendUsersTemplatedNotificationV1AdminParams{
                Body:      ..., // *lobbyclientmodels.ModelNotificationWithTemplateRequestV1
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'blocks'
    name: 'syncNativeBlockedUser'
    summary: 'sync blocked user using server to server call to native first party server.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        blocksService := lobby.BlocksService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := blocksService.SyncNativeBlockedUserShort(
            &blocks.SyncNativeBlockedUserParams{
                Body:      ..., // []*lobbyclientmodels.ModelNativeUserBlockRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'friends'
    name: 'syncNativeFriends'
    summary: 'sync friends using server to server call to native first party server.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        friendsService := lobby.FriendsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := friendsService.SyncNativeFriendsShort(
            &friends.SyncNativeFriendsParams{
                Body:      ..., // []*lobbyclientmodels.ModelNativeFriendRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'admin'
    name: 'updateLocalizationTemplate'
    summary: 'update template draft'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        adminService := lobby.AdminService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminService.UpdateLocalizationTemplateShort(
            &admin.UpdateLocalizationTemplateParams{
                Body:             ..., // *lobbyclientmodels.ModelUpdateTemplateRequest
                Namespace:        ..., // string
                TemplateLanguage: ..., // string
                TemplateSlug:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'notification'
    name: 'updateNotificationTopicV1Admin'
    summary: 'update topic information by topic name'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        notificationService := lobby.NotificationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationService.UpdateNotificationTopicV1AdminShort(
            &notification.UpdateNotificationTopicV1AdminParams{
                Body:      ..., // *lobbyclientmodels.ModelUpdateTopicRequest
                Namespace: ..., // string
                TopicName: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'notification'
    name: 'updateTemplateLocalizationV1Admin'
    summary: 'update template localization'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        notificationService := lobby.NotificationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationService.UpdateTemplateLocalizationV1AdminShort(
            &notification.UpdateTemplateLocalizationV1AdminParams{
                Body:             ..., // *lobbyclientmodels.ModelUpdateTemplateRequest
                Namespace:        ..., // string
                TemplateLanguage: ..., // string
                TemplateSlug:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'notification'
    name: 'updateTopicByTopicName'
    summary: 'update topic information by topic name'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        notificationService := lobby.NotificationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := notificationService.UpdateTopicByTopicNameShort(
            &notification.UpdateTopicByTopicNameParams{
                Body:      ..., // *lobbyclientmodels.ModelUpdateTopicRequest
                Namespace: ..., // string
                Topic:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'friends'
    name: 'userAcceptFriendRequest'
    summary: 'user accept friend'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        friendsService := lobby.FriendsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := friendsService.UserAcceptFriendRequestShort(
            &friends.UserAcceptFriendRequestParams{
                Body:      ..., // *lobbyclientmodels.ModelUserAcceptFriendRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'friends'
    name: 'userCancelFriendRequest'
    summary: 'user cancel a friend request'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        friendsService := lobby.FriendsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := friendsService.UserCancelFriendRequestShort(
            &friends.UserCancelFriendRequestParams{
                Body:      ..., // *lobbyclientmodels.ModelUserCancelFriendRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'friends'
    name: 'userGetFriendshipStatus'
    summary: 'user get friendship status'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        friendsService := lobby.FriendsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := friendsService.UserGetFriendshipStatusShort(
            &friends.UserGetFriendshipStatusParams{
                FriendID:  ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'friends'
    name: 'userRejectFriendRequest'
    summary: 'user reject a friend request'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        friendsService := lobby.FriendsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := friendsService.UserRejectFriendRequestShort(
            &friends.UserRejectFriendRequestParams{
                Body:      ..., // *lobbyclientmodels.ModelUserRejectFriendRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'friends'
    name: 'userRequestFriend'
    summary: 'user add friend'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        friendsService := lobby.FriendsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := friendsService.UserRequestFriendShort(
            &friends.UserRequestFriendParams{
                Body:      ..., // *lobbyclientmodels.ModelUserRequestFriendRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'presence'
    name: 'UsersPresenceHandlerV1'
    summary: 'Query users presence'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        presenceService := lobby.PresenceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := presenceService.UsersPresenceHandlerV1Short(
            &presence.UsersPresenceHandlerV1Params{
                Namespace: ..., // string
                CountOnly: ..., // *bool
                UserIds:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'presence'
    name: 'UsersPresenceHandlerV2'
    summary: 'Query users presence'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        presenceService := lobby.PresenceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := presenceService.UsersPresenceHandlerV2Short(
            &presence.UsersPresenceHandlerV2Params{
                Body:      ..., // *lobbyclientmodels.ModelRequestUserPresence
                Namespace: ..., // string
                CountOnly: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'friends'
    name: 'userUnfriendRequest'
    summary: 'user unfriend a friend'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLobbyClient(configRepo)
        friendsService := lobby.FriendsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := friendsService.UserUnfriendRequestShort(
            &friends.UserUnfriendRequestParams{
                Body:      ..., // *lobbyclientmodels.ModelUserUnfriendRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }
# end lobby
# begin loginqueue
  - ns: 'Admin V1'
    name: 'adminGetConfiguration'
    summary: 'Get Configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLoginqueueClient(configRepo)
        adminV1Service := loginqueue.AdminV1Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminV1Service.AdminGetConfigurationShort(
            &admin_v1.AdminGetConfigurationParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin V1'
    name: 'adminGetStatus'
    summary: 'Get login queue status'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLoginqueueClient(configRepo)
        adminV1Service := loginqueue.AdminV1Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminV1Service.AdminGetStatusShort(
            &admin_v1.AdminGetStatusParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin V1'
    name: 'adminUpdateConfiguration'
    summary: 'Update Configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLoginqueueClient(configRepo)
        adminV1Service := loginqueue.AdminV1Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminV1Service.AdminUpdateConfigurationShort(
            &admin_v1.AdminUpdateConfigurationParams{
                Body:      ..., // *loginqueueclientmodels.ApimodelsConfigurationRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Ticket V1'
    name: 'cancelTicket'
    summary: 'Cancel ticket'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLoginqueueClient(configRepo)
        ticketV1Service := loginqueue.TicketV1Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := ticketV1Service.CancelTicketShort(
            &ticket_v1.CancelTicketParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Ticket V1'
    name: 'refreshTicket'
    summary: 'Refresh ticket'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewLoginqueueClient(configRepo)
        ticketV1Service := loginqueue.TicketV1Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := ticketV1Service.RefreshTicketShort(
            &ticket_v1.RefreshTicketParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }
# end loginqueue
# begin match2
  - ns: 'Backfill'
    name: 'AcceptBackfill'
    summary: 'Accept a backfill proposal'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        backfillService := match2.BackfillService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := backfillService.AcceptBackfillShort(
            &backfill.AcceptBackfillParams{
                Body:       ..., // *match2clientmodels.APIBackFillAcceptRequest
                BackfillID: ..., // string
                Namespace:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PlayFeatureFlag'
    name: 'adminDeletePlayFeatureFlag'
    summary: 'admin Delete Play Feature Flag'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        playFeatureFlagService := match2.PlayFeatureFlagService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := playFeatureFlagService.AdminDeletePlayFeatureFlagShort(
            &play_feature_flag.AdminDeletePlayFeatureFlagParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Config'
    name: 'adminGetAllConfigV1'
    summary: 'admin get all namespaces config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        configService := match2.ConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configService.AdminGetAllConfigV1Short(
            &config.AdminGetAllConfigV1Params{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Config'
    name: 'adminGetConfigV1'
    summary: 'admin get namespace config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        configService := match2.ConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configService.AdminGetConfigV1Short(
            &config.AdminGetConfigV1Params{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Config'
    name: 'adminGetLogConfig'
    summary: 'Get Log Configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        configService := match2.ConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configService.AdminGetLogConfigShort(
            &config.AdminGetLogConfigParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Match-Pools'
    name: 'adminGetMatchPoolTickets'
    summary: 'Get tickets in queue for a specific match pool'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        matchPoolsService := match2.MatchPoolsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := matchPoolsService.AdminGetMatchPoolTicketsShort(
            &match_pools.AdminGetMatchPoolTicketsParams{
                Namespace: ..., // string
                Pool:      ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PlayFeatureFlag'
    name: 'adminGetPlayFeatureFlag'
    summary: 'admin Play Feature Flag'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        playFeatureFlagService := match2.PlayFeatureFlagService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := playFeatureFlagService.AdminGetPlayFeatureFlagShort(
            &play_feature_flag.AdminGetPlayFeatureFlagParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'XRay Config'
    name: 'adminGetXRayConfig'
    summary: 'Admin get XRay config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        xRayConfigService := match2.XRayConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := xRayConfigService.AdminGetXRayConfigShort(
            &x_ray_config.AdminGetXRayConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Config'
    name: 'adminPatchConfigV1'
    summary: 'admin patch update namespace config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        configService := match2.ConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configService.AdminPatchConfigV1Short(
            &config.AdminPatchConfigV1Params{
                Body:      ..., // *match2clientmodels.APIPatchNamespaceConfigRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Config'
    name: 'adminPatchUpdateLogConfig'
    summary: 'Patch Update Log Configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        configService := match2.ConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configService.AdminPatchUpdateLogConfigShort(
            &config.AdminPatchUpdateLogConfigParams{
                Body: ..., // *match2clientmodels.LogconfigConfiguration
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Backfill'
    name: 'AdminQueryBackfill'
    summary: 'Admin Query backfill ticket '
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        backfillService := match2.BackfillService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := backfillService.AdminQueryBackfillShort(
            &backfill.AdminQueryBackfillParams{
                Namespace: ..., // string
                FromTime:  ..., // *string
                IsActive:  ..., // *bool
                Limit:     ..., // *int64
                MatchPool: ..., // *string
                Offset:    ..., // *int64
                PlayerID:  ..., // *string
                Region:    ..., // *string
                SessionID: ..., // *string
                ToTime:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'XRay Config'
    name: 'adminUpdateXRayConfig'
    summary: 'Admin update XRay config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        xRayConfigService := match2.XRayConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := xRayConfigService.AdminUpdateXRayConfigShort(
            &x_ray_config.AdminUpdateXRayConfigParams{
                Body:      ..., // *match2clientmodels.ModelsXRayConfigHTTPUpdateRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PlayFeatureFlag'
    name: 'adminUpsertPlayFeatureFlag'
    summary: 'admin Upsert Play Feature Flag'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        playFeatureFlagService := match2.PlayFeatureFlagService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := playFeatureFlagService.AdminUpsertPlayFeatureFlagShort(
            &play_feature_flag.AdminUpsertPlayFeatureFlagParams{
                Body:      ..., // *match2clientmodels.ModelsPlayFeatureFlag
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Backfill'
    name: 'CreateBackfill'
    summary: 'Create a backfill ticket'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        backfillService := match2.BackfillService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := backfillService.CreateBackfillShort(
            &backfill.CreateBackfillParams{
                Body:      ..., // *match2clientmodels.APIBackFillCreateRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Match-Functions'
    name: 'CreateMatchFunction'
    summary: 'Create a match function'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        matchFunctionsService := match2.MatchFunctionsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := matchFunctionsService.CreateMatchFunctionShort(
            &match_functions.CreateMatchFunctionParams{
                Body:      ..., // *match2clientmodels.APIMatchFunctionRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Match-Pools'
    name: 'CreateMatchPool'
    summary: 'Create a match pool'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        matchPoolsService := match2.MatchPoolsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := matchPoolsService.CreateMatchPoolShort(
            &match_pools.CreateMatchPoolParams{
                Body:      ..., // *match2clientmodels.APIMatchPool
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Match-Tickets'
    name: 'CreateMatchTicket'
    summary: 'Create a matchmaking ticket'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        matchTicketsService := match2.MatchTicketsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := matchTicketsService.CreateMatchTicketShort(
            &match_tickets.CreateMatchTicketParams{
                Body:      ..., // *match2clientmodels.APIMatchTicketRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Rule-Sets'
    name: 'CreateRuleSet'
    summary: 'Create a match rule set'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        ruleSetsService := match2.RuleSetsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := ruleSetsService.CreateRuleSetShort(
            &rule_sets.CreateRuleSetParams{
                Body:      ..., // *match2clientmodels.APIRuleSetPayload
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Backfill'
    name: 'DeleteBackfill'
    summary: 'Delete a backfill ticket'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        backfillService := match2.BackfillService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := backfillService.DeleteBackfillShort(
            &backfill.DeleteBackfillParams{
                BackfillID: ..., // string
                Namespace:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Match-Functions'
    name: 'DeleteMatchFunction'
    summary: 'Delete a match function'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        matchFunctionsService := match2.MatchFunctionsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := matchFunctionsService.DeleteMatchFunctionShort(
            &match_functions.DeleteMatchFunctionParams{
                Name:      ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Match-Pools'
    name: 'DeleteMatchPool'
    summary: 'Delete a match pool'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        matchPoolsService := match2.MatchPoolsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := matchPoolsService.DeleteMatchPoolShort(
            &match_pools.DeleteMatchPoolParams{
                Namespace: ..., // string
                Pool:      ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Match-Tickets'
    name: 'DeleteMatchTicket'
    summary: 'Delete a match ticket'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        matchTicketsService := match2.MatchTicketsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := matchTicketsService.DeleteMatchTicketShort(
            &match_tickets.DeleteMatchTicketParams{
                Namespace: ..., // string
                Ticketid:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Rule-Sets'
    name: 'DeleteRuleSet'
    summary: 'Delete a rule set'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        ruleSetsService := match2.RuleSetsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := ruleSetsService.DeleteRuleSetShort(
            &rule_sets.DeleteRuleSetParams{
                Namespace: ..., // string
                Ruleset:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Environment-Variables'
    name: 'EnvironmentVariableList'
    summary: 'List environment variables'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        environmentVariablesService := match2.EnvironmentVariablesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := environmentVariablesService.EnvironmentVariableListShort(
            &environment_variables.EnvironmentVariableListParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Backfill'
    name: 'GetBackfill'
    summary: 'Get a backfill ticket'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        backfillService := match2.BackfillService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := backfillService.GetBackfillShort(
            &backfill.GetBackfillParams{
                BackfillID: ..., // string
                Namespace:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Backfill'
    name: 'GetBackfillProposal'
    summary: 'Get backfill proposal'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        backfillService := match2.BackfillService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := backfillService.GetBackfillProposalShort(
            &backfill.GetBackfillProposalParams{
                Namespace: ..., // string
                SessionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: ''
    name: 'GetHealthcheckInfo'
    summary: ''
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        Service := match2.EmptyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := Service.GetHealthcheckInfoShort(
            &.GetHealthcheckInfoParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: ''
    name: 'GetHealthcheckInfoV1'
    summary: ''
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        Service := match2.EmptyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := Service.GetHealthcheckInfoV1Short(
            &.GetHealthcheckInfoV1Params{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Match-Tickets'
    name: 'GetMyMatchTickets'
    summary: 'Get my match tickets'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        matchTicketsService := match2.MatchTicketsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := matchTicketsService.GetMyMatchTicketsShort(
            &match_tickets.GetMyMatchTicketsParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                MatchPool: ..., // *string
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Match-Pools'
    name: 'GetPlayerMetric'
    summary: 'Get metrics player for a specific match pool'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        matchPoolsService := match2.MatchPoolsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := matchPoolsService.GetPlayerMetricShort(
            &match_pools.GetPlayerMetricParams{
                Namespace: ..., // string
                Pool:      ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Match-Functions'
    name: 'MatchFunctionGet'
    summary: 'Get custom match function by name'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        matchFunctionsService := match2.MatchFunctionsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := matchFunctionsService.MatchFunctionGetShort(
            &match_functions.MatchFunctionGetParams{
                Name:      ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Match-Functions'
    name: 'MatchFunctionList'
    summary: 'List existing match functions'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        matchFunctionsService := match2.MatchFunctionsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := matchFunctionsService.MatchFunctionListShort(
            &match_functions.MatchFunctionListParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Match-Pools'
    name: 'MatchPoolDetails'
    summary: 'Get details for a specific match pool'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        matchPoolsService := match2.MatchPoolsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := matchPoolsService.MatchPoolDetailsShort(
            &match_pools.MatchPoolDetailsParams{
                Namespace: ..., // string
                Pool:      ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Match-Pools'
    name: 'MatchPoolList'
    summary: 'List match pools'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        matchPoolsService := match2.MatchPoolsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := matchPoolsService.MatchPoolListShort(
            &match_pools.MatchPoolListParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Name:      ..., // *string
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Match-Pools'
    name: 'MatchPoolMetric'
    summary: 'Get metrics for a specific match pool'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        matchPoolsService := match2.MatchPoolsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := matchPoolsService.MatchPoolMetricShort(
            &match_pools.MatchPoolMetricParams{
                Namespace: ..., // string
                Pool:      ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Match-Tickets'
    name: 'MatchTicketDetails'
    summary: 'Get details for a specific match ticket'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        matchTicketsService := match2.MatchTicketsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := matchTicketsService.MatchTicketDetailsShort(
            &match_tickets.MatchTicketDetailsParams{
                Namespace: ..., // string
                Ticketid:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Match-Pools'
    name: 'PostMatchErrorMetric'
    summary: 'Post metrics for external flow failure in a specific match pool'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        matchPoolsService := match2.MatchPoolsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := matchPoolsService.PostMatchErrorMetricShort(
            &match_pools.PostMatchErrorMetricParams{
                Body:      ..., // *match2clientmodels.APIExternalFailureMetricRecord
                Namespace: ..., // string
                Pool:      ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Match-Pools'
    name: 'PublicGetPlayerMetric'
    summary: 'Get metrics player for a specific match pool'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        matchPoolsService := match2.MatchPoolsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := matchPoolsService.PublicGetPlayerMetricShort(
            &match_pools.PublicGetPlayerMetricParams{
                Namespace: ..., // string
                Pool:      ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Backfill'
    name: 'RejectBackfill'
    summary: 'Reject a backfill proposal'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        backfillService := match2.BackfillService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := backfillService.RejectBackfillShort(
            &backfill.RejectBackfillParams{
                Body:       ..., // *match2clientmodels.APIBackFillRejectRequest
                BackfillID: ..., // string
                Namespace:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Rule-Sets'
    name: 'RuleSetDetails'
    summary: 'Get details for a specific rule set'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        ruleSetsService := match2.RuleSetsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := ruleSetsService.RuleSetDetailsShort(
            &rule_sets.RuleSetDetailsParams{
                Namespace: ..., // string
                Ruleset:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Rule-Sets'
    name: 'RuleSetList'
    summary: 'List existing rule sets'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        ruleSetsService := match2.RuleSetsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := ruleSetsService.RuleSetListShort(
            &rule_sets.RuleSetListParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Name:      ..., // *string
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Match-Functions'
    name: 'UpdateMatchFunction'
    summary: 'Update a match function'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        matchFunctionsService := match2.MatchFunctionsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := matchFunctionsService.UpdateMatchFunctionShort(
            &match_functions.UpdateMatchFunctionParams{
                Body:      ..., // *match2clientmodels.APIMatchFunctionRequest
                Name:      ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Match-Pools'
    name: 'UpdateMatchPool'
    summary: 'Update a match pool'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        matchPoolsService := match2.MatchPoolsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := matchPoolsService.UpdateMatchPoolShort(
            &match_pools.UpdateMatchPoolParams{
                Body:      ..., // *match2clientmodels.APIMatchPoolConfig
                Namespace: ..., // string
                Pool:      ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Rule-Sets'
    name: 'UpdateRuleSet'
    summary: 'Update a match rule set'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        ruleSetsService := match2.RuleSetsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := ruleSetsService.UpdateRuleSetShort(
            &rule_sets.UpdateRuleSetParams{
                Body:      ..., // *match2clientmodels.APIRuleSetPayload
                Namespace: ..., // string
                Ruleset:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: ''
    name: 'versionCheckHandler'
    summary: ''
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewMatch2Client(configRepo)
        Service := match2.EmptyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := Service.VersionCheckHandlerShort(
            &.VersionCheckHandlerParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }
# end match2
# begin platform
  - ns: 'Item'
    name: 'acquireItem'
    summary: 'Acquire item'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.AcquireItemShort(
            &item.AcquireItemParams{
                Body:      ..., // *platformclientmodels.ItemAcquireRequest
                ItemID:    ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Ticket'
    name: 'acquireUserTicket'
    summary: 'Acquire ticket'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        ticketService := platform.TicketService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := ticketService.AcquireUserTicketShort(
            &ticket.AcquireUserTicketParams{
                Body:      ..., // *platformclientmodels.TicketAcquireRequest
                BoothName: ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Order'
    name: 'adminCreateUserOrder'
    summary: 'Admin Create an order'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        orderService := platform.OrderService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := orderService.AdminCreateUserOrderShort(
            &order.AdminCreateUserOrderParams{
                Body:      ..., // *platformclientmodels.AdminOrderCreate
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'adminGetIAPOrderLineItems'
    summary: 'Query IAP order line items'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.AdminGetIAPOrderLineItemsShort(
            &iap.AdminGetIAPOrderLineItemsParams{
                IAPOrderNo: ..., // string
                Namespace:  ..., // string
                UserID:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'adminGetSteamJobInfo'
    summary: 'Query steam report job info'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.AdminGetSteamJobInfoShort(
            &iap.AdminGetSteamJobInfoParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'adminRefundIAPOrder'
    summary: 'Refund IAP Order'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.AdminRefundIAPOrderShort(
            &iap.AdminRefundIAPOrderParams{
                IAPOrderNo: ..., // string
                Namespace:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'adminResetSteamJobTime'
    summary: 'Reset steam report job with a special time'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.AdminResetSteamJobTimeShort(
            &iap.AdminResetSteamJobTimeParams{
                Body:      ..., // *platformclientmodels.ResetJobRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'adminSyncSteamAbnormalTransaction'
    summary: 'Sync Abnormal transaction, Sync steam order by transaction. only works when steam sync mode is TRANSACTION.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.AdminSyncSteamAbnormalTransactionShort(
            &iap.AdminSyncSteamAbnormalTransactionParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'adminSyncSteamIAPByTransaction'
    summary: 'Manual sync steam transaction. only works when steam sync mode is TRANSACTION.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.AdminSyncSteamIAPByTransactionShort(
            &iap.AdminSyncSteamIAPByTransactionParams{
                Body:      ..., // *platformclientmodels.SteamSyncByTransactionRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Anonymization'
    name: 'anonymizeCampaign'
    summary: 'Anonymize campaign'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        anonymizationService := platform.AnonymizationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := anonymizationService.AnonymizeCampaignShort(
            &anonymization.AnonymizeCampaignParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Anonymization'
    name: 'anonymizeEntitlement'
    summary: 'anonymize entitlement'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        anonymizationService := platform.AnonymizationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := anonymizationService.AnonymizeEntitlementShort(
            &anonymization.AnonymizeEntitlementParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Anonymization'
    name: 'anonymizeFulfillment'
    summary: 'anonymize fulfillment'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        anonymizationService := platform.AnonymizationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := anonymizationService.AnonymizeFulfillmentShort(
            &anonymization.AnonymizeFulfillmentParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Anonymization'
    name: 'anonymizeIntegration'
    summary: 'anonymize integrations'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        anonymizationService := platform.AnonymizationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := anonymizationService.AnonymizeIntegrationShort(
            &anonymization.AnonymizeIntegrationParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Anonymization'
    name: 'anonymizeOrder'
    summary: 'anonymize order'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        anonymizationService := platform.AnonymizationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := anonymizationService.AnonymizeOrderShort(
            &anonymization.AnonymizeOrderParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Anonymization'
    name: 'anonymizePayment'
    summary: 'anonymize payment'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        anonymizationService := platform.AnonymizationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := anonymizationService.AnonymizePaymentShort(
            &anonymization.AnonymizePaymentParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Anonymization'
    name: 'anonymizeRevocation'
    summary: 'anonymize revocation'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        anonymizationService := platform.AnonymizationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := anonymizationService.AnonymizeRevocationShort(
            &anonymization.AnonymizeRevocationParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Anonymization'
    name: 'anonymizeSubscription'
    summary: 'anonymize subscription'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        anonymizationService := platform.AnonymizationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := anonymizationService.AnonymizeSubscriptionShort(
            &anonymization.AnonymizeSubscriptionParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Anonymization'
    name: 'anonymizeWallet'
    summary: 'anonymize wallet'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        anonymizationService := platform.AnonymizationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := anonymizationService.AnonymizeWalletShort(
            &anonymization.AnonymizeWalletParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Campaign'
    name: 'applyUserRedemption'
    summary: 'Redeem code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        campaignService := platform.CampaignService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := campaignService.ApplyUserRedemptionShort(
            &campaign.ApplyUserRedemptionParams{
                Body:      ..., // *platformclientmodels.RedeemRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Wallet'
    name: 'bulkCredit'
    summary: 'Credit different users'' wallets'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        walletService := platform.WalletService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := walletService.BulkCreditShort(
            &wallet.BulkCreditParams{
                Body:      ..., // []*platformclientmodels.BulkCreditRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Wallet'
    name: 'bulkDebit'
    summary: 'Debit different users'' wallets'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        walletService := platform.WalletService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := walletService.BulkDebitShort(
            &wallet.BulkDebitParams{
                Body:      ..., // []*platformclientmodels.BulkDebitRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Campaign'
    name: 'bulkDisableCodes'
    summary: 'Bulk disable codes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        campaignService := platform.CampaignService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := campaignService.BulkDisableCodesShort(
            &campaign.BulkDisableCodesParams{
                CampaignID: ..., // string
                Namespace:  ..., // string
                BatchName:  ..., // *string
                BatchNo:    ..., // []int32
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Campaign'
    name: 'bulkEnableCodes'
    summary: 'Bulk enable codes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        campaignService := platform.CampaignService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := campaignService.BulkEnableCodesShort(
            &campaign.BulkEnableCodesParams{
                CampaignID: ..., // string
                Namespace:  ..., // string
                BatchName:  ..., // *string
                BatchNo:    ..., // []int32
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'bulkGetLocaleItems'
    summary: 'Bulk get locale items'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.BulkGetLocaleItemsShort(
            &item.BulkGetLocaleItemsParams{
                Namespace:  ..., // string
                ActiveOnly: ..., // *bool
                Language:   ..., // *string
                Region:     ..., // *string
                StoreID:    ..., // *string
                ItemIds:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'bulkUpdateRegionData'
    summary: 'Update item''s region data in bulk'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.BulkUpdateRegionDataShort(
            &item.BulkUpdateRegionDataParams{
                Body:      ..., // *platformclientmodels.BulkRegionDataChangeRequest
                Namespace: ..., // string
                StoreID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Subscription'
    name: 'cancelSubscription'
    summary: 'Cancel a subscription'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        subscriptionService := platform.SubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := subscriptionService.CancelSubscriptionShort(
            &subscription.CancelSubscriptionParams{
                Body:           ..., // *platformclientmodels.CancelRequest
                Namespace:      ..., // string
                SubscriptionID: ..., // string
                UserID:         ..., // string
                Force:          ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Payment'
    name: 'chargePaymentOrder'
    summary: 'Charge payment order without payment flow'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentService := platform.PaymentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentService.ChargePaymentOrderShort(
            &payment.ChargePaymentOrderParams{
                Body:           ..., // *platformclientmodels.PaymentOrderChargeRequest
                Namespace:      ..., // string
                PaymentOrderNo: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Wallet'
    name: 'checkBalance'
    summary: 'Check if a user has enough balance'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        walletService := platform.WalletService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := walletService.CheckBalanceShort(
            &wallet.CheckBalanceParams{
                Request:      ..., // *platformclientmodels.DebitByWalletPlatformRequest
                CurrencyCode: ..., // string
                Namespace:    ..., // string
                UserID:       ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Reward'
    name: 'checkEventCondition'
    summary: 'Check if event payload match reward condition'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        rewardService := platform.RewardService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rewardService.CheckEventConditionShort(
            &reward.CheckEventConditionParams{
                Body:      ..., // *platformclientmodels.EventPayload
                Namespace: ..., // string
                RewardID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Subscription'
    name: 'checkUserSubscriptionSubscribableByItemId'
    summary: 'Check user subscription subscribable'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        subscriptionService := platform.SubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := subscriptionService.CheckUserSubscriptionSubscribableByItemIDShort(
            &subscription.CheckUserSubscriptionSubscribableByItemIDParams{
                Namespace: ..., // string
                UserID:    ..., // string
                ItemID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Wallet'
    name: 'checkWallet'
    summary: 'Check wallet by balance origin and currency code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        walletService := platform.WalletService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := walletService.CheckWalletShort(
            &wallet.CheckWalletParams{
                CurrencyCode: ..., // string
                Namespace:    ..., // string
                UserID:       ..., // string
                Origin:       ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Store'
    name: 'cloneStore'
    summary: 'Clone a store'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        storeService := platform.StoreService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := storeService.CloneStoreShort(
            &store.CloneStoreParams{
                Namespace:     ..., // string
                StoreID:       ..., // string
                TargetStoreID: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'TradeAction'
    name: 'commit'
    summary: 'commit a chain of actions or operations'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        tradeActionService := platform.TradeActionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := tradeActionService.CommitShort(
            &trade_action.CommitParams{
                Body:      ..., // *platformclientmodels.TradeChainedActionCommitRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'consumeUserEntitlement'
    summary: 'Consume user entitlement'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.ConsumeUserEntitlementShort(
            &entitlement.ConsumeUserEntitlementParams{
                Body:          ..., // *platformclientmodels.AdminEntitlementDecrement
                EntitlementID: ..., // string
                Namespace:     ..., // string
                UserID:        ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Order'
    name: 'countOfPurchasedItem'
    summary: 'Get the count of purchased item'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        orderService := platform.OrderService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := orderService.CountOfPurchasedItemShort(
            &order.CountOfPurchasedItemParams{
                Namespace: ..., // string
                UserID:    ..., // string
                ItemID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Campaign'
    name: 'createCampaign'
    summary: 'Create campaign'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        campaignService := platform.CampaignService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := campaignService.CreateCampaignShort(
            &campaign.CreateCampaignParams{
                Body:      ..., // *platformclientmodels.CampaignCreate
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Category'
    name: 'createCategory'
    summary: 'Create category'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        categoryService := platform.CategoryService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := categoryService.CreateCategoryShort(
            &category.CreateCategoryParams{
                Body:      ..., // *platformclientmodels.CategoryCreate
                Namespace: ..., // string
                StoreID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Campaign'
    name: 'createCodes'
    summary: 'Create campaign codes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        campaignService := platform.CampaignService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := campaignService.CreateCodesShort(
            &campaign.CreateCodesParams{
                Body:       ..., // *platformclientmodels.CodeCreate
                CampaignID: ..., // string
                Namespace:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Currency'
    name: 'createCurrency'
    summary: 'Create a currency'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        currencyService := platform.CurrencyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := currencyService.CreateCurrencyShort(
            &currency.CreateCurrencyParams{
                Body:      ..., // *platformclientmodels.CurrencyCreate
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'FulfillmentScript'
    name: 'createFulfillmentScript'
    summary: 'Create fulfillment script'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        fulfillmentScriptService := platform.FulfillmentScriptService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := fulfillmentScriptService.CreateFulfillmentScriptShort(
            &fulfillment_script.CreateFulfillmentScriptParams{
                Body: ..., // *platformclientmodels.FulfillmentScriptCreate
                ID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'createItem'
    summary: 'Create an Item'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.CreateItemShort(
            &item.CreateItemParams{
                Body:      ..., // *platformclientmodels.ItemCreate
                Namespace: ..., // string
                StoreID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'createItemTypeConfig'
    summary: 'Create an Item Type Config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.CreateItemTypeConfigShort(
            &item.CreateItemTypeConfigParams{
                Body: ..., // *platformclientmodels.ItemTypeConfigCreate
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'KeyGroup'
    name: 'createKeyGroup'
    summary: 'Create key group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        keyGroupService := platform.KeyGroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := keyGroupService.CreateKeyGroupShort(
            &key_group.CreateKeyGroupParams{
                Body:      ..., // *platformclientmodels.KeyGroupCreate
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Payment(Dedicated)'
    name: 'createPaymentOrderByDedicated'
    summary: 'Create payment order by dedicated server'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentDedicatedService := platform.PaymentDedicatedService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentDedicatedService.CreatePaymentOrderByDedicatedShort(
            &payment_dedicated.CreatePaymentOrderByDedicatedParams{
                Body:      ..., // *platformclientmodels.ExternalPaymentOrderCreate
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'createPaymentProviderConfig'
    summary: 'Create payment provider config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.CreatePaymentProviderConfigShort(
            &payment_config.CreatePaymentProviderConfigParams{
                Body: ..., // *platformclientmodels.PaymentProviderConfigEdit
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Reward'
    name: 'createReward'
    summary: 'Create a reward'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        rewardService := platform.RewardService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rewardService.CreateRewardShort(
            &reward.CreateRewardParams{
                Body:      ..., // *platformclientmodels.RewardCreate
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Section'
    name: 'createSection'
    summary: 'Create a section'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        sectionService := platform.SectionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := sectionService.CreateSectionShort(
            &section.CreateSectionParams{
                Body:      ..., // *platformclientmodels.SectionCreate
                Namespace: ..., // string
                StoreID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Store'
    name: 'createStore'
    summary: 'Create a store'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        storeService := platform.StoreService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := storeService.CreateStoreShort(
            &store.CreateStoreParams{
                Body:      ..., // *platformclientmodels.StoreCreate
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Payment'
    name: 'createUserPaymentOrder'
    summary: 'Create payment order'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentService := platform.PaymentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentService.CreateUserPaymentOrderShort(
            &payment.CreateUserPaymentOrderParams{
                Body:      ..., // *platformclientmodels.PaymentOrderCreate
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'View'
    name: 'createView'
    summary: 'Create a view'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        viewService := platform.ViewService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := viewService.CreateViewShort(
            &view.CreateViewParams{
                Body:      ..., // *platformclientmodels.ViewCreate
                Namespace: ..., // string
                StoreID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Wallet'
    name: 'creditUserWallet'
    summary: 'Credit a user wallet by currency code and balance origin'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        walletService := platform.WalletService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := walletService.CreditUserWalletShort(
            &wallet.CreditUserWalletParams{
                Body:         ..., // *platformclientmodels.CreditRequest
                CurrencyCode: ..., // string
                Namespace:    ..., // string
                UserID:       ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Wallet'
    name: 'debitByWalletPlatform'
    summary: 'Debit user wallet by currency code and client platform'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        walletService := platform.WalletService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := walletService.DebitByWalletPlatformShort(
            &wallet.DebitByWalletPlatformParams{
                Request:      ..., // *platformclientmodels.DebitByWalletPlatformRequest
                CurrencyCode: ..., // string
                Namespace:    ..., // string
                UserID:       ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Wallet'
    name: 'debitUserWallet'
    summary: 'Debit a user wallet'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        walletService := platform.WalletService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := walletService.DebitUserWalletShort(
            &wallet.DebitUserWalletParams{
                Body:      ..., // *platformclientmodels.DebitRequest
                Namespace: ..., // string
                UserID:    ..., // string
                WalletID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Wallet'
    name: 'debitUserWalletByCurrencyCode'
    summary: 'Debit a user wallet by currency code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        walletService := platform.WalletService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := walletService.DebitUserWalletByCurrencyCodeShort(
            &wallet.DebitUserWalletByCurrencyCodeParams{
                Body:         ..., // *platformclientmodels.DebitByCurrencyCodeRequest
                CurrencyCode: ..., // string
                Namespace:    ..., // string
                UserID:       ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'debugMatchedPaymentMerchantConfig'
    summary: 'Debug matched payment merchant config(internal)'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.DebugMatchedPaymentMerchantConfigShort(
            &payment_config.DebugMatchedPaymentMerchantConfigParams{
                Namespace: ..., // *string
                Region:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'debugMatchedPaymentProviderConfig'
    summary: 'Debug matched payment provider config(internal)'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.DebugMatchedPaymentProviderConfigShort(
            &payment_config.DebugMatchedPaymentProviderConfigParams{
                Namespace: ..., // *string
                Region:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Ticket'
    name: 'decreaseTicketSale'
    summary: 'Decrease ticket sale'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        ticketService := platform.TicketService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := ticketService.DecreaseTicketSaleShort(
            &ticket.DecreaseTicketSaleParams{
                Body:      ..., // *platformclientmodels.TicketSaleDecrementRequest
                BoothName: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'defeatureItem'
    summary: 'Defeature an item'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.DefeatureItemShort(
            &item.DefeatureItemParams{
                Feature:   ..., // string
                ItemID:    ..., // string
                Namespace: ..., // string
                StoreID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'deleteAppleIAPConfig'
    summary: 'Delete apple iap config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.DeleteAppleIAPConfigShort(
            &iap.DeleteAppleIAPConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Category'
    name: 'deleteCategory'
    summary: 'Delete category'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        categoryService := platform.CategoryService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := categoryService.DeleteCategoryShort(
            &category.DeleteCategoryParams{
                CategoryPath: ..., // string
                Namespace:    ..., // string
                StoreID:      ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Currency'
    name: 'deleteCurrency'
    summary: 'Delete a currency'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        currencyService := platform.CurrencyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := currencyService.DeleteCurrencyShort(
            &currency.DeleteCurrencyParams{
                CurrencyCode: ..., // string
                Namespace:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'DLC'
    name: 'deleteDLCItemConfig'
    summary: 'delete a DLC item config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        dlcService := platform.DLCService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dlcService.DeleteDLCItemConfigShort(
            &dlc.DeleteDLCItemConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'deleteEpicGamesIAPConfig'
    summary: 'Delete epic games iap config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.DeleteEpicGamesIAPConfigShort(
            &iap.DeleteEpicGamesIAPConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'FulfillmentScript'
    name: 'deleteFulfillmentScript'
    summary: 'Delete fulfillment script'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        fulfillmentScriptService := platform.FulfillmentScriptService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := fulfillmentScriptService.DeleteFulfillmentScriptShort(
            &fulfillment_script.DeleteFulfillmentScriptParams{
                ID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'deleteGoogleIAPConfig'
    summary: 'Delete google iap config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.DeleteGoogleIAPConfigShort(
            &iap.DeleteGoogleIAPConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'deleteIAPItemConfig'
    summary: 'delete a iap item config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.DeleteIAPItemConfigShort(
            &iap.DeleteIAPItemConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'deleteItem'
    summary: 'Delete an item'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.DeleteItemShort(
            &item.DeleteItemParams{
                ItemID:          ..., // string
                Namespace:       ..., // string
                FeaturesToCheck: ..., // []string
                Force:           ..., // *bool
                StoreID:         ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'deleteItemTypeConfig'
    summary: 'Delete an item type config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.DeleteItemTypeConfigShort(
            &item.DeleteItemTypeConfigParams{
                ID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'ServicePluginConfig'
    name: 'deleteLootBoxPluginConfig'
    summary: 'Delete lootbox plugin config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        servicePluginConfigService := platform.ServicePluginConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := servicePluginConfigService.DeleteLootBoxPluginConfigShort(
            &service_plugin_config.DeleteLootBoxPluginConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'deleteOculusIAPConfig'
    summary: 'Delete oculus iap config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.DeleteOculusIAPConfigShort(
            &iap.DeleteOculusIAPConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'deletePaymentProviderConfig'
    summary: 'Delete payment provider config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.DeletePaymentProviderConfigShort(
            &payment_config.DeletePaymentProviderConfigParams{
                ID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'DLC'
    name: 'deletePlatformDLCConfig'
    summary: 'delete a Platform DLC config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        dlcService := platform.DLCService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dlcService.DeletePlatformDLCConfigShort(
            &dlc.DeletePlatformDLCConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'deletePlaystationIAPConfig'
    summary: 'Delete playstation iap config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.DeletePlaystationIAPConfigShort(
            &iap.DeletePlaystationIAPConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Store'
    name: 'deletePublishedStore'
    summary: 'Delete published store'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        storeService := platform.StoreService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := storeService.DeletePublishedStoreShort(
            &store.DeletePublishedStoreParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Revocation'
    name: 'deleteRevocationConfig'
    summary: 'Delete revocation config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        revocationService := platform.RevocationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := revocationService.DeleteRevocationConfigShort(
            &revocation.DeleteRevocationConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'ServicePluginConfig'
    name: 'deleteRevocationPluginConfig'
    summary: 'Delete revocation plugin config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        servicePluginConfigService := platform.ServicePluginConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := servicePluginConfigService.DeleteRevocationPluginConfigShort(
            &service_plugin_config.DeleteRevocationPluginConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Reward'
    name: 'deleteReward'
    summary: 'Delete a reward'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        rewardService := platform.RewardService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rewardService.DeleteRewardShort(
            &reward.DeleteRewardParams{
                Namespace: ..., // string
                RewardID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Reward'
    name: 'deleteRewardConditionRecord'
    summary: 'Delete a reward condition record'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        rewardService := platform.RewardService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rewardService.DeleteRewardConditionRecordShort(
            &reward.DeleteRewardConditionRecordParams{
                Body:      ..., // *platformclientmodels.DeleteRewardConditionRequest
                Namespace: ..., // string
                RewardID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Section'
    name: 'deleteSection'
    summary: 'Delete a section'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        sectionService := platform.SectionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := sectionService.DeleteSectionShort(
            &section.DeleteSectionParams{
                Namespace: ..., // string
                SectionID: ..., // string
                StoreID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'ServicePluginConfig'
    name: 'deleteSectionPluginConfig'
    summary: 'Delete section plugin config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        servicePluginConfigService := platform.ServicePluginConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := servicePluginConfigService.DeleteSectionPluginConfigShort(
            &service_plugin_config.DeleteSectionPluginConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'ServicePluginConfig'
    name: 'deleteServicePluginConfig'
    summary: 'Delete service plugin config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        servicePluginConfigService := platform.ServicePluginConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := servicePluginConfigService.DeleteServicePluginConfigShort(
            &service_plugin_config.DeleteServicePluginConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'deleteSteamIAPConfig'
    summary: 'Delete steam iap config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.DeleteSteamIAPConfigShort(
            &iap.DeleteSteamIAPConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Store'
    name: 'deleteStore'
    summary: 'Delete a store'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        storeService := platform.StoreService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := storeService.DeleteStoreShort(
            &store.DeleteStoreParams{
                Namespace: ..., // string
                StoreID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'deleteTwitchIAPConfig'
    summary: 'Delete twitch iap config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.DeleteTwitchIAPConfigShort(
            &iap.DeleteTwitchIAPConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Subscription'
    name: 'deleteUserSubscription'
    summary: 'Delete user subscription'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        subscriptionService := platform.SubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := subscriptionService.DeleteUserSubscriptionShort(
            &subscription.DeleteUserSubscriptionParams{
                Namespace:      ..., // string
                SubscriptionID: ..., // string
                UserID:         ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'View'
    name: 'deleteView'
    summary: 'Delete a view'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        viewService := platform.ViewService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := viewService.DeleteViewShort(
            &view.DeleteViewParams{
                Namespace: ..., // string
                ViewID:    ..., // string
                StoreID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'deleteXblAPConfig'
    summary: 'Delete xbl iap config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.DeleteXblAPConfigShort(
            &iap.DeleteXblAPConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Campaign'
    name: 'disableCode'
    summary: 'Disable code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        campaignService := platform.CampaignService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := campaignService.DisableCodeShort(
            &campaign.DisableCodeParams{
                Code:      ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'disableItem'
    summary: 'Disable an item'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.DisableItemShort(
            &item.DisableItemParams{
                Body:      ..., // *platformclientmodels.ChangeStatusItemRequest
                ItemID:    ..., // string
                Namespace: ..., // string
                StoreID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'disableUserEntitlement'
    summary: 'Disable user entitlement'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.DisableUserEntitlementShort(
            &entitlement.DisableUserEntitlementParams{
                EntitlementID: ..., // string
                Namespace:     ..., // string
                UserID:        ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Wallet'
    name: 'disableUserWallet'
    summary: 'Disable a user wallet'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        walletService := platform.WalletService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := walletService.DisableUserWalletShort(
            &wallet.DisableUserWalletParams{
                Namespace: ..., // string
                UserID:    ..., // string
                WalletID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Revocation'
    name: 'doRevocation'
    summary: 'Do revocation.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        revocationService := platform.RevocationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := revocationService.DoRevocationShort(
            &revocation.DoRevocationParams{
                Body:      ..., // *platformclientmodels.RevocationRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Campaign'
    name: 'download'
    summary: 'Download codes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        campaignService := platform.CampaignService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := campaignService.DownloadShort(
            &campaign.DownloadParams{
                CampaignID:    ..., // string
                Namespace:     ..., // string
                BatchName:     ..., // *string
                BatchNo:       ..., // []int32
                WithBatchName: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Category'
    name: 'downloadCategories'
    summary: 'Download store''s structured categories'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        categoryService := platform.CategoryService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := categoryService.DownloadCategoriesShort(
            &category.DownloadCategoriesParams{
                Namespace: ..., // string
                Language:  ..., // *string
                StoreID:   ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Store'
    name: 'downloadCSVTemplates'
    summary: 'Download store csv templates'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        storeService := platform.StoreService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := storeService.DownloadCSVTemplatesShort(
            &store.DownloadCSVTemplatesParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Invoice'
    name: 'downloadInvoiceDetails'
    summary: 'Download invoice details'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        invoiceService := platform.InvoiceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := invoiceService.DownloadInvoiceDetailsShort(
            &invoice.DownloadInvoiceDetailsParams{
                Namespace: ..., // string
                EndTime:   ..., // string
                Feature:   ..., // string
                ItemID:    ..., // string
                ItemType:  ..., // string
                StartTime: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Order'
    name: 'downloadUserOrderReceipt'
    summary: 'Download user order receipt'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        orderService := platform.OrderService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := orderService.DownloadUserOrderReceiptShort(
            &order.DownloadUserOrderReceiptParams{
                Namespace: ..., // string
                OrderNo:   ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Campaign'
    name: 'enableCode'
    summary: 'Enable code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        campaignService := platform.CampaignService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := campaignService.EnableCodeShort(
            &campaign.EnableCodeParams{
                Code:      ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'enableEntitlementOriginFeature'
    summary: 'Enable entitlement origin feature'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.EnableEntitlementOriginFeatureShort(
            &entitlement.EnableEntitlementOriginFeatureParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'enableItem'
    summary: 'Enable an item'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.EnableItemShort(
            &item.EnableItemParams{
                ItemID:    ..., // string
                Namespace: ..., // string
                StoreID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'enableUserEntitlement'
    summary: 'Enable user entitlement'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.EnableUserEntitlementShort(
            &entitlement.EnableUserEntitlementParams{
                EntitlementID: ..., // string
                Namespace:     ..., // string
                UserID:        ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Wallet'
    name: 'enableUserWallet'
    summary: 'Enable a user wallet'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        walletService := platform.WalletService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := walletService.EnableUserWalletShort(
            &wallet.EnableUserWalletParams{
                Namespace: ..., // string
                UserID:    ..., // string
                WalletID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'existsAnyUserActiveEntitlement'
    summary: 'Exists any user active entitlement'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.ExistsAnyUserActiveEntitlementShort(
            &entitlement.ExistsAnyUserActiveEntitlementParams{
                Namespace: ..., // string
                UserID:    ..., // string
                AppIds:    ..., // []string
                ItemIds:   ..., // []string
                Platform:  ..., // *string
                Skus:      ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'existsAnyUserActiveEntitlementByItemIds'
    summary: 'Exists any user active entitlement'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.ExistsAnyUserActiveEntitlementByItemIdsShort(
            &entitlement.ExistsAnyUserActiveEntitlementByItemIdsParams{
                Namespace: ..., // string
                UserID:    ..., // string
                Platform:  ..., // *string
                ItemIds:   ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Reward'
    name: 'exportRewards'
    summary: 'Export all reward configurations'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        rewardService := platform.RewardService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rewardService.ExportRewardsShort(
            &reward.ExportRewardsParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Store'
    name: 'exportStore'
    summary: 'Export a store'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        storeService := platform.StoreService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := storeService.ExportStoreShort(
            &store.ExportStoreParams{
                Namespace: ..., // string
                StoreID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Store'
    name: 'exportStore_1'
    summary: 'Export a store'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        storeService := platform.StoreService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := storeService.ExportStore1Short(
            &store.ExportStore1Params{
                Body:      ..., // *platformclientmodels.ExportStoreRequest
                Namespace: ..., // string
                StoreID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Store'
    name: 'exportStoreByCSV'
    summary: 'Export a store to CSV format'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        storeService := platform.StoreService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := storeService.ExportStoreByCSVShort(
            &store.ExportStoreByCSVParams{
                Body:      ..., // *platformclientmodels.ExportStoreToCSVRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'featureItem'
    summary: 'Feature an item'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.FeatureItemShort(
            &item.FeatureItemParams{
                Feature:   ..., // string
                ItemID:    ..., // string
                Namespace: ..., // string
                StoreID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Fulfillment'
    name: 'fulfillItem'
    summary: 'Fulfill item'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        fulfillmentService := platform.FulfillmentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := fulfillmentService.FulfillItemShort(
            &fulfillment.FulfillItemParams{
                Body:      ..., // *platformclientmodels.FulfillmentRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Fulfillment'
    name: 'fulfillItems'
    summary: 'Fulfill items by transactionId'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        fulfillmentService := platform.FulfillmentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := fulfillmentService.FulfillItemsShort(
            &fulfillment.FulfillItemsParams{
                Body:          ..., // *platformclientmodels.FulfillmentV2Request
                Namespace:     ..., // string
                TransactionID: ..., // string
                UserID:        ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Fulfillment'
    name: 'fulfillItemsV3'
    summary: 'Fulfill items by transactionId'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        fulfillmentService := platform.FulfillmentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := fulfillmentService.FulfillItemsV3Short(
            &fulfillment.FulfillItemsV3Params{
                Body:          ..., // *platformclientmodels.FulfillmentV2Request
                Namespace:     ..., // string
                TransactionID: ..., // string
                UserID:        ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Fulfillment'
    name: 'fulfillRewards'
    summary: 'Fulfill rewards without content'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        fulfillmentService := platform.FulfillmentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := fulfillmentService.FulfillRewardsShort(
            &fulfillment.FulfillRewardsParams{
                Body:      ..., // *platformclientmodels.RewardsRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Fulfillment'
    name: 'fulfillRewardsV2'
    summary: 'Fulfill rewards'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        fulfillmentService := platform.FulfillmentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := fulfillmentService.FulfillRewardsV2Short(
            &fulfillment.FulfillRewardsV2Params{
                Body:      ..., // *platformclientmodels.RewardsRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Order'
    name: 'fulfillUserOrder'
    summary: 'Fulfill an order'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        orderService := platform.OrderService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := orderService.FulfillUserOrderShort(
            &order.FulfillUserOrderParams{
                Namespace: ..., // string
                OrderNo:   ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'DLC'
    name: 'geDLCDurableRewardShortMap'
    summary: 'Get dlc durable reward simple map'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        dlcService := platform.DLCService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dlcService.GeDLCDurableRewardShortMapShort(
            &dlc.GeDLCDurableRewardShortMapParams{
                Namespace: ..., // string
                DLCType:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Invoice'
    name: 'generateInvoiceSummary'
    summary: 'Generate invoice summary'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        invoiceService := platform.InvoiceService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := invoiceService.GenerateInvoiceSummaryShort(
            &invoice.GenerateInvoiceSummaryParams{
                Namespace: ..., // string
                EndTime:   ..., // string
                Feature:   ..., // string
                ItemID:    ..., // string
                ItemType:  ..., // string
                StartTime: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'getAggregatePaymentProviders'
    summary: 'Get aggregate payment providers'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.GetAggregatePaymentProvidersShort(
            &payment_config.GetAggregatePaymentProvidersParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'getApp'
    summary: 'Get an app info'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.GetAppShort(
            &item.GetAppParams{
                ItemID:     ..., // string
                Namespace:  ..., // string
                ActiveOnly: ..., // *bool
                StoreID:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'getAppleConfigVersion'
    summary: 'Get apple config version'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.GetAppleConfigVersionShort(
            &iap.GetAppleConfigVersionParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'getAppleIAPConfig'
    summary: 'Get apple iap config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.GetAppleIAPConfigShort(
            &iap.GetAppleIAPConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'getAvailablePredicateTypes'
    summary: 'Get available predicate types'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.GetAvailablePredicateTypesShort(
            &item.GetAvailablePredicateTypesParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'getBulkItemIdBySkus'
    summary: 'Get multiple itemId by sku'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.GetBulkItemIDBySkusShort(
            &item.GetBulkItemIDBySkusParams{
                Namespace: ..., // string
                Sku:       ..., // []string
                StoreID:   ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Campaign'
    name: 'getCampaign'
    summary: 'Get campaign'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        campaignService := platform.CampaignService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := campaignService.GetCampaignShort(
            &campaign.GetCampaignParams{
                CampaignID: ..., // string
                Namespace:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Campaign'
    name: 'getCampaignDynamic'
    summary: 'Get campaign dynamic'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        campaignService := platform.CampaignService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := campaignService.GetCampaignDynamicShort(
            &campaign.GetCampaignDynamicParams{
                CampaignID: ..., // string
                Namespace:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Store'
    name: 'getCatalogConfig'
    summary: 'Get catalog config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        storeService := platform.StoreService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := storeService.GetCatalogConfigShort(
            &store.GetCatalogConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Store'
    name: 'getCatalogDefinition'
    summary: 'Get catalog definition'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        storeService := platform.StoreService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := storeService.GetCatalogDefinitionShort(
            &store.GetCatalogDefinitionParams{
                Namespace:   ..., // string
                CatalogType: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Category'
    name: 'getCategory'
    summary: 'Get category'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        categoryService := platform.CategoryService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := categoryService.GetCategoryShort(
            &category.GetCategoryParams{
                CategoryPath: ..., // string
                Namespace:    ..., // string
                StoreID:      ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Category'
    name: 'getChildCategories'
    summary: 'Get child categories'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        categoryService := platform.CategoryService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := categoryService.GetChildCategoriesShort(
            &category.GetChildCategoriesParams{
                CategoryPath: ..., // string
                Namespace:    ..., // string
                StoreID:      ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Campaign'
    name: 'getCode'
    summary: 'Get code info'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        campaignService := platform.CampaignService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := campaignService.GetCodeShort(
            &campaign.GetCodeParams{
                Code:          ..., // string
                Namespace:     ..., // string
                Redeemable:    ..., // *bool
                WithBatchName: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Currency'
    name: 'getCurrencyConfig'
    summary: 'Get currency config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        currencyService := platform.CurrencyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := currencyService.GetCurrencyConfigShort(
            &currency.GetCurrencyConfigParams{
                CurrencyCode: ..., // string
                Namespace:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Currency'
    name: 'getCurrencySummary'
    summary: 'Get currency summary'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        currencyService := platform.CurrencyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := currencyService.GetCurrencySummaryShort(
            &currency.GetCurrencySummaryParams{
                CurrencyCode: ..., // string
                Namespace:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Category'
    name: 'getDescendantCategories'
    summary: 'Get descendant categories'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        categoryService := platform.CategoryService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := categoryService.GetDescendantCategoriesShort(
            &category.GetDescendantCategoriesParams{
                CategoryPath: ..., // string
                Namespace:    ..., // string
                StoreID:      ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'DLC'
    name: 'getDLCItemConfig'
    summary: 'Get DLC item config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        dlcService := platform.DLCService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dlcService.GetDLCItemConfigShort(
            &dlc.GetDLCItemConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'DLC'
    name: 'getDLCItemConfigHistory'
    summary: 'Get DLC item config history'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        dlcService := platform.DLCService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dlcService.GetDLCItemConfigHistoryShort(
            &dlc.GetDLCItemConfigHistoryParams{
                Namespace: ..., // string
                DLCID:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'getEntitlement'
    summary: 'Get entitlement'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.GetEntitlementShort(
            &entitlement.GetEntitlementParams{
                EntitlementID: ..., // string
                Namespace:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'getEntitlementConfigInfo'
    summary: 'Get entitlement config info'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.GetEntitlementConfigInfoShort(
            &entitlement.GetEntitlementConfigInfoParams{
                Namespace:    ..., // string
                WithoutCache: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'getEpicGamesIAPConfig'
    summary: 'Get epicgames iap config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.GetEpicGamesIAPConfigShort(
            &iap.GetEpicGamesIAPConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'getEstimatedPrice'
    summary: 'Get estimated price'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.GetEstimatedPriceShort(
            &item.GetEstimatedPriceParams{
                Namespace: ..., // string
                Platform:  ..., // *string
                Region:    ..., // *string
                StoreID:   ..., // *string
                ItemIds:   ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'FulfillmentScript'
    name: 'getFulfillmentScript'
    summary: 'Get fulfillment script by id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        fulfillmentScriptService := platform.FulfillmentScriptService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := fulfillmentScriptService.GetFulfillmentScriptShort(
            &fulfillment_script.GetFulfillmentScriptParams{
                ID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'getGoogleIAPConfig'
    summary: 'Get google iap config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.GetGoogleIAPConfigShort(
            &iap.GetGoogleIAPConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'getIAPItemConfig'
    summary: 'Get iap item config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.GetIAPItemConfigShort(
            &iap.GetIAPItemConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'getIAPItemMapping'
    summary: 'Get iap item mapping'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.GetIAPItemMappingShort(
            &iap.GetIAPItemMappingParams{
                Namespace: ..., // string
                Platform:  ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'getIAPOrderConsumeDetails'
    summary: 'Get IAP Order Consume Details by IAP Order Number.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.GetIAPOrderConsumeDetailsShort(
            &iap.GetIAPOrderConsumeDetailsParams{
                IAPOrderNo: ..., // string
                Namespace:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'getItem'
    summary: 'Get an item'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.GetItemShort(
            &item.GetItemParams{
                ItemID:     ..., // string
                Namespace:  ..., // string
                ActiveOnly: ..., // *bool
                StoreID:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'getItemByAppId'
    summary: 'Get item by appId'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.GetItemByAppIDShort(
            &item.GetItemByAppIDParams{
                Namespace:  ..., // string
                ActiveOnly: ..., // *bool
                StoreID:    ..., // *string
                AppID:      ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'getItemBySku'
    summary: 'Get item by sku'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.GetItemBySkuShort(
            &item.GetItemBySkuParams{
                Namespace:  ..., // string
                ActiveOnly: ..., // *bool
                StoreID:    ..., // *string
                Sku:        ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'getItemDynamicData'
    summary: 'Get item dynamic data'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.GetItemDynamicDataShort(
            &item.GetItemDynamicDataParams{
                ItemID:    ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'getItemIdBySku'
    summary: 'Get itemId by sku'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.GetItemIDBySkuShort(
            &item.GetItemIDBySkuParams{
                Namespace:  ..., // string
                ActiveOnly: ..., // *bool
                StoreID:    ..., // *string
                Sku:        ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'getItems'
    summary: 'Get items'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.GetItemsShort(
            &item.GetItemsParams{
                Namespace:  ..., // string
                ActiveOnly: ..., // *bool
                StoreID:    ..., // *string
                ItemIds:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'getItemTypeConfig'
    summary: 'Get an item type config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.GetItemTypeConfigShort(
            &item.GetItemTypeConfigParams{
                ID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'KeyGroup'
    name: 'getKeyGroup'
    summary: 'Get key group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        keyGroupService := platform.KeyGroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := keyGroupService.GetKeyGroupShort(
            &key_group.GetKeyGroupParams{
                KeyGroupID: ..., // string
                Namespace:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'KeyGroup'
    name: 'getKeyGroupByBoothName'
    summary: 'Get key group by booth name'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        keyGroupService := platform.KeyGroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := keyGroupService.GetKeyGroupByBoothNameShort(
            &key_group.GetKeyGroupByBoothNameParams{
                Namespace: ..., // string
                BoothName: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'KeyGroup'
    name: 'getKeyGroupDynamic'
    summary: 'Get key group dynamic'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        keyGroupService := platform.KeyGroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := keyGroupService.GetKeyGroupDynamicShort(
            &key_group.GetKeyGroupDynamicParams{
                KeyGroupID: ..., // string
                Namespace:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'getLocaleItem'
    summary: 'Get an item in locale'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.GetLocaleItemShort(
            &item.GetLocaleItemParams{
                ItemID:         ..., // string
                Namespace:      ..., // string
                ActiveOnly:     ..., // *bool
                Language:       ..., // *string
                PopulateBundle: ..., // *bool
                Region:         ..., // *string
                StoreID:        ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'getLocaleItemBySku'
    summary: 'Get an item by sku in locale'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.GetLocaleItemBySkuShort(
            &item.GetLocaleItemBySkuParams{
                Namespace:      ..., // string
                ActiveOnly:     ..., // *bool
                Language:       ..., // *string
                PopulateBundle: ..., // *bool
                Region:         ..., // *string
                StoreID:        ..., // *string
                Sku:            ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'ServicePluginConfig'
    name: 'getLootBoxGrpcInfo'
    summary: 'Get lootbox plugin gRPC info'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        servicePluginConfigService := platform.ServicePluginConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := servicePluginConfigService.GetLootBoxGrpcInfoShort(
            &service_plugin_config.GetLootBoxGrpcInfoParams{
                Namespace: ..., // string
                Force:     ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'ServicePluginConfig'
    name: 'getLootBoxPluginConfig'
    summary: 'Get lootbox plugin config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        servicePluginConfigService := platform.ServicePluginConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := servicePluginConfigService.GetLootBoxPluginConfigShort(
            &service_plugin_config.GetLootBoxPluginConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'getOculusIAPConfig'
    summary: 'Get oculus iap config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.GetOculusIAPConfigShort(
            &iap.GetOculusIAPConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Order'
    name: 'getOrder'
    summary: 'Get order'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        orderService := platform.OrderService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := orderService.GetOrderShort(
            &order.GetOrderParams{
                Namespace: ..., // string
                OrderNo:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Order'
    name: 'getOrderStatistics'
    summary: 'Get Order Statistics'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        orderService := platform.OrderService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := orderService.GetOrderStatisticsShort(
            &order.GetOrderStatisticsParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentCallbackConfig'
    name: 'getPaymentCallbackConfig'
    summary: 'Get payment callback configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentCallbackConfigService := platform.PaymentCallbackConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentCallbackConfigService.GetPaymentCallbackConfigShort(
            &payment_callback_config.GetPaymentCallbackConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentStation'
    name: 'getPaymentCustomization'
    summary: 'Get payment provider customization'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentStationService := platform.PaymentStationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentStationService.GetPaymentCustomizationShort(
            &payment_station.GetPaymentCustomizationParams{
                Namespace:       ..., // string
                Sandbox:         ..., // *bool
                PaymentProvider: ..., // string
                Region:          ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'getPaymentMerchantConfig'
    summary: 'Get payment domain whitelist config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.GetPaymentMerchantConfigShort(
            &payment_config.GetPaymentMerchantConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'getPaymentMerchantConfig_1'
    summary: 'Get payment merchant config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.GetPaymentMerchantConfig1Short(
            &payment_config.GetPaymentMerchantConfig1Params{
                ID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Payment'
    name: 'getPaymentOrder'
    summary: 'Get payment order'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentService := platform.PaymentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentService.GetPaymentOrderShort(
            &payment.GetPaymentOrderParams{
                Namespace:      ..., // string
                PaymentOrderNo: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Payment'
    name: 'getPaymentOrderChargeStatus'
    summary: 'Get payment order charge status'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentService := platform.PaymentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentService.GetPaymentOrderChargeStatusShort(
            &payment.GetPaymentOrderChargeStatusParams{
                Namespace:      ..., // string
                PaymentOrderNo: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentStation'
    name: 'getPaymentPublicConfig'
    summary: 'Get payment provider public config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentStationService := platform.PaymentStationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentStationService.GetPaymentPublicConfigShort(
            &payment_station.GetPaymentPublicConfigParams{
                Namespace:       ..., // string
                Sandbox:         ..., // *bool
                PaymentProvider: ..., // string
                Region:          ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'getPaymentTaxConfig'
    summary: 'Get payment global tax config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.GetPaymentTaxConfigShort(
            &payment_config.GetPaymentTaxConfigParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentStation'
    name: 'getPaymentTaxValue'
    summary: 'get tax result of a payment order'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentStationService := platform.PaymentStationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentStationService.GetPaymentTaxValueShort(
            &payment_station.GetPaymentTaxValueParams{
                Namespace:       ..., // string
                ZipCode:         ..., // *string
                PaymentOrderNo:  ..., // string
                PaymentProvider: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'DLC'
    name: 'getPlatformDLCConfig'
    summary: 'Get Platform DLC config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        dlcService := platform.DLCService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dlcService.GetPlatformDLCConfigShort(
            &dlc.GetPlatformDLCConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'getPlatformEntitlementConfig'
    summary: 'Get platform entitlement config list'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.GetPlatformEntitlementConfigShort(
            &entitlement.GetPlatformEntitlementConfigParams{
                Namespace: ..., // string
                Platform:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Wallet'
    name: 'getPlatformWalletConfig'
    summary: 'Get platform wallet config list'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        walletService := platform.WalletService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := walletService.GetPlatformWalletConfigShort(
            &wallet.GetPlatformWalletConfigParams{
                Namespace: ..., // string
                Platform:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'getPlayStationIAPConfig'
    summary: 'Get playstation iap config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.GetPlayStationIAPConfigShort(
            &iap.GetPlayStationIAPConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Platform'
    name: 'getPsnEntitlementOwnership'
    summary: 'Get user psn entitlement ownership by entitlement label'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        platformService := platform.PlatformService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := platformService.GetPsnEntitlementOwnershipShort(
            &platform.GetPsnEntitlementOwnershipParams{
                Body:             ..., // *platformclientmodels.PsnEntitlementOwnershipRequest
                EntitlementLabel: ..., // string
                Namespace:        ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Store'
    name: 'getPublishedStore'
    summary: 'Get published store'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        storeService := platform.StoreService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := storeService.GetPublishedStoreShort(
            &store.GetPublishedStoreParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Store'
    name: 'getPublishedStoreBackup'
    summary: 'Get a published store''s backup'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        storeService := platform.StoreService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := storeService.GetPublishedStoreBackupShort(
            &store.GetPublishedStoreBackupParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Revocation'
    name: 'getRevocationConfig'
    summary: 'Get revocation configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        revocationService := platform.RevocationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := revocationService.GetRevocationConfigShort(
            &revocation.GetRevocationConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'ServicePluginConfig'
    name: 'getRevocationPluginConfig'
    summary: 'Get revocation plugin config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        servicePluginConfigService := platform.ServicePluginConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := servicePluginConfigService.GetRevocationPluginConfigShort(
            &service_plugin_config.GetRevocationPluginConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Reward'
    name: 'getReward'
    summary: 'Get a reward'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        rewardService := platform.RewardService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rewardService.GetRewardShort(
            &reward.GetRewardParams{
                Namespace: ..., // string
                RewardID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Reward'
    name: 'getReward_1'
    summary: 'Get a reward'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        rewardService := platform.RewardService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rewardService.GetReward1Short(
            &reward.GetReward1Params{
                Namespace: ..., // string
                RewardID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Reward'
    name: 'getRewardByCode'
    summary: 'Get a reward by code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        rewardService := platform.RewardService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rewardService.GetRewardByCodeShort(
            &reward.GetRewardByCodeParams{
                Namespace:  ..., // string
                RewardCode: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Category'
    name: 'getRootCategories'
    summary: 'Get root categories'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        categoryService := platform.CategoryService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := categoryService.GetRootCategoriesShort(
            &category.GetRootCategoriesParams{
                Namespace: ..., // string
                StoreID:   ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Section'
    name: 'getSection'
    summary: 'Get a section'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        sectionService := platform.SectionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := sectionService.GetSectionShort(
            &section.GetSectionParams{
                Namespace: ..., // string
                SectionID: ..., // string
                StoreID:   ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'ServicePluginConfig'
    name: 'getSectionPluginConfig'
    summary: 'Get section plugin config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        servicePluginConfigService := platform.ServicePluginConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := servicePluginConfigService.GetSectionPluginConfigShort(
            &service_plugin_config.GetSectionPluginConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'ServicePluginConfig'
    name: 'getServicePluginConfig'
    summary: 'Get service plugin config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        servicePluginConfigService := platform.ServicePluginConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := servicePluginConfigService.GetServicePluginConfigShort(
            &service_plugin_config.GetServicePluginConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'getSpecialPaymentProviders'
    summary: 'Get special payment providers'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.GetSpecialPaymentProvidersShort(
            &payment_config.GetSpecialPaymentProvidersParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'CatalogChanges'
    name: 'getStatistic'
    summary: 'Get catalog changes statistics'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        catalogChangesService := platform.CatalogChangesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := catalogChangesService.GetStatisticShort(
            &catalog_changes.GetStatisticParams{
                Namespace:      ..., // string
                StoreID:        ..., // string
                Action:         ..., // *string
                ItemSku:        ..., // *string
                ItemType:       ..., // *string
                Type:           ..., // *string
                UpdatedAtEnd:   ..., // *string
                UpdatedAtStart: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'getSteamIAPConfig'
    summary: 'Get steam iap config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.GetSteamIAPConfigShort(
            &iap.GetSteamIAPConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Store'
    name: 'getStore'
    summary: 'Get a store'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        storeService := platform.StoreService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := storeService.GetStoreShort(
            &store.GetStoreParams{
                Namespace: ..., // string
                StoreID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP(Subscription)'
    name: 'getSubscriptionHistory'
    summary: 'Get User Subscription Transaction Update History'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapSubscriptionService := platform.IAPSubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapSubscriptionService.GetSubscriptionHistoryShort(
            &iap_subscription.GetSubscriptionHistoryParams{
                ID:        ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
                Limit:     ..., // *int32
                Offset:    ..., // *int32
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP(Subscription)'
    name: 'getThirdPartyPlatformSubscriptionOwnershipByGroupId'
    summary: 'Get user third party platform subscription ownership by subscription group id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapSubscriptionService := platform.IAPSubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapSubscriptionService.GetThirdPartyPlatformSubscriptionOwnershipByGroupIDShort(
            &iap_subscription.GetThirdPartyPlatformSubscriptionOwnershipByGroupIDParams{
                Namespace: ..., // string
                Platform:  ..., // string
                UserID:    ..., // string
                GroupID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP(Subscription)'
    name: 'getThirdPartyPlatformSubscriptionOwnershipByProductId'
    summary: 'Get user third party platform subscription ownership by subscription product id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapSubscriptionService := platform.IAPSubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapSubscriptionService.GetThirdPartyPlatformSubscriptionOwnershipByProductIDShort(
            &iap_subscription.GetThirdPartyPlatformSubscriptionOwnershipByProductIDParams{
                Namespace: ..., // string
                Platform:  ..., // string
                UserID:    ..., // string
                ProductID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP(Subscription)'
    name: 'getThirdPartySubscriptionDetails'
    summary: 'Admin get user subscription transaction details.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapSubscriptionService := platform.IAPSubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapSubscriptionService.GetThirdPartySubscriptionDetailsShort(
            &iap_subscription.GetThirdPartySubscriptionDetailsParams{
                ID:        ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP(Subscription)'
    name: 'getThirdPartyUserSubscriptionDetails'
    summary: 'Admin get user subscription details.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapSubscriptionService := platform.IAPSubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapSubscriptionService.GetThirdPartyUserSubscriptionDetailsShort(
            &iap_subscription.GetThirdPartyUserSubscriptionDetailsParams{
                ID:        ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Ticket'
    name: 'getTicketBoothID'
    summary: 'Get ticket booth ID'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        ticketService := platform.TicketService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := ticketService.GetTicketBoothIDShort(
            &ticket.GetTicketBoothIDParams{
                BoothName: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Ticket'
    name: 'getTicketDynamic'
    summary: 'Get ticket dynamic'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        ticketService := platform.TicketService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := ticketService.GetTicketDynamicShort(
            &ticket.GetTicketDynamicParams{
                BoothName: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'TradeAction'
    name: 'getTradeHistoryByCriteria'
    summary: 'Retrieve trade history by criteria'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        tradeActionService := platform.TradeActionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := tradeActionService.GetTradeHistoryByCriteriaShort(
            &trade_action.GetTradeHistoryByCriteriaParams{
                Namespace: ..., // string
                Limit:     ..., // *int32
                Offset:    ..., // *int32
                Status:    ..., // *string
                Type:      ..., // *string
                UserID:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'TradeAction'
    name: 'getTradeHistoryByTransactionId'
    summary: 'Retrieve trade history by transaction ID'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        tradeActionService := platform.TradeActionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := tradeActionService.GetTradeHistoryByTransactionIDShort(
            &trade_action.GetTradeHistoryByTransactionIDParams{
                Namespace:     ..., // string
                TransactionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'getTwitchIAPConfig'
    summary: 'Get twitch iap config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.GetTwitchIAPConfigShort(
            &iap.GetTwitchIAPConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'getUserActiveEntitlementsByItemIds'
    summary: 'Get user entitlements by itemIds'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.GetUserActiveEntitlementsByItemIdsShort(
            &entitlement.GetUserActiveEntitlementsByItemIdsParams{
                Namespace: ..., // string
                UserID:    ..., // string
                Ids:       ..., // []string
                Platform:  ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'getUserAppEntitlementByAppId'
    summary: 'Get user app entitlement by appId'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.GetUserAppEntitlementByAppIDShort(
            &entitlement.GetUserAppEntitlementByAppIDParams{
                Namespace:  ..., // string
                UserID:     ..., // string
                ActiveOnly: ..., // *bool
                AppID:      ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'getUserAppEntitlementOwnershipByAppId'
    summary: 'Get user app entitlement ownership by appId'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.GetUserAppEntitlementOwnershipByAppIDShort(
            &entitlement.GetUserAppEntitlementOwnershipByAppIDParams{
                Namespace: ..., // string
                UserID:    ..., // string
                AppID:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'DLC'
    name: 'getUserDLC'
    summary: 'Get user dlc records'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        dlcService := platform.DLCService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dlcService.GetUserDLCShort(
            &dlc.GetUserDLCParams{
                Namespace:            ..., // string
                UserID:               ..., // string
                IncludeAllNamespaces: ..., // *bool
                Status:               ..., // *string
                Type:                 ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'DLC'
    name: 'getUserDLCByPlatform'
    summary: 'Get user dlc by platform'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        dlcService := platform.DLCService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dlcService.GetUserDLCByPlatformShort(
            &dlc.GetUserDLCByPlatformParams{
                Namespace: ..., // string
                UserID:    ..., // string
                Type:      ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'getUserEntitlement'
    summary: 'Get user entitlement'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.GetUserEntitlementShort(
            &entitlement.GetUserEntitlementParams{
                EntitlementID: ..., // string
                Namespace:     ..., // string
                UserID:        ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'getUserEntitlementByItemId'
    summary: 'Get user entitlement by itemId'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.GetUserEntitlementByItemIDShort(
            &entitlement.GetUserEntitlementByItemIDParams{
                Namespace:        ..., // string
                UserID:           ..., // string
                ActiveOnly:       ..., // *bool
                EntitlementClazz: ..., // *string
                Platform:         ..., // *string
                ItemID:           ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'getUserEntitlementBySku'
    summary: 'Get user entitlement by sku'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.GetUserEntitlementBySkuShort(
            &entitlement.GetUserEntitlementBySkuParams{
                Namespace:        ..., // string
                UserID:           ..., // string
                ActiveOnly:       ..., // *bool
                EntitlementClazz: ..., // *string
                Platform:         ..., // *string
                Sku:              ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'getUserEntitlementHistories'
    summary: 'Get user entitlement history'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.GetUserEntitlementHistoriesShort(
            &entitlement.GetUserEntitlementHistoriesParams{
                EntitlementID: ..., // string
                Namespace:     ..., // string
                UserID:        ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'getUserEntitlementOwnershipByItemId'
    summary: 'Get user entitlement ownership by itemId'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.GetUserEntitlementOwnershipByItemIDShort(
            &entitlement.GetUserEntitlementOwnershipByItemIDParams{
                Namespace:        ..., // string
                UserID:           ..., // string
                EntitlementClazz: ..., // *string
                Platform:         ..., // *string
                ItemID:           ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'getUserEntitlementOwnershipByItemIds'
    summary: 'Get user entitlement ownership by itemIds'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.GetUserEntitlementOwnershipByItemIdsShort(
            &entitlement.GetUserEntitlementOwnershipByItemIdsParams{
                Namespace: ..., // string
                UserID:    ..., // string
                Ids:       ..., // []string
                Platform:  ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'getUserEntitlementOwnershipBySku'
    summary: 'Get user entitlement ownership by sku'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.GetUserEntitlementOwnershipBySkuShort(
            &entitlement.GetUserEntitlementOwnershipBySkuParams{
                Namespace:        ..., // string
                UserID:           ..., // string
                EntitlementClazz: ..., // *string
                Platform:         ..., // *string
                Sku:              ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'getUserEntitlementsByIds'
    summary: 'Get user entitlements by ids.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.GetUserEntitlementsByIdsShort(
            &entitlement.GetUserEntitlementsByIdsParams{
                Namespace: ..., // string
                UserID:    ..., // string
                Ids:       ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Order'
    name: 'getUserOrder'
    summary: 'Get an order'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        orderService := platform.OrderService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := orderService.GetUserOrderShort(
            &order.GetUserOrderParams{
                Namespace: ..., // string
                OrderNo:   ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Order'
    name: 'getUserOrderGrant'
    summary: 'Get user order grant'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        orderService := platform.OrderService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := orderService.GetUserOrderGrantShort(
            &order.GetUserOrderGrantParams{
                Namespace: ..., // string
                OrderNo:   ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Order'
    name: 'getUserOrderHistories'
    summary: 'Get user order histories'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        orderService := platform.OrderService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := orderService.GetUserOrderHistoriesShort(
            &order.GetUserOrderHistoriesParams{
                Namespace: ..., // string
                OrderNo:   ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Platform Account Closure'
    name: 'getUserPlatformAccountClosureHistories'
    summary: 'Get user platform account closure history'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        platformAccountClosureService := platform.PlatformAccountClosureService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := platformAccountClosureService.GetUserPlatformAccountClosureHistoriesShort(
            &platform_account_closure.GetUserPlatformAccountClosureHistoriesParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Subscription'
    name: 'getUserSubscription'
    summary: 'Get user subscription'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        subscriptionService := platform.SubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := subscriptionService.GetUserSubscriptionShort(
            &subscription.GetUserSubscriptionParams{
                Namespace:      ..., // string
                SubscriptionID: ..., // string
                UserID:         ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Subscription'
    name: 'getUserSubscriptionActivities'
    summary: 'Get user subscription activity'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        subscriptionService := platform.SubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := subscriptionService.GetUserSubscriptionActivitiesShort(
            &subscription.GetUserSubscriptionActivitiesParams{
                Namespace:      ..., // string
                UserID:         ..., // string
                ExcludeSystem:  ..., // *bool
                Limit:          ..., // *int32
                Offset:         ..., // *int32
                SubscriptionID: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Subscription'
    name: 'getUserSubscriptionBillingHistories'
    summary: 'Get user subscription billing histories'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        subscriptionService := platform.SubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := subscriptionService.GetUserSubscriptionBillingHistoriesShort(
            &subscription.GetUserSubscriptionBillingHistoriesParams{
                Namespace:      ..., // string
                SubscriptionID: ..., // string
                UserID:         ..., // string
                ExcludeFree:    ..., // *bool
                Limit:          ..., // *int32
                Offset:         ..., // *int32
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Wallet'
    name: 'getUserWallet'
    summary: 'Get a user wallet'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        walletService := platform.WalletService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := walletService.GetUserWalletShort(
            &wallet.GetUserWalletParams{
                Namespace: ..., // string
                UserID:    ..., // string
                WalletID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'View'
    name: 'getView'
    summary: 'Get a view'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        viewService := platform.ViewService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := viewService.GetViewShort(
            &view.GetViewParams{
                Namespace: ..., // string
                ViewID:    ..., // string
                StoreID:   ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Wallet'
    name: 'getWallet'
    summary: 'Get a wallet by wallet id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        walletService := platform.WalletService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := walletService.GetWalletShort(
            &wallet.GetWalletParams{
                Namespace: ..., // string
                WalletID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Wallet'
    name: 'getWalletConfig'
    summary: 'Get wallet config of a namespace'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        walletService := platform.WalletService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := walletService.GetWalletConfigShort(
            &wallet.GetWalletConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'getXblIAPConfig'
    summary: 'Get xbox iap config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.GetXblIAPConfigShort(
            &iap.GetXblIAPConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Achievement(Platform)'
    name: 'getXblUserAchievements'
    summary: 'Get xbox live user achievements.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        achievementPlatformService := platform.AchievementPlatformService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := achievementPlatformService.GetXblUserAchievementsShort(
            &achievement_platform.GetXblUserAchievementsParams{
                Namespace:  ..., // string
                UserID:     ..., // string
                XboxUserID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Platform'
    name: 'getXboxEntitlementOwnership'
    summary: 'Get Xbox entitlement ownership by product sku.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        platformService := platform.PlatformService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := platformService.GetXboxEntitlementOwnershipShort(
            &platform.GetXboxEntitlementOwnershipParams{
                Body:       ..., // *platformclientmodels.XblEntitlementOwnershipRequest
                Namespace:  ..., // string
                ProductSku: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Subscription'
    name: 'grantDaysToSubscription'
    summary: 'Grant days to a subscription'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        subscriptionService := platform.SubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := subscriptionService.GrantDaysToSubscriptionShort(
            &subscription.GrantDaysToSubscriptionParams{
                Body:           ..., // *platformclientmodels.GrantSubscriptionDaysRequest
                Namespace:      ..., // string
                SubscriptionID: ..., // string
                UserID:         ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'grantEntitlements'
    summary: 'Grant entitlements to different users'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.GrantEntitlementsShort(
            &entitlement.GrantEntitlementsParams{
                Body:      ..., // *platformclientmodels.BulkEntitlementGrantRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'grantUserEntitlement'
    summary: 'Grant user entitlement'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.GrantUserEntitlementShort(
            &entitlement.GrantUserEntitlementParams{
                Body:      ..., // []*platformclientmodels.EntitlementGrant
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Reward'
    name: 'importRewards'
    summary: 'Import reward configurations'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        rewardService := platform.RewardService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rewardService.ImportRewardsShort(
            &reward.ImportRewardsParams{
                File:            ..., // *[]byte
                Namespace:       ..., // string
                ReplaceExisting: ..., // bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Store'
    name: 'importStore'
    summary: 'Import a store'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        storeService := platform.StoreService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := storeService.ImportStoreShort(
            &store.ImportStoreParams{
                File:      ..., // *[]byte
                Namespace: ..., // string
                StoreID:   ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Store'
    name: 'importStore_1'
    summary: 'Import a store'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        storeService := platform.StoreService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := storeService.ImportStore1Short(
            &store.ImportStore1Params{
                File:       ..., // *[]byte
                Namespace:  ..., // string
                StoreID:    ..., // *string
                StrictMode: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Store'
    name: 'importStoreByCSV'
    summary: 'Import store using CSV format'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        storeService := platform.StoreService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := storeService.ImportStoreByCSVShort(
            &store.ImportStoreByCSVParams{
                Category:  ..., // *[]byte
                Display:   ..., // *[]byte
                Item:      ..., // *[]byte
                Notes:     ..., // *string
                Section:   ..., // *[]byte
                Namespace: ..., // string
                StoreID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Ticket'
    name: 'increaseTicketSale'
    summary: 'Increase ticket sale'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        ticketService := platform.TicketService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := ticketService.IncreaseTicketSaleShort(
            &ticket.IncreaseTicketSaleParams{
                Body:      ..., // *platformclientmodels.TicketSaleIncrementRequest
                BoothName: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'listBasicItemsByFeatures'
    summary: 'List basic items by features'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.ListBasicItemsByFeaturesShort(
            &item.ListBasicItemsByFeaturesParams{
                Namespace:  ..., // string
                ActiveOnly: ..., // *bool
                Features:   ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Category'
    name: 'listCategoriesBasic'
    summary: 'List categories'' basic info'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        categoryService := platform.CategoryService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := categoryService.ListCategoriesBasicShort(
            &category.ListCategoriesBasicParams{
                Namespace: ..., // string
                StoreID:   ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Currency'
    name: 'listCurrencies'
    summary: 'List currencies'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        currencyService := platform.CurrencyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := currencyService.ListCurrenciesShort(
            &currency.ListCurrenciesParams{
                Namespace:    ..., // string
                CurrencyType: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Payment'
    name: 'listExtOrderNoByExtTxId'
    summary: 'List external order No by external transaction id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentService := platform.PaymentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentService.ListExtOrderNoByExtTxIDShort(
            &payment.ListExtOrderNoByExtTxIDParams{
                Namespace: ..., // string
                ExtTxID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'FulfillmentScript'
    name: 'listFulfillmentScripts'
    summary: 'List all fulfillment scripts'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        fulfillmentScriptService := platform.FulfillmentScriptService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := fulfillmentScriptService.ListFulfillmentScriptsShort(
            &fulfillment_script.ListFulfillmentScriptsParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'listItemTypeConfigs'
    summary: 'Get all item type configs'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.ListItemTypeConfigsShort(
            &item.ListItemTypeConfigsParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'KeyGroup'
    name: 'listKeys'
    summary: 'List keys of a key group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        keyGroupService := platform.KeyGroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := keyGroupService.ListKeysShort(
            &key_group.ListKeysParams{
                KeyGroupID: ..., // string
                Namespace:  ..., // string
                Limit:      ..., // *int32
                Offset:     ..., // *int32
                Status:     ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Store'
    name: 'listStores'
    summary: 'List stores'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        storeService := platform.StoreService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := storeService.ListStoresShort(
            &store.ListStoresParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Wallet'
    name: 'listUserCurrencyTransactions'
    summary: 'List user currency transactions'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        walletService := platform.WalletService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := walletService.ListUserCurrencyTransactionsShort(
            &wallet.ListUserCurrencyTransactionsParams{
                CurrencyCode: ..., // string
                Namespace:    ..., // string
                UserID:       ..., // string
                Limit:        ..., // *int32
                Offset:       ..., // *int32
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Wallet'
    name: 'listUserWalletTransactions'
    summary: 'List user wallet transactions'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        walletService := platform.WalletService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := walletService.ListUserWalletTransactionsShort(
            &wallet.ListUserWalletTransactionsParams{
                Namespace: ..., // string
                UserID:    ..., // string
                WalletID:  ..., // string
                Limit:     ..., // *int32
                Offset:    ..., // *int32
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'View'
    name: 'listViews'
    summary: 'List all views'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        viewService := platform.ViewService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := viewService.ListViewsShort(
            &view.ListViewsParams{
                Namespace: ..., // string
                StoreID:   ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'mockFulfillIAPItem'
    summary: 'Mock fulfill iap item.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.MockFulfillIAPItemShort(
            &iap.MockFulfillIAPItemParams{
                Body:      ..., // *platformclientmodels.MockIAPReceipt
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Clawback'
    name: 'mockPlayStationStreamEvent'
    summary: 'Mock Sync PlayStation Clawback event.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        clawbackService := platform.ClawbackService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := clawbackService.MockPlayStationStreamEventShort(
            &clawback.MockPlayStationStreamEventParams{
                Body:      ..., // *platformclientmodels.StreamEvent
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Clawback'
    name: 'mockXblClawbackEvent'
    summary: 'Mock Sync XBox Clawback event.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        clawbackService := platform.ClawbackService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := clawbackService.MockXblClawbackEventShort(
            &clawback.MockXblClawbackEventParams{
                Body:      ..., // *platformclientmodels.XblClawbackEvent
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentStation'
    name: 'pay'
    summary: 'Do payment'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentStationService := platform.PaymentStationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentStationService.PayShort(
            &payment_station.PayParams{
                Body:            ..., // *platformclientmodels.PaymentToken
                Namespace:       ..., // string
                PaymentOrderNo:  ..., // string
                PaymentProvider: ..., // *string
                ZipCode:         ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Wallet'
    name: 'payWithUserWallet'
    summary: 'Pay with user wallet by currency code and client platform'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        walletService := platform.WalletService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := walletService.PayWithUserWalletShort(
            &wallet.PayWithUserWalletParams{
                Body:         ..., // *platformclientmodels.PaymentRequest
                CurrencyCode: ..., // string
                Namespace:    ..., // string
                UserID:       ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Subscription'
    name: 'platformSubscribeSubscription'
    summary: 'Free subscribe by platform'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        subscriptionService := platform.SubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := subscriptionService.PlatformSubscribeSubscriptionShort(
            &subscription.PlatformSubscribeSubscriptionParams{
                Body:      ..., // *platformclientmodels.PlatformSubscribeRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Fulfillment'
    name: 'preCheckFulfillItem'
    summary: 'Pre check fulfillment items'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        fulfillmentService := platform.FulfillmentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := fulfillmentService.PreCheckFulfillItemShort(
            &fulfillment.PreCheckFulfillItemParams{
                Body:      ..., // *platformclientmodels.PreCheckFulfillmentRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'preCheckRevokeUserEntitlementByUseCount'
    summary: 'Check if specified count of user entitlement can be revoked'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.PreCheckRevokeUserEntitlementByUseCountShort(
            &entitlement.PreCheckRevokeUserEntitlementByUseCountParams{
                EntitlementID: ..., // string
                Namespace:     ..., // string
                UserID:        ..., // string
                Quantity:      ..., // int32
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Order'
    name: 'processUserOrderNotification'
    summary: 'Web hook for payment notification'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        orderService := platform.OrderService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := orderService.ProcessUserOrderNotificationShort(
            &order.ProcessUserOrderNotificationParams{
                Body:      ..., // *platformclientmodels.TradeNotification
                Namespace: ..., // string
                OrderNo:   ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Subscription'
    name: 'processUserSubscriptionNotification'
    summary: 'Web hook for payment notification'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        subscriptionService := platform.SubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := subscriptionService.ProcessUserSubscriptionNotificationShort(
            &subscription.ProcessUserSubscriptionNotificationParams{
                Body:           ..., // *platformclientmodels.TradeNotification
                Namespace:      ..., // string
                SubscriptionID: ..., // string
                UserID:         ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'publicBulkGetItems'
    summary: 'Bulk get locale items'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.PublicBulkGetItemsShort(
            &item.PublicBulkGetItemsParams{
                Namespace:              ..., // string
                AutoCalcEstimatedPrice: ..., // *bool
                Language:               ..., // *string
                Region:                 ..., // *string
                StoreID:                ..., // *string
                ItemIds:                ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Subscription'
    name: 'publicCancelSubscription'
    summary: 'Cancel a subscription'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        subscriptionService := platform.SubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := subscriptionService.PublicCancelSubscriptionShort(
            &subscription.PublicCancelSubscriptionParams{
                Body:           ..., // *platformclientmodels.CancelRequest
                Namespace:      ..., // string
                SubscriptionID: ..., // string
                UserID:         ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Order'
    name: 'publicCancelUserOrder'
    summary: 'Cancel user order'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        orderService := platform.OrderService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := orderService.PublicCancelUserOrderShort(
            &order.PublicCancelUserOrderParams{
                Namespace: ..., // string
                OrderNo:   ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Subscription'
    name: 'publicChangeSubscriptionBillingAccount'
    summary: 'Request to change a subscription billing account'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        subscriptionService := platform.SubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := subscriptionService.PublicChangeSubscriptionBillingAccountShort(
            &subscription.PublicChangeSubscriptionBillingAccountParams{
                Namespace:      ..., // string
                SubscriptionID: ..., // string
                UserID:         ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentStation'
    name: 'publicCheckPaymentOrderPaidStatus'
    summary: 'Check payment order paid status'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentStationService := platform.PaymentStationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentStationService.PublicCheckPaymentOrderPaidStatusShort(
            &payment_station.PublicCheckPaymentOrderPaidStatusParams{
                Namespace:      ..., // string
                PaymentOrderNo: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Subscription'
    name: 'publicCheckUserSubscriptionSubscribableByItemId'
    summary: 'Check user subscription subscribable'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        subscriptionService := platform.SubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := subscriptionService.PublicCheckUserSubscriptionSubscribableByItemIDShort(
            &subscription.PublicCheckUserSubscriptionSubscribableByItemIDParams{
                Namespace: ..., // string
                UserID:    ..., // string
                ItemID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'publicConsumeUserEntitlement'
    summary: 'Consume user entitlement'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.PublicConsumeUserEntitlementShort(
            &entitlement.PublicConsumeUserEntitlementParams{
                Body:          ..., // *platformclientmodels.PublicEntitlementDecrement
                EntitlementID: ..., // string
                Namespace:     ..., // string
                UserID:        ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Order'
    name: 'publicCreateUserOrder'
    summary: 'Create an order'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        orderService := platform.OrderService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := orderService.PublicCreateUserOrderShort(
            &order.PublicCreateUserOrderParams{
                Body:      ..., // *platformclientmodels.OrderCreate
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentAccount'
    name: 'publicDeletePaymentAccount'
    summary: 'Delete payment account'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentAccountService := platform.PaymentAccountService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentAccountService.PublicDeletePaymentAccountShort(
            &payment_account.PublicDeletePaymentAccountParams{
                ID:        ..., // string
                Namespace: ..., // string
                Type:      ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Order'
    name: 'publicDownloadUserOrderReceipt'
    summary: 'Download user order receipt'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        orderService := platform.OrderService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := orderService.PublicDownloadUserOrderReceiptShort(
            &order.PublicDownloadUserOrderReceiptParams{
                Namespace: ..., // string
                OrderNo:   ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'publicExistsAnyMyActiveEntitlement'
    summary: 'Exists any my active entitlement'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.PublicExistsAnyMyActiveEntitlementShort(
            &entitlement.PublicExistsAnyMyActiveEntitlementParams{
                Namespace: ..., // string
                AppIds:    ..., // []string
                ItemIds:   ..., // []string
                Skus:      ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'publicExistsAnyUserActiveEntitlement'
    summary: 'Exists any user active entitlement'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.PublicExistsAnyUserActiveEntitlementShort(
            &entitlement.PublicExistsAnyUserActiveEntitlementParams{
                Namespace: ..., // string
                UserID:    ..., // string
                AppIds:    ..., // []string
                ItemIds:   ..., // []string
                Skus:      ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'publicFulfillAppleIAPItem'
    summary: 'Fulfill apple iap item.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.PublicFulfillAppleIAPItemShort(
            &iap.PublicFulfillAppleIAPItemParams{
                Body:      ..., // *platformclientmodels.AppleIAPReceipt
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'publicFulfillGoogleIAPItem'
    summary: 'Fulfill google iap item.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.PublicFulfillGoogleIAPItemShort(
            &iap.PublicFulfillGoogleIAPItemParams{
                Body:      ..., // *platformclientmodels.GoogleIAPReceipt
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'publicGetApp'
    summary: 'Get an app in locale'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.PublicGetAppShort(
            &item.PublicGetAppParams{
                ItemID:    ..., // string
                Namespace: ..., // string
                Language:  ..., // *string
                Region:    ..., // *string
                StoreID:   ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Category'
    name: 'publicGetCategory'
    summary: 'Get Category'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        categoryService := platform.CategoryService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := categoryService.PublicGetCategoryShort(
            &category.PublicGetCategoryParams{
                CategoryPath: ..., // string
                Namespace:    ..., // string
                Language:     ..., // *string
                StoreID:      ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Category'
    name: 'publicGetChildCategories'
    summary: 'Get Child Categories'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        categoryService := platform.CategoryService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := categoryService.PublicGetChildCategoriesShort(
            &category.PublicGetChildCategoriesParams{
                CategoryPath: ..., // string
                Namespace:    ..., // string
                Language:     ..., // *string
                StoreID:      ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Category'
    name: 'publicGetDescendantCategories'
    summary: 'Get Descendant Categories'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        categoryService := platform.CategoryService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := categoryService.PublicGetDescendantCategoriesShort(
            &category.PublicGetDescendantCategoriesParams{
                CategoryPath: ..., // string
                Namespace:    ..., // string
                Language:     ..., // *string
                StoreID:      ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'publicGetEntitlementOwnershipToken'
    summary: 'Gets an entitlement ownership token'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.PublicGetEntitlementOwnershipTokenShort(
            &entitlement.PublicGetEntitlementOwnershipTokenParams{
                Namespace: ..., // string
                AppIds:    ..., // []string
                ItemIds:   ..., // []string
                Skus:      ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'publicGetEstimatedPrice'
    summary: 'Get estimated price'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.PublicGetEstimatedPriceShort(
            &item.PublicGetEstimatedPriceParams{
                Namespace: ..., // string
                Region:    ..., // *string
                StoreID:   ..., // *string
                ItemIds:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'publicGetItem'
    summary: 'Get an item in locale'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.PublicGetItemShort(
            &item.PublicGetItemParams{
                ItemID:                 ..., // string
                Namespace:              ..., // string
                AutoCalcEstimatedPrice: ..., // *bool
                Language:               ..., // *string
                PopulateBundle:         ..., // *bool
                Region:                 ..., // *string
                StoreID:                ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'publicGetItemByAppId'
    summary: 'Get item by appId'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.PublicGetItemByAppIDShort(
            &item.PublicGetItemByAppIDParams{
                Namespace: ..., // string
                Language:  ..., // *string
                Region:    ..., // *string
                StoreID:   ..., // *string
                AppID:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'publicGetItemBySku'
    summary: 'Get item by sku'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.PublicGetItemBySkuShort(
            &item.PublicGetItemBySkuParams{
                Namespace:              ..., // string
                AutoCalcEstimatedPrice: ..., // *bool
                Language:               ..., // *string
                Region:                 ..., // *string
                StoreID:                ..., // *string
                Sku:                    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'publicGetItemDynamicData'
    summary: 'Get item dynamic data'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.PublicGetItemDynamicDataShort(
            &item.PublicGetItemDynamicDataParams{
                ItemID:    ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'publicGetMyAppEntitlementOwnershipByAppId'
    summary: 'Get my app entitlement ownership by appId'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.PublicGetMyAppEntitlementOwnershipByAppIDShort(
            &entitlement.PublicGetMyAppEntitlementOwnershipByAppIDParams{
                Namespace: ..., // string
                AppID:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'DLC'
    name: 'publicGetMyDLCContent'
    summary: 'Get user dlc reward contents'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        dlcService := platform.DLCService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dlcService.PublicGetMyDLCContentShort(
            &dlc.PublicGetMyDLCContentParams{
                IncludeAllNamespaces: ..., // *bool
                Type:                 ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'publicGetMyEntitlementOwnershipByItemId'
    summary: 'Get my entitlement ownership by itemId'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.PublicGetMyEntitlementOwnershipByItemIDShort(
            &entitlement.PublicGetMyEntitlementOwnershipByItemIDParams{
                Namespace:        ..., // string
                EntitlementClazz: ..., // *string
                ItemID:           ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'publicGetMyEntitlementOwnershipBySku'
    summary: 'Get my entitlement ownership by sku'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.PublicGetMyEntitlementOwnershipBySkuShort(
            &entitlement.PublicGetMyEntitlementOwnershipBySkuParams{
                Namespace:        ..., // string
                EntitlementClazz: ..., // *string
                Sku:              ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Wallet'
    name: 'publicGetMyWallet'
    summary: 'Get my wallet by currency code and namespace'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        walletService := platform.WalletService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := walletService.PublicGetMyWalletShort(
            &wallet.PublicGetMyWalletParams{
                CurrencyCode: ..., // string
                Namespace:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentAccount'
    name: 'publicGetPaymentAccounts'
    summary: 'Get payment accounts'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentAccountService := platform.PaymentAccountService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentAccountService.PublicGetPaymentAccountsShort(
            &payment_account.PublicGetPaymentAccountsParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentStation'
    name: 'publicGetPaymentMethods'
    summary: 'Get payment methods'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentStationService := platform.PaymentStationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentStationService.PublicGetPaymentMethodsShort(
            &payment_station.PublicGetPaymentMethodsParams{
                Namespace:      ..., // string
                PaymentOrderNo: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentStation'
    name: 'publicGetPaymentUrl'
    summary: 'Get payment url'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentStationService := platform.PaymentStationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentStationService.PublicGetPaymentURLShort(
            &payment_station.PublicGetPaymentURLParams{
                Body:      ..., // *platformclientmodels.PaymentURLCreate
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentStation'
    name: 'publicGetQRCode'
    summary: 'Get qrcode'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentStationService := platform.PaymentStationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentStationService.PublicGetQRCodeShort(
            &payment_station.PublicGetQRCodeParams{
                Namespace: ..., // string
                Code:      ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Category'
    name: 'publicGetRootCategories'
    summary: 'Get Root Categories'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        categoryService := platform.CategoryService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := categoryService.PublicGetRootCategoriesShort(
            &category.PublicGetRootCategoriesParams{
                Namespace: ..., // string
                Language:  ..., // *string
                StoreID:   ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentStation'
    name: 'publicGetUnpaidPaymentOrder'
    summary: 'Get payment order info'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentStationService := platform.PaymentStationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentStationService.PublicGetUnpaidPaymentOrderShort(
            &payment_station.PublicGetUnpaidPaymentOrderParams{
                Namespace:      ..., // string
                PaymentOrderNo: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'publicGetUserAppEntitlementByAppId'
    summary: 'Get user app entitlement by appId'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.PublicGetUserAppEntitlementByAppIDShort(
            &entitlement.PublicGetUserAppEntitlementByAppIDParams{
                Namespace: ..., // string
                UserID:    ..., // string
                AppID:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'publicGetUserAppEntitlementOwnershipByAppId'
    summary: 'Get user app entitlement ownership by appId'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.PublicGetUserAppEntitlementOwnershipByAppIDShort(
            &entitlement.PublicGetUserAppEntitlementOwnershipByAppIDParams{
                Namespace: ..., // string
                UserID:    ..., // string
                AppID:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'publicGetUserEntitlement'
    summary: 'Get user entitlement'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.PublicGetUserEntitlementShort(
            &entitlement.PublicGetUserEntitlementParams{
                EntitlementID: ..., // string
                Namespace:     ..., // string
                UserID:        ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'publicGetUserEntitlementByItemId'
    summary: 'Get user entitlement by itemId'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.PublicGetUserEntitlementByItemIDShort(
            &entitlement.PublicGetUserEntitlementByItemIDParams{
                Namespace:        ..., // string
                UserID:           ..., // string
                EntitlementClazz: ..., // *string
                ItemID:           ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'publicGetUserEntitlementBySku'
    summary: 'Get user entitlement by sku'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.PublicGetUserEntitlementBySkuShort(
            &entitlement.PublicGetUserEntitlementBySkuParams{
                Namespace:        ..., // string
                UserID:           ..., // string
                EntitlementClazz: ..., // *string
                Sku:              ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'publicGetUserEntitlementOwnershipByItemId'
    summary: 'Get user entitlement ownership by itemId'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.PublicGetUserEntitlementOwnershipByItemIDShort(
            &entitlement.PublicGetUserEntitlementOwnershipByItemIDParams{
                Namespace:        ..., // string
                UserID:           ..., // string
                EntitlementClazz: ..., // *string
                ItemID:           ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'publicGetUserEntitlementOwnershipByItemIds'
    summary: 'Get user entitlement ownership by itemIds'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.PublicGetUserEntitlementOwnershipByItemIdsShort(
            &entitlement.PublicGetUserEntitlementOwnershipByItemIdsParams{
                Namespace: ..., // string
                UserID:    ..., // string
                Ids:       ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'publicGetUserEntitlementOwnershipBySku'
    summary: 'Get user entitlement ownership by sku'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.PublicGetUserEntitlementOwnershipBySkuShort(
            &entitlement.PublicGetUserEntitlementOwnershipBySkuParams{
                Namespace:        ..., // string
                UserID:           ..., // string
                EntitlementClazz: ..., // *string
                Sku:              ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'publicGetUserEntitlementsByIds'
    summary: 'Get user entitlements by ids.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.PublicGetUserEntitlementsByIdsShort(
            &entitlement.PublicGetUserEntitlementsByIdsParams{
                Namespace:             ..., // string
                UserID:                ..., // string
                AvailablePlatformOnly: ..., // *bool
                Ids:                   ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Order'
    name: 'publicGetUserOrder'
    summary: 'Get user order'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        orderService := platform.OrderService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := orderService.PublicGetUserOrderShort(
            &order.PublicGetUserOrderParams{
                Namespace: ..., // string
                OrderNo:   ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Order'
    name: 'publicGetUserOrderHistories'
    summary: 'Get user order histories'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        orderService := platform.OrderService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := orderService.PublicGetUserOrderHistoriesShort(
            &order.PublicGetUserOrderHistoriesParams{
                Namespace: ..., // string
                OrderNo:   ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Subscription'
    name: 'publicGetUserSubscription'
    summary: 'Get user subscription'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        subscriptionService := platform.SubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := subscriptionService.PublicGetUserSubscriptionShort(
            &subscription.PublicGetUserSubscriptionParams{
                Namespace:      ..., // string
                SubscriptionID: ..., // string
                UserID:         ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Subscription'
    name: 'publicGetUserSubscriptionBillingHistories'
    summary: 'Get user subscription billing histories'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        subscriptionService := platform.SubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := subscriptionService.PublicGetUserSubscriptionBillingHistoriesShort(
            &subscription.PublicGetUserSubscriptionBillingHistoriesParams{
                Namespace:      ..., // string
                SubscriptionID: ..., // string
                UserID:         ..., // string
                ExcludeFree:    ..., // *bool
                Limit:          ..., // *int32
                Offset:         ..., // *int32
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Wallet'
    name: 'publicGetWallet'
    summary: 'Get a wallet by currency code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        walletService := platform.WalletService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := walletService.PublicGetWalletShort(
            &wallet.PublicGetWalletParams{
                CurrencyCode: ..., // string
                Namespace:    ..., // string
                UserID:       ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Section'
    name: 'publicListActiveSections'
    summary: 'List active section contents'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        sectionService := platform.SectionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := sectionService.PublicListActiveSectionsShort(
            &section.PublicListActiveSectionsParams{
                Namespace:              ..., // string
                UserID:                 ..., // string
                AutoCalcEstimatedPrice: ..., // *bool
                Language:               ..., // *string
                Region:                 ..., // *string
                StoreID:                ..., // *string
                ViewID:                 ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Currency'
    name: 'publicListCurrencies'
    summary: 'list currencies'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        currencyService := platform.CurrencyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := currencyService.PublicListCurrenciesShort(
            &currency.PublicListCurrenciesParams{
                Namespace:    ..., // string
                CurrencyType: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Store'
    name: 'publicListStores'
    summary: 'List all stores'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        storeService := platform.StoreService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := storeService.PublicListStoresShort(
            &store.PublicListStoresParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Wallet'
    name: 'publicListUserWalletTransactions'
    summary: 'List wallet transactions by currency code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        walletService := platform.WalletService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := walletService.PublicListUserWalletTransactionsShort(
            &wallet.PublicListUserWalletTransactionsParams{
                CurrencyCode: ..., // string
                Namespace:    ..., // string
                UserID:       ..., // string
                Limit:        ..., // *int32
                Offset:       ..., // *int32
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'View'
    name: 'publicListViews'
    summary: 'Get all views'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        viewService := platform.ViewService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := viewService.PublicListViewsShort(
            &view.PublicListViewsParams{
                Namespace: ..., // string
                UserID:    ..., // string
                Language:  ..., // *string
                StoreID:   ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentStation'
    name: 'publicNormalizePaymentReturnUrl'
    summary: 'Normalize payment return url'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentStationService := platform.PaymentStationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentStationService.PublicNormalizePaymentReturnURLShort(
            &payment_station.PublicNormalizePaymentReturnURLParams{
                Namespace:       ..., // string
                PayerID:         ..., // *string
                Foreinginvoice:  ..., // *string
                InvoiceID:       ..., // *string
                Payload:         ..., // *string
                RedirectResult:  ..., // *string
                ResultCode:      ..., // *string
                SessionID:       ..., // *string
                Status:          ..., // *string
                Token:           ..., // *string
                Type:            ..., // *string
                UserID:          ..., // *string
                OrderNo:         ..., // string
                PaymentOrderNo:  ..., // string
                PaymentProvider: ..., // string
                ReturnURL:       ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Order'
    name: 'publicPreviewOrderPrice'
    summary: 'Preview order price with discount code.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        orderService := platform.OrderService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := orderService.PublicPreviewOrderPriceShort(
            &order.PublicPreviewOrderPriceParams{
                Body:      ..., // *platformclientmodels.OrderDiscountPreviewRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'publicQueryItems'
    summary: 'Query items by criteria'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.PublicQueryItemsShort(
            &item.PublicQueryItemsParams{
                Namespace:              ..., // string
                AppType:                ..., // *string
                AutoCalcEstimatedPrice: ..., // *bool
                BaseAppID:              ..., // *string
                CategoryPath:           ..., // *string
                Features:               ..., // *string
                IncludeSubCategoryItem: ..., // *bool
                ItemType:               ..., // *string
                Language:               ..., // *string
                Limit:                  ..., // *int32
                Offset:                 ..., // *int32
                Region:                 ..., // *string
                SortBy:                 ..., // []string
                StoreID:                ..., // *string
                Tags:                   ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'publicQueryUserEntitlements'
    summary: 'Query user entitlements'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.PublicQueryUserEntitlementsShort(
            &entitlement.PublicQueryUserEntitlementsParams{
                Namespace:        ..., // string
                UserID:           ..., // string
                AppType:          ..., // *string
                EntitlementClazz: ..., // *string
                EntitlementName:  ..., // *string
                Features:         ..., // []string
                ItemID:           ..., // []string
                Limit:            ..., // *int32
                Offset:           ..., // *int32
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'publicQueryUserEntitlementsByAppType'
    summary: 'Query app entitlements by appType'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.PublicQueryUserEntitlementsByAppTypeShort(
            &entitlement.PublicQueryUserEntitlementsByAppTypeParams{
                Namespace: ..., // string
                UserID:    ..., // string
                Limit:     ..., // *int32
                Offset:    ..., // *int32
                AppType:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Order'
    name: 'publicQueryUserOrders'
    summary: 'Query user orders'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        orderService := platform.OrderService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := orderService.PublicQueryUserOrdersShort(
            &order.PublicQueryUserOrdersParams{
                Namespace:  ..., // string
                UserID:     ..., // string
                Discounted: ..., // *bool
                ItemID:     ..., // *string
                Limit:      ..., // *int32
                Offset:     ..., // *int32
                Status:     ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Subscription'
    name: 'publicQueryUserSubscriptions'
    summary: 'Query user subscriptions'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        subscriptionService := platform.SubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := subscriptionService.PublicQueryUserSubscriptionsShort(
            &subscription.PublicQueryUserSubscriptionsParams{
                Namespace:    ..., // string
                UserID:       ..., // string
                ChargeStatus: ..., // *string
                ItemID:       ..., // *string
                Limit:        ..., // *int32
                Offset:       ..., // *int32
                Sku:          ..., // *string
                Status:       ..., // *string
                SubscribedBy: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP(Subscription)'
    name: 'publicQueryUserThirdPartySubscription'
    summary: 'Query user subscription.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapSubscriptionService := platform.IAPSubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapSubscriptionService.PublicQueryUserThirdPartySubscriptionShort(
            &iap_subscription.PublicQueryUserThirdPartySubscriptionParams{
                Namespace:  ..., // string
                Platform:   ..., // string
                UserID:     ..., // string
                ActiveOnly: ..., // *bool
                GroupID:    ..., // *string
                Limit:      ..., // *int32
                Offset:     ..., // *int32
                ProductID:  ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'publicReconcilePlayStationStore'
    summary: 'Synchronize with entitlements in PSN Store.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.PublicReconcilePlayStationStoreShort(
            &iap.PublicReconcilePlayStationStoreParams{
                Body:      ..., // *platformclientmodels.PlayStationReconcileRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'publicReconcilePlayStationStoreWithMultipleServiceLabels'
    summary: 'Synchronize with entitlements in PSN Store with multiple service labels.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.PublicReconcilePlayStationStoreWithMultipleServiceLabelsShort(
            &iap.PublicReconcilePlayStationStoreWithMultipleServiceLabelsParams{
                Body:      ..., // *platformclientmodels.PlayStationMultiServiceLabelsReconcileRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Fulfillment'
    name: 'publicRedeemCode'
    summary: 'Redeem campaign code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        fulfillmentService := platform.FulfillmentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := fulfillmentService.PublicRedeemCodeShort(
            &fulfillment.PublicRedeemCodeParams{
                Body:      ..., // *platformclientmodels.FulfillCodeRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'publicSearchItems'
    summary: 'Search items by keyword'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.PublicSearchItemsShort(
            &item.PublicSearchItemsParams{
                Namespace:              ..., // string
                AutoCalcEstimatedPrice: ..., // *bool
                ItemType:               ..., // *string
                Limit:                  ..., // *int32
                Offset:                 ..., // *int32
                Region:                 ..., // *string
                StoreID:                ..., // *string
                Keyword:                ..., // string
                Language:               ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'publicSellUserEntitlement'
    summary: 'Sell user entitlement'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.PublicSellUserEntitlementShort(
            &entitlement.PublicSellUserEntitlementParams{
                Body:          ..., // *platformclientmodels.EntitlementSoldRequest
                EntitlementID: ..., // string
                Namespace:     ..., // string
                UserID:        ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'publicSplitUserEntitlement'
    summary: 'Split user entitlement'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.PublicSplitUserEntitlementShort(
            &entitlement.PublicSplitUserEntitlementParams{
                Body:          ..., // *platformclientmodels.EntitlementSplitRequest
                EntitlementID: ..., // string
                Namespace:     ..., // string
                UserID:        ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Subscription'
    name: 'publicSubscribeSubscription'
    summary: 'Subscribe a subscription'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        subscriptionService := platform.SubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := subscriptionService.PublicSubscribeSubscriptionShort(
            &subscription.PublicSubscribeSubscriptionParams{
                Body:      ..., // *platformclientmodels.SubscribeRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'DLC'
    name: 'publicSyncPsnDlcInventory'
    summary: 'Synchronize with dlc entitlements in PSN Store.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        dlcService := platform.DLCService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dlcService.PublicSyncPsnDLCInventoryShort(
            &dlc.PublicSyncPsnDLCInventoryParams{
                Body:      ..., // *platformclientmodels.PlayStationDLCSyncRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'DLC'
    name: 'publicSyncPsnDlcInventoryWithMultipleServiceLabels'
    summary: 'Synchronize with dlc entitlements in PSN Store with multiple service labels.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        dlcService := platform.DLCService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dlcService.PublicSyncPsnDLCInventoryWithMultipleServiceLabelsShort(
            &dlc.PublicSyncPsnDLCInventoryWithMultipleServiceLabelsParams{
                Body:      ..., // *platformclientmodels.PlayStationDLCSyncMultiServiceLabelsRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'publicTransferUserEntitlement'
    summary: 'Transfer user entitlement'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.PublicTransferUserEntitlementShort(
            &entitlement.PublicTransferUserEntitlementParams{
                Body:          ..., // *platformclientmodels.EntitlementTransferRequest
                EntitlementID: ..., // string
                Namespace:     ..., // string
                UserID:        ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'publicUserEntitlementHistory'
    summary: 'Get user entitlements histories.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.PublicUserEntitlementHistoryShort(
            &entitlement.PublicUserEntitlementHistoryParams{
                Namespace:        ..., // string
                UserID:           ..., // string
                EndDate:          ..., // *string
                EntitlementClazz: ..., // *string
                Limit:            ..., // *int32
                Offset:           ..., // *int32
                StartDate:        ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'publicValidateItemPurchaseCondition'
    summary: 'Validate user item purchase condition'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.PublicValidateItemPurchaseConditionShort(
            &item.PublicValidateItemPurchaseConditionParams{
                Body:      ..., // *platformclientmodels.ItemPurchaseConditionValidateRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'CatalogChanges'
    name: 'publishAll'
    summary: 'Publish all unpublished changes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        catalogChangesService := platform.CatalogChangesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := catalogChangesService.PublishAllShort(
            &catalog_changes.PublishAllParams{
                Namespace: ..., // string
                StoreID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'CatalogChanges'
    name: 'publishSelected'
    summary: 'Publish selected unpublished changes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        catalogChangesService := platform.CatalogChangesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := catalogChangesService.PublishSelectedShort(
            &catalog_changes.PublishSelectedParams{
                Namespace: ..., // string
                StoreID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Section'
    name: 'purgeExpiredSection'
    summary: 'purge expired section'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        sectionService := platform.SectionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := sectionService.PurgeExpiredSectionShort(
            &section.PurgeExpiredSectionParams{
                Namespace: ..., // string
                StoreID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'queryAbnormalTransactions'
    summary: 'Query steam abnormal transactions'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.QueryAbnormalTransactionsShort(
            &iap.QueryAbnormalTransactionsParams{
                Namespace: ..., // string
                Limit:     ..., // *int32
                Offset:    ..., // *int32
                OrderID:   ..., // *string
                SteamID:   ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'queryAllUserIAPOrders'
    summary: 'Query all user IAP orders'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.QueryAllUserIAPOrdersShort(
            &iap.QueryAllUserIAPOrdersParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Campaign'
    name: 'queryCampaignBatchNames'
    summary: 'Query campaign batch names'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        campaignService := platform.CampaignService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := campaignService.QueryCampaignBatchNamesShort(
            &campaign.QueryCampaignBatchNamesParams{
                CampaignID: ..., // string
                Namespace:  ..., // string
                BatchName:  ..., // *string
                Limit:      ..., // *int32
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Campaign'
    name: 'queryCampaigns'
    summary: 'Query campaigns'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        campaignService := platform.CampaignService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := campaignService.QueryCampaignsShort(
            &campaign.QueryCampaignsParams{
                Namespace: ..., // string
                Limit:     ..., // *int32
                Name:      ..., // *string
                Offset:    ..., // *int32
                Tag:       ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'CatalogChanges'
    name: 'queryChanges'
    summary: 'Query catalog changes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        catalogChangesService := platform.CatalogChangesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := catalogChangesService.QueryChangesShort(
            &catalog_changes.QueryChangesParams{
                Namespace:      ..., // string
                StoreID:        ..., // string
                Action:         ..., // *string
                ItemSku:        ..., // *string
                ItemType:       ..., // *string
                Limit:          ..., // *int32
                Offset:         ..., // *int32
                Selected:       ..., // *bool
                SortBy:         ..., // []string
                Status:         ..., // *string
                Type:           ..., // *string
                UpdatedAtEnd:   ..., // *string
                UpdatedAtStart: ..., // *string
                WithTotal:      ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Campaign'
    name: 'queryCodes'
    summary: 'Query codes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        campaignService := platform.CampaignService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := campaignService.QueryCodesShort(
            &campaign.QueryCodesParams{
                CampaignID:    ..., // string
                Namespace:     ..., // string
                ActiveOnly:    ..., // *bool
                BatchName:     ..., // *string
                BatchNo:       ..., // []int32
                Code:          ..., // *string
                Limit:         ..., // *int32
                Offset:        ..., // *int32
                WithBatchName: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'queryEntitlements'
    summary: 'Query entitlements by Item Ids'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.QueryEntitlementsShort(
            &entitlement.QueryEntitlementsParams{
                Namespace:  ..., // string
                ActiveOnly: ..., // *bool
                ItemIds:    ..., // []string
                Limit:      ..., // *int32
                Offset:     ..., // *int32
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'queryEntitlements_1'
    summary: 'Query entitlements'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.QueryEntitlements1Short(
            &entitlement.QueryEntitlements1Params{
                Namespace:        ..., // string
                ActiveOnly:       ..., // *bool
                AppType:          ..., // *string
                EntitlementClazz: ..., // *string
                EntitlementName:  ..., // *string
                ItemID:           ..., // []string
                Limit:            ..., // *int32
                Offset:           ..., // *int32
                Origin:           ..., // *string
                UserID:           ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Fulfillment'
    name: 'queryFulfillmentHistories'
    summary: 'Query fulfillment histories'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        fulfillmentService := platform.FulfillmentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := fulfillmentService.QueryFulfillmentHistoriesShort(
            &fulfillment.QueryFulfillmentHistoriesParams{
                Namespace: ..., // string
                Limit:     ..., // *int32
                Offset:    ..., // *int32
                Status:    ..., // *string
                UserID:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Fulfillment'
    name: 'queryFulfillments'
    summary: 'Query fulfillments'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        fulfillmentService := platform.FulfillmentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := fulfillmentService.QueryFulfillmentsShort(
            &fulfillment.QueryFulfillmentsParams{
                Namespace:     ..., // string
                EndTime:       ..., // *string
                Limit:         ..., // *int32
                Offset:        ..., // *int32
                StartTime:     ..., // *string
                State:         ..., // *string
                TransactionID: ..., // *string
                UserID:        ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Clawback'
    name: 'queryIAPClawbackHistory'
    summary: 'Query clawback history'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        clawbackService := platform.ClawbackService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := clawbackService.QueryIAPClawbackHistoryShort(
            &clawback.QueryIAPClawbackHistoryParams{
                Namespace:       ..., // string
                EndTime:         ..., // *string
                EventType:       ..., // *string
                ExternalOrderID: ..., // *string
                Limit:           ..., // *int32
                Offset:          ..., // *int32
                StartTime:       ..., // *string
                Status:          ..., // *string
                UserID:          ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Store'
    name: 'queryImportHistory'
    summary: 'Query import store history'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        storeService := platform.StoreService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := storeService.QueryImportHistoryShort(
            &store.QueryImportHistoryParams{
                Namespace: ..., // string
                StoreID:   ..., // string
                End:       ..., // *string
                Limit:     ..., // *int32
                Offset:    ..., // *int32
                SortBy:    ..., // *string
                Start:     ..., // *string
                Success:   ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'queryItemReferences'
    summary: 'Get item references'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.QueryItemReferencesShort(
            &item.QueryItemReferencesParams{
                ItemID:          ..., // string
                Namespace:       ..., // string
                FeaturesToCheck: ..., // []string
                StoreID:         ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'queryItems'
    summary: 'Query items by criteria'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.QueryItemsShort(
            &item.QueryItemsParams{
                Namespace:              ..., // string
                ActiveOnly:             ..., // *bool
                AppType:                ..., // *string
                AvailableDate:          ..., // *string
                BaseAppID:              ..., // *string
                CategoryPath:           ..., // *string
                Features:               ..., // *string
                IncludeSubCategoryItem: ..., // *bool
                ItemType:               ..., // *string
                Limit:                  ..., // *int32
                Offset:                 ..., // *int32
                Region:                 ..., // *string
                SortBy:                 ..., // []string
                StoreID:                ..., // *string
                Tags:                   ..., // *string
                TargetNamespace:        ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'queryItemsV2'
    summary: 'Query items by criteria'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.QueryItemsV2Short(
            &item.QueryItemsV2Params{
                Namespace:              ..., // string
                AppType:                ..., // *string
                AvailableDate:          ..., // *string
                BaseAppID:              ..., // *string
                CategoryPath:           ..., // *string
                Features:               ..., // *string
                IncludeSubCategoryItem: ..., // *bool
                ItemName:               ..., // *string
                ItemStatus:             ..., // *string
                ItemType:               ..., // []string
                Limit:                  ..., // *int32
                Offset:                 ..., // *int32
                Region:                 ..., // *string
                SectionExclusive:       ..., // *bool
                SortBy:                 ..., // []string
                StoreID:                ..., // *string
                Tags:                   ..., // *string
                TargetNamespace:        ..., // *string
                WithTotal:              ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'KeyGroup'
    name: 'queryKeyGroups'
    summary: 'Query key groups'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        keyGroupService := platform.KeyGroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := keyGroupService.QueryKeyGroupsShort(
            &key_group.QueryKeyGroupsParams{
                Namespace: ..., // string
                Limit:     ..., // *int32
                Name:      ..., // *string
                Offset:    ..., // *int32
                Tag:       ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Order'
    name: 'queryOrders'
    summary: 'Query orders'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        orderService := platform.OrderService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := orderService.QueryOrdersShort(
            &order.QueryOrdersParams{
                Namespace: ..., // string
                EndTime:   ..., // *string
                Limit:     ..., // *int32
                Offset:    ..., // *int32
                OrderNos:  ..., // []string
                SortBy:    ..., // *string
                StartTime: ..., // *string
                Status:    ..., // *string
                WithTotal: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Payment'
    name: 'queryPaymentNotifications'
    summary: 'Query payment notifications'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentService := platform.PaymentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentService.QueryPaymentNotificationsShort(
            &payment.QueryPaymentNotificationsParams{
                Namespace:          ..., // string
                EndDate:            ..., // *string
                ExternalID:         ..., // *string
                Limit:              ..., // *int32
                NotificationSource: ..., // *string
                NotificationType:   ..., // *string
                Offset:             ..., // *int32
                PaymentOrderNo:     ..., // *string
                StartDate:          ..., // *string
                Status:             ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Payment'
    name: 'queryPaymentOrders'
    summary: 'Query payment orders'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentService := platform.PaymentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentService.QueryPaymentOrdersShort(
            &payment.QueryPaymentOrdersParams{
                Namespace: ..., // string
                Channel:   ..., // *string
                ExtTxID:   ..., // *string
                Limit:     ..., // *int32
                Offset:    ..., // *int32
                Status:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'queryPaymentProviderConfig'
    summary: 'Query payment provider config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.QueryPaymentProviderConfigShort(
            &payment_config.QueryPaymentProviderConfigParams{
                Limit:     ..., // *int32
                Namespace: ..., // *string
                Offset:    ..., // *int32
                Region:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Campaign'
    name: 'queryRedeemHistory'
    summary: 'Query redeem history'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        campaignService := platform.CampaignService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := campaignService.QueryRedeemHistoryShort(
            &campaign.QueryRedeemHistoryParams{
                CampaignID: ..., // string
                Namespace:  ..., // string
                Code:       ..., // *string
                Limit:      ..., // *int32
                Offset:     ..., // *int32
                UserID:     ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Revocation'
    name: 'queryRevocationHistories'
    summary: 'Query revocation histories'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        revocationService := platform.RevocationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := revocationService.QueryRevocationHistoriesShort(
            &revocation.QueryRevocationHistoriesParams{
                Namespace:     ..., // string
                EndTime:       ..., // *string
                Limit:         ..., // *int32
                Offset:        ..., // *int32
                Source:        ..., // *string
                StartTime:     ..., // *string
                Status:        ..., // *string
                TransactionID: ..., // *string
                UserID:        ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Reward'
    name: 'queryRewards'
    summary: 'Query rewards by criteria'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        rewardService := platform.RewardService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rewardService.QueryRewardsShort(
            &reward.QueryRewardsParams{
                Namespace:  ..., // string
                EventTopic: ..., // *string
                Limit:      ..., // *int32
                Offset:     ..., // *int32
                SortBy:     ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Reward'
    name: 'queryRewards_1'
    summary: 'Query rewards by criteria'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        rewardService := platform.RewardService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rewardService.QueryRewards1Short(
            &reward.QueryRewards1Params{
                Namespace:  ..., // string
                EventTopic: ..., // *string
                Limit:      ..., // *int32
                Offset:     ..., // *int32
                SortBy:     ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Section'
    name: 'querySections'
    summary: 'List sections'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        sectionService := platform.SectionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := sectionService.QuerySectionsShort(
            &section.QuerySectionsParams{
                Namespace: ..., // string
                End:       ..., // *string
                Limit:     ..., // *int32
                Offset:    ..., // *int32
                Start:     ..., // *string
                StoreID:   ..., // *string
                ViewID:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'querySteamReportHistories'
    summary: 'Get IAP steam report process histories, default sort by created at'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.QuerySteamReportHistoriesShort(
            &iap.QuerySteamReportHistoriesParams{
                Namespace:     ..., // string
                Limit:         ..., // *int32
                Offset:        ..., // *int32
                OrderID:       ..., // *string
                ProcessStatus: ..., // *string
                SteamID:       ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Subscription'
    name: 'querySubscriptions'
    summary: 'Query subscriptions'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        subscriptionService := platform.SubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := subscriptionService.QuerySubscriptionsShort(
            &subscription.QuerySubscriptionsParams{
                Namespace:    ..., // string
                ChargeStatus: ..., // *string
                ItemID:       ..., // *string
                Limit:        ..., // *int32
                Offset:       ..., // *int32
                Sku:          ..., // *string
                Status:       ..., // *string
                SubscribedBy: ..., // *string
                UserID:       ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP(Notification)'
    name: 'queryThirdPartyNotifications'
    summary: 'Query third party notifications'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapNotificationService := platform.IAPNotificationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapNotificationService.QueryThirdPartyNotificationsShort(
            &iap_notification.QueryThirdPartyNotificationsParams{
                Namespace:  ..., // string
                EndDate:    ..., // *string
                ExternalID: ..., // *string
                Limit:      ..., // *int32
                Offset:     ..., // *int32
                Source:     ..., // *string
                StartDate:  ..., // *string
                Status:     ..., // *string
                Type:       ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP(Subscription)'
    name: 'queryThirdPartySubscription'
    summary: 'Query subscriptions.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapSubscriptionService := platform.IAPSubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapSubscriptionService.QueryThirdPartySubscriptionShort(
            &iap_subscription.QueryThirdPartySubscriptionParams{
                Namespace:  ..., // string
                ActiveOnly: ..., // *bool
                GroupID:    ..., // *string
                Limit:      ..., // *int32
                Offset:     ..., // *int32
                Platform:   ..., // *string
                ProductID:  ..., // *string
                UserID:     ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'queryUncategorizedItems'
    summary: 'Query uncategorized items'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.QueryUncategorizedItemsShort(
            &item.QueryUncategorizedItemsParams{
                Namespace:  ..., // string
                ActiveOnly: ..., // *bool
                Limit:      ..., // *int32
                Offset:     ..., // *int32
                SortBy:     ..., // []string
                StoreID:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Wallet'
    name: 'queryUserCurrencyWallets'
    summary: 'Get user currency wallet summary'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        walletService := platform.WalletService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := walletService.QueryUserCurrencyWalletsShort(
            &wallet.QueryUserCurrencyWalletsParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'queryUserEntitlements'
    summary: 'Query user entitlements'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.QueryUserEntitlementsShort(
            &entitlement.QueryUserEntitlementsParams{
                Namespace:        ..., // string
                UserID:           ..., // string
                ActiveOnly:       ..., // *bool
                AppType:          ..., // *string
                CollectionID:     ..., // *string
                EntitlementClazz: ..., // *string
                EntitlementName:  ..., // *string
                Features:         ..., // []string
                FuzzyMatchName:   ..., // *bool
                IgnoreActiveDate: ..., // *bool
                ItemID:           ..., // []string
                Limit:            ..., // *int32
                Offset:           ..., // *int32
                Origin:           ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'queryUserEntitlementsByAppType'
    summary: 'Query app entitlements by appType'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.QueryUserEntitlementsByAppTypeShort(
            &entitlement.QueryUserEntitlementsByAppTypeParams{
                Namespace:  ..., // string
                UserID:     ..., // string
                ActiveOnly: ..., // *bool
                Limit:      ..., // *int32
                Offset:     ..., // *int32
                AppType:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'queryUserIAPConsumeHistory'
    summary: 'Query IAP consume history'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.QueryUserIAPConsumeHistoryShort(
            &iap.QueryUserIAPConsumeHistoryParams{
                Namespace: ..., // string
                UserID:    ..., // string
                EndTime:   ..., // *string
                Limit:     ..., // *int32
                Offset:    ..., // *int32
                StartTime: ..., // *string
                Status:    ..., // *string
                Type:      ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'queryUserIAPOrders'
    summary: 'Query IAP orders'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.QueryUserIAPOrdersShort(
            &iap.QueryUserIAPOrdersParams{
                Namespace: ..., // string
                UserID:    ..., // string
                EndTime:   ..., // *string
                Limit:     ..., // *int32
                Offset:    ..., // *int32
                ProductID: ..., // *string
                StartTime: ..., // *string
                Status:    ..., // *string
                Type:      ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Order'
    name: 'queryUserOrders'
    summary: 'Query user orders'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        orderService := platform.OrderService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := orderService.QueryUserOrdersShort(
            &order.QueryUserOrdersParams{
                Namespace:  ..., // string
                UserID:     ..., // string
                Discounted: ..., // *bool
                ItemID:     ..., // *string
                Limit:      ..., // *int32
                Offset:     ..., // *int32
                Status:     ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Subscription'
    name: 'queryUserSubscriptions'
    summary: 'Query user subscriptions'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        subscriptionService := platform.SubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := subscriptionService.QueryUserSubscriptionsShort(
            &subscription.QueryUserSubscriptionsParams{
                Namespace:    ..., // string
                UserID:       ..., // string
                ChargeStatus: ..., // *string
                ItemID:       ..., // *string
                Limit:        ..., // *int32
                Offset:       ..., // *int32
                Sku:          ..., // *string
                Status:       ..., // *string
                SubscribedBy: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP(Subscription)'
    name: 'queryUserThirdPartySubscription'
    summary: 'Query user subscription.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapSubscriptionService := platform.IAPSubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapSubscriptionService.QueryUserThirdPartySubscriptionShort(
            &iap_subscription.QueryUserThirdPartySubscriptionParams{
                Namespace:  ..., // string
                UserID:     ..., // string
                ActiveOnly: ..., // *bool
                GroupID:    ..., // *string
                Limit:      ..., // *int32
                Offset:     ..., // *int32
                Platform:   ..., // *string
                ProductID:  ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP(Subscription)'
    name: 'queryUserThirdPartySubscriptionTransactions'
    summary: 'Admin query user subscription transactions.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapSubscriptionService := platform.IAPSubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapSubscriptionService.QueryUserThirdPartySubscriptionTransactionsShort(
            &iap_subscription.QueryUserThirdPartySubscriptionTransactionsParams{
                Namespace:  ..., // string
                UserID:     ..., // string
                ActiveOnly: ..., // *bool
                GroupID:    ..., // *string
                Limit:      ..., // *int32
                Offset:     ..., // *int32
                Platform:   ..., // *string
                ProductID:  ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Wallet'
    name: 'queryWallets'
    summary: 'Query wallets'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        walletService := platform.WalletService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := walletService.QueryWalletsShort(
            &wallet.QueryWalletsParams{
                Namespace:    ..., // string
                CurrencyCode: ..., // *string
                Limit:        ..., // *int32
                Offset:       ..., // *int32
                Origin:       ..., // *string
                UserID:       ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Subscription'
    name: 'recurringChargeSubscription'
    summary: 'Recurring charge subscription'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        subscriptionService := platform.SubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := subscriptionService.RecurringChargeSubscriptionShort(
            &subscription.RecurringChargeSubscriptionParams{
                Namespace:      ..., // string
                SubscriptionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Fulfillment'
    name: 'redeemCode'
    summary: 'Redeem campaign code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        fulfillmentService := platform.FulfillmentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := fulfillmentService.RedeemCodeShort(
            &fulfillment.RedeemCodeParams{
                Body:      ..., // *platformclientmodels.FulfillCodeRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Order'
    name: 'refundOrder'
    summary: 'Refund order'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        orderService := platform.OrderService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := orderService.RefundOrderShort(
            &order.RefundOrderParams{
                Body:      ..., // *platformclientmodels.OrderRefundCreate
                Namespace: ..., // string
                OrderNo:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Payment(Dedicated)'
    name: 'refundPaymentOrderByDedicated'
    summary: 'Refund payment order by dedicated server'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentDedicatedService := platform.PaymentDedicatedService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentDedicatedService.RefundPaymentOrderByDedicatedShort(
            &payment_dedicated.RefundPaymentOrderByDedicatedParams{
                Body:           ..., // *platformclientmodels.PaymentOrderRefund
                Namespace:      ..., // string
                PaymentOrderNo: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Payment'
    name: 'refundUserPaymentOrder'
    summary: 'Refund payment order'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentService := platform.PaymentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentService.RefundUserPaymentOrderShort(
            &payment.RefundUserPaymentOrderParams{
                Body:           ..., // *platformclientmodels.PaymentOrderRefund
                Namespace:      ..., // string
                PaymentOrderNo: ..., // string
                UserID:         ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Session(Platform)'
    name: 'registerXblSessions'
    summary: 'Register/Update a session'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        sessionPlatformService := platform.SessionPlatformService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := sessionPlatformService.RegisterXblSessionsShort(
            &session_platform.RegisterXblSessionsParams{
                Body:      ..., // *platformclientmodels.XblUserSessionRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Campaign'
    name: 'renameBatch'
    summary: 'Rename batch'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        campaignService := platform.CampaignService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := campaignService.RenameBatchShort(
            &campaign.RenameBatchParams{
                Body:       ..., // *platformclientmodels.CampaignBatchNameChange
                CampaignID: ..., // string
                Namespace:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Wallet'
    name: 'resetPlatformWalletConfig'
    summary: 'Reset platform wallet config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        walletService := platform.WalletService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := walletService.ResetPlatformWalletConfigShort(
            &wallet.ResetPlatformWalletConfigParams{
                Namespace: ..., // string
                Platform:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'DLC'
    name: 'restoreDLCItemConfigHistory'
    summary: 'Restore DLC item config history'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        dlcService := platform.DLCService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dlcService.RestoreDLCItemConfigHistoryShort(
            &dlc.RestoreDLCItemConfigHistoryParams{
                ID:        ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Fulfillment'
    name: 'retryFulfillItems'
    summary: 'Retry fulfill items by transactionId'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        fulfillmentService := platform.FulfillmentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := fulfillmentService.RetryFulfillItemsShort(
            &fulfillment.RetryFulfillItemsParams{
                Namespace:     ..., // string
                TransactionID: ..., // string
                UserID:        ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Fulfillment'
    name: 'retryFulfillItemsV3'
    summary: 'Retry fulfill items by transactionId'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        fulfillmentService := platform.FulfillmentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := fulfillmentService.RetryFulfillItemsV3Short(
            &fulfillment.RetryFulfillItemsV3Params{
                Namespace:     ..., // string
                TransactionID: ..., // string
                UserID:        ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'returnItem'
    summary: 'Return item'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.ReturnItemShort(
            &item.ReturnItemParams{
                Body:      ..., // *platformclientmodels.ItemReturnRequest
                ItemID:    ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'revokeAllEntitlements'
    summary: 'Revoke all entitlements of a user (This API is for testing purpose only)'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.RevokeAllEntitlementsShort(
            &entitlement.RevokeAllEntitlementsParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'revokeEntitlements'
    summary: 'Revoke entitlements by Ids'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.RevokeEntitlementsShort(
            &entitlement.RevokeEntitlementsParams{
                Body:      ..., // []string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Fulfillment'
    name: 'revokeItems'
    summary: 'Revoke items by transactionId'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        fulfillmentService := platform.FulfillmentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := fulfillmentService.RevokeItemsShort(
            &fulfillment.RevokeItemsParams{
                Namespace:     ..., // string
                TransactionID: ..., // string
                UserID:        ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Fulfillment'
    name: 'revokeItemsV3'
    summary: 'Revoke items by transactionId'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        fulfillmentService := platform.FulfillmentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := fulfillmentService.RevokeItemsV3Short(
            &fulfillment.RevokeItemsV3Params{
                Namespace:     ..., // string
                TransactionID: ..., // string
                UserID:        ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'revokeUseCount'
    summary: 'Revoke specified use count of user entitlement'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.RevokeUseCountShort(
            &entitlement.RevokeUseCountParams{
                Body:          ..., // *platformclientmodels.RevokeUseCountRequest
                EntitlementID: ..., // string
                Namespace:     ..., // string
                UserID:        ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'revokeUserEntitlement'
    summary: 'Revoke user entitlement'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.RevokeUserEntitlementShort(
            &entitlement.RevokeUserEntitlementParams{
                Body:          ..., // *platformclientmodels.EntitlementRevokeRequest
                EntitlementID: ..., // string
                Namespace:     ..., // string
                UserID:        ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'revokeUserEntitlementByUseCount'
    summary: 'Revoke specified count of user entitlement.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.RevokeUserEntitlementByUseCountShort(
            &entitlement.RevokeUserEntitlementByUseCountParams{
                Body:          ..., // *platformclientmodels.RevokeUseCountRequest
                EntitlementID: ..., // string
                Namespace:     ..., // string
                UserID:        ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'revokeUserEntitlements'
    summary: 'Revoke user''s entitlements by ids'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.RevokeUserEntitlementsShort(
            &entitlement.RevokeUserEntitlementsParams{
                Namespace:      ..., // string
                UserID:         ..., // string
                EntitlementIds: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Store'
    name: 'rollbackPublishedStore'
    summary: 'Rollback a published store'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        storeService := platform.StoreService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := storeService.RollbackPublishedStoreShort(
            &store.RollbackPublishedStoreParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'searchItems'
    summary: 'Search items by keyword'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.SearchItemsShort(
            &item.SearchItemsParams{
                Namespace:  ..., // string
                ActiveOnly: ..., // *bool
                ItemType:   ..., // *string
                Limit:      ..., // *int32
                Offset:     ..., // *int32
                SortBy:     ..., // *string
                StoreID:    ..., // *string
                Keyword:    ..., // string
                Language:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'searchItemTypeConfig'
    summary: 'Get an item type config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.SearchItemTypeConfigShort(
            &item.SearchItemTypeConfigParams{
                Clazz:    ..., // *string
                ItemType: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'CatalogChanges'
    name: 'selectAllRecords'
    summary: 'Select all changes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        catalogChangesService := platform.CatalogChangesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := catalogChangesService.SelectAllRecordsShort(
            &catalog_changes.SelectAllRecordsParams{
                Namespace: ..., // string
                StoreID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'CatalogChanges'
    name: 'selectAllRecordsByCriteria'
    summary: 'Select all changes by criteria'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        catalogChangesService := platform.CatalogChangesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := catalogChangesService.SelectAllRecordsByCriteriaShort(
            &catalog_changes.SelectAllRecordsByCriteriaParams{
                Namespace:      ..., // string
                StoreID:        ..., // string
                Action:         ..., // *string
                ItemSku:        ..., // *string
                ItemType:       ..., // *string
                Selected:       ..., // *bool
                Type:           ..., // *string
                UpdatedAtEnd:   ..., // *string
                UpdatedAtStart: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'CatalogChanges'
    name: 'selectRecord'
    summary: 'Select a change'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        catalogChangesService := platform.CatalogChangesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := catalogChangesService.SelectRecordShort(
            &catalog_changes.SelectRecordParams{
                ChangeID:  ..., // string
                Namespace: ..., // string
                StoreID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'sellUserEntitlement'
    summary: 'Sell user entitlement'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.SellUserEntitlementShort(
            &entitlement.SellUserEntitlementParams{
                Body:          ..., // *platformclientmodels.AdminEntitlementSoldRequest
                EntitlementID: ..., // string
                Namespace:     ..., // string
                UserID:        ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Payment'
    name: 'simulatePaymentOrderNotification'
    summary: 'Simulate payment notification'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentService := platform.PaymentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentService.SimulatePaymentOrderNotificationShort(
            &payment.SimulatePaymentOrderNotificationParams{
                Body:           ..., // *platformclientmodels.PaymentOrderNotifySimulation
                Namespace:      ..., // string
                PaymentOrderNo: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'DLC'
    name: 'syncEpicGameDLC'
    summary: 'Sync epic games dlc items'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        dlcService := platform.DLCService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dlcService.SyncEpicGameDLCShort(
            &dlc.SyncEpicGameDLCParams{
                Body:      ..., // *platformclientmodels.EpicGamesDLCSyncRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'syncEpicGamesInventory'
    summary: 'Sync epic games inventory.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.SyncEpicGamesInventoryShort(
            &iap.SyncEpicGamesInventoryParams{
                Body:      ..., // *platformclientmodels.EpicGamesReconcileRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'syncInGameItem'
    summary: 'Sync an in game item'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.SyncInGameItemShort(
            &item.SyncInGameItemParams{
                Body:      ..., // *platformclientmodels.InGameItemSync
                Namespace: ..., // string
                StoreID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'syncOculusConsumableEntitlements'
    summary: 'Sync Oculus entitlements.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.SyncOculusConsumableEntitlementsShort(
            &iap.SyncOculusConsumableEntitlementsParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'DLC'
    name: 'syncOculusDLC'
    summary: 'Sync oculus dlc.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        dlcService := platform.DLCService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dlcService.SyncOculusDLCShort(
            &dlc.SyncOculusDLCParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Order(Dedicated)'
    name: 'syncOrders'
    summary: 'Sync orders'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        orderDedicatedService := platform.OrderDedicatedService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := orderDedicatedService.SyncOrdersShort(
            &order_dedicated.SyncOrdersParams{
                NextEvaluatedKey: ..., // *string
                End:              ..., // string
                Start:            ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Payment(Dedicated)'
    name: 'syncPaymentOrders'
    summary: 'Sync payment orders'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentDedicatedService := platform.PaymentDedicatedService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentDedicatedService.SyncPaymentOrdersShort(
            &payment_dedicated.SyncPaymentOrdersParams{
                NextEvaluatedKey: ..., // *string
                End:              ..., // string
                Start:            ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'syncSteamAbnormalTransaction'
    summary: 'Sync Abnormal transaction, Sync steam order by transaction. only works when steam sync mode is TRANSACTION.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.SyncSteamAbnormalTransactionShort(
            &iap.SyncSteamAbnormalTransactionParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'DLC'
    name: 'syncSteamDLC'
    summary: 'Sync steam dlc.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        dlcService := platform.DLCService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dlcService.SyncSteamDLCShort(
            &dlc.SyncSteamDLCParams{
                Body:      ..., // *platformclientmodels.SteamDLCSyncRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'syncSteamIAPByTransaction'
    summary: 'Sync steam in app purchase by transaction.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.SyncSteamIAPByTransactionShort(
            &iap.SyncSteamIAPByTransactionParams{
                Body:      ..., // *platformclientmodels.SteamSyncByTransactionRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'syncSteamInventory'
    summary: 'Sync steam inventory.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.SyncSteamInventoryShort(
            &iap.SyncSteamInventoryParams{
                Body:      ..., // *platformclientmodels.SteamSyncRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP(Subscription)'
    name: 'syncSubscription'
    summary: 'Sync Subscription Status'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapSubscriptionService := platform.IAPSubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapSubscriptionService.SyncSubscriptionShort(
            &iap_subscription.SyncSubscriptionParams{
                ID:        ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP(Subscription)'
    name: 'syncSubscriptionTransaction'
    summary: 'Sync Subscription transaction'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapSubscriptionService := platform.IAPSubscriptionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapSubscriptionService.SyncSubscriptionTransactionShort(
            &iap_subscription.SyncSubscriptionTransactionParams{
                ID:        ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'syncTwitchDropsEntitlement'
    summary: 'Sync my game twitch drops entitlements.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.SyncTwitchDropsEntitlementShort(
            &iap.SyncTwitchDropsEntitlementParams{
                Body:      ..., // *platformclientmodels.TwitchSyncRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'syncTwitchDropsEntitlement_1'
    summary: 'Sync twitch drops entitlements.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.SyncTwitchDropsEntitlement1Short(
            &iap.SyncTwitchDropsEntitlement1Params{
                Body:      ..., // *platformclientmodels.TwitchSyncRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'DLC'
    name: 'syncXboxDLC'
    summary: 'Sync Xbox dlc items.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        dlcService := platform.DLCService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dlcService.SyncXboxDLCShort(
            &dlc.SyncXboxDLCParams{
                Body:      ..., // *platformclientmodels.XblDLCSyncRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'syncXboxInventory'
    summary: 'Sync Xbox inventory.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.SyncXboxInventoryShort(
            &iap.SyncXboxInventoryParams{
                Body:      ..., // *platformclientmodels.XblReconcileRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'testAdyenConfig'
    summary: 'Test adyen configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.TestAdyenConfigShort(
            &payment_config.TestAdyenConfigParams{
                Body:    ..., // *platformclientmodels.AdyenConfig
                Sandbox: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'testAdyenConfigById'
    summary: 'Test adyen configuration by id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.TestAdyenConfigByIDShort(
            &payment_config.TestAdyenConfigByIDParams{
                ID:      ..., // string
                Sandbox: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'testAliPayConfig'
    summary: 'Test Alipay configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.TestAliPayConfigShort(
            &payment_config.TestAliPayConfigParams{
                Body:    ..., // *platformclientmodels.AliPayConfig
                Sandbox: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'testAliPayConfigById'
    summary: 'Test Alipay configuration by id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.TestAliPayConfigByIDShort(
            &payment_config.TestAliPayConfigByIDParams{
                ID:      ..., // string
                Sandbox: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'testCheckoutConfig'
    summary: 'Test checkout.com configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.TestCheckoutConfigShort(
            &payment_config.TestCheckoutConfigParams{
                Body:    ..., // *platformclientmodels.CheckoutConfig
                Sandbox: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'testCheckoutConfigById'
    summary: 'Test checkout.com configuration by id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.TestCheckoutConfigByIDShort(
            &payment_config.TestCheckoutConfigByIDParams{
                ID:      ..., // string
                Sandbox: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'testNeonPayConfig'
    summary: 'Test Neon Pay configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.TestNeonPayConfigShort(
            &payment_config.TestNeonPayConfigParams{
                Body:    ..., // *platformclientmodels.NeonPayConfig
                Sandbox: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'testNeonPayConfigById'
    summary: 'Test Neon Pay configuration by id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.TestNeonPayConfigByIDShort(
            &payment_config.TestNeonPayConfigByIDParams{
                ID:      ..., // string
                Sandbox: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'testPayPalConfig'
    summary: 'Test PayPal configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.TestPayPalConfigShort(
            &payment_config.TestPayPalConfigParams{
                Body:    ..., // *platformclientmodels.PayPalConfig
                Sandbox: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'testPayPalConfigById'
    summary: 'Test PayPal configuration by id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.TestPayPalConfigByIDShort(
            &payment_config.TestPayPalConfigByIDParams{
                ID:      ..., // string
                Sandbox: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'testStripeConfig'
    summary: 'Test stripe configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.TestStripeConfigShort(
            &payment_config.TestStripeConfigParams{
                Body:    ..., // *platformclientmodels.StripeConfig
                Sandbox: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'testStripeConfigById'
    summary: 'Test stripe configuration by id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.TestStripeConfigByIDShort(
            &payment_config.TestStripeConfigByIDParams{
                ID:      ..., // string
                Sandbox: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'testWxPayConfig'
    summary: 'Test wxPay configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.TestWxPayConfigShort(
            &payment_config.TestWxPayConfigParams{
                Body: ..., // *platformclientmodels.WxPayConfigRequest
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'testWxPayConfigById'
    summary: 'Test wxPay configuration by id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.TestWxPayConfigByIDShort(
            &payment_config.TestWxPayConfigByIDParams{
                ID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'testXsollaConfig'
    summary: 'Test xsolla configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.TestXsollaConfigShort(
            &payment_config.TestXsollaConfigParams{
                Body: ..., // *platformclientmodels.XsollaConfig
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'testXsollaConfigById'
    summary: 'Test xsolla configuration by id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.TestXsollaConfigByIDShort(
            &payment_config.TestXsollaConfigByIDParams{
                ID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Achievement(Platform)'
    name: 'unlockSteamUserAchievement'
    summary: 'Unlock steam achievement.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        achievementPlatformService := platform.AchievementPlatformService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := achievementPlatformService.UnlockSteamUserAchievementShort(
            &achievement_platform.UnlockSteamUserAchievementParams{
                Body:      ..., // *platformclientmodels.SteamAchievementUpdateRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'CatalogChanges'
    name: 'unselectAllRecords'
    summary: 'Unselect all changes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        catalogChangesService := platform.CatalogChangesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := catalogChangesService.UnselectAllRecordsShort(
            &catalog_changes.UnselectAllRecordsParams{
                Namespace: ..., // string
                StoreID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'CatalogChanges'
    name: 'unselectRecord'
    summary: 'Unselect a change'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        catalogChangesService := platform.CatalogChangesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := catalogChangesService.UnselectRecordShort(
            &catalog_changes.UnselectRecordParams{
                ChangeID:  ..., // string
                Namespace: ..., // string
                StoreID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'updateAdyenConfig'
    summary: 'Update adyen config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.UpdateAdyenConfigShort(
            &payment_config.UpdateAdyenConfigParams{
                Body:     ..., // *platformclientmodels.AdyenConfig
                ID:       ..., // string
                Sandbox:  ..., // *bool
                Validate: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'updateAliPayConfig'
    summary: 'Update alipay configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.UpdateAliPayConfigShort(
            &payment_config.UpdateAliPayConfigParams{
                Body:     ..., // *platformclientmodels.AliPayConfig
                ID:       ..., // string
                Sandbox:  ..., // *bool
                Validate: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'updateApp'
    summary: 'Update an app'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.UpdateAppShort(
            &item.UpdateAppParams{
                Body:      ..., // *platformclientmodels.AppUpdate
                ItemID:    ..., // string
                Namespace: ..., // string
                StoreID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'updateAppleIAPConfig'
    summary: 'Update apple iap config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.UpdateAppleIAPConfigShort(
            &iap.UpdateAppleIAPConfigParams{
                Body:      ..., // *platformclientmodels.AppleIAPConfigRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'updateAppleP8File'
    summary: 'Upload Apple Store p8 file'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.UpdateAppleP8FileShort(
            &iap.UpdateAppleP8FileParams{
                File:      ..., // *[]byte
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Campaign'
    name: 'updateCampaign'
    summary: 'Update campaign'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        campaignService := platform.CampaignService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := campaignService.UpdateCampaignShort(
            &campaign.UpdateCampaignParams{
                Body:       ..., // *platformclientmodels.CampaignUpdate
                CampaignID: ..., // string
                Namespace:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Store'
    name: 'updateCatalogConfig'
    summary: 'Update catalog config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        storeService := platform.StoreService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := storeService.UpdateCatalogConfigShort(
            &store.UpdateCatalogConfigParams{
                Body:      ..., // *platformclientmodels.CatalogConfigUpdate
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Category'
    name: 'updateCategory'
    summary: 'Update category'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        categoryService := platform.CategoryService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := categoryService.UpdateCategoryShort(
            &category.UpdateCategoryParams{
                Body:         ..., // *platformclientmodels.CategoryUpdate
                CategoryPath: ..., // string
                Namespace:    ..., // string
                StoreID:      ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'updateCheckoutConfig'
    summary: 'Update checkout.com config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.UpdateCheckoutConfigShort(
            &payment_config.UpdateCheckoutConfigParams{
                Body:     ..., // *platformclientmodels.CheckoutConfig
                ID:       ..., // string
                Sandbox:  ..., // *bool
                Validate: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Currency'
    name: 'updateCurrency'
    summary: 'Update a currency'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        currencyService := platform.CurrencyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := currencyService.UpdateCurrencyShort(
            &currency.UpdateCurrencyParams{
                Body:         ..., // *platformclientmodels.CurrencyUpdate
                CurrencyCode: ..., // string
                Namespace:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'DLC'
    name: 'updateDLCItemConfig'
    summary: 'Update DLC item config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        dlcService := platform.DLCService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dlcService.UpdateDLCItemConfigShort(
            &dlc.UpdateDLCItemConfigParams{
                Body:      ..., // *platformclientmodels.DLCItemConfigUpdate
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'updateEpicGamesIAPConfig'
    summary: 'Update epic games iap config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.UpdateEpicGamesIAPConfigShort(
            &iap.UpdateEpicGamesIAPConfigParams{
                Body:      ..., // *platformclientmodels.EpicGamesIAPConfigRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'FulfillmentScript'
    name: 'updateFulfillmentScript'
    summary: 'Update fulfillment script'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        fulfillmentScriptService := platform.FulfillmentScriptService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := fulfillmentScriptService.UpdateFulfillmentScriptShort(
            &fulfillment_script.UpdateFulfillmentScriptParams{
                Body: ..., // *platformclientmodels.FulfillmentScriptUpdate
                ID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'updateGoogleIAPConfig'
    summary: 'Update google iap config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.UpdateGoogleIAPConfigShort(
            &iap.UpdateGoogleIAPConfigParams{
                Body:      ..., // *platformclientmodels.GoogleIAPConfigRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'updateGoogleP12File'
    summary: 'Upload google play p12 file'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.UpdateGoogleP12FileShort(
            &iap.UpdateGoogleP12FileParams{
                File:      ..., // *[]byte
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'updateIAPItemConfig'
    summary: 'Update iap item config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.UpdateIAPItemConfigShort(
            &iap.UpdateIAPItemConfigParams{
                Body:      ..., // *platformclientmodels.IAPItemConfigUpdate
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'updateItem'
    summary: 'Update an item'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.UpdateItemShort(
            &item.UpdateItemParams{
                Body:      ..., // *platformclientmodels.ItemUpdate
                ItemID:    ..., // string
                Namespace: ..., // string
                StoreID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'updateItemPurchaseCondition'
    summary: 'Update item purchase condition'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.UpdateItemPurchaseConditionShort(
            &item.UpdateItemPurchaseConditionParams{
                Body:      ..., // *platformclientmodels.PurchaseConditionUpdate
                ItemID:    ..., // string
                Namespace: ..., // string
                StoreID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'updateItemTypeConfig'
    summary: 'Update an Item Type Config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.UpdateItemTypeConfigShort(
            &item.UpdateItemTypeConfigParams{
                Body: ..., // *platformclientmodels.ItemTypeConfigUpdate
                ID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'KeyGroup'
    name: 'updateKeyGroup'
    summary: 'Update key group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        keyGroupService := platform.KeyGroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := keyGroupService.UpdateKeyGroupShort(
            &key_group.UpdateKeyGroupParams{
                Body:       ..., // *platformclientmodels.KeyGroupUpdate
                KeyGroupID: ..., // string
                Namespace:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'ServicePluginConfig'
    name: 'updateLootBoxPluginConfig'
    summary: 'Update lootbox plugin config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        servicePluginConfigService := platform.ServicePluginConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := servicePluginConfigService.UpdateLootBoxPluginConfigShort(
            &service_plugin_config.UpdateLootBoxPluginConfigParams{
                Body:      ..., // *platformclientmodels.LootBoxPluginConfigUpdate
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'updateNeonPayConfig'
    summary: 'Update Neon Pay config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.UpdateNeonPayConfigShort(
            &payment_config.UpdateNeonPayConfigParams{
                Body:     ..., // *platformclientmodels.NeonPayConfig
                ID:       ..., // string
                Sandbox:  ..., // *bool
                Validate: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'updateOculusIAPConfig'
    summary: 'Update oculus iap config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.UpdateOculusIAPConfigShort(
            &iap.UpdateOculusIAPConfigParams{
                Body:      ..., // *platformclientmodels.OculusIAPConfigRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentCallbackConfig'
    name: 'updatePaymentCallbackConfig'
    summary: 'Update payment callback configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentCallbackConfigService := platform.PaymentCallbackConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentCallbackConfigService.UpdatePaymentCallbackConfigShort(
            &payment_callback_config.UpdatePaymentCallbackConfigParams{
                Body:      ..., // *platformclientmodels.PaymentCallbackConfigUpdate
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'updatePaymentDomainWhitelistConfig'
    summary: 'Update payment domain whitelist config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.UpdatePaymentDomainWhitelistConfigShort(
            &payment_config.UpdatePaymentDomainWhitelistConfigParams{
                Body:      ..., // *platformclientmodels.PaymentDomainWhitelistConfigEdit
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'updatePaymentProviderConfig'
    summary: 'Update payment provider config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.UpdatePaymentProviderConfigShort(
            &payment_config.UpdatePaymentProviderConfigParams{
                Body: ..., // *platformclientmodels.PaymentProviderConfigEdit
                ID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'updatePaymentTaxConfig'
    summary: 'Update payment global tax config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.UpdatePaymentTaxConfigShort(
            &payment_config.UpdatePaymentTaxConfigParams{
                Body: ..., // *platformclientmodels.PaymentTaxConfigEdit
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'updatePayPalConfig'
    summary: 'Update PayPal config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.UpdatePayPalConfigShort(
            &payment_config.UpdatePayPalConfigParams{
                Body:     ..., // *platformclientmodels.PayPalConfig
                ID:       ..., // string
                Sandbox:  ..., // *bool
                Validate: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'DLC'
    name: 'updatePlatformDLCConfig'
    summary: 'Update Platform DLC config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        dlcService := platform.DLCService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dlcService.UpdatePlatformDLCConfigShort(
            &dlc.UpdatePlatformDLCConfigParams{
                Body:      ..., // *platformclientmodels.PlatformDLCConfigUpdate
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'updatePlatformEntitlementConfig'
    summary: 'Update platform entitlement config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.UpdatePlatformEntitlementConfigShort(
            &entitlement.UpdatePlatformEntitlementConfigParams{
                Body:      ..., // *platformclientmodels.EntitlementPlatformConfigUpdate
                Namespace: ..., // string
                Platform:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Wallet'
    name: 'updatePlatformWalletConfig'
    summary: 'Update platform wallet config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        walletService := platform.WalletService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := walletService.UpdatePlatformWalletConfigShort(
            &wallet.UpdatePlatformWalletConfigParams{
                Body:      ..., // *platformclientmodels.PlatformWalletConfigUpdate
                Namespace: ..., // string
                Platform:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'updatePlaystationIAPConfig'
    summary: 'Update playstation iap config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.UpdatePlaystationIAPConfigShort(
            &iap.UpdatePlaystationIAPConfigParams{
                Body:      ..., // *platformclientmodels.PlaystationIAPConfigRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Revocation'
    name: 'updateRevocationConfig'
    summary: 'Update revocation configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        revocationService := platform.RevocationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := revocationService.UpdateRevocationConfigShort(
            &revocation.UpdateRevocationConfigParams{
                Body:      ..., // *platformclientmodels.RevocationConfigUpdate
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'ServicePluginConfig'
    name: 'updateRevocationPluginConfig'
    summary: 'Update revocation plugin config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        servicePluginConfigService := platform.ServicePluginConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := servicePluginConfigService.UpdateRevocationPluginConfigShort(
            &service_plugin_config.UpdateRevocationPluginConfigParams{
                Body:      ..., // *platformclientmodels.RevocationPluginConfigUpdate
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Reward'
    name: 'updateReward'
    summary: 'Update a reward'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        rewardService := platform.RewardService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rewardService.UpdateRewardShort(
            &reward.UpdateRewardParams{
                Body:      ..., // *platformclientmodels.RewardUpdate
                Namespace: ..., // string
                RewardID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Section'
    name: 'updateSection'
    summary: 'Update s section'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        sectionService := platform.SectionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := sectionService.UpdateSectionShort(
            &section.UpdateSectionParams{
                Body:      ..., // *platformclientmodels.SectionUpdate
                Namespace: ..., // string
                SectionID: ..., // string
                StoreID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'ServicePluginConfig'
    name: 'updateSectionPluginConfig'
    summary: 'Update section plugin config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        servicePluginConfigService := platform.ServicePluginConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := servicePluginConfigService.UpdateSectionPluginConfigShort(
            &service_plugin_config.UpdateSectionPluginConfigParams{
                Body:      ..., // *platformclientmodels.SectionPluginConfigUpdate
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'ServicePluginConfig'
    name: 'updateServicePluginConfig'
    summary: 'Update service plugin config service'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        servicePluginConfigService := platform.ServicePluginConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := servicePluginConfigService.UpdateServicePluginConfigShort(
            &service_plugin_config.UpdateServicePluginConfigParams{
                Body:      ..., // *platformclientmodels.ServicePluginConfigUpdate
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'updateSteamIAPConfig'
    summary: 'Update steam iap config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.UpdateSteamIAPConfigShort(
            &iap.UpdateSteamIAPConfigParams{
                Body:      ..., // *platformclientmodels.SteamIAPConfigRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Store'
    name: 'updateStore'
    summary: 'Update a store'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        storeService := platform.StoreService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := storeService.UpdateStoreShort(
            &store.UpdateStoreParams{
                Body:      ..., // *platformclientmodels.StoreUpdate
                Namespace: ..., // string
                StoreID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'updateStripeConfig'
    summary: 'Update stripe config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.UpdateStripeConfigShort(
            &payment_config.UpdateStripeConfigParams{
                Body:     ..., // *platformclientmodels.StripeConfig
                ID:       ..., // string
                Sandbox:  ..., // *bool
                Validate: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'updateTwitchIAPConfig'
    summary: 'Update twitch iap config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.UpdateTwitchIAPConfigShort(
            &iap.UpdateTwitchIAPConfigParams{
                Body:      ..., // *platformclientmodels.TwitchIAPConfigRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Entitlement'
    name: 'updateUserEntitlement'
    summary: 'Update user entitlement'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        entitlementService := platform.EntitlementService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := entitlementService.UpdateUserEntitlementShort(
            &entitlement.UpdateUserEntitlementParams{
                Body:          ..., // *platformclientmodels.EntitlementUpdate
                EntitlementID: ..., // string
                Namespace:     ..., // string
                UserID:        ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Order'
    name: 'updateUserOrderStatus'
    summary: 'Update order status'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        orderService := platform.OrderService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := orderService.UpdateUserOrderStatusShort(
            &order.UpdateUserOrderStatusParams{
                Body:      ..., // *platformclientmodels.OrderUpdate
                Namespace: ..., // string
                OrderNo:   ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'View'
    name: 'updateView'
    summary: 'Update a view'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        viewService := platform.ViewService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := viewService.UpdateViewShort(
            &view.UpdateViewParams{
                Body:      ..., // *platformclientmodels.ViewUpdate
                Namespace: ..., // string
                ViewID:    ..., // string
                StoreID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Wallet'
    name: 'updateWalletConfig'
    summary: 'Update wallet config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        walletService := platform.WalletService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := walletService.UpdateWalletConfigShort(
            &wallet.UpdateWalletConfigParams{
                Body:      ..., // *platformclientmodels.WalletConfigUpdate
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'updateWxPayConfig'
    summary: 'Update wxpay configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.UpdateWxPayConfigShort(
            &payment_config.UpdateWxPayConfigParams{
                Body:     ..., // *platformclientmodels.WxPayConfigRequest
                ID:       ..., // string
                Validate: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'updateWxPayConfigCert'
    summary: 'Upload wxpay cert file'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.UpdateWxPayConfigCertShort(
            &payment_config.UpdateWxPayConfigCertParams{
                File: ..., // *[]byte
                ID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'updateXblBPCertFile'
    summary: 'Upload xbl business partner cert file'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.UpdateXblBPCertFileShort(
            &iap.UpdateXblBPCertFileParams{
                File:      ..., // *[]byte
                Password:  ..., // *string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'updateXblIAPConfig'
    summary: 'Update xbl iap config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.UpdateXblIAPConfigShort(
            &iap.UpdateXblIAPConfigParams{
                Body:      ..., // *platformclientmodels.XblIAPConfigRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Achievement(Platform)'
    name: 'updateXblUserAchievement'
    summary: 'Update xbox live achievements.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        achievementPlatformService := platform.AchievementPlatformService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := achievementPlatformService.UpdateXblUserAchievementShort(
            &achievement_platform.UpdateXblUserAchievementParams{
                Body:      ..., // *platformclientmodels.XblAchievementUpdateRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'updateXsollaConfig'
    summary: 'Update xsolla configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.UpdateXsollaConfigShort(
            &payment_config.UpdateXsollaConfigParams{
                Body:     ..., // *platformclientmodels.XsollaConfig
                ID:       ..., // string
                Validate: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'PaymentConfig'
    name: 'updateXsollaUIConfig'
    summary: 'Update xsolla UI configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        paymentConfigService := platform.PaymentConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := paymentConfigService.UpdateXsollaUIConfigShort(
            &payment_config.UpdateXsollaUIConfigParams{
                Body: ..., // *platformclientmodels.XsollaPaywallConfigRequest
                ID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'KeyGroup'
    name: 'uploadKeys'
    summary: 'Upload keys to key group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        keyGroupService := platform.KeyGroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := keyGroupService.UploadKeysShort(
            &key_group.UploadKeysParams{
                File:       ..., // *[]byte
                KeyGroupID: ..., // string
                Namespace:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'ServicePluginConfig'
    name: 'uploadRevocationPluginConfigCert'
    summary: 'Upload revocation plugin custom config tls cert'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        servicePluginConfigService := platform.ServicePluginConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := servicePluginConfigService.UploadRevocationPluginConfigCertShort(
            &service_plugin_config.UploadRevocationPluginConfigCertParams{
                File:      ..., // *[]byte
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'ServicePluginConfig'
    name: 'uploadSectionPluginConfigCert'
    summary: 'Upload section plugin custom config tls cert'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        servicePluginConfigService := platform.ServicePluginConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := servicePluginConfigService.UploadSectionPluginConfigCertShort(
            &service_plugin_config.UploadSectionPluginConfigCertParams{
                File:      ..., // *[]byte
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'ServicePluginConfig'
    name: 'uplodLootBoxPluginConfigCert'
    summary: 'Upload lootbox plugin custom config tls cert'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        servicePluginConfigService := platform.ServicePluginConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := servicePluginConfigService.UplodLootBoxPluginConfigCertShort(
            &service_plugin_config.UplodLootBoxPluginConfigCertParams{
                File:      ..., // *[]byte
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'V2PublicFulfillAppleIAPItem'
    summary: 'Fulfill apple iap item V2.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.V2PublicFulfillAppleIAPItemShort(
            &iap.V2PublicFulfillAppleIAPItemParams{
                Body:      ..., // *platformclientmodels.AppleIAPRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'validateExistedPlaystationIAPConfig'
    summary: 'Validate existed playstation iap config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.ValidateExistedPlaystationIAPConfigShort(
            &iap.ValidateExistedPlaystationIAPConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Item'
    name: 'validateItemPurchaseCondition'
    summary: 'Validate user purchase condition'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        itemService := platform.ItemService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := itemService.ValidateItemPurchaseConditionShort(
            &item.ValidateItemPurchaseConditionParams{
                Body:      ..., // *platformclientmodels.ItemPurchaseConditionValidateRequest
                Namespace: ..., // string
                Platform:  ..., // *string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'IAP'
    name: 'validatePlaystationIAPConfig'
    summary: 'Validate playstation iap config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewPlatformClient(configRepo)
        iapService := platform.IAPService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := iapService.ValidatePlaystationIAPConfigShort(
            &iap.ValidatePlaystationIAPConfigParams{
                Body:      ..., // *platformclientmodels.PlaystationIAPConfigRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }
# end platform
# begin reporting
  - ns: 'Admin Extension Categories and Auto Moderation Actions'
    name: 'adminCreateExtensionCategory'
    summary: 'Create Extension Category'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewReportingClient(configRepo)
        adminExtensionCategoriesAndAutoModerationActionsService := reporting.AdminExtensionCategoriesAndAutoModerationActionsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminExtensionCategoriesAndAutoModerationActionsService.AdminCreateExtensionCategoryShort(
            &admin_extension_categories_and_auto_moderation_actions.AdminCreateExtensionCategoryParams{
                Body: ..., // *reportingclientmodels.RestapiExtensionCategoryAPIRequest
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Extension Categories and Auto Moderation Actions'
    name: 'adminCreateModAction'
    summary: 'Create Auto Moderation Action data'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewReportingClient(configRepo)
        adminExtensionCategoriesAndAutoModerationActionsService := reporting.AdminExtensionCategoriesAndAutoModerationActionsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminExtensionCategoriesAndAutoModerationActionsService.AdminCreateModActionShort(
            &admin_extension_categories_and_auto_moderation_actions.AdminCreateModActionParams{
                Body: ..., // *reportingclientmodels.RestapiActionAPIRequest
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Extension Categories and Auto Moderation Actions'
    name: 'adminFindActionList'
    summary: 'Find Auto Moderation Action List'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewReportingClient(configRepo)
        adminExtensionCategoriesAndAutoModerationActionsService := reporting.AdminExtensionCategoriesAndAutoModerationActionsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminExtensionCategoriesAndAutoModerationActionsService.AdminFindActionListShort(
            &admin_extension_categories_and_auto_moderation_actions.AdminFindActionListParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Extension Categories and Auto Moderation Actions'
    name: 'adminFindExtensionCategoryList'
    summary: 'Find Extension Category List'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewReportingClient(configRepo)
        adminExtensionCategoriesAndAutoModerationActionsService := reporting.AdminExtensionCategoriesAndAutoModerationActionsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminExtensionCategoriesAndAutoModerationActionsService.AdminFindExtensionCategoryListShort(
            &admin_extension_categories_and_auto_moderation_actions.AdminFindExtensionCategoryListParams{
                Order:  ..., // *string
                SortBy: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Reasons'
    name: 'adminGetAllReasons'
    summary: 'Get all reasons'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewReportingClient(configRepo)
        adminReasonsService := reporting.AdminReasonsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminReasonsService.AdminGetAllReasonsShort(
            &admin_reasons.AdminGetAllReasonsParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Reasons'
    name: 'adminGetReason'
    summary: 'Get a single reason'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewReportingClient(configRepo)
        adminReasonsService := reporting.AdminReasonsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminReasonsService.AdminGetReasonShort(
            &admin_reasons.AdminGetReasonParams{
                Namespace: ..., // string
                ReasonID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Reasons'
    name: 'adminGetReasons'
    summary: 'Get list of reasons'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewReportingClient(configRepo)
        adminReasonsService := reporting.AdminReasonsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminReasonsService.AdminGetReasonsShort(
            &admin_reasons.AdminGetReasonsParams{
                Namespace: ..., // string
                Group:     ..., // *string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                Title:     ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Reasons'
    name: 'adminGetUnusedReasons'
    summary: 'Get list of reasons that not used by moderation rules'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewReportingClient(configRepo)
        adminReasonsService := reporting.AdminReasonsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminReasonsService.AdminGetUnusedReasonsShort(
            &admin_reasons.AdminGetUnusedReasonsParams{
                Namespace:         ..., // string
                ExtensionCategory: ..., // *string
                Category:          ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Reasons'
    name: 'adminListReasonGroups'
    summary: 'List reason groups under a namespace'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewReportingClient(configRepo)
        adminReasonsService := reporting.AdminReasonsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminReasonsService.AdminListReasonGroupsShort(
            &admin_reasons.AdminListReasonGroupsParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Reports'
    name: 'adminSubmitReport'
    summary: 'Submit a report by admin'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewReportingClient(configRepo)
        adminReportsService := reporting.AdminReportsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminReportsService.AdminSubmitReportShort(
            &admin_reports.AdminSubmitReportParams{
                Body:      ..., // *reportingclientmodels.RestapiSubmitReportRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Moderation Rule'
    name: 'createModerationRule'
    summary: 'Create auto moderation rule'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewReportingClient(configRepo)
        adminModerationRuleService := reporting.AdminModerationRuleService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminModerationRuleService.CreateModerationRuleShort(
            &admin_moderation_rule.CreateModerationRuleParams{
                Body:      ..., // *reportingclientmodels.RestapiModerationRuleRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Reasons'
    name: 'createReason'
    summary: 'Create a report reason'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewReportingClient(configRepo)
        adminReasonsService := reporting.AdminReasonsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminReasonsService.CreateReasonShort(
            &admin_reasons.CreateReasonParams{
                Body:      ..., // *reportingclientmodels.RestapiCreateReasonRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Reasons'
    name: 'createReasonGroup'
    summary: 'Create a reason group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewReportingClient(configRepo)
        adminReasonsService := reporting.AdminReasonsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminReasonsService.CreateReasonGroupShort(
            &admin_reasons.CreateReasonGroupParams{
                Body:      ..., // *reportingclientmodels.RestapiCreateReasonGroupRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Moderation Rule'
    name: 'deleteModerationRule'
    summary: 'Delete auto moderation rule'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewReportingClient(configRepo)
        adminModerationRuleService := reporting.AdminModerationRuleService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminModerationRuleService.DeleteModerationRuleShort(
            &admin_moderation_rule.DeleteModerationRuleParams{
                Namespace: ..., // string
                RuleID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Reasons'
    name: 'deleteReason'
    summary: 'Delete a report reason'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewReportingClient(configRepo)
        adminReasonsService := reporting.AdminReasonsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminReasonsService.DeleteReasonShort(
            &admin_reasons.DeleteReasonParams{
                Namespace: ..., // string
                ReasonID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Reasons'
    name: 'deleteReasonGroup'
    summary: 'Delete a reason group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewReportingClient(configRepo)
        adminReasonsService := reporting.AdminReasonsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminReasonsService.DeleteReasonGroupShort(
            &admin_reasons.DeleteReasonGroupParams{
                GroupID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Tickets'
    name: 'deleteTicket'
    summary: 'Delete single ticket'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewReportingClient(configRepo)
        adminTicketsService := reporting.AdminTicketsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminTicketsService.DeleteTicketShort(
            &admin_tickets.DeleteTicketParams{
                Namespace: ..., // string
                TicketID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Configurations'
    name: 'Get'
    summary: 'Get configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewReportingClient(configRepo)
        adminConfigurationsService := reporting.AdminConfigurationsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminConfigurationsService.GetShort(
            &admin_configurations.GetParams{
                Namespace: ..., // string
                Category:  ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Moderation Rule'
    name: 'getModerationRuleDetails'
    summary: 'Get auto moderation rule'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewReportingClient(configRepo)
        adminModerationRuleService := reporting.AdminModerationRuleService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminModerationRuleService.GetModerationRuleDetailsShort(
            &admin_moderation_rule.GetModerationRuleDetailsParams{
                Namespace: ..., // string
                RuleID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Moderation Rule'
    name: 'getModerationRules'
    summary: 'Get auto moderation rules'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewReportingClient(configRepo)
        adminModerationRuleService := reporting.AdminModerationRuleService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminModerationRuleService.GetModerationRulesShort(
            &admin_moderation_rule.GetModerationRulesParams{
                Namespace:         ..., // string
                Category:          ..., // *string
                ExtensionCategory: ..., // *string
                Limit:             ..., // *int64
                Offset:            ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Reasons'
    name: 'getReasonGroup'
    summary: 'Get reason group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewReportingClient(configRepo)
        adminReasonsService := reporting.AdminReasonsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminReasonsService.GetReasonGroupShort(
            &admin_reasons.GetReasonGroupParams{
                GroupID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Tickets'
    name: 'getReportsByTicket'
    summary: 'Get reports by ticket ID'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewReportingClient(configRepo)
        adminTicketsService := reporting.AdminTicketsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminTicketsService.GetReportsByTicketShort(
            &admin_tickets.GetReportsByTicketParams{
                Namespace: ..., // string
                TicketID:  ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Tickets'
    name: 'getTicketDetail'
    summary: 'Get single ticket'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewReportingClient(configRepo)
        adminTicketsService := reporting.AdminTicketsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminTicketsService.GetTicketDetailShort(
            &admin_tickets.GetTicketDetailParams{
                Namespace: ..., // string
                TicketID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Reports'
    name: 'listReports'
    summary: 'List reports'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewReportingClient(configRepo)
        adminReportsService := reporting.AdminReportsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminReportsService.ListReportsShort(
            &admin_reports.ListReportsParams{
                Namespace:      ..., // string
                Category:       ..., // *string
                Limit:          ..., // *int64
                Offset:         ..., // *int64
                ReportedUserID: ..., // *string
                SortBy:         ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Tickets'
    name: 'listTickets'
    summary: 'List report tickets'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewReportingClient(configRepo)
        adminTicketsService := reporting.AdminTicketsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminTicketsService.ListTicketsShort(
            &admin_tickets.ListTicketsParams{
                Namespace:         ..., // string
                Category:          ..., // *string
                ExtensionCategory: ..., // *string
                Limit:             ..., // *int64
                Offset:            ..., // *int64
                Order:             ..., // *string
                ReportedUserID:    ..., // *string
                SortBy:            ..., // *string
                Status:            ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Reasons'
    name: 'publicGetReasons'
    summary: 'Get list of reasons'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewReportingClient(configRepo)
        publicReasonsService := reporting.PublicReasonsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicReasonsService.PublicGetReasonsShort(
            &public_reasons.PublicGetReasonsParams{
                Namespace: ..., // string
                Group:     ..., // *string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                Title:     ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Reasons'
    name: 'publicListReasonGroups'
    summary: 'List reason groups under a namespace'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewReportingClient(configRepo)
        publicReasonsService := reporting.PublicReasonsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicReasonsService.PublicListReasonGroupsShort(
            &public_reasons.PublicListReasonGroupsParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Reports'
    name: 'submitReport'
    summary: 'Submit a report'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewReportingClient(configRepo)
        publicReportsService := reporting.PublicReportsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicReportsService.SubmitReportShort(
            &public_reports.SubmitReportParams{
                Body:      ..., // *reportingclientmodels.RestapiSubmitReportRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Tickets'
    name: 'ticketStatistic'
    summary: 'Ticket statistic'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewReportingClient(configRepo)
        adminTicketsService := reporting.AdminTicketsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminTicketsService.TicketStatisticShort(
            &admin_tickets.TicketStatisticParams{
                Namespace:         ..., // string
                ExtensionCategory: ..., // *string
                Category:          ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Moderation Rule'
    name: 'updateModerationRule'
    summary: 'Update auto moderation rule'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewReportingClient(configRepo)
        adminModerationRuleService := reporting.AdminModerationRuleService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminModerationRuleService.UpdateModerationRuleShort(
            &admin_moderation_rule.UpdateModerationRuleParams{
                Body:      ..., // *reportingclientmodels.RestapiModerationRuleRequest
                Namespace: ..., // string
                RuleID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Moderation Rule'
    name: 'updateModerationRuleStatus'
    summary: 'Enable/Disable auto moderation rule'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewReportingClient(configRepo)
        adminModerationRuleService := reporting.AdminModerationRuleService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminModerationRuleService.UpdateModerationRuleStatusShort(
            &admin_moderation_rule.UpdateModerationRuleStatusParams{
                Body:      ..., // *reportingclientmodels.RestapiModerationRuleActiveRequest
                Namespace: ..., // string
                RuleID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Reasons'
    name: 'updateReason'
    summary: 'Update a report reason'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewReportingClient(configRepo)
        adminReasonsService := reporting.AdminReasonsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminReasonsService.UpdateReasonShort(
            &admin_reasons.UpdateReasonParams{
                Body:      ..., // *reportingclientmodels.RestapiCreateReasonRequest
                Namespace: ..., // string
                ReasonID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Reasons'
    name: 'updateReasonGroup'
    summary: 'Update a reason group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewReportingClient(configRepo)
        adminReasonsService := reporting.AdminReasonsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminReasonsService.UpdateReasonGroupShort(
            &admin_reasons.UpdateReasonGroupParams{
                Body:      ..., // *reportingclientmodels.RestapiUpdateReasonGroupRequest
                GroupID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Tickets'
    name: 'updateTicketResolutions'
    summary: 'Update ticket resolution to a given status'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewReportingClient(configRepo)
        adminTicketsService := reporting.AdminTicketsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminTicketsService.UpdateTicketResolutionsShort(
            &admin_tickets.UpdateTicketResolutionsParams{
                Body:      ..., // *reportingclientmodels.RestapiUpdateTicketResolutionsRequest
                Namespace: ..., // string
                TicketID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Configurations'
    name: 'Upsert'
    summary: 'Create/Update configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewReportingClient(configRepo)
        adminConfigurationsService := reporting.AdminConfigurationsService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminConfigurationsService.UpsertShort(
            &admin_configurations.UpsertParams{
                Body:      ..., // *reportingclientmodels.RestapiReportingLimit
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }
# end reporting
# begin seasonpass
  - ns: 'Season'
    name: 'bulkGetUserSeasonProgression'
    summary: 'Bulk get user current season progression'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        seasonService := seasonpass.SeasonService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := seasonService.BulkGetUserSeasonProgressionShort(
            &season.BulkGetUserSeasonProgressionParams{
                Body:      ..., // *seasonpassclientmodels.BulkUserProgressionRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Season'
    name: 'checkSeasonPurchasable'
    summary: 'Check pass or tier purchasable'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        seasonService := seasonpass.SeasonService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := seasonService.CheckSeasonPurchasableShort(
            &season.CheckSeasonPurchasableParams{
                Body:      ..., // *seasonpassclientmodels.UserPurchasable
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Season'
    name: 'cloneSeason'
    summary: 'Clone a season'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        seasonService := seasonpass.SeasonService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := seasonService.CloneSeasonShort(
            &season.CloneSeasonParams{
                Body:      ..., // *seasonpassclientmodels.SeasonCloneRequest
                Namespace: ..., // string
                SeasonID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Pass'
    name: 'createPass'
    summary: 'Create a pass'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        passService := seasonpass.PassService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := passService.CreatePassShort(
            &pass.CreatePassParams{
                Body:      ..., // *seasonpassclientmodels.PassCreate
                Namespace: ..., // string
                SeasonID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Reward'
    name: 'createReward'
    summary: 'Create a reward'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        rewardService := seasonpass.RewardService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rewardService.CreateRewardShort(
            &reward.CreateRewardParams{
                Body:      ..., // *seasonpassclientmodels.RewardCreate
                Namespace: ..., // string
                SeasonID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Season'
    name: 'createSeason'
    summary: 'Create a season'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        seasonService := seasonpass.SeasonService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := seasonService.CreateSeasonShort(
            &season.CreateSeasonParams{
                Body:      ..., // *seasonpassclientmodels.SeasonCreate
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Tier'
    name: 'createTier'
    summary: 'Create tier'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        tierService := seasonpass.TierService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := tierService.CreateTierShort(
            &tier.CreateTierParams{
                Body:      ..., // *seasonpassclientmodels.TierCreate
                Namespace: ..., // string
                SeasonID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Pass'
    name: 'deletePass'
    summary: 'Delete a pass'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        passService := seasonpass.PassService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := passService.DeletePassShort(
            &pass.DeletePassParams{
                Code:      ..., // string
                Namespace: ..., // string
                SeasonID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Reward'
    name: 'deleteReward'
    summary: 'Delete a reward'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        rewardService := seasonpass.RewardService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rewardService.DeleteRewardShort(
            &reward.DeleteRewardParams{
                Code:      ..., // string
                Namespace: ..., // string
                SeasonID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Season'
    name: 'deleteSeason'
    summary: 'Delete a season'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        seasonService := seasonpass.SeasonService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := seasonService.DeleteSeasonShort(
            &season.DeleteSeasonParams{
                Namespace: ..., // string
                SeasonID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Tier'
    name: 'deleteTier'
    summary: 'Delete a tier'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        tierService := seasonpass.TierService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := tierService.DeleteTierShort(
            &tier.DeleteTierParams{
                ID:        ..., // string
                Namespace: ..., // string
                SeasonID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Season'
    name: 'existsAnyPassByPassCodes'
    summary: 'Get ownership for any pass codes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        seasonService := seasonpass.SeasonService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := seasonService.ExistsAnyPassByPassCodesShort(
            &season.ExistsAnyPassByPassCodesParams{
                Namespace: ..., // string
                UserID:    ..., // string
                PassCodes: ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Export'
    name: 'exportSeason'
    summary: 'export season service data'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        exportService := seasonpass.ExportService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := exportService.ExportSeasonShort(
            &export.ExportSeasonParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Season'
    name: 'getCurrentSeason'
    summary: 'Get current published season summary'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        seasonService := seasonpass.SeasonService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := seasonService.GetCurrentSeasonShort(
            &season.GetCurrentSeasonParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Season'
    name: 'getCurrentUserSeasonProgression'
    summary: 'Get current user season progression'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        seasonService := seasonpass.SeasonService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := seasonService.GetCurrentUserSeasonProgressionShort(
            &season.GetCurrentUserSeasonProgressionParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Season'
    name: 'getFullSeason'
    summary: 'Get a season full content'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        seasonService := seasonpass.SeasonService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := seasonService.GetFullSeasonShort(
            &season.GetFullSeasonParams{
                Namespace: ..., // string
                SeasonID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Utilities'
    name: 'getItemReferences'
    summary: 'Get season pass ecommerce item references'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        utilitiesService := seasonpass.UtilitiesService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := utilitiesService.GetItemReferencesShort(
            &utilities.GetItemReferencesParams{
                Namespace: ..., // string
                ItemID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Pass'
    name: 'getPass'
    summary: 'Get a pass'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        passService := seasonpass.PassService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := passService.GetPassShort(
            &pass.GetPassParams{
                Code:      ..., // string
                Namespace: ..., // string
                SeasonID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Reward'
    name: 'getReward'
    summary: 'Get a reward'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        rewardService := seasonpass.RewardService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rewardService.GetRewardShort(
            &reward.GetRewardParams{
                Code:      ..., // string
                Namespace: ..., // string
                SeasonID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Season'
    name: 'getSeason'
    summary: 'Get a season'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        seasonService := seasonpass.SeasonService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := seasonService.GetSeasonShort(
            &season.GetSeasonParams{
                Namespace: ..., // string
                SeasonID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Season'
    name: 'getUserParticipatedSeasons'
    summary: 'Get user participated season data'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        seasonService := seasonpass.SeasonService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := seasonService.GetUserParticipatedSeasonsShort(
            &season.GetUserParticipatedSeasonsParams{
                Namespace: ..., // string
                UserID:    ..., // string
                Limit:     ..., // *int32
                Offset:    ..., // *int32
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Season'
    name: 'getUserSeason'
    summary: 'Get user season data'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        seasonService := seasonpass.SeasonService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := seasonService.GetUserSeasonShort(
            &season.GetUserSeasonParams{
                Namespace: ..., // string
                SeasonID:  ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Tier'
    name: 'grantUserExp'
    summary: 'Grant exp to user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        tierService := seasonpass.TierService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := tierService.GrantUserExpShort(
            &tier.GrantUserExpParams{
                Body:      ..., // *seasonpassclientmodels.UserExpGrant
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Pass'
    name: 'grantUserPass'
    summary: 'Grant pass to user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        passService := seasonpass.PassService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := passService.GrantUserPassShort(
            &pass.GrantUserPassParams{
                Body:      ..., // *seasonpassclientmodels.UserPassGrant
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Tier'
    name: 'grantUserTier'
    summary: 'Grant tier to user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        tierService := seasonpass.TierService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := tierService.GrantUserTierShort(
            &tier.GrantUserTierParams{
                Body:      ..., // *seasonpassclientmodels.UserTierGrant
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Reward'
    name: 'publicBulkClaimUserRewards'
    summary: 'Bulk claim all remained rewards'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        rewardService := seasonpass.RewardService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rewardService.PublicBulkClaimUserRewardsShort(
            &reward.PublicBulkClaimUserRewardsParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Reward'
    name: 'publicClaimUserReward'
    summary: 'Claim tier reward'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        rewardService := seasonpass.RewardService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rewardService.PublicClaimUserRewardShort(
            &reward.PublicClaimUserRewardParams{
                Body:      ..., // *seasonpassclientmodels.UserRewardClaim
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Season'
    name: 'publicGetCurrentSeason'
    summary: 'Get current published season'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        seasonService := seasonpass.SeasonService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := seasonService.PublicGetCurrentSeasonShort(
            &season.PublicGetCurrentSeasonParams{
                Namespace: ..., // string
                Language:  ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Season'
    name: 'publicGetCurrentUserSeason'
    summary: 'Get current user season data'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        seasonService := seasonpass.SeasonService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := seasonService.PublicGetCurrentUserSeasonShort(
            &season.PublicGetCurrentUserSeasonParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Season'
    name: 'publicGetUserSeason'
    summary: 'Get user season data'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        seasonService := seasonpass.SeasonService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := seasonService.PublicGetUserSeasonShort(
            &season.PublicGetUserSeasonParams{
                Namespace: ..., // string
                SeasonID:  ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Season'
    name: 'publishSeason'
    summary: 'Publish a season'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        seasonService := seasonpass.SeasonService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := seasonService.PublishSeasonShort(
            &season.PublishSeasonParams{
                Namespace: ..., // string
                SeasonID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Pass'
    name: 'queryPasses'
    summary: 'Query all passes for a season'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        passService := seasonpass.PassService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := passService.QueryPassesShort(
            &pass.QueryPassesParams{
                Namespace: ..., // string
                SeasonID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Reward'
    name: 'queryRewards'
    summary: 'Query all rewards for a season'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        rewardService := seasonpass.RewardService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rewardService.QueryRewardsShort(
            &reward.QueryRewardsParams{
                Namespace: ..., // string
                SeasonID:  ..., // string
                Q:         ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Season'
    name: 'querySeasons'
    summary: 'Query seasons'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        seasonService := seasonpass.SeasonService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := seasonService.QuerySeasonsShort(
            &season.QuerySeasonsParams{
                Namespace: ..., // string
                Limit:     ..., // *int32
                Offset:    ..., // *int32
                Status:    ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Tier'
    name: 'queryTiers'
    summary: 'Query paginated tiers for a season'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        tierService := seasonpass.TierService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := tierService.QueryTiersShort(
            &tier.QueryTiersParams{
                Namespace: ..., // string
                SeasonID:  ..., // string
                Limit:     ..., // *int32
                Offset:    ..., // *int32
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Season'
    name: 'queryUserExpGrantHistory'
    summary: 'Query user season exp acquisition history'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        seasonService := seasonpass.SeasonService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := seasonService.QueryUserExpGrantHistoryShort(
            &season.QueryUserExpGrantHistoryParams{
                Namespace: ..., // string
                UserID:    ..., // string
                From:      ..., // *string
                Limit:     ..., // *int32
                Offset:    ..., // *int32
                SeasonID:  ..., // *string
                Source:    ..., // *string
                Tags:      ..., // []string
                To:        ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Season'
    name: 'queryUserExpGrantHistoryTag'
    summary: 'Query user season exp acquisition history''s reason tag list'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        seasonService := seasonpass.SeasonService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := seasonService.QueryUserExpGrantHistoryTagShort(
            &season.QueryUserExpGrantHistoryTagParams{
                Namespace: ..., // string
                UserID:    ..., // string
                SeasonID:  ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Tier'
    name: 'reorderTier'
    summary: 'Reorder a tier'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        tierService := seasonpass.TierService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := tierService.ReorderTierShort(
            &tier.ReorderTierParams{
                Body:      ..., // *seasonpassclientmodels.TierReorder
                ID:        ..., // string
                Namespace: ..., // string
                SeasonID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Season'
    name: 'resetUserSeason'
    summary: 'Reset user data in current season'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        seasonService := seasonpass.SeasonService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := seasonService.ResetUserSeasonShort(
            &season.ResetUserSeasonParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Season'
    name: 'retireSeason'
    summary: 'Retire a season'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        seasonService := seasonpass.SeasonService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := seasonService.RetireSeasonShort(
            &season.RetireSeasonParams{
                Namespace: ..., // string
                SeasonID:  ..., // string
                Force:     ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Season'
    name: 'unpublishSeason'
    summary: 'Unpublish a season'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        seasonService := seasonpass.SeasonService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := seasonService.UnpublishSeasonShort(
            &season.UnpublishSeasonParams{
                Namespace: ..., // string
                SeasonID:  ..., // string
                Force:     ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Pass'
    name: 'updatePass'
    summary: 'Update a pass'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        passService := seasonpass.PassService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := passService.UpdatePassShort(
            &pass.UpdatePassParams{
                Body:      ..., // *seasonpassclientmodels.PassUpdate
                Code:      ..., // string
                Namespace: ..., // string
                SeasonID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Reward'
    name: 'updateReward'
    summary: 'Update a reward'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        rewardService := seasonpass.RewardService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := rewardService.UpdateRewardShort(
            &reward.UpdateRewardParams{
                Body:      ..., // *seasonpassclientmodels.RewardUpdate
                Code:      ..., // string
                Namespace: ..., // string
                SeasonID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Season'
    name: 'updateSeason'
    summary: 'Update a season'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        seasonService := seasonpass.SeasonService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := seasonService.UpdateSeasonShort(
            &season.UpdateSeasonParams{
                Body:      ..., // *seasonpassclientmodels.SeasonUpdate
                Namespace: ..., // string
                SeasonID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Tier'
    name: 'updateTier'
    summary: 'Update a tier'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSeasonpassClient(configRepo)
        tierService := seasonpass.TierService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := tierService.UpdateTierShort(
            &tier.UpdateTierParams{
                Body:      ..., // *seasonpassclientmodels.TierInput
                ID:        ..., // string
                Namespace: ..., // string
                SeasonID:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }
# end seasonpass
# begin session
  - ns: 'Configuration Template'
    name: 'adminCreateConfigurationAlertV1'
    summary: 'Create configuration alert.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        configurationTemplateService := session.ConfigurationTemplateService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationTemplateService.AdminCreateConfigurationAlertV1Short(
            &configuration_template.AdminCreateConfigurationAlertV1Params{
                Body:      ..., // *sessionclientmodels.ApimodelsConfigAlertRequestCreate
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration Template'
    name: 'adminCreateConfigurationTemplateV1'
    summary: 'Create configuration template.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        configurationTemplateService := session.ConfigurationTemplateService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationTemplateService.AdminCreateConfigurationTemplateV1Short(
            &configuration_template.AdminCreateConfigurationTemplateV1Params{
                Body:      ..., // *sessionclientmodels.ApimodelsCreateConfigurationTemplateRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Game Session'
    name: 'adminDeleteBulkGameSessions'
    summary: 'Delete bulk game sessions.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        gameSessionService := session.GameSessionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameSessionService.AdminDeleteBulkGameSessionsShort(
            &game_session.AdminDeleteBulkGameSessionsParams{
                Body:      ..., // *sessionclientmodels.ApimodelsDeleteBulkGameSessionRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Party'
    name: 'adminDeleteBulkParties'
    summary: 'Delete bulk parties.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        partyService := session.PartyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := partyService.AdminDeleteBulkPartiesShort(
            &party.AdminDeleteBulkPartiesParams{
                Body:      ..., // *sessionclientmodels.ApimodelsDeleteBulkPartySessionRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration Template'
    name: 'adminDeleteConfigurationAlertV1'
    summary: 'Delete configuration alert.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        configurationTemplateService := session.ConfigurationTemplateService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationTemplateService.AdminDeleteConfigurationAlertV1Short(
            &configuration_template.AdminDeleteConfigurationAlertV1Params{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration Template'
    name: 'adminDeleteConfigurationTemplateV1'
    summary: 'Delete configuration template.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        configurationTemplateService := session.ConfigurationTemplateService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationTemplateService.AdminDeleteConfigurationTemplateV1Short(
            &configuration_template.AdminDeleteConfigurationTemplateV1Params{
                Name:      ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Global Configuration'
    name: 'adminDeleteGlobalConfiguration'
    summary: 'Delete of global configuration data.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        globalConfigurationService := session.GlobalConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := globalConfigurationService.AdminDeleteGlobalConfigurationShort(
            &global_configuration.AdminDeleteGlobalConfigurationParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Platform Credential'
    name: 'adminDeletePlatformCredentials'
    summary: 'Delete platform credentials.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        platformCredentialService := session.PlatformCredentialService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := platformCredentialService.AdminDeletePlatformCredentialsShort(
            &platform_credential.AdminDeletePlatformCredentialsParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Platform Credential'
    name: 'adminDeletePlatformCredentialsByPlatformId'
    summary: 'Delete platform credentials by platformId'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        platformCredentialService := session.PlatformCredentialService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := platformCredentialService.AdminDeletePlatformCredentialsByPlatformIDShort(
            &platform_credential.AdminDeletePlatformCredentialsByPlatformIDParams{
                Namespace:  ..., // string
                PlatformID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Session Storage'
    name: 'adminDeleteUserSessionStorage'
    summary: 'Delete Session Storage.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        sessionStorageService := session.SessionStorageService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := sessionStorageService.AdminDeleteUserSessionStorageShort(
            &session_storage.AdminDeleteUserSessionStorageParams{
                Namespace: ..., // string
                SessionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration Template'
    name: 'adminGetAllConfigurationTemplatesV1'
    summary: 'Get configuration templates.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        configurationTemplateService := session.ConfigurationTemplateService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationTemplateService.AdminGetAllConfigurationTemplatesV1Short(
            &configuration_template.AdminGetAllConfigurationTemplatesV1Params{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Name:      ..., // *string
                Offset:    ..., // *int64
                Order:     ..., // *string
                OrderBy:   ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration Template'
    name: 'adminGetConfigurationAlertV1'
    summary: 'Get configuration alert.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        configurationTemplateService := session.ConfigurationTemplateService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationTemplateService.AdminGetConfigurationAlertV1Short(
            &configuration_template.AdminGetConfigurationAlertV1Params{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration Template'
    name: 'adminGetConfigurationTemplateV1'
    summary: 'Get configuration template.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        configurationTemplateService := session.ConfigurationTemplateService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationTemplateService.AdminGetConfigurationTemplateV1Short(
            &configuration_template.AdminGetConfigurationTemplateV1Params{
                Name:      ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration Template'
    name: 'adminGetDSMCConfiguration'
    summary: 'Get dsmc configuration .'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        configurationTemplateService := session.ConfigurationTemplateService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationTemplateService.AdminGetDSMCConfigurationShort(
            &configuration_template.AdminGetDSMCConfigurationParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'DSMC Default Configuration'
    name: 'adminGetDSMCConfigurationDefault'
    summary: 'Get dsmc default configuration.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        dSMCDefaultConfigurationService := session.DSMCDefaultConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := dSMCDefaultConfigurationService.AdminGetDSMCConfigurationDefaultShort(
            &d_s_m_c_default_configuration.AdminGetDSMCConfigurationDefaultParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Native Session'
    name: 'adminGetListNativeSession'
    summary: 'List of native sessions.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        nativeSessionService := session.NativeSessionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := nativeSessionService.AdminGetListNativeSessionShort(
            &native_session.AdminGetListNativeSessionParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                Order:     ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Config'
    name: 'adminGetLogConfig'
    summary: 'Get Log Configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        configService := session.ConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configService.AdminGetLogConfigShort(
            &config.AdminGetLogConfigParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Max Active'
    name: 'adminGetMemberActiveSession'
    summary: 'Get Member Active Session.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        maxActiveService := session.MaxActiveService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := maxActiveService.AdminGetMemberActiveSessionShort(
            &max_active.AdminGetMemberActiveSessionParams{
                Name:      ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Platform Credential'
    name: 'adminGetPlatformCredentials'
    summary: 'Get platform credentials. These are used to sync PSN sessions.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        platformCredentialService := session.PlatformCredentialService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := platformCredentialService.AdminGetPlatformCredentialsShort(
            &platform_credential.AdminGetPlatformCredentialsParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Player'
    name: 'adminGetPlayerAttributes'
    summary: 'Get player attributes.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        playerService := session.PlayerService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := playerService.AdminGetPlayerAttributesShort(
            &player.AdminGetPlayerAttributesParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Recent Player'
    name: 'adminGetRecentPlayer'
    summary: 'Query recent player with given user id.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        recentPlayerService := session.RecentPlayerService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := recentPlayerService.AdminGetRecentPlayerShort(
            &recent_player.AdminGetRecentPlayerParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                UserID:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Recent Player'
    name: 'adminGetRecentTeamPlayer'
    summary: 'Query recent player who were on the same team with given user id.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        recentPlayerService := session.RecentPlayerService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := recentPlayerService.AdminGetRecentTeamPlayerShort(
            &recent_player.AdminGetRecentTeamPlayerParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                UserID:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Game Session'
    name: 'adminKickGameSessionMember'
    summary: 'Kick member from a game session.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        gameSessionService := session.GameSessionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameSessionService.AdminKickGameSessionMemberShort(
            &game_session.AdminKickGameSessionMemberParams{
                MemberID:  ..., // string
                Namespace: ..., // string
                SessionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Environment Variable'
    name: 'adminListEnvironmentVariables'
    summary: 'List of environment variables.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        environmentVariableService := session.EnvironmentVariableService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := environmentVariableService.AdminListEnvironmentVariablesShort(
            &environment_variable.AdminListEnvironmentVariablesParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Global Configuration'
    name: 'adminListGlobalConfiguration'
    summary: 'Record of global configuration data.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        globalConfigurationService := session.GlobalConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := globalConfigurationService.AdminListGlobalConfigurationShort(
            &global_configuration.AdminListGlobalConfigurationParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Config'
    name: 'adminPatchUpdateLogConfig'
    summary: 'Patch Update Log Configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        configService := session.ConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configService.AdminPatchUpdateLogConfigShort(
            &config.AdminPatchUpdateLogConfigParams{
                Body: ..., // *sessionclientmodels.LogconfigConfiguration
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Game Session'
    name: 'adminQueryGameSessions'
    summary: 'Get all game sessions.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        gameSessionService := session.GameSessionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameSessionService.AdminQueryGameSessionsShort(
            &game_session.AdminQueryGameSessionsParams{
                Namespace:         ..., // string
                ConfigurationName: ..., // *string
                DsPodName:         ..., // *string
                FromTime:          ..., // *string
                GameMode:          ..., // *string
                IsPersistent:      ..., // *string
                IsSoftDeleted:     ..., // *string
                Joinability:       ..., // *string
                Limit:             ..., // *int64
                MatchPool:         ..., // *string
                MemberID:          ..., // *string
                Offset:            ..., // *int64
                Order:             ..., // *string
                OrderBy:           ..., // *string
                SessionID:         ..., // *string
                Status:            ..., // *string
                StatusV2:          ..., // *string
                ToTime:            ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Game Session'
    name: 'adminQueryGameSessionsByAttributes'
    summary: 'Query game sessions by admin'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        gameSessionService := session.GameSessionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameSessionService.AdminQueryGameSessionsByAttributesShort(
            &game_session.AdminQueryGameSessionsByAttributesParams{
                Body:      ..., // map[string]interface{}
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Party'
    name: 'adminQueryParties'
    summary: 'Query parties.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        partyService := session.PartyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := partyService.AdminQueryPartiesShort(
            &party.AdminQueryPartiesParams{
                Namespace:         ..., // string
                ConfigurationName: ..., // *string
                FromTime:          ..., // *string
                IsSoftDeleted:     ..., // *string
                Joinability:       ..., // *string
                Key:               ..., // *string
                LeaderID:          ..., // *string
                Limit:             ..., // *int64
                MemberID:          ..., // *string
                MemberStatus:      ..., // *string
                Offset:            ..., // *int64
                Order:             ..., // *string
                OrderBy:           ..., // *string
                PartyID:           ..., // *string
                ToTime:            ..., // *string
                Value:             ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Player'
    name: 'adminQueryPlayerAttributes'
    summary: 'Query player attributes.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        playerService := session.PlayerService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := playerService.AdminQueryPlayerAttributesShort(
            &player.AdminQueryPlayerAttributesParams{
                Namespace: ..., // string
                Users:     ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Session Storage'
    name: 'adminReadPartySessionStorage'
    summary: 'Read Party Session Storage.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        sessionStorageService := session.SessionStorageService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := sessionStorageService.AdminReadPartySessionStorageShort(
            &session_storage.AdminReadPartySessionStorageParams{
                Namespace: ..., // string
                PartyID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Session Storage'
    name: 'adminReadSessionStorage'
    summary: 'Read Session Storage.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        sessionStorageService := session.SessionStorageService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := sessionStorageService.AdminReadSessionStorageShort(
            &session_storage.AdminReadSessionStorageParams{
                Namespace: ..., // string
                SessionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Session Storage'
    name: 'adminReadUserSessionStorage'
    summary: 'Read Session Storage User.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        sessionStorageService := session.SessionStorageService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := sessionStorageService.AdminReadUserSessionStorageShort(
            &session_storage.AdminReadUserSessionStorageParams{
                Namespace: ..., // string
                SessionID: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Max Active'
    name: 'adminReconcileMaxActiveSession'
    summary: 'Reconcile Max Active Session.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        maxActiveService := session.MaxActiveService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := maxActiveService.AdminReconcileMaxActiveSessionShort(
            &max_active.AdminReconcileMaxActiveSessionParams{
                Body:      ..., // *sessionclientmodels.ModelsRequestReconcileMaxActiveSession
                Name:      ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Game Session'
    name: 'adminSetDSReady'
    summary: 'Admin set the DS ready to accept connection'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        gameSessionService := session.GameSessionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameSessionService.AdminSetDSReadyShort(
            &game_session.AdminSetDSReadyParams{
                Body:      ..., // *sessionclientmodels.ApimodelsSetDSReadyRequest
                Namespace: ..., // string
                SessionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration Template'
    name: 'adminSyncDSMCConfiguration'
    summary: 'sync dsmc configuration.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        configurationTemplateService := session.ConfigurationTemplateService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationTemplateService.AdminSyncDSMCConfigurationShort(
            &configuration_template.AdminSyncDSMCConfigurationParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Party'
    name: 'adminSyncNativeSession'
    summary: 'Trigger user''s active party session to native platform.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        partyService := session.PartyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := partyService.AdminSyncNativeSessionShort(
            &party.AdminSyncNativeSessionParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Platform Credential'
    name: 'adminSyncPlatformCredentials'
    summary: 'Sync Platform Credentials.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        platformCredentialService := session.PlatformCredentialService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := platformCredentialService.AdminSyncPlatformCredentialsShort(
            &platform_credential.AdminSyncPlatformCredentialsParams{
                Namespace:  ..., // string
                PlatformID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration Template'
    name: 'adminUpdateConfigurationAlertV1'
    summary: 'Update configuration alert.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        configurationTemplateService := session.ConfigurationTemplateService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationTemplateService.AdminUpdateConfigurationAlertV1Short(
            &configuration_template.AdminUpdateConfigurationAlertV1Params{
                Body:      ..., // *sessionclientmodels.ApimodelsConfigAlertRequestCreate
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Configuration Template'
    name: 'adminUpdateConfigurationTemplateV1'
    summary: 'Update configuration template.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        configurationTemplateService := session.ConfigurationTemplateService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := configurationTemplateService.AdminUpdateConfigurationTemplateV1Short(
            &configuration_template.AdminUpdateConfigurationTemplateV1Params{
                Body:      ..., // *sessionclientmodels.ApimodelsUpdateConfigurationTemplateRequest
                Name:      ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Game Session'
    name: 'adminUpdateDSInformation'
    summary: 'Update Game Session DS Information for Asynchronous Process.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        gameSessionService := session.GameSessionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameSessionService.AdminUpdateDSInformationShort(
            &game_session.AdminUpdateDSInformationParams{
                Body:      ..., // *sessionclientmodels.ApimodelsUpdateGamesessionDSInformationRequest
                Namespace: ..., // string
                SessionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Game Session'
    name: 'adminUpdateGameSessionMember'
    summary: 'Update status of a game session member.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        gameSessionService := session.GameSessionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameSessionService.AdminUpdateGameSessionMemberShort(
            &game_session.AdminUpdateGameSessionMemberParams{
                MemberID:   ..., // string
                Namespace:  ..., // string
                SessionID:  ..., // string
                StatusType: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Global Configuration'
    name: 'adminUpdateGlobalConfiguration'
    summary: 'Upsert global configuration data.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        globalConfigurationService := session.GlobalConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := globalConfigurationService.AdminUpdateGlobalConfigurationShort(
            &global_configuration.AdminUpdateGlobalConfigurationParams{
                Body: ..., // *sessionclientmodels.ApimodelsPutGlobalConfigurationRequest
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Platform Credential'
    name: 'adminUpdatePlatformCredentials'
    summary: 'Update platform credentials. Currently only used to sync PSN sessions.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        platformCredentialService := session.PlatformCredentialService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := platformCredentialService.AdminUpdatePlatformCredentialsShort(
            &platform_credential.AdminUpdatePlatformCredentialsParams{
                Body:      ..., // *sessionclientmodels.ApimodelsPutPlatformCredentialsRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Certificate'
    name: 'adminUploadXBoxCertificate'
    summary: 'Upload certificates for XBox.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        certificateService := session.CertificateService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := certificateService.AdminUploadXBoxCertificateShort(
            &certificate.AdminUploadXBoxCertificateParams{
                Description: ..., // *string
                Certname:    ..., // string
                File:        ..., // []byte
                Password:    ..., // string
                Namespace:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Game Session'
    name: 'appendTeamGameSession'
    summary: 'Append new member or team to session. Please use patchUpdateGameSession instead'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        gameSessionService := session.GameSessionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameSessionService.AppendTeamGameSessionShort(
            &game_session.AppendTeamGameSessionParams{
                Body:      ..., // *sessionclientmodels.ApimodelsAppendTeamGameSessionRequest
                Namespace: ..., // string
                SessionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Game Session'
    name: 'createGameSession'
    summary: 'Create a game session.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        gameSessionService := session.GameSessionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameSessionService.CreateGameSessionShort(
            &game_session.CreateGameSessionParams{
                Body:                    ..., // *sessionclientmodels.ApimodelsCreateGameSessionRequest
                Namespace:               ..., // string
                ResolveMaxActiveSession: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Game Session'
    name: 'deleteGameSession'
    summary: 'Delete a game session.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        gameSessionService := session.GameSessionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameSessionService.DeleteGameSessionShort(
            &game_session.DeleteGameSessionParams{
                Namespace: ..., // string
                SessionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Game Session'
    name: 'gameSessionGenerateCode'
    summary: 'Generate a game session code.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        gameSessionService := session.GameSessionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameSessionService.GameSessionGenerateCodeShort(
            &game_session.GameSessionGenerateCodeParams{
                Namespace: ..., // string
                SessionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Game Session'
    name: 'getGameSession'
    summary: 'Get game session detail.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        gameSessionService := session.GameSessionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameSessionService.GetGameSessionShort(
            &game_session.GetGameSessionParams{
                Namespace: ..., // string
                SessionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Game Session'
    name: 'getGameSessionByPodName'
    summary: 'Get game session detail.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        gameSessionService := session.GameSessionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameSessionService.GetGameSessionByPodNameShort(
            &game_session.GetGameSessionByPodNameParams{
                Namespace: ..., // string
                PodName:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: ''
    name: 'GetHealthcheckInfo'
    summary: ''
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        Service := session.EmptyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := Service.GetHealthcheckInfoShort(
            &.GetHealthcheckInfoParams{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: ''
    name: 'GetHealthcheckInfoV1'
    summary: ''
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        Service := session.EmptyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := Service.GetHealthcheckInfoV1Short(
            &.GetHealthcheckInfoV1Params{}
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Game Session'
    name: 'getSessionServerSecret'
    summary: 'Get Server Secret.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        gameSessionService := session.GameSessionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameSessionService.GetSessionServerSecretShort(
            &game_session.GetSessionServerSecretParams{
                Namespace: ..., // string
                SessionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Game Session'
    name: 'joinGameSession'
    summary: 'Join a game session.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        gameSessionService := session.GameSessionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameSessionService.JoinGameSessionShort(
            &game_session.JoinGameSessionParams{
                Namespace: ..., // string
                SessionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Game Session'
    name: 'leaveGameSession'
    summary: 'Leave a game session.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        gameSessionService := session.GameSessionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameSessionService.LeaveGameSessionShort(
            &game_session.LeaveGameSessionParams{
                Namespace: ..., // string
                SessionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Game Session'
    name: 'patchUpdateGameSession'
    summary: 'Patch update a game session.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        gameSessionService := session.GameSessionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameSessionService.PatchUpdateGameSessionShort(
            &game_session.PatchUpdateGameSessionParams{
                Body:      ..., // *sessionclientmodels.ApimodelsUpdateGameSessionRequest
                Namespace: ..., // string
                SessionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Party'
    name: 'publicCreateParty'
    summary: 'Create a party.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        partyService := session.PartyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := partyService.PublicCreatePartyShort(
            &party.PublicCreatePartyParams{
                Body:      ..., // *sessionclientmodels.ApimodelsCreatePartyRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Player'
    name: 'publicDeletePlayerAttributes'
    summary: 'Remove player attributes.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        playerService := session.PlayerService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := playerService.PublicDeletePlayerAttributesShort(
            &player.PublicDeletePlayerAttributesParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Game Session'
    name: 'publicGameSessionCancel'
    summary: 'cancel a game session invitation.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        gameSessionService := session.GameSessionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameSessionService.PublicGameSessionCancelShort(
            &game_session.PublicGameSessionCancelParams{
                Namespace: ..., // string
                SessionID: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Game Session'
    name: 'publicGameSessionInvite'
    summary: 'Invite a user to a game session.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        gameSessionService := session.GameSessionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameSessionService.PublicGameSessionInviteShort(
            &game_session.PublicGameSessionInviteParams{
                Body:      ..., // *sessionclientmodels.ApimodelsSessionInviteRequest
                Namespace: ..., // string
                SessionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Game Session'
    name: 'publicGameSessionReject'
    summary: 'Reject a game session invitation.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        gameSessionService := session.GameSessionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameSessionService.PublicGameSessionRejectShort(
            &game_session.PublicGameSessionRejectParams{
                Namespace: ..., // string
                SessionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Party'
    name: 'publicGeneratePartyCode'
    summary: 'Generate party code.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        partyService := session.PartyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := partyService.PublicGeneratePartyCodeShort(
            &party.PublicGeneratePartyCodeParams{
                Namespace: ..., // string
                PartyID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Player'
    name: 'publicGetBulkPlayerCurrentPlatform'
    summary: 'Get player current platform in bulk.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        playerService := session.PlayerService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := playerService.PublicGetBulkPlayerCurrentPlatformShort(
            &player.PublicGetBulkPlayerCurrentPlatformParams{
                Body:      ..., // *sessionclientmodels.ApimodelsPlayersCurrentPlatformRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Party'
    name: 'publicGetParty'
    summary: 'Get party details.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        partyService := session.PartyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := partyService.PublicGetPartyShort(
            &party.PublicGetPartyParams{
                Namespace: ..., // string
                PartyID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Player'
    name: 'publicGetPlayerAttributes'
    summary: 'Get player attributes.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        playerService := session.PlayerService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := playerService.PublicGetPlayerAttributesShort(
            &player.PublicGetPlayerAttributesParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Recent Player'
    name: 'publicGetRecentPlayer'
    summary: 'Query user''s recent player'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        recentPlayerService := session.RecentPlayerService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := recentPlayerService.PublicGetRecentPlayerShort(
            &recent_player.PublicGetRecentPlayerParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Recent Player'
    name: 'publicGetRecentTeamPlayer'
    summary: 'Query user''s recent player who were on the same team.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        recentPlayerService := session.RecentPlayerService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := recentPlayerService.PublicGetRecentTeamPlayerShort(
            &recent_player.PublicGetRecentTeamPlayerParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Game Session'
    name: 'publicKickGameSessionMember'
    summary: 'Kick member from a game session, only leader can kick member.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        gameSessionService := session.GameSessionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameSessionService.PublicKickGameSessionMemberShort(
            &game_session.PublicKickGameSessionMemberParams{
                MemberID:  ..., // string
                Namespace: ..., // string
                SessionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Party'
    name: 'publicPartyCancel'
    summary: 'Cancel a party invitation.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        partyService := session.PartyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := partyService.PublicPartyCancelShort(
            &party.PublicPartyCancelParams{
                Namespace: ..., // string
                PartyID:   ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Party'
    name: 'publicPartyInvite'
    summary: 'Invite a user to a party.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        partyService := session.PartyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := partyService.PublicPartyInviteShort(
            &party.PublicPartyInviteParams{
                Body:      ..., // *sessionclientmodels.ApimodelsSessionInviteRequest
                Namespace: ..., // string
                PartyID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Party'
    name: 'publicPartyJoin'
    summary: 'Join a party.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        partyService := session.PartyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := partyService.PublicPartyJoinShort(
            &party.PublicPartyJoinParams{
                Namespace: ..., // string
                PartyID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Party'
    name: 'publicPartyJoinCode'
    summary: 'Join a party by code.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        partyService := session.PartyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := partyService.PublicPartyJoinCodeShort(
            &party.PublicPartyJoinCodeParams{
                Body:      ..., // *sessionclientmodels.ApimodelsJoinByCodeRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Party'
    name: 'publicPartyKick'
    summary: 'Kick a player from a party.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        partyService := session.PartyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := partyService.PublicPartyKickShort(
            &party.PublicPartyKickParams{
                Namespace: ..., // string
                PartyID:   ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Party'
    name: 'publicPartyLeave'
    summary: 'Leave a party.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        partyService := session.PartyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := partyService.PublicPartyLeaveShort(
            &party.PublicPartyLeaveParams{
                Namespace: ..., // string
                PartyID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Party'
    name: 'publicPartyReject'
    summary: 'Reject a party invitation.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        partyService := session.PartyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := partyService.PublicPartyRejectShort(
            &party.PublicPartyRejectParams{
                Namespace: ..., // string
                PartyID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Party'
    name: 'publicPatchUpdateParty'
    summary: 'Patch update a party.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        partyService := session.PartyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := partyService.PublicPatchUpdatePartyShort(
            &party.PublicPatchUpdatePartyParams{
                Body:      ..., // *sessionclientmodels.ApimodelsUpdatePartyRequest
                Namespace: ..., // string
                PartyID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Game Session'
    name: 'publicPromoteGameSessionLeader'
    summary: 'Promote new game session leader.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        gameSessionService := session.GameSessionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameSessionService.PublicPromoteGameSessionLeaderShort(
            &game_session.PublicPromoteGameSessionLeaderParams{
                Body:      ..., // *sessionclientmodels.ApimodelsPromoteLeaderRequest
                Namespace: ..., // string
                SessionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Party'
    name: 'publicPromotePartyLeader'
    summary: 'Promote new party leader.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        partyService := session.PartyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := partyService.PublicPromotePartyLeaderShort(
            &party.PublicPromotePartyLeaderParams{
                Body:      ..., // *sessionclientmodels.ApimodelsPromoteLeaderRequest
                Namespace: ..., // string
                PartyID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Game Session'
    name: 'publicQueryGameSessionsByAttributes'
    summary: 'Query game sessions'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        gameSessionService := session.GameSessionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameSessionService.PublicQueryGameSessionsByAttributesShort(
            &game_session.PublicQueryGameSessionsByAttributesParams{
                Body:      ..., // map[string]interface{}
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Game Session'
    name: 'publicQueryMyGameSessions'
    summary: 'Query user''s game sessions'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        gameSessionService := session.GameSessionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameSessionService.PublicQueryMyGameSessionsShort(
            &game_session.PublicQueryMyGameSessionsParams{
                Namespace: ..., // string
                Order:     ..., // *string
                OrderBy:   ..., // *string
                Status:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Party'
    name: 'publicQueryMyParties'
    summary: 'Query my parties. Require valid JWT.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        partyService := session.PartyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := partyService.PublicQueryMyPartiesShort(
            &party.PublicQueryMyPartiesParams{
                Namespace: ..., // string
                Order:     ..., // *string
                OrderBy:   ..., // *string
                Status:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Session Storage'
    name: 'publicReadPartySessionStorage'
    summary: 'Read Party Session Storage.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        sessionStorageService := session.SessionStorageService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := sessionStorageService.PublicReadPartySessionStorageShort(
            &session_storage.PublicReadPartySessionStorageParams{
                Namespace: ..., // string
                PartyID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Game Session'
    name: 'publicRevokeGameSessionCode'
    summary: 'Revoke game session code.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        gameSessionService := session.GameSessionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameSessionService.PublicRevokeGameSessionCodeShort(
            &game_session.PublicRevokeGameSessionCodeParams{
                Namespace: ..., // string
                SessionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Party'
    name: 'publicRevokePartyCode'
    summary: 'Revoke party code.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        partyService := session.PartyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := partyService.PublicRevokePartyCodeShort(
            &party.PublicRevokePartyCodeParams{
                Namespace: ..., // string
                PartyID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Game Session'
    name: 'publicSessionJoinCode'
    summary: 'Join a session by code.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        gameSessionService := session.GameSessionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameSessionService.PublicSessionJoinCodeShort(
            &game_session.PublicSessionJoinCodeParams{
                Body:      ..., // *sessionclientmodels.ApimodelsJoinByCodeRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Player'
    name: 'publicStorePlayerAttributes'
    summary: 'Store player attributes.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        playerService := session.PlayerService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := playerService.PublicStorePlayerAttributesShort(
            &player.PublicStorePlayerAttributesParams{
                Body:      ..., // *sessionclientmodels.ApimodelsPlayerAttributesRequestBody
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Session Storage'
    name: 'publicUpdateInsertPartySessionStorageReserved'
    summary: 'Update Insert Party Session Storage User.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        sessionStorageService := session.SessionStorageService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := sessionStorageService.PublicUpdateInsertPartySessionStorageReservedShort(
            &session_storage.PublicUpdateInsertPartySessionStorageReservedParams{
                Body:      ..., // map[string]interface{}
                Namespace: ..., // string
                PartyID:   ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Session Storage'
    name: 'publicUpdateInsertSessionStorage'
    summary: 'Update Insert Session Storage User.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        sessionStorageService := session.SessionStorageService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := sessionStorageService.PublicUpdateInsertSessionStorageShort(
            &session_storage.PublicUpdateInsertSessionStorageParams{
                Body:      ..., // map[string]interface{}
                Namespace: ..., // string
                SessionID: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Session Storage'
    name: 'publicUpdateInsertSessionStorageLeader'
    summary: 'Update Insert Session Storage Leader.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        sessionStorageService := session.SessionStorageService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := sessionStorageService.PublicUpdateInsertSessionStorageLeaderShort(
            &session_storage.PublicUpdateInsertSessionStorageLeaderParams{
                Body:      ..., // map[string]interface{}
                Namespace: ..., // string
                SessionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Party'
    name: 'publicUpdateParty'
    summary: 'Update a party.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        partyService := session.PartyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := partyService.PublicUpdatePartyShort(
            &party.PublicUpdatePartyParams{
                Body:      ..., // *sessionclientmodels.ApimodelsUpdatePartyRequest
                Namespace: ..., // string
                PartyID:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Game Session'
    name: 'updateGameSession'
    summary: 'Update a game session.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        gameSessionService := session.GameSessionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameSessionService.UpdateGameSessionShort(
            &game_session.UpdateGameSessionParams{
                Body:      ..., // *sessionclientmodels.ApimodelsUpdateGameSessionRequest
                Namespace: ..., // string
                SessionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Game Session'
    name: 'updateGameSessionBackfillTicketID'
    summary: 'Update game session backfill ticket id .'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionClient(configRepo)
        gameSessionService := session.GameSessionService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameSessionService.UpdateGameSessionBackfillTicketIDShort(
            &game_session.UpdateGameSessionBackfillTicketIDParams{
                Body:      ..., // *sessionclientmodels.ApimodelsUpdateGameSessionBackfillRequest
                Namespace: ..., // string
                SessionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }
# end session
# begin sessionhistory
  - ns: 'XRay'
    name: 'createXrayBulkTicketObservability'
    summary: 'Create bulk ticket observability request'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionhistoryClient(configRepo)
        xRayService := sessionhistory.XRayService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := xRayService.CreateXrayBulkTicketObservabilityShort(
            &x_ray.CreateXrayBulkTicketObservabilityParams{
                Body:      ..., // *sessionhistoryclientmodels.ApimodelsXRayBulkTicketObservabilityRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'XRay'
    name: 'createXrayTicketObservability'
    summary: 'Create ticket observability request'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSessionhistoryClient(configRepo)
        xRayService := sessionhistory.XRayService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := xRayService.CreateXrayTicketObservabilityShort(
            &x_ray.CreateXrayTicketObservabilityParams{
                Body:      ..., // *sessionhistoryclientmodels.ApimodelsXRayTicketObservabilityRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }
# end sessionhistory
# begin social
  - ns: 'UserStatistic'
    name: 'AdminListUsersStatItems'
    summary: '(Legacy) Admin list user''s statItems'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.AdminListUsersStatItemsShort(
            &user_statistic.AdminListUsersStatItemsParams{
                Namespace:     ..., // string
                UserID:        ..., // string
                AdditionalKey: ..., // *string
                StatCodes:     ..., // []string
                Tags:          ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'StatCycleConfiguration'
    name: 'bulkAddStats'
    summary: 'Bulk add stat cycle to stats'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        statCycleConfigurationService := social.StatCycleConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := statCycleConfigurationService.BulkAddStatsShort(
            &stat_cycle_configuration.BulkAddStatsParams{
                Body:      ..., // *socialclientmodels.BulkCycleStatsAdd
                CycleID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'bulkCreateUserStatItems'
    summary: 'Bulk create user''s statItems'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.BulkCreateUserStatItemsShort(
            &user_statistic.BulkCreateUserStatItemsParams{
                Body:      ..., // []*socialclientmodels.BulkStatItemCreate
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'bulkFetchOrDefaultStatItems'
    summary: 'Bulk fetch user stat item values'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.BulkFetchOrDefaultStatItemsShort(
            &user_statistic.BulkFetchOrDefaultStatItemsParams{
                Namespace: ..., // string
                StatCode:  ..., // string
                UserIds:   ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'bulkFetchOrDefaultStatItems_1'
    summary: 'Bulk fetch user stat item values'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.BulkFetchOrDefaultStatItems1Short(
            &user_statistic.BulkFetchOrDefaultStatItems1Params{
                Namespace:     ..., // string
                AdditionalKey: ..., // *string
                StatCode:      ..., // string
                UserIds:       ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'bulkFetchStatItems'
    summary: 'Bulk fetch statitems value'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.BulkFetchStatItemsShort(
            &user_statistic.BulkFetchStatItemsParams{
                Namespace: ..., // string
                StatCode:  ..., // string
                UserIds:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'bulkFetchStatItems_1'
    summary: 'Public bulk fetch statitems value'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.BulkFetchStatItems1Short(
            &user_statistic.BulkFetchStatItems1Params{
                Namespace: ..., // string
                StatCode:  ..., // string
                UserIds:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'bulkGetOrDefaultByUserId'
    summary: 'Bulk get user''s statitems value by user id and multiple stat codes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.BulkGetOrDefaultByUserIDShort(
            &user_statistic.BulkGetOrDefaultByUserIDParams{
                Body:          ..., // *socialclientmodels.BulkUserStatItemByStatCodes
                Namespace:     ..., // string
                UserID:        ..., // string
                AdditionalKey: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'StatCycleConfiguration'
    name: 'bulkGetStatCycle'
    summary: 'Bulk get stat cycle'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        statCycleConfigurationService := social.StatCycleConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := statCycleConfigurationService.BulkGetStatCycleShort(
            &stat_cycle_configuration.BulkGetStatCycleParams{
                Body:      ..., // *socialclientmodels.BulkStatCycleRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'StatCycleConfiguration'
    name: 'bulkGetStatCycle_1'
    summary: 'Bulk get stat cycle'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        statCycleConfigurationService := social.StatCycleConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := statCycleConfigurationService.BulkGetStatCycle1Short(
            &stat_cycle_configuration.BulkGetStatCycle1Params{
                Body:      ..., // *socialclientmodels.BulkStatCycleRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'bulkIncUserStatItem'
    summary: 'Bulk update multiple user''s statitems value'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.BulkIncUserStatItemShort(
            &user_statistic.BulkIncUserStatItemParams{
                Body:      ..., // []*socialclientmodels.BulkUserStatItemInc
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'bulkIncUserStatItem_1'
    summary: 'Bulk update user''s statitems value'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.BulkIncUserStatItem1Short(
            &user_statistic.BulkIncUserStatItem1Params{
                Body:      ..., // []*socialclientmodels.BulkStatItemInc
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'bulkIncUserStatItemValue'
    summary: 'Bulk update multiple user''s statitems value'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.BulkIncUserStatItemValueShort(
            &user_statistic.BulkIncUserStatItemValueParams{
                Body:      ..., // []*socialclientmodels.BulkUserStatItemInc
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'bulkIncUserStatItemValue_1'
    summary: 'Bulk update user''s statitems value'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.BulkIncUserStatItemValue1Short(
            &user_statistic.BulkIncUserStatItemValue1Params{
                Body:      ..., // []*socialclientmodels.BulkStatItemInc
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'bulkIncUserStatItemValue_2'
    summary: 'Public bulk update user''s statitems value'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.BulkIncUserStatItemValue2Short(
            &user_statistic.BulkIncUserStatItemValue2Params{
                Body:      ..., // []*socialclientmodels.BulkStatItemInc
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'bulkResetUserStatItem'
    summary: 'Bulk reset multiple user''s statitems value'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.BulkResetUserStatItemShort(
            &user_statistic.BulkResetUserStatItemParams{
                Body:      ..., // []*socialclientmodels.BulkUserStatItemReset
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'bulkResetUserStatItem_1'
    summary: 'Bulk reset user''s statitems value'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.BulkResetUserStatItem1Short(
            &user_statistic.BulkResetUserStatItem1Params{
                Body:      ..., // []*socialclientmodels.BulkStatItemReset
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'bulkResetUserStatItem_2'
    summary: 'Public bulk reset multiple user''s statitems value'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.BulkResetUserStatItem2Short(
            &user_statistic.BulkResetUserStatItem2Params{
                Body:      ..., // []*socialclientmodels.BulkUserStatItemReset
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'bulkResetUserStatItem_3'
    summary: 'Public bulk reset user''s statitems value'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.BulkResetUserStatItem3Short(
            &user_statistic.BulkResetUserStatItem3Params{
                Body:      ..., // []*socialclientmodels.BulkStatItemReset
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'bulkResetUserStatItemValues'
    summary: 'Bulk reset user''s statitem values'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.BulkResetUserStatItemValuesShort(
            &user_statistic.BulkResetUserStatItemValuesParams{
                Body:          ..., // []*socialclientmodels.ADTOObjectForResettingUserStatItems
                Namespace:     ..., // string
                UserID:        ..., // string
                AdditionalKey: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'bulkUpdateUserStatItem'
    summary: 'Bulk update user''s statitems value'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.BulkUpdateUserStatItemShort(
            &user_statistic.BulkUpdateUserStatItemParams{
                Body:          ..., // []*socialclientmodels.BulkStatItemUpdate
                Namespace:     ..., // string
                UserID:        ..., // string
                AdditionalKey: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'bulkUpdateUserStatItem_1'
    summary: 'Public bulk update multiple user''s statitems value'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.BulkUpdateUserStatItem1Short(
            &user_statistic.BulkUpdateUserStatItem1Params{
                Body:      ..., // []*socialclientmodels.BulkUserStatItemUpdate
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'bulkUpdateUserStatItem_2'
    summary: 'Public bulk update user''s statitems value'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.BulkUpdateUserStatItem2Short(
            &user_statistic.BulkUpdateUserStatItem2Params{
                Body:          ..., // []*socialclientmodels.BulkStatItemUpdate
                Namespace:     ..., // string
                UserID:        ..., // string
                AdditionalKey: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'bulkUpdateUserStatItemV2'
    summary: 'Bulk update multiple user''s statitems value'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.BulkUpdateUserStatItemV2Short(
            &user_statistic.BulkUpdateUserStatItemV2Params{
                Body:      ..., // []*socialclientmodels.BulkUserStatItemUpdate
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'StatConfiguration'
    name: 'createStat'
    summary: 'Create stat'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        statConfigurationService := social.StatConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := statConfigurationService.CreateStatShort(
            &stat_configuration.CreateStatParams{
                Body:      ..., // *socialclientmodels.StatCreate
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'StatConfiguration'
    name: 'createStat_1'
    summary: 'Create stat'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        statConfigurationService := social.StatConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := statConfigurationService.CreateStat1Short(
            &stat_configuration.CreateStat1Params{
                Body:      ..., // *socialclientmodels.StatCreate
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'StatCycleConfiguration'
    name: 'createStatCycle'
    summary: 'Create stat cycle'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        statCycleConfigurationService := social.StatCycleConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := statCycleConfigurationService.CreateStatCycleShort(
            &stat_cycle_configuration.CreateStatCycleParams{
                Body:      ..., // *socialclientmodels.StatCycleCreate
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'createUserStatItem'
    summary: 'Create user''s statItem'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.CreateUserStatItemShort(
            &user_statistic.CreateUserStatItemParams{
                Namespace: ..., // string
                StatCode:  ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'SlotConfig'
    name: 'deleteNamespaceSlotConfig'
    summary: 'Deletes a namespace slot configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        slotConfigService := social.SlotConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := slotConfigService.DeleteNamespaceSlotConfigShort(
            &slot_config.DeleteNamespaceSlotConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'StatConfiguration'
    name: 'deleteStat'
    summary: 'Deletes stat'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        statConfigurationService := social.StatConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := statConfigurationService.DeleteStatShort(
            &stat_configuration.DeleteStatParams{
                Namespace: ..., // string
                StatCode:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'StatCycleConfiguration'
    name: 'deleteStatCycle'
    summary: 'Deletes stat cycle'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        statCycleConfigurationService := social.StatCycleConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := statCycleConfigurationService.DeleteStatCycleShort(
            &stat_cycle_configuration.DeleteStatCycleParams{
                CycleID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'StatConfiguration'
    name: 'deleteTiedStat'
    summary: 'Deletes tied stat'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        statConfigurationService := social.StatConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := statConfigurationService.DeleteTiedStatShort(
            &stat_configuration.DeleteTiedStatParams{
                Namespace: ..., // string
                StatCode:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'SlotConfig'
    name: 'deleteUserSlotConfig'
    summary: 'Deletes a user slot configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        slotConfigService := social.SlotConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := slotConfigService.DeleteUserSlotConfigShort(
            &slot_config.DeleteUserSlotConfigParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'deleteUserStatItems'
    summary: 'Delete User''s statItems'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.DeleteUserStatItemsShort(
            &user_statistic.DeleteUserStatItemsParams{
                Namespace: ..., // string
                StatCode:  ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'deleteUserStatItems_1'
    summary: 'Delete User''s statItems'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.DeleteUserStatItems1Short(
            &user_statistic.DeleteUserStatItems1Params{
                Namespace: ..., // string
                StatCode:  ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'deleteUserStatItems_2'
    summary: 'Delete User''s statItems'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.DeleteUserStatItems2Short(
            &user_statistic.DeleteUserStatItems2Params{
                Namespace:     ..., // string
                StatCode:      ..., // string
                UserID:        ..., // string
                AdditionalKey: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'StatCycleConfiguration'
    name: 'exportStatCycle'
    summary: 'Export all stat cycle configurations'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        statCycleConfigurationService := social.StatCycleConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := statCycleConfigurationService.ExportStatCycleShort(
            &stat_cycle_configuration.ExportStatCycleParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'StatConfiguration'
    name: 'exportStats'
    summary: 'Export all stat configurations'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        statConfigurationService := social.StatConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := statConfigurationService.ExportStatsShort(
            &stat_configuration.ExportStatsParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'GlobalStatistic'
    name: 'getGlobalStatItemByStatCode'
    summary: 'Get global statItem by stat code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        globalStatisticService := social.GlobalStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := globalStatisticService.GetGlobalStatItemByStatCodeShort(
            &global_statistic.GetGlobalStatItemByStatCodeParams{
                Namespace: ..., // string
                StatCode:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'GlobalStatistic'
    name: 'getGlobalStatItemByStatCode_1'
    summary: 'Get global statItem by stat code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        globalStatisticService := social.GlobalStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := globalStatisticService.GetGlobalStatItemByStatCode1Short(
            &global_statistic.GetGlobalStatItemByStatCode1Params{
                Namespace: ..., // string
                StatCode:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'GlobalStatistic'
    name: 'getGlobalStatItems'
    summary: 'List global statItems'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        globalStatisticService := social.GlobalStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := globalStatisticService.GetGlobalStatItemsShort(
            &global_statistic.GetGlobalStatItemsParams{
                Namespace: ..., // string
                Limit:     ..., // *int32
                Offset:    ..., // *int32
                StatCodes: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'GlobalStatistic'
    name: 'getGlobalStatItems_1'
    summary: 'List global statItems'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        globalStatisticService := social.GlobalStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := globalStatisticService.GetGlobalStatItems1Short(
            &global_statistic.GetGlobalStatItems1Params{
                Namespace: ..., // string
                Limit:     ..., // *int32
                Offset:    ..., // *int32
                StatCodes: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'SlotConfig'
    name: 'getNamespaceSlotConfig'
    summary: 'Returns a namespace slot configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        slotConfigService := social.SlotConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := slotConfigService.GetNamespaceSlotConfigShort(
            &slot_config.GetNamespaceSlotConfigParams{
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'GameProfile'
    name: 'getProfile'
    summary: 'Returns profile for a user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        gameProfileService := social.GameProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameProfileService.GetProfileShort(
            &game_profile.GetProfileParams{
                Namespace: ..., // string
                ProfileID: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Slot'
    name: 'getSlotData'
    summary: 'Returns slot data'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        slotService := social.SlotService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := slotService.GetSlotDataShort(
            &slot.GetSlotDataParams{
                Namespace: ..., // string
                SlotID:    ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'StatConfiguration'
    name: 'getStat'
    summary: 'Get stat by statCode'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        statConfigurationService := social.StatConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := statConfigurationService.GetStatShort(
            &stat_configuration.GetStatParams{
                Namespace: ..., // string
                StatCode:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'StatCycleConfiguration'
    name: 'getStatCycle'
    summary: 'Get stat cycle'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        statCycleConfigurationService := social.StatCycleConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := statCycleConfigurationService.GetStatCycleShort(
            &stat_cycle_configuration.GetStatCycleParams{
                CycleID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'StatCycleConfiguration'
    name: 'getStatCycle_1'
    summary: 'Get stat cycle'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        statCycleConfigurationService := social.StatCycleConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := statCycleConfigurationService.GetStatCycle1Short(
            &stat_cycle_configuration.GetStatCycle1Params{
                CycleID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'StatCycleConfiguration'
    name: 'getStatCycles'
    summary: 'List stat cycles'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        statCycleConfigurationService := social.StatCycleConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := statCycleConfigurationService.GetStatCyclesShort(
            &stat_cycle_configuration.GetStatCyclesParams{
                Namespace: ..., // string
                CycleType: ..., // *string
                Limit:     ..., // *int32
                Name:      ..., // *string
                Offset:    ..., // *int32
                SortBy:    ..., // *string
                Status:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'StatCycleConfiguration'
    name: 'getStatCycles_1'
    summary: 'List stat cycles'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        statCycleConfigurationService := social.StatCycleConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := statCycleConfigurationService.GetStatCycles1Short(
            &stat_cycle_configuration.GetStatCycles1Params{
                Namespace: ..., // string
                CycleType: ..., // *string
                Limit:     ..., // *int32
                Name:      ..., // *string
                Offset:    ..., // *int32
                SortBy:    ..., // *string
                Status:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'getStatItems'
    summary: 'Get users'' stat item values by stat code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.GetStatItemsShort(
            &user_statistic.GetStatItemsParams{
                Namespace: ..., // string
                StatCode:  ..., // string
                Limit:     ..., // *int32
                Offset:    ..., // *int32
                SortBy:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'StatConfiguration'
    name: 'getStats'
    summary: 'List stats'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        statConfigurationService := social.StatConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := statConfigurationService.GetStatsShort(
            &stat_configuration.GetStatsParams{
                Namespace: ..., // string
                CycleIds:  ..., // *string
                IsGlobal:  ..., // *bool
                IsPublic:  ..., // *bool
                Limit:     ..., // *int32
                Offset:    ..., // *int32
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Slot'
    name: 'getUserNamespaceSlots'
    summary: 'Returns list of slots for given user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        slotService := social.SlotService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := slotService.GetUserNamespaceSlotsShort(
            &slot.GetUserNamespaceSlotsParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'GameProfile'
    name: 'getUserProfiles'
    summary: 'Returns all profiles'' header for a user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        gameProfileService := social.GameProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameProfileService.GetUserProfilesShort(
            &game_profile.GetUserProfilesParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'SlotConfig'
    name: 'getUserSlotConfig'
    summary: 'Returns a user slot configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        slotConfigService := social.SlotConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := slotConfigService.GetUserSlotConfigShort(
            &slot_config.GetUserSlotConfigParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatisticCycle'
    name: 'getUserStatCycleItems'
    summary: 'List user''s statCycleItems by statCycle'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticCycleService := social.UserStatisticCycleService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticCycleService.GetUserStatCycleItemsShort(
            &user_statistic_cycle.GetUserStatCycleItemsParams{
                CycleID:   ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
                IsPublic:  ..., // *bool
                Limit:     ..., // *int32
                Offset:    ..., // *int32
                SortBy:    ..., // *string
                StatCodes: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatisticCycle'
    name: 'getUserStatCycleItems_1'
    summary: 'List user''s statCycleItems by statCycle'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticCycleService := social.UserStatisticCycleService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticCycleService.GetUserStatCycleItems1Short(
            &user_statistic_cycle.GetUserStatCycleItems1Params{
                CycleID:   ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
                Limit:     ..., // *int32
                Offset:    ..., // *int32
                SortBy:    ..., // *string
                StatCodes: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'getUserStatItems'
    summary: 'List user''s statItems'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.GetUserStatItemsShort(
            &user_statistic.GetUserStatItemsParams{
                Namespace: ..., // string
                UserID:    ..., // string
                IsPublic:  ..., // *bool
                Limit:     ..., // *int32
                Offset:    ..., // *int32
                SortBy:    ..., // *string
                StatCodes: ..., // *string
                Tags:      ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'StatCycleConfiguration'
    name: 'importStatCycle'
    summary: 'Import stat cycle configurations'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        statCycleConfigurationService := social.StatCycleConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := statCycleConfigurationService.ImportStatCycleShort(
            &stat_cycle_configuration.ImportStatCycleParams{
                File:            ..., // *[]byte
                Namespace:       ..., // string
                ReplaceExisting: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'StatConfiguration'
    name: 'importStats'
    summary: 'Import stat configurations'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        statConfigurationService := social.StatConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := statConfigurationService.ImportStatsShort(
            &stat_configuration.ImportStatsParams{
                File:            ..., // *[]byte
                Namespace:       ..., // string
                ReplaceExisting: ..., // *bool
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'incUserStatItemValue'
    summary: 'Update user''s statitem value'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.IncUserStatItemValueShort(
            &user_statistic.IncUserStatItemValueParams{
                Body:      ..., // *socialclientmodels.StatItemInc
                Namespace: ..., // string
                StatCode:  ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'publicBulkCreateUserStatItems'
    summary: 'Bulk create user''s statItems'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.PublicBulkCreateUserStatItemsShort(
            &user_statistic.PublicBulkCreateUserStatItemsParams{
                Body:      ..., // []*socialclientmodels.BulkStatItemCreate
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'publicBulkIncUserStatItem'
    summary: 'Public bulk update multiple user''s statitems value'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.PublicBulkIncUserStatItemShort(
            &user_statistic.PublicBulkIncUserStatItemParams{
                Body:      ..., // []*socialclientmodels.BulkUserStatItemInc
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'publicBulkIncUserStatItem_1'
    summary: 'Public bulk update user''s statitems value'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.PublicBulkIncUserStatItem1Short(
            &user_statistic.PublicBulkIncUserStatItem1Params{
                Body:      ..., // []*socialclientmodels.BulkStatItemInc
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'publicBulkIncUserStatItemValue'
    summary: 'Public bulk update multiple user''s statitems value'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.PublicBulkIncUserStatItemValueShort(
            &user_statistic.PublicBulkIncUserStatItemValueParams{
                Body:      ..., // []*socialclientmodels.BulkUserStatItemInc
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'GameProfile'
    name: 'publicCreateProfile'
    summary: 'Create a new profile for user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        gameProfileService := social.GameProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameProfileService.PublicCreateProfileShort(
            &game_profile.PublicCreateProfileParams{
                Body:      ..., // *socialclientmodels.GameProfileRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Slot'
    name: 'publicCreateUserNamespaceSlot'
    summary: 'Creates a slot'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        slotService := social.SlotService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := slotService.PublicCreateUserNamespaceSlotShort(
            &slot.PublicCreateUserNamespaceSlotParams{
                Checksum:        ..., // *string
                CustomAttribute: ..., // *string
                File:            ..., // *[]byte
                Namespace:       ..., // string
                UserID:          ..., // string
                Label:           ..., // *string
                Tags:            ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'publicCreateUserStatItem'
    summary: 'Create user''s statItem'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.PublicCreateUserStatItemShort(
            &user_statistic.PublicCreateUserStatItemParams{
                Namespace: ..., // string
                StatCode:  ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'GameProfile'
    name: 'publicDeleteProfile'
    summary: 'Deletes game profile'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        gameProfileService := social.GameProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameProfileService.PublicDeleteProfileShort(
            &game_profile.PublicDeleteProfileParams{
                Namespace: ..., // string
                ProfileID: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Slot'
    name: 'publicDeleteUserNamespaceSlot'
    summary: 'Deletes the slot'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        slotService := social.SlotService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := slotService.PublicDeleteUserNamespaceSlotShort(
            &slot.PublicDeleteUserNamespaceSlotParams{
                Namespace: ..., // string
                SlotID:    ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'GameProfile'
    name: 'publicGetProfile'
    summary: 'Returns profile for a user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        gameProfileService := social.GameProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameProfileService.PublicGetProfileShort(
            &game_profile.PublicGetProfileParams{
                Namespace: ..., // string
                ProfileID: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'GameProfile'
    name: 'publicGetProfileAttribute'
    summary: 'Returns game profile attribute'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        gameProfileService := social.GameProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameProfileService.PublicGetProfileAttributeShort(
            &game_profile.PublicGetProfileAttributeParams{
                AttributeName: ..., // string
                Namespace:     ..., // string
                ProfileID:     ..., // string
                UserID:        ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Slot'
    name: 'publicGetSlotData'
    summary: 'Returns slot data'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        slotService := social.SlotService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := slotService.PublicGetSlotDataShort(
            &slot.PublicGetSlotDataParams{
                Namespace: ..., // string
                SlotID:    ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'GameProfile'
    name: 'publicGetUserGameProfiles'
    summary: 'Returns all profiles for specified users'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        gameProfileService := social.GameProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameProfileService.PublicGetUserGameProfilesShort(
            &game_profile.PublicGetUserGameProfilesParams{
                Namespace: ..., // string
                UserIds:   ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Slot'
    name: 'publicGetUserNamespaceSlots'
    summary: 'Returns slots for given user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        slotService := social.SlotService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := slotService.PublicGetUserNamespaceSlotsShort(
            &slot.PublicGetUserNamespaceSlotsParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'GameProfile'
    name: 'publicGetUserProfiles'
    summary: 'Returns all profiles'' header for a user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        gameProfileService := social.GameProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameProfileService.PublicGetUserProfilesShort(
            &game_profile.PublicGetUserProfilesParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'publicIncUserStatItem'
    summary: 'Public update user''s statitem value'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.PublicIncUserStatItemShort(
            &user_statistic.PublicIncUserStatItemParams{
                Body:      ..., // *socialclientmodels.StatItemInc
                Namespace: ..., // string
                StatCode:  ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'publicIncUserStatItemValue'
    summary: 'Public update user''s statitem value'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.PublicIncUserStatItemValueShort(
            &user_statistic.PublicIncUserStatItemValueParams{
                Body:      ..., // *socialclientmodels.StatItemInc
                Namespace: ..., // string
                StatCode:  ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'publicListAllMyStatItems'
    summary: 'Public list user''s statItems'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.PublicListAllMyStatItemsShort(
            &user_statistic.PublicListAllMyStatItemsParams{
                Namespace:     ..., // string
                AdditionalKey: ..., // *string
                StatCodes:     ..., // []string
                Tags:          ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatisticCycle'
    name: 'publicListMyStatCycleItems'
    summary: 'List user''s statCycleItems by statCycle'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticCycleService := social.UserStatisticCycleService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticCycleService.PublicListMyStatCycleItemsShort(
            &user_statistic_cycle.PublicListMyStatCycleItemsParams{
                CycleID:   ..., // string
                Namespace: ..., // string
                Limit:     ..., // *int32
                Offset:    ..., // *int32
                SortBy:    ..., // *string
                StatCodes: ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'publicListMyStatItems'
    summary: 'Public list user''s statItems'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.PublicListMyStatItemsShort(
            &user_statistic.PublicListMyStatItemsParams{
                Namespace: ..., // string
                Limit:     ..., // *int32
                Offset:    ..., // *int32
                SortBy:    ..., // *string
                StatCodes: ..., // []string
                Tags:      ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'publicQueryUserStatItems'
    summary: 'Public list user''s statItems'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.PublicQueryUserStatItemsShort(
            &user_statistic.PublicQueryUserStatItemsParams{
                Namespace:     ..., // string
                UserID:        ..., // string
                AdditionalKey: ..., // *string
                StatCodes:     ..., // []string
                Tags:          ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'publicQueryUserStatItems_1'
    summary: 'Public list user''s statItems'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.PublicQueryUserStatItems1Short(
            &user_statistic.PublicQueryUserStatItems1Params{
                Namespace: ..., // string
                UserID:    ..., // string
                Limit:     ..., // *int32
                Offset:    ..., // *int32
                SortBy:    ..., // *string
                StatCodes: ..., // *string
                Tags:      ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'publicQueryUserStatItems_2'
    summary: 'Public list user''s statItems'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.PublicQueryUserStatItems2Short(
            &user_statistic.PublicQueryUserStatItems2Params{
                Namespace:     ..., // string
                UserID:        ..., // string
                AdditionalKey: ..., // *string
                StatCodes:     ..., // []string
                Tags:          ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'GameProfile'
    name: 'publicUpdateAttribute'
    summary: 'Updates game profile attribute'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        gameProfileService := social.GameProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameProfileService.PublicUpdateAttributeShort(
            &game_profile.PublicUpdateAttributeParams{
                Body:          ..., // *socialclientmodels.Attribute
                AttributeName: ..., // string
                Namespace:     ..., // string
                ProfileID:     ..., // string
                UserID:        ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'GameProfile'
    name: 'publicUpdateProfile'
    summary: 'Updates user game profile'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        gameProfileService := social.GameProfileService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := gameProfileService.PublicUpdateProfileShort(
            &game_profile.PublicUpdateProfileParams{
                Body:      ..., // *socialclientmodels.GameProfileRequest
                Namespace: ..., // string
                ProfileID: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Slot'
    name: 'publicUpdateUserNamespaceSlot'
    summary: 'Updates a slot'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        slotService := social.SlotService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := slotService.PublicUpdateUserNamespaceSlotShort(
            &slot.PublicUpdateUserNamespaceSlotParams{
                Checksum:        ..., // *string
                CustomAttribute: ..., // *string
                File:            ..., // *[]byte
                Namespace:       ..., // string
                SlotID:          ..., // string
                UserID:          ..., // string
                Label:           ..., // *string
                Tags:            ..., // []string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Slot'
    name: 'publicUpdateUserNamespaceSlotMetadata'
    summary: 'Updates the slot metadata'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        slotService := social.SlotService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := slotService.PublicUpdateUserNamespaceSlotMetadataShort(
            &slot.PublicUpdateUserNamespaceSlotMetadataParams{
                Body:      ..., // *socialclientmodels.SlotMetadataUpdate
                Namespace: ..., // string
                SlotID:    ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'StatConfiguration'
    name: 'queryStats'
    summary: 'Query stats by keyword'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        statConfigurationService := social.StatConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := statConfigurationService.QueryStatsShort(
            &stat_configuration.QueryStatsParams{
                Namespace: ..., // string
                IsGlobal:  ..., // *bool
                IsPublic:  ..., // *bool
                Limit:     ..., // *int32
                Offset:    ..., // *int32
                Keyword:   ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'resetUserStatItemValue'
    summary: 'Reset user''s statitem value'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.ResetUserStatItemValueShort(
            &user_statistic.ResetUserStatItemValueParams{
                Body:          ..., // *socialclientmodels.StatResetInfo
                Namespace:     ..., // string
                StatCode:      ..., // string
                UserID:        ..., // string
                AdditionalKey: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'resetUserStatItemValue_1'
    summary: 'Public reset user''s statitem value'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.ResetUserStatItemValue1Short(
            &user_statistic.ResetUserStatItemValue1Params{
                Namespace: ..., // string
                StatCode:  ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'StatCycleConfiguration'
    name: 'stopStatCycle'
    summary: 'Stop stat cycle'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        statCycleConfigurationService := social.StatCycleConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := statCycleConfigurationService.StopStatCycleShort(
            &stat_cycle_configuration.StopStatCycleParams{
                CycleID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'SlotConfig'
    name: 'updateNamespaceSlotConfig'
    summary: 'Update a namespace slot configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        slotConfigService := social.SlotConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := slotConfigService.UpdateNamespaceSlotConfigShort(
            &slot_config.UpdateNamespaceSlotConfigParams{
                Body:      ..., // *socialclientmodels.SlotConfigUpdate
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'StatConfiguration'
    name: 'updateStat'
    summary: 'Update stat'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        statConfigurationService := social.StatConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := statConfigurationService.UpdateStatShort(
            &stat_configuration.UpdateStatParams{
                Body:      ..., // *socialclientmodels.StatUpdate
                Namespace: ..., // string
                StatCode:  ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'StatCycleConfiguration'
    name: 'updateStatCycle'
    summary: 'Update stat cycle'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        statCycleConfigurationService := social.StatCycleConfigurationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := statCycleConfigurationService.UpdateStatCycleShort(
            &stat_cycle_configuration.UpdateStatCycleParams{
                Body:      ..., // *socialclientmodels.StatCycleUpdate
                CycleID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'SlotConfig'
    name: 'updateUserSlotConfig'
    summary: 'Update a user slot configuration'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        slotConfigService := social.SlotConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := slotConfigService.UpdateUserSlotConfigShort(
            &slot_config.UpdateUserSlotConfigParams{
                Body:      ..., // *socialclientmodels.SlotConfigUpdate
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'updateUserStatItemValue'
    summary: 'Update user''s statitem value'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.UpdateUserStatItemValueShort(
            &user_statistic.UpdateUserStatItemValueParams{
                Body:          ..., // *socialclientmodels.StatItemUpdate
                Namespace:     ..., // string
                StatCode:      ..., // string
                UserID:        ..., // string
                AdditionalKey: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'UserStatistic'
    name: 'updateUserStatItemValue_1'
    summary: 'Public update user''s statitem value'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewSocialClient(configRepo)
        userStatisticService := social.UserStatisticService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := userStatisticService.UpdateUserStatItemValue1Short(
            &user_statistic.UpdateUserStatItemValue1Params{
                Body:          ..., // *socialclientmodels.StatItemUpdate
                Namespace:     ..., // string
                StatCode:      ..., // string
                UserID:        ..., // string
                AdditionalKey: ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }
# end social
# begin ugc
  - ns: 'Public Download Count (Legacy)'
    name: 'AddDownloadCount'
    summary: 'Add unique download count to a content'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicDownloadCountLegacyService := ugc.PublicDownloadCountLegacyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicDownloadCountLegacyService.AddDownloadCountShort(
            &public_download_count_legacy.AddDownloadCountParams{
                ContentID: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Staging Content'
    name: 'AdminApproveStagingContent'
    summary: 'Approve or reject content'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminStagingContentService := ugc.AdminStagingContentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminStagingContentService.AdminApproveStagingContentShort(
            &admin_staging_content.AdminApproveStagingContentParams{
                Body:      ..., // *ugcclientmodels.ModelsApproveStagingContentRequest
                ContentID: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content V2'
    name: 'AdminBulkGetContentByIDsV2'
    summary: 'Bulk get content by content IDs'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentV2Service := ugc.AdminContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentV2Service.AdminBulkGetContentByIDsV2Short(
            &admin_content_v2.AdminBulkGetContentByIDsV2Params{
                Body:      ..., // *ugcclientmodels.ModelsAdminGetContentBulkRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content V2'
    name: 'AdminCopyContent'
    summary: 'Copy contents from a channel to another'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentV2Service := ugc.AdminContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentV2Service.AdminCopyContentShort(
            &admin_content_v2.AdminCopyContentParams{
                Body:      ..., // *ugcclientmodels.ModelsCopyContentRequest
                ChannelID: ..., // string
                ContentID: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Channel'
    name: 'AdminCreateChannel'
    summary: 'Create Channel'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminChannelService := ugc.AdminChannelService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminChannelService.AdminCreateChannelShort(
            &admin_channel.AdminCreateChannelParams{
                Body:      ..., // *ugcclientmodels.ModelsChannelRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content V2'
    name: 'AdminCreateContentV2'
    summary: 'Create a content'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentV2Service := ugc.AdminContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentV2Service.AdminCreateContentV2Short(
            &admin_content_v2.AdminCreateContentV2Params{
                Body:      ..., // *ugcclientmodels.ModelsAdminContentRequestV2
                ChannelID: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Group'
    name: 'AdminCreateGroup'
    summary: 'Create groups'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminGroupService := ugc.AdminGroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminGroupService.AdminCreateGroupShort(
            &admin_group.AdminCreateGroupParams{
                Body:      ..., // *ugcclientmodels.ModelsCreateGroupRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Tag'
    name: 'AdminCreateTag'
    summary: 'Create tags'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminTagService := ugc.AdminTagService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminTagService.AdminCreateTagShort(
            &admin_tag.AdminCreateTagParams{
                Body:      ..., // *ugcclientmodels.ModelsCreateTagRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Type'
    name: 'AdminCreateType'
    summary: 'Create types'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminTypeService := ugc.AdminTypeService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminTypeService.AdminCreateTypeShort(
            &admin_type.AdminCreateTypeParams{
                Body:      ..., // *ugcclientmodels.ModelsCreateTypeRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Anonymization'
    name: 'AdminDeleteAllUserChannels'
    summary: 'Delete all user channel'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        anonymizationService := ugc.AnonymizationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := anonymizationService.AdminDeleteAllUserChannelsShort(
            &anonymization.AdminDeleteAllUserChannelsParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Anonymization'
    name: 'AdminDeleteAllUserContents'
    summary: 'Delete all user content'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        anonymizationService := ugc.AnonymizationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := anonymizationService.AdminDeleteAllUserContentsShort(
            &anonymization.AdminDeleteAllUserContentsParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Anonymization'
    name: 'AdminDeleteAllUserGroup'
    summary: 'Delete all user group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        anonymizationService := ugc.AnonymizationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := anonymizationService.AdminDeleteAllUserGroupShort(
            &anonymization.AdminDeleteAllUserGroupParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Anonymization'
    name: 'AdminDeleteAllUserStates'
    summary: 'Remove all user related state: likes, downloads, followers, following'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        anonymizationService := ugc.AnonymizationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := anonymizationService.AdminDeleteAllUserStatesShort(
            &anonymization.AdminDeleteAllUserStatesParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Channel'
    name: 'AdminDeleteChannel'
    summary: 'Delete Channel'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminChannelService := ugc.AdminChannelService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminChannelService.AdminDeleteChannelShort(
            &admin_channel.AdminDeleteChannelParams{
                ChannelID: ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content'
    name: 'AdminDeleteContent'
    summary: 'Delete content'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentService := ugc.AdminContentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentService.AdminDeleteContentShort(
            &admin_content.AdminDeleteContentParams{
                ChannelID: ..., // string
                ContentID: ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content V2'
    name: 'AdminDeleteContentByShareCodeV2'
    summary: 'Delete content by share code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentV2Service := ugc.AdminContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentV2Service.AdminDeleteContentByShareCodeV2Short(
            &admin_content_v2.AdminDeleteContentByShareCodeV2Params{
                ChannelID: ..., // string
                Namespace: ..., // string
                ShareCode: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content'
    name: 'AdminDeleteContentScreenshot'
    summary: 'Delete content''s screenshot'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentService := ugc.AdminContentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentService.AdminDeleteContentScreenshotShort(
            &admin_content.AdminDeleteContentScreenshotParams{
                ContentID:    ..., // string
                Namespace:    ..., // string
                ScreenshotID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content V2'
    name: 'AdminDeleteContentScreenshotV2'
    summary: 'Delete screenshots content'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentV2Service := ugc.AdminContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentV2Service.AdminDeleteContentScreenshotV2Short(
            &admin_content_v2.AdminDeleteContentScreenshotV2Params{
                ContentID:    ..., // string
                Namespace:    ..., // string
                ScreenshotID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Group'
    name: 'AdminDeleteGroup'
    summary: 'Delete group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminGroupService := ugc.AdminGroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminGroupService.AdminDeleteGroupShort(
            &admin_group.AdminDeleteGroupParams{
                GroupID:   ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content V2'
    name: 'AdminDeleteOfficialContentV2'
    summary: 'Delete official content'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentV2Service := ugc.AdminContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentV2Service.AdminDeleteOfficialContentV2Short(
            &admin_content_v2.AdminDeleteOfficialContentV2Params{
                ChannelID: ..., // string
                ContentID: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Tag'
    name: 'AdminDeleteTag'
    summary: 'Delete tags'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminTagService := ugc.AdminTagService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminTagService.AdminDeleteTagShort(
            &admin_tag.AdminDeleteTagParams{
                Namespace: ..., // string
                TagID:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Type'
    name: 'AdminDeleteType'
    summary: 'Delete type'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminTypeService := ugc.AdminTypeService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminTypeService.AdminDeleteTypeShort(
            &admin_type.AdminDeleteTypeParams{
                Namespace: ..., // string
                TypeID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content V2'
    name: 'AdminDeleteUserContentV2'
    summary: 'Delete user content'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentV2Service := ugc.AdminContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentV2Service.AdminDeleteUserContentV2Short(
            &admin_content_v2.AdminDeleteUserContentV2Params{
                ChannelID: ..., // string
                ContentID: ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content'
    name: 'AdminDownloadContentPreview'
    summary: 'Get content preview'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentService := ugc.AdminContentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentService.AdminDownloadContentPreviewShort(
            &admin_content.AdminDownloadContentPreviewParams{
                ContentID: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content V2'
    name: 'AdminGenerateOfficialContentUploadURLV2'
    summary: 'Generate official content upload URL'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentV2Service := ugc.AdminContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentV2Service.AdminGenerateOfficialContentUploadURLV2Short(
            &admin_content_v2.AdminGenerateOfficialContentUploadURLV2Params{
                Body:      ..., // *ugcclientmodels.ModelsGenerateContentUploadURLRequest
                ChannelID: ..., // string
                ContentID: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content V2'
    name: 'AdminGenerateUserContentUploadURLV2'
    summary: 'Generate user content upload URL'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentV2Service := ugc.AdminContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentV2Service.AdminGenerateUserContentUploadURLV2Short(
            &admin_content_v2.AdminGenerateUserContentUploadURLV2Params{
                Body:      ..., // *ugcclientmodels.ModelsGenerateContentUploadURLRequest
                ChannelID: ..., // string
                ContentID: ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Group'
    name: 'AdminGetAllGroups'
    summary: 'Get all user groups'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminGroupService := ugc.AdminGroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminGroupService.AdminGetAllGroupsShort(
            &admin_group.AdminGetAllGroupsParams{
                Namespace: ..., // string
                UserID:    ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Channel'
    name: 'AdminGetChannel'
    summary: 'Get Channels'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminChannelService := ugc.AdminChannelService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminChannelService.AdminGetChannelShort(
            &admin_channel.AdminGetChannelParams{
                Namespace: ..., // string
                UserID:    ..., // string
                Limit:     ..., // *int64
                Name:      ..., // *string
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Config'
    name: 'AdminGetConfigs'
    summary: 'Get configs'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminConfigService := ugc.AdminConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminConfigService.AdminGetConfigsShort(
            &admin_config.AdminGetConfigsParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content'
    name: 'AdminGetContent'
    summary: 'Get user''s generated contents'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentService := ugc.AdminContentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentService.AdminGetContentShort(
            &admin_content.AdminGetContentParams{
                Namespace: ..., // string
                UserID:    ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content'
    name: 'AdminGetContentBulk'
    summary: 'Bulk get content by content IDs'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentService := ugc.AdminContentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentService.AdminGetContentBulkShort(
            &admin_content.AdminGetContentBulkParams{
                Body:      ..., // *ugcclientmodels.ModelsAdminGetContentBulkRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content'
    name: 'AdminGetContentBulkByShareCodes'
    summary: 'Bulk get content by content sharecodes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentService := ugc.AdminContentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentService.AdminGetContentBulkByShareCodesShort(
            &admin_content.AdminGetContentBulkByShareCodesParams{
                Body:      ..., // *ugcclientmodels.ModelsGetContentBulkByShareCodesRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content V2'
    name: 'AdminGetContentBulkByShareCodesV2'
    summary: 'Bulk get content by content sharecodes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentV2Service := ugc.AdminContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentV2Service.AdminGetContentBulkByShareCodesV2Short(
            &admin_content_v2.AdminGetContentBulkByShareCodesV2Params{
                Body:      ..., // *ugcclientmodels.ModelsGetContentBulkByShareCodesRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content V2'
    name: 'AdminGetContentByChannelIDV2'
    summary: 'List contents specific to a channel'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentV2Service := ugc.AdminContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentV2Service.AdminGetContentByChannelIDV2Short(
            &admin_content_v2.AdminGetContentByChannelIDV2Params{
                ChannelID: ..., // string
                Namespace: ..., // string
                Limit:     ..., // *int64
                Name:      ..., // *string
                Offset:    ..., // *int64
                SortBy:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content V2'
    name: 'AdminGetContentByContentIDV2'
    summary: 'Get content by content ID'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentV2Service := ugc.AdminContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentV2Service.AdminGetContentByContentIDV2Short(
            &admin_content_v2.AdminGetContentByContentIDV2Params{
                ContentID: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content V2'
    name: 'AdminGetContentByShareCodeV2'
    summary: 'Get content by sharecode'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentV2Service := ugc.AdminContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentV2Service.AdminGetContentByShareCodeV2Short(
            &admin_content_v2.AdminGetContentByShareCodeV2Params{
                Namespace: ..., // string
                ShareCode: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content V2'
    name: 'AdminGetContentByUserIDV2'
    summary: 'Get user''s generated contents'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentV2Service := ugc.AdminContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentV2Service.AdminGetContentByUserIDV2Short(
            &admin_content_v2.AdminGetContentByUserIDV2Params{
                Namespace: ..., // string
                UserID:    ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                SortBy:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Group'
    name: 'AdminGetGroup'
    summary: 'Get specific user group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminGroupService := ugc.AdminGroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminGroupService.AdminGetGroupShort(
            &admin_group.AdminGetGroupParams{
                GroupID:   ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Group'
    name: 'AdminGetGroupContents'
    summary: '(Legacy) Get contents belong to a group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminGroupService := ugc.AdminGroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminGroupService.AdminGetGroupContentsShort(
            &admin_group.AdminGetGroupContentsParams{
                GroupID:   ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Group'
    name: 'AdminGetOfficialGroupContentsV2'
    summary: 'Get contents belong to a group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminGroupService := ugc.AdminGroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminGroupService.AdminGetOfficialGroupContentsV2Short(
            &admin_group.AdminGetOfficialGroupContentsV2Params{
                GroupID:   ..., // string
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content'
    name: 'AdminGetSpecificContent'
    summary: 'Get user specific content'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentService := ugc.AdminContentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentService.AdminGetSpecificContentShort(
            &admin_content.AdminGetSpecificContentParams{
                ContentID: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Staging Content'
    name: 'AdminGetStagingContentByID'
    summary: 'Get staging content by id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminStagingContentService := ugc.AdminStagingContentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminStagingContentService.AdminGetStagingContentByIDShort(
            &admin_staging_content.AdminGetStagingContentByIDParams{
                ContentID: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Tag'
    name: 'AdminGetTag'
    summary: 'Get tags'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminTagService := ugc.AdminTagService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminTagService.AdminGetTagShort(
            &admin_tag.AdminGetTagParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Type'
    name: 'AdminGetType'
    summary: 'Get types'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminTypeService := ugc.AdminTypeService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminTypeService.AdminGetTypeShort(
            &admin_type.AdminGetTypeParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content'
    name: 'AdminGetUserContentByShareCode'
    summary: 'Get content by sharecode'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentService := ugc.AdminContentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentService.AdminGetUserContentByShareCodeShort(
            &admin_content.AdminGetUserContentByShareCodeParams{
                Namespace: ..., // string
                ShareCode: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Group'
    name: 'AdminGetUserGroupContentsV2'
    summary: 'Get contents belong to a group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminGroupService := ugc.AdminGroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminGroupService.AdminGetUserGroupContentsV2Short(
            &admin_group.AdminGetUserGroupContentsV2Params{
                GroupID:   ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content'
    name: 'AdminHideUserContent'
    summary: 'Hide/Unhide user''s generated contents'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentService := ugc.AdminContentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentService.AdminHideUserContentShort(
            &admin_content.AdminHideUserContentParams{
                Body:      ..., // *ugcclientmodels.ModelsHideContentRequest
                ContentID: ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content V2'
    name: 'AdminListContentV2'
    summary: 'List contents'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentV2Service := ugc.AdminContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentV2Service.AdminListContentV2Short(
            &admin_content_v2.AdminListContentV2Params{
                Namespace:  ..., // string
                IsOfficial: ..., // *bool
                Limit:      ..., // *int64
                Name:       ..., // *string
                Offset:     ..., // *int64
                SortBy:     ..., // *string
                SubType:    ..., // *string
                Tags:       ..., // []string
                Type:       ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Staging Content'
    name: 'AdminListStagingContents'
    summary: 'List staging contents'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminStagingContentService := ugc.AdminStagingContentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminStagingContentService.AdminListStagingContentsShort(
            &admin_staging_content.AdminListStagingContentsParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                SortBy:    ..., // *string
                Status:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Staging Content'
    name: 'AdminListUserStagingContents'
    summary: 'List user staging contents'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminStagingContentService := ugc.AdminStagingContentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminStagingContentService.AdminListUserStagingContentsShort(
            &admin_staging_content.AdminListUserStagingContentsParams{
                Namespace: ..., // string
                UserID:    ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                SortBy:    ..., // *string
                Status:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content'
    name: 'AdminSearchChannelSpecificContent'
    summary: 'Search contents specific to a channel'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentService := ugc.AdminContentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentService.AdminSearchChannelSpecificContentShort(
            &admin_content.AdminSearchChannelSpecificContentParams{
                ChannelID:  ..., // string
                Namespace:  ..., // string
                Creator:    ..., // *string
                Ishidden:   ..., // *string
                Isofficial: ..., // *string
                Limit:      ..., // *int64
                Name:       ..., // *string
                Offset:     ..., // *int64
                Orderby:    ..., // *string
                Sortby:     ..., // *string
                Subtype:    ..., // *string
                Tags:       ..., // []string
                Type:       ..., // *string
                UserID:     ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content'
    name: 'AdminSearchContent'
    summary: 'Search contents'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentService := ugc.AdminContentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentService.AdminSearchContentShort(
            &admin_content.AdminSearchContentParams{
                Namespace:  ..., // string
                Creator:    ..., // *string
                Ishidden:   ..., // *string
                Isofficial: ..., // *string
                Limit:      ..., // *int64
                Name:       ..., // *string
                Offset:     ..., // *int64
                Orderby:    ..., // *string
                Sortby:     ..., // *string
                Subtype:    ..., // *string
                Tags:       ..., // []string
                Type:       ..., // *string
                UserID:     ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Channel'
    name: 'AdminUpdateChannel'
    summary: 'Update Channel'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminChannelService := ugc.AdminChannelService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminChannelService.AdminUpdateChannelShort(
            &admin_channel.AdminUpdateChannelParams{
                Body:      ..., // *ugcclientmodels.ModelsUpdateChannelRequest
                ChannelID: ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Config'
    name: 'AdminUpdateConfig'
    summary: 'Update config'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminConfigService := ugc.AdminConfigService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminConfigService.AdminUpdateConfigShort(
            &admin_config.AdminUpdateConfigParams{
                Body:      ..., // *ugcclientmodels.ModelsUpdateConfigRequest
                Key:       ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content V2'
    name: 'AdminUpdateContentByShareCodeV2'
    summary: 'Update content to S3 bucket by share code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentV2Service := ugc.AdminContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentV2Service.AdminUpdateContentByShareCodeV2Short(
            &admin_content_v2.AdminUpdateContentByShareCodeV2Params{
                Body:      ..., // *ugcclientmodels.ModelsAdminUpdateContentRequestV2
                ChannelID: ..., // string
                Namespace: ..., // string
                ShareCode: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content'
    name: 'AdminUpdateContentDirect'
    summary: 'Update content to a channel'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentService := ugc.AdminContentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentService.AdminUpdateContentDirectShort(
            &admin_content.AdminUpdateContentDirectParams{
                Body:      ..., // *ugcclientmodels.ModelsCreateContentRequest
                ChannelID: ..., // string
                ContentID: ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content V2'
    name: 'AdminUpdateContentHideStatusV2'
    summary: 'Hide/Unhide user''s generated contents'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentV2Service := ugc.AdminContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentV2Service.AdminUpdateContentHideStatusV2Short(
            &admin_content_v2.AdminUpdateContentHideStatusV2Params{
                Body:      ..., // *ugcclientmodels.ModelsHideContentRequest
                ContentID: ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content'
    name: 'AdminUpdateContentS3'
    summary: 'Update content to S3 bucket'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentService := ugc.AdminContentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentService.AdminUpdateContentS3Short(
            &admin_content.AdminUpdateContentS3Params{
                Body:      ..., // *ugcclientmodels.ModelsAdminUpdateContentRequest
                ChannelID: ..., // string
                ContentID: ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content'
    name: 'AdminUpdateContentS3ByShareCode'
    summary: 'Update content to S3 bucket by share code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentService := ugc.AdminContentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentService.AdminUpdateContentS3ByShareCodeShort(
            &admin_content.AdminUpdateContentS3ByShareCodeParams{
                Body:      ..., // *ugcclientmodels.ModelsAdminUpdateContentRequest
                ChannelID: ..., // string
                Namespace: ..., // string
                ShareCode: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Group'
    name: 'AdminUpdateGroup'
    summary: 'Update group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminGroupService := ugc.AdminGroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminGroupService.AdminUpdateGroupShort(
            &admin_group.AdminUpdateGroupParams{
                Body:      ..., // *ugcclientmodels.ModelsCreateGroupRequest
                GroupID:   ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content V2'
    name: 'AdminUpdateOfficialContentFileLocation'
    summary: 'Update content file location'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentV2Service := ugc.AdminContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentV2Service.AdminUpdateOfficialContentFileLocationShort(
            &admin_content_v2.AdminUpdateOfficialContentFileLocationParams{
                Body:      ..., // *ugcclientmodels.ModelsUpdateFileLocationRequest
                ChannelID: ..., // string
                ContentID: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content V2'
    name: 'AdminUpdateOfficialContentV2'
    summary: 'Update official content'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentV2Service := ugc.AdminContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentV2Service.AdminUpdateOfficialContentV2Short(
            &admin_content_v2.AdminUpdateOfficialContentV2Params{
                Body:      ..., // *ugcclientmodels.ModelsAdminUpdateContentRequestV2
                ChannelID: ..., // string
                ContentID: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content'
    name: 'AdminUpdateScreenshots'
    summary: 'Update screenshot of content'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentService := ugc.AdminContentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentService.AdminUpdateScreenshotsShort(
            &admin_content.AdminUpdateScreenshotsParams{
                Body:      ..., // *ugcclientmodels.ModelsUpdateScreenshotRequest
                ContentID: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content V2'
    name: 'AdminUpdateScreenshotsV2'
    summary: 'Update screenshot of content'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentV2Service := ugc.AdminContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentV2Service.AdminUpdateScreenshotsV2Short(
            &admin_content_v2.AdminUpdateScreenshotsV2Params{
                Body:      ..., // *ugcclientmodels.ModelsUpdateScreenshotRequest
                ContentID: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Tag'
    name: 'AdminUpdateTag'
    summary: 'Update tag'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminTagService := ugc.AdminTagService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminTagService.AdminUpdateTagShort(
            &admin_tag.AdminUpdateTagParams{
                Body:      ..., // *ugcclientmodels.ModelsCreateTagRequest
                Namespace: ..., // string
                TagID:     ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Type'
    name: 'AdminUpdateType'
    summary: 'Update types'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminTypeService := ugc.AdminTypeService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminTypeService.AdminUpdateTypeShort(
            &admin_type.AdminUpdateTypeParams{
                Body:      ..., // *ugcclientmodels.ModelsCreateTypeRequest
                Namespace: ..., // string
                TypeID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content V2'
    name: 'AdminUpdateUserContentFileLocation'
    summary: 'Update user content file location'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentV2Service := ugc.AdminContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentV2Service.AdminUpdateUserContentFileLocationShort(
            &admin_content_v2.AdminUpdateUserContentFileLocationParams{
                Body:      ..., // *ugcclientmodels.ModelsUpdateFileLocationRequest
                ChannelID: ..., // string
                ContentID: ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content V2'
    name: 'AdminUpdateUserContentV2'
    summary: 'Update user content'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentV2Service := ugc.AdminContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentV2Service.AdminUpdateUserContentV2Short(
            &admin_content_v2.AdminUpdateUserContentV2Params{
                Body:      ..., // *ugcclientmodels.ModelsAdminUpdateContentRequestV2
                ChannelID: ..., // string
                ContentID: ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content'
    name: 'AdminUploadContentDirect'
    summary: 'Upload content to a channel'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentService := ugc.AdminContentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentService.AdminUploadContentDirectShort(
            &admin_content.AdminUploadContentDirectParams{
                Body:      ..., // *ugcclientmodels.ModelsCreateContentRequest
                ChannelID: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content'
    name: 'AdminUploadContentS3'
    summary: 'Upload content to S3 bucket'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentService := ugc.AdminContentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentService.AdminUploadContentS3Short(
            &admin_content.AdminUploadContentS3Params{
                Body:      ..., // *ugcclientmodels.ModelsCreateContentRequestS3
                ChannelID: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content'
    name: 'AdminUploadContentScreenshot'
    summary: 'Upload screenshots for official content'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentService := ugc.AdminContentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentService.AdminUploadContentScreenshotShort(
            &admin_content.AdminUploadContentScreenshotParams{
                Body:      ..., // *ugcclientmodels.ModelsCreateScreenshotRequest
                ContentID: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content V2'
    name: 'AdminUploadContentScreenshotV2'
    summary: 'Upload screenshots for content'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentV2Service := ugc.AdminContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentV2Service.AdminUploadContentScreenshotV2Short(
            &admin_content_v2.AdminUploadContentScreenshotV2Params{
                Body:      ..., // *ugcclientmodels.ModelsCreateScreenshotRequest
                ContentID: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content (Legacy)'
    name: 'CreateContentDirect'
    summary: 'Upload content to a channel'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentLegacyService := ugc.PublicContentLegacyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentLegacyService.CreateContentDirectShort(
            &public_content_legacy.CreateContentDirectParams{
                Body:      ..., // *ugcclientmodels.ModelsCreateContentRequest
                ChannelID: ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content (Legacy)'
    name: 'CreateContentS3'
    summary: 'Upload content to S3 bucket'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentLegacyService := ugc.PublicContentLegacyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentLegacyService.CreateContentS3Short(
            &public_content_legacy.CreateContentS3Params{
                Body:      ..., // *ugcclientmodels.ModelsPublicCreateContentRequestS3
                ChannelID: ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Group'
    name: 'CreateGroup'
    summary: 'Create groups'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicGroupService := ugc.PublicGroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicGroupService.CreateGroupShort(
            &public_group.CreateGroupParams{
                Body:      ..., // *ugcclientmodels.ModelsCreateGroupRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Anonymization'
    name: 'DeleteAllUserChannel'
    summary: 'Delete all user channel'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        anonymizationService := ugc.AnonymizationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := anonymizationService.DeleteAllUserChannelShort(
            &anonymization.DeleteAllUserChannelParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Anonymization'
    name: 'DeleteAllUserContents'
    summary: 'Delete all user content'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        anonymizationService := ugc.AnonymizationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := anonymizationService.DeleteAllUserContentsShort(
            &anonymization.DeleteAllUserContentsParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Anonymization'
    name: 'DeleteAllUserGroup'
    summary: 'Delete all user group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        anonymizationService := ugc.AnonymizationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := anonymizationService.DeleteAllUserGroupShort(
            &anonymization.DeleteAllUserGroupParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Anonymization'
    name: 'DeleteAllUserStates'
    summary: 'Remove all user related state: likes, downloads, followers, following'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        anonymizationService := ugc.AnonymizationService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := anonymizationService.DeleteAllUserStatesShort(
            &anonymization.DeleteAllUserStatesParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Channel'
    name: 'DeleteChannel'
    summary: 'Delete Channel'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicChannelService := ugc.PublicChannelService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicChannelService.DeleteChannelShort(
            &public_channel.DeleteChannelParams{
                ChannelID: ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content (Legacy)'
    name: 'DeleteContent'
    summary: 'Delete content'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentLegacyService := ugc.PublicContentLegacyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentLegacyService.DeleteContentShort(
            &public_content_legacy.DeleteContentParams{
                ChannelID: ..., // string
                ContentID: ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content'
    name: 'DeleteContentByShareCode'
    summary: 'Delete content by share code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentService := ugc.AdminContentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentService.DeleteContentByShareCodeShort(
            &admin_content.DeleteContentByShareCodeParams{
                ChannelID: ..., // string
                Namespace: ..., // string
                ShareCode: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content (Legacy)'
    name: 'DeleteContentScreenshot'
    summary: 'Delete content''s screenshot'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentLegacyService := ugc.PublicContentLegacyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentLegacyService.DeleteContentScreenshotShort(
            &public_content_legacy.DeleteContentScreenshotParams{
                ContentID:    ..., // string
                Namespace:    ..., // string
                ScreenshotID: ..., // string
                UserID:       ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content V2'
    name: 'DeleteContentScreenshotV2'
    summary: 'Delete screenshots content'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentV2Service := ugc.PublicContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentV2Service.DeleteContentScreenshotV2Short(
            &public_content_v2.DeleteContentScreenshotV2Params{
                ContentID:    ..., // string
                Namespace:    ..., // string
                ScreenshotID: ..., // string
                UserID:       ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Group'
    name: 'DeleteGroup'
    summary: 'Delete group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicGroupService := ugc.PublicGroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicGroupService.DeleteGroupShort(
            &public_group.DeleteGroupParams{
                GroupID:   ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Staging Content'
    name: 'DeleteUserStagingContentByID'
    summary: 'Delete user staging content by id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicStagingContentService := ugc.PublicStagingContentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicStagingContentService.DeleteUserStagingContentByIDShort(
            &public_staging_content.DeleteUserStagingContentByIDParams{
                ContentID: ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Channel'
    name: 'GetChannels'
    summary: 'Get Channels'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicChannelService := ugc.PublicChannelService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicChannelService.GetChannelsShort(
            &public_channel.GetChannelsParams{
                Namespace: ..., // string
                UserID:    ..., // string
                Limit:     ..., // *int64
                Name:      ..., // *string
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Follow'
    name: 'GetFollowedContent'
    summary: 'Get contents from followed creators'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicFollowService := ugc.PublicFollowService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicFollowService.GetFollowedContentShort(
            &public_follow.GetFollowedContentParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Follow'
    name: 'GetFollowedUsers'
    summary: 'Get followed creators'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicFollowService := ugc.PublicFollowService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicFollowService.GetFollowedUsersShort(
            &public_follow.GetFollowedUsersParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Group'
    name: 'GetGroup'
    summary: 'Get user''s groups'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicGroupService := ugc.PublicGroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicGroupService.GetGroupShort(
            &public_group.GetGroupParams{
                GroupID:   ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Group'
    name: 'GetGroupContent'
    summary: '(Legacy) Get contents belong to a group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicGroupService := ugc.PublicGroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicGroupService.GetGroupContentShort(
            &public_group.GetGroupContentParams{
                GroupID:   ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Group'
    name: 'GetGroups'
    summary: 'Get all user groups'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicGroupService := ugc.PublicGroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicGroupService.GetGroupsShort(
            &public_group.GetGroupsParams{
                Namespace: ..., // string
                UserID:    ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Like (Legacy)'
    name: 'GetLikedContent'
    summary: 'Get liked contents'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicLikeLegacyService := ugc.PublicLikeLegacyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicLikeLegacyService.GetLikedContentShort(
            &public_like_legacy.GetLikedContentParams{
                Namespace:  ..., // string
                Isofficial: ..., // *bool
                Limit:      ..., // *int64
                Name:       ..., // *string
                Offset:     ..., // *int64
                Orderby:    ..., // *string
                Sortby:     ..., // *string
                Subtype:    ..., // *string
                Tags:       ..., // []string
                Type:       ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Follow'
    name: 'GetPublicFollowers'
    summary: 'Get list of followers'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicFollowService := ugc.PublicFollowService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicFollowService.GetPublicFollowersShort(
            &public_follow.GetPublicFollowersParams{
                Namespace: ..., // string
                UserID:    ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Follow'
    name: 'GetPublicFollowing'
    summary: 'Get list of following'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicFollowService := ugc.PublicFollowService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicFollowService.GetPublicFollowingShort(
            &public_follow.GetPublicFollowingParams{
                Namespace: ..., // string
                UserID:    ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Tag'
    name: 'GetTag'
    summary: 'Get tags'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicTagService := ugc.PublicTagService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicTagService.GetTagShort(
            &public_tag.GetTagParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Type'
    name: 'GetType'
    summary: 'Get types'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicTypeService := ugc.PublicTypeService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicTypeService.GetTypeShort(
            &public_type.GetTypeParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Staging Content'
    name: 'GetUserStagingContentByID'
    summary: 'Get user staging content by id'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicStagingContentService := ugc.PublicStagingContentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicStagingContentService.GetUserStagingContentByIDShort(
            &public_staging_content.GetUserStagingContentByIDParams{
                ContentID: ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content'
    name: 'ListContentVersions'
    summary: 'List content''s payload versions'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentService := ugc.AdminContentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentService.ListContentVersionsShort(
            &admin_content.ListContentVersionsParams{
                ContentID: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content V2'
    name: 'ListContentVersionsV2'
    summary: 'List content''s payload versions'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentV2Service := ugc.AdminContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentV2Service.ListContentVersionsV2Short(
            &admin_content_v2.ListContentVersionsV2Params{
                ContentID: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Staging Content'
    name: 'ListUserStagingContents'
    summary: 'List user staging contents'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicStagingContentService := ugc.PublicStagingContentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicStagingContentService.ListUserStagingContentsShort(
            &public_staging_content.ListUserStagingContentsParams{
                Namespace: ..., // string
                UserID:    ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                SortBy:    ..., // *string
                Status:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Download Count V2'
    name: 'PublicAddDownloadCountV2'
    summary: 'Add unique download count to a content'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicDownloadCountV2Service := ugc.PublicDownloadCountV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicDownloadCountV2Service.PublicAddDownloadCountV2Short(
            &public_download_count_v2.PublicAddDownloadCountV2Params{
                ContentID: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content V2'
    name: 'PublicBulkGetContentByIDV2'
    summary: 'Get contents by content Ids '
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentV2Service := ugc.PublicContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentV2Service.PublicBulkGetContentByIDV2Short(
            &public_content_v2.PublicBulkGetContentByIDV2Params{
                Body:      ..., // *ugcclientmodels.ModelsPublicGetContentBulkRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Channel'
    name: 'PublicCreateChannel'
    summary: 'Create Channel'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicChannelService := ugc.PublicChannelService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicChannelService.PublicCreateChannelShort(
            &public_channel.PublicCreateChannelParams{
                Body:      ..., // *ugcclientmodels.ModelsPublicChannelRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content V2'
    name: 'PublicCreateContentV2'
    summary: 'Create a content'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentV2Service := ugc.PublicContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentV2Service.PublicCreateContentV2Short(
            &public_content_v2.PublicCreateContentV2Params{
                Body:      ..., // *ugcclientmodels.ModelsContentRequestV2
                ChannelID: ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content (Legacy)'
    name: 'PublicDeleteContentByShareCode'
    summary: 'Delete content by share code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentLegacyService := ugc.PublicContentLegacyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentLegacyService.PublicDeleteContentByShareCodeShort(
            &public_content_legacy.PublicDeleteContentByShareCodeParams{
                ChannelID: ..., // string
                Namespace: ..., // string
                ShareCode: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content V2'
    name: 'PublicDeleteContentByShareCodeV2'
    summary: 'Delete content by share code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentV2Service := ugc.PublicContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentV2Service.PublicDeleteContentByShareCodeV2Short(
            &public_content_v2.PublicDeleteContentByShareCodeV2Params{
                ChannelID: ..., // string
                Namespace: ..., // string
                ShareCode: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content V2'
    name: 'PublicDeleteContentV2'
    summary: 'Delete content'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentV2Service := ugc.PublicContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentV2Service.PublicDeleteContentV2Short(
            &public_content_v2.PublicDeleteContentV2Params{
                ChannelID: ..., // string
                ContentID: ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content (Legacy)'
    name: 'PublicDownloadContentByContentID'
    summary: 'Get user specific content'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentLegacyService := ugc.PublicContentLegacyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentLegacyService.PublicDownloadContentByContentIDShort(
            &public_content_legacy.PublicDownloadContentByContentIDParams{
                ContentID: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content (Legacy)'
    name: 'PublicDownloadContentByShareCode'
    summary: 'Get content by sharecode'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentLegacyService := ugc.PublicContentLegacyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentLegacyService.PublicDownloadContentByShareCodeShort(
            &public_content_legacy.PublicDownloadContentByShareCodeParams{
                Namespace: ..., // string
                ShareCode: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content (Legacy)'
    name: 'PublicDownloadContentPreview'
    summary: 'Get content preview'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentLegacyService := ugc.PublicContentLegacyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentLegacyService.PublicDownloadContentPreviewShort(
            &public_content_legacy.PublicDownloadContentPreviewParams{
                ContentID: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content V2'
    name: 'PublicGenerateContentUploadURLV2'
    summary: 'Generate content upload URL'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentV2Service := ugc.PublicContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentV2Service.PublicGenerateContentUploadURLV2Short(
            &public_content_v2.PublicGenerateContentUploadURLV2Params{
                Body:      ..., // *ugcclientmodels.ModelsGenerateContentUploadURLRequest
                ChannelID: ..., // string
                ContentID: ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content (Legacy)'
    name: 'PublicGetContentBulk'
    summary: 'Get contents by content Ids '
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentLegacyService := ugc.PublicContentLegacyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentLegacyService.PublicGetContentBulkShort(
            &public_content_legacy.PublicGetContentBulkParams{
                Body:      ..., // *ugcclientmodels.ModelsPublicGetContentBulkRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content (Legacy)'
    name: 'PublicGetContentBulkByShareCodes'
    summary: 'Bulk get content by content sharecodes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentLegacyService := ugc.PublicContentLegacyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentLegacyService.PublicGetContentBulkByShareCodesShort(
            &public_content_legacy.PublicGetContentBulkByShareCodesParams{
                Body:      ..., // *ugcclientmodels.ModelsGetContentBulkByShareCodesRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content V2'
    name: 'PublicGetContentBulkByShareCodesV2'
    summary: 'Bulk get content by content sharecodes'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentV2Service := ugc.PublicContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentV2Service.PublicGetContentBulkByShareCodesV2Short(
            &public_content_v2.PublicGetContentBulkByShareCodesV2Params{
                Body:      ..., // *ugcclientmodels.ModelsGetContentBulkByShareCodesRequest
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content V2'
    name: 'PublicGetContentByChannelIDV2'
    summary: 'List contents specific to a channel'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentV2Service := ugc.PublicContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentV2Service.PublicGetContentByChannelIDV2Short(
            &public_content_v2.PublicGetContentByChannelIDV2Params{
                ChannelID: ..., // string
                Namespace: ..., // string
                Limit:     ..., // *int64
                Name:      ..., // *string
                Offset:    ..., // *int64
                SortBy:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content V2'
    name: 'PublicGetContentByIDV2'
    summary: 'Get content by content ID'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentV2Service := ugc.PublicContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentV2Service.PublicGetContentByIDV2Short(
            &public_content_v2.PublicGetContentByIDV2Params{
                ContentID: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content V2'
    name: 'PublicGetContentByShareCodeV2'
    summary: 'Get content by sharecode'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentV2Service := ugc.PublicContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentV2Service.PublicGetContentByShareCodeV2Short(
            &public_content_v2.PublicGetContentByShareCodeV2Params{
                Namespace: ..., // string
                ShareCode: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content V2'
    name: 'PublicGetContentByUserIDV2'
    summary: 'Get content by user ID'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentV2Service := ugc.PublicContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentV2Service.PublicGetContentByUserIDV2Short(
            &public_content_v2.PublicGetContentByUserIDV2Params{
                Namespace: ..., // string
                UserID:    ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                SortBy:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Creator'
    name: 'PublicGetCreator'
    summary: 'Get creator stats: number of total like by other user, number of total following and follower user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicCreatorService := ugc.PublicCreatorService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicCreatorService.PublicGetCreatorShort(
            &public_creator.PublicGetCreatorParams{
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Group'
    name: 'PublicGetGroupContentsV2'
    summary: 'Get contents belong to a group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicGroupService := ugc.PublicGroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicGroupService.PublicGetGroupContentsV2Short(
            &public_group.PublicGetGroupContentsV2Params{
                GroupID:   ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content (Legacy)'
    name: 'PublicGetUserContent'
    summary: 'Get user''s generated contents'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentLegacyService := ugc.PublicContentLegacyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentLegacyService.PublicGetUserContentShort(
            &public_content_legacy.PublicGetUserContentParams{
                Namespace: ..., // string
                UserID:    ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Download Count V2'
    name: 'PublicListContentDownloaderV2'
    summary: 'List content downloader'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicDownloadCountV2Service := ugc.PublicDownloadCountV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicDownloadCountV2Service.PublicListContentDownloaderV2Short(
            &public_download_count_v2.PublicListContentDownloaderV2Params{
                ContentID: ..., // string
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                SortBy:    ..., // *string
                UserID:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Like V2'
    name: 'PublicListContentLikeV2'
    summary: 'Retrieve list of user that like specified content.'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicLikeV2Service := ugc.PublicLikeV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicLikeV2Service.PublicListContentLikeV2Short(
            &public_like_v2.PublicListContentLikeV2Params{
                ContentID: ..., // string
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                SortBy:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content V2'
    name: 'PublicListContentV2'
    summary: 'List contents'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentV2Service := ugc.PublicContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentV2Service.PublicListContentV2Short(
            &public_content_v2.PublicListContentV2Params{
                Namespace:  ..., // string
                IsOfficial: ..., // *bool
                Limit:      ..., // *int64
                Name:       ..., // *string
                Offset:     ..., // *int64
                SortBy:     ..., // *string
                SubType:    ..., // *string
                Tags:       ..., // []string
                Type:       ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content (Legacy)'
    name: 'PublicSearchContent'
    summary: 'Search contents'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentLegacyService := ugc.PublicContentLegacyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentLegacyService.PublicSearchContentShort(
            &public_content_legacy.PublicSearchContentParams{
                Namespace:  ..., // string
                Creator:    ..., // *string
                Ishidden:   ..., // *string
                Isofficial: ..., // *string
                Limit:      ..., // *int64
                Name:       ..., // *string
                Offset:     ..., // *int64
                Orderby:    ..., // *string
                Sortby:     ..., // *string
                Subtype:    ..., // *string
                Tags:       ..., // []string
                Type:       ..., // *string
                UserID:     ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Creator'
    name: 'PublicSearchCreator'
    summary: 'Search creator'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicCreatorService := ugc.PublicCreatorService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicCreatorService.PublicSearchCreatorShort(
            &public_creator.PublicSearchCreatorParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
                Orderby:   ..., // *string
                Sortby:    ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content (Legacy)'
    name: 'PublicUpdateContentByShareCode'
    summary: 'Update content to S3 bucket by share code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentLegacyService := ugc.PublicContentLegacyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentLegacyService.PublicUpdateContentByShareCodeShort(
            &public_content_legacy.PublicUpdateContentByShareCodeParams{
                Body:      ..., // *ugcclientmodels.ModelsUpdateContentRequest
                ChannelID: ..., // string
                Namespace: ..., // string
                ShareCode: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content V2'
    name: 'PublicUpdateContentByShareCodeV2'
    summary: 'Update content to S3 bucket by share code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentV2Service := ugc.PublicContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentV2Service.PublicUpdateContentByShareCodeV2Short(
            &public_content_v2.PublicUpdateContentByShareCodeV2Params{
                Body:      ..., // *ugcclientmodels.ModelsUpdateContentRequestV2
                ChannelID: ..., // string
                Namespace: ..., // string
                ShareCode: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content V2'
    name: 'PublicUpdateContentFileLocation'
    summary: 'Update content file location'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentV2Service := ugc.PublicContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentV2Service.PublicUpdateContentFileLocationShort(
            &public_content_v2.PublicUpdateContentFileLocationParams{
                Body:      ..., // *ugcclientmodels.ModelsUpdateFileLocationRequest
                ChannelID: ..., // string
                ContentID: ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content V2'
    name: 'PublicUpdateContentV2'
    summary: 'Update content'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentV2Service := ugc.PublicContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentV2Service.PublicUpdateContentV2Short(
            &public_content_v2.PublicUpdateContentV2Params{
                Body:      ..., // *ugcclientmodels.ModelsUpdateContentRequestV2
                ChannelID: ..., // string
                ContentID: ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content'
    name: 'RollbackContentVersion'
    summary: 'Rollback content''s payload version'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentService := ugc.AdminContentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentService.RollbackContentVersionShort(
            &admin_content.RollbackContentVersionParams{
                ContentID: ..., // string
                Namespace: ..., // string
                VersionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content V2'
    name: 'RollbackContentVersionV2'
    summary: 'Rollback content''s payload version'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentV2Service := ugc.AdminContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentV2Service.RollbackContentVersionV2Short(
            &admin_content_v2.RollbackContentVersionV2Params{
                ContentID: ..., // string
                Namespace: ..., // string
                VersionID: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content (Legacy)'
    name: 'SearchChannelSpecificContent'
    summary: 'Search contents specific to a channel'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentLegacyService := ugc.PublicContentLegacyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentLegacyService.SearchChannelSpecificContentShort(
            &public_content_legacy.SearchChannelSpecificContentParams{
                ChannelID:  ..., // string
                Namespace:  ..., // string
                Creator:    ..., // *string
                Ishidden:   ..., // *string
                Isofficial: ..., // *string
                Limit:      ..., // *int64
                Name:       ..., // *string
                Offset:     ..., // *int64
                Orderby:    ..., // *string
                Sortby:     ..., // *string
                Subtype:    ..., // *string
                Tags:       ..., // []string
                Type:       ..., // *string
                UserID:     ..., // *string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Channel'
    name: 'SingleAdminDeleteChannel'
    summary: 'Delete Channel'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminChannelService := ugc.AdminChannelService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminChannelService.SingleAdminDeleteChannelShort(
            &admin_channel.SingleAdminDeleteChannelParams{
                ChannelID: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content'
    name: 'SingleAdminDeleteContent'
    summary: 'Delete content'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentService := ugc.AdminContentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentService.SingleAdminDeleteContentShort(
            &admin_content.SingleAdminDeleteContentParams{
                ChannelID: ..., // string
                ContentID: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Group'
    name: 'SingleAdminDeleteGroup'
    summary: 'Delete group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminGroupService := ugc.AdminGroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminGroupService.SingleAdminDeleteGroupShort(
            &admin_group.SingleAdminDeleteGroupParams{
                GroupID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Group'
    name: 'SingleAdminGetAllGroups'
    summary: 'Get all user groups'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminGroupService := ugc.AdminGroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminGroupService.SingleAdminGetAllGroupsShort(
            &admin_group.SingleAdminGetAllGroupsParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Channel'
    name: 'SingleAdminGetChannel'
    summary: 'Get Channels'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminChannelService := ugc.AdminChannelService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminChannelService.SingleAdminGetChannelShort(
            &admin_channel.SingleAdminGetChannelParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content'
    name: 'SingleAdminGetContent'
    summary: 'Get user''s generated contents'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentService := ugc.AdminContentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentService.SingleAdminGetContentShort(
            &admin_content.SingleAdminGetContentParams{
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Group'
    name: 'SingleAdminGetGroup'
    summary: 'Get specific user group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminGroupService := ugc.AdminGroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminGroupService.SingleAdminGetGroupShort(
            &admin_group.SingleAdminGetGroupParams{
                GroupID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Group'
    name: 'SingleAdminGetGroupContents'
    summary: '(Legacy) Get contents belong to a group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminGroupService := ugc.AdminGroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminGroupService.SingleAdminGetGroupContentsShort(
            &admin_group.SingleAdminGetGroupContentsParams{
                GroupID:   ..., // string
                Namespace: ..., // string
                Limit:     ..., // *int64
                Offset:    ..., // *int64
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Channel'
    name: 'SingleAdminUpdateChannel'
    summary: 'Update Channel'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminChannelService := ugc.AdminChannelService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminChannelService.SingleAdminUpdateChannelShort(
            &admin_channel.SingleAdminUpdateChannelParams{
                Body:      ..., // *ugcclientmodels.ModelsUpdateChannelRequest
                ChannelID: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content'
    name: 'SingleAdminUpdateContentDirect'
    summary: 'Update content to a channel'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentService := ugc.AdminContentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentService.SingleAdminUpdateContentDirectShort(
            &admin_content.SingleAdminUpdateContentDirectParams{
                Body:      ..., // *ugcclientmodels.ModelsCreateContentRequest
                ChannelID: ..., // string
                ContentID: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Content'
    name: 'SingleAdminUpdateContentS3'
    summary: 'Update content to S3 bucket'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminContentService := ugc.AdminContentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminContentService.SingleAdminUpdateContentS3Short(
            &admin_content.SingleAdminUpdateContentS3Params{
                Body:      ..., // *ugcclientmodels.ModelsAdminUpdateContentRequest
                ChannelID: ..., // string
                ContentID: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Admin Group'
    name: 'SingleAdminUpdateGroup'
    summary: 'Update group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        adminGroupService := ugc.AdminGroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := adminGroupService.SingleAdminUpdateGroupShort(
            &admin_group.SingleAdminUpdateGroupParams{
                Body:      ..., // *ugcclientmodels.ModelsCreateGroupRequest
                GroupID:   ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Channel'
    name: 'UpdateChannel'
    summary: 'Update Channel'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicChannelService := ugc.PublicChannelService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicChannelService.UpdateChannelShort(
            &public_channel.UpdateChannelParams{
                Body:      ..., // *ugcclientmodels.ModelsUpdateChannelRequest
                ChannelID: ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content (Legacy)'
    name: 'UpdateContentDirect'
    summary: 'Update content to a channel'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentLegacyService := ugc.PublicContentLegacyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentLegacyService.UpdateContentDirectShort(
            &public_content_legacy.UpdateContentDirectParams{
                Body:      ..., // *ugcclientmodels.ModelsCreateContentRequest
                ChannelID: ..., // string
                ContentID: ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Like (Legacy)'
    name: 'UpdateContentLikeStatus'
    summary: 'Update like/unlike status to a content'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicLikeLegacyService := ugc.PublicLikeLegacyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicLikeLegacyService.UpdateContentLikeStatusShort(
            &public_like_legacy.UpdateContentLikeStatusParams{
                Body:      ..., // *ugcclientmodels.ModelsContentLikeRequest
                ContentID: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Like V2'
    name: 'UpdateContentLikeStatusV2'
    summary: 'Update like/unlike status to a content'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicLikeV2Service := ugc.PublicLikeV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicLikeV2Service.UpdateContentLikeStatusV2Short(
            &public_like_v2.UpdateContentLikeStatusV2Params{
                Body:      ..., // *ugcclientmodels.ModelsContentLikeRequest
                ContentID: ..., // string
                Namespace: ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content (Legacy)'
    name: 'UpdateContentS3'
    summary: 'Update content to S3 bucket'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentLegacyService := ugc.PublicContentLegacyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentLegacyService.UpdateContentS3Short(
            &public_content_legacy.UpdateContentS3Params{
                Body:      ..., // *ugcclientmodels.ModelsUpdateContentRequest
                ChannelID: ..., // string
                ContentID: ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content (Legacy)'
    name: 'UpdateContentShareCode'
    summary: 'Update content sharecode'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentLegacyService := ugc.PublicContentLegacyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentLegacyService.UpdateContentShareCodeShort(
            &public_content_legacy.UpdateContentShareCodeParams{
                Body:      ..., // *ugcclientmodels.ModelsUpdateContentShareCodeRequest
                ChannelID: ..., // string
                ContentID: ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content V2'
    name: 'UpdateContentShareCodeV2'
    summary: 'Update content share code'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentV2Service := ugc.PublicContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentV2Service.UpdateContentShareCodeV2Short(
            &public_content_v2.UpdateContentShareCodeV2Params{
                Body:      ..., // *ugcclientmodels.ModelsUpdateContentShareCodeRequest
                ChannelID: ..., // string
                ContentID: ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Group'
    name: 'UpdateGroup'
    summary: 'Update group'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicGroupService := ugc.PublicGroupService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicGroupService.UpdateGroupShort(
            &public_group.UpdateGroupParams{
                Body:      ..., // *ugcclientmodels.ModelsCreateGroupRequest
                GroupID:   ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content (Legacy)'
    name: 'UpdateScreenshots'
    summary: 'Update screenshot of content'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentLegacyService := ugc.PublicContentLegacyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentLegacyService.UpdateScreenshotsShort(
            &public_content_legacy.UpdateScreenshotsParams{
                Body:      ..., // *ugcclientmodels.ModelsUpdateScreenshotRequest
                ContentID: ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content V2'
    name: 'UpdateScreenshotsV2'
    summary: 'Update screenshot of content'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentV2Service := ugc.PublicContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentV2Service.UpdateScreenshotsV2Short(
            &public_content_v2.UpdateScreenshotsV2Params{
                Body:      ..., // *ugcclientmodels.ModelsUpdateScreenshotRequest
                ContentID: ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Staging Content'
    name: 'UpdateStagingContent'
    summary: 'Update staging content'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicStagingContentService := ugc.PublicStagingContentService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicStagingContentService.UpdateStagingContentShort(
            &public_staging_content.UpdateStagingContentParams{
                Body:      ..., // *ugcclientmodels.ModelsUpdateStagingContentRequest
                ContentID: ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Follow'
    name: 'UpdateUserFollowStatus'
    summary: 'Update follow/unfollow status to a user'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicFollowService := ugc.PublicFollowService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicFollowService.UpdateUserFollowStatusShort(
            &public_follow.UpdateUserFollowStatusParams{
                Body:      ..., // *ugcclientmodels.ModelsUserFollowRequest
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content (Legacy)'
    name: 'UploadContentScreenshot'
    summary: 'Upload screenshots for content'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentLegacyService := ugc.PublicContentLegacyService{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentLegacyService.UploadContentScreenshotShort(
            &public_content_legacy.UploadContentScreenshotParams{
                Body:      ..., // *ugcclientmodels.ModelsCreateScreenshotRequest
                ContentID: ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }

  - ns: 'Public Content V2'
    name: 'UploadContentScreenshotV2'
    summary: 'Upload screenshots for content'
    schema:
      type: object
      properties: {}
      additionalProperties: false
    examples:
      - args: {}
    staticData:
      snippet: |
        // Create new or acquire an existing instance of ConfigRepository and TokenRepository.
        //   configRepo := ...
        //   tokenRepo  := ...

        // Create new or acquire existing instance of the service.
        client := factory.NewUgcClient(configRepo)
        publicContentV2Service := ugc.PublicContentV2Service{
            Client:           client,
            ConfigRepository: configRepo,
            TokenRepository:  tokenRepo,
        }

        // Call the API.
        res, err := publicContentV2Service.UploadContentScreenshotV2Short(
            &public_content_v2.UploadContentScreenshotV2Params{
                Body:      ..., // *ugcclientmodels.ModelsCreateScreenshotRequest
                ContentID: ..., // string
                Namespace: ..., // string
                UserID:    ..., // string
            }
        )

        // Handle errors.
        if err != nil {
            // handleError(err)
        }

        // Handle success.
        if res != nil {
            // doSomething(res)
        }
# end ugc